import test from "node:test";
import assert from "node:assert/strict";

test("package.json, demo, and Tauri stay anchored to canonical dist runtime artifacts", () => {
  assert.fail("replace placeholder with static dist contract assertions");
});
