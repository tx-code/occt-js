---
phase: 05-exact-model-lifecycle-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - src/importer.hpp
  - src/js-interface.cpp
  - src/importer-step.cpp
  - src/importer-iges.cpp
  - src/importer-brep.cpp
  - src/importer-xde.cpp
  - src/exact-model-store.hpp
  - src/exact-model-store.cpp
  - test/exact_model_lifecycle_contract.test.mjs
autonomous: true
requirements:
  - LIFE-01
must_haves:
  truths:
    - "The root wasm module exposes a separate exact-model open lane without changing the current stateless Read* contract."
    - "Exact-model handles are retained inside wasm through an explicit store with deterministic retain/release semantics."
    - "Exact open returns the existing mesh-oriented payload plus an exact-model handle that later phases can use."
  artifacts:
    - "src/exact-model-store.hpp"
    - "src/exact-model-store.cpp"
    - "src/importer.hpp"
    - "src/js-interface.cpp"
    - "test/exact_model_lifecycle_contract.test.mjs"
  key_links:
    - "OpenExact* bindings -> ExactModelStore"
    - "ExactModelStore -> importer-step.cpp / importer-iges.cpp / importer-brep.cpp / importer-xde.cpp"
    - "test/exact_model_lifecycle_contract.test.mjs -> OpenExact* / RetainExactModel / ReleaseExactModel"
---

<objective>
Add the retained exact-model store and exact open/release root APIs.

Purpose: satisfy LIFE-01 and establish the lifecycle half of LIFE-02 by adding a separate exact-model lane beside the current stateless imports.
Output: new root exact open/release bindings, a wasm-side exact-model store, and a smoke contract test proving exact handles exist while `Read*` remains unchanged.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-exact-model-lifecycle-contract/05-RESEARCH.md
@.planning/phases/05-exact-model-lifecycle-contract/05-VALIDATION.md
@AGENTS.md
@CMakeLists.txt
@src/importer.hpp
@src/js-interface.cpp
@src/importer-utils.cpp
@src/importer-step.cpp
@src/importer-iges.cpp
@src/importer-brep.cpp
@src/importer-xde.cpp
@dist/occt-js.d.ts
@test/load_occt_factory.mjs
@test/simple_part.step
@test/cube_10x10.igs
@test/as1_pe_203.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a failing root smoke contract for the exact-model lifecycle lane</name>
  <read_first>test/load_occt_factory.mjs, test/test_multi_format_exports.mjs, test/test_step_iges_root_mode.mjs, src/js-interface.cpp, dist/occt-js.d.ts, AGENTS.md, .planning/phases/05-exact-model-lifecycle-contract/05-RESEARCH.md, .planning/phases/05-exact-model-lifecycle-contract/05-VALIDATION.md</read_first>
  <files>test/exact_model_lifecycle_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact open APIs return mesh payload plus exactModelId` fails until STEP, IGES, and BREP exact open entrypoints return `success: true`, a numeric `exactModelId`, and the same top-level payload keys used by current stateless imports (`sourceFormat`, `rootNodes`, `geometries`, `materials`, `warnings`, `stats`).
    - Test 2: a subtest named `exact lifecycle lane does not change stateless ReadStepFile output` fails unless `ReadStepFile` continues to return the current import payload without an `exactModelId` property.
    - Test 3: a subtest named `releaseExactModel succeeds for a fresh exact handle` fails until a freshly opened exact handle can be released through the root module.
  </behavior>
  <action>Create `test/exact_model_lifecycle_contract.test.mjs` as a Node built-in test file that loads the built root carrier via `test/load_occt_factory.mjs`, reads `dist/occt-js.wasm`, and exercises STEP, IGES, and BREP fixtures from `test/simple_part.step`, `test/cube_10x10.igs`, and `test/as1_pe_203.brep`. Add the exact subtest names from `<behavior>`. Assert that `OpenExactStepModel`, `OpenExactIgesModel`, and `OpenExactBrepModel` return `success === true`, `typeof exactModelId === "number"`, and non-empty `rootNodes` / `geometries`; assert that `ReadStepFile` does not expose `exactModelId`; and assert that `ReleaseExactModel(exactModelId)` returns an object with `ok === true` for a fresh handle.</action>
  <acceptance_criteria>
    - `rg -n "exact open APIs return mesh payload plus exactModelId|exact lifecycle lane does not change stateless ReadStepFile output|releaseExactModel succeeds for a fresh exact handle" test/exact_model_lifecycle_contract.test.mjs` returns matches.
    - `rg -n "OpenExactStepModel|OpenExactIgesModel|OpenExactBrepModel|ReleaseExactModel" test/exact_model_lifecycle_contract.test.mjs` returns matches.
    - `node --test test/exact_model_lifecycle_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_model_lifecycle_contract.test.mjs</automated>
  </verify>
  <done>The root lifecycle smoke test proves that the new exact open lane exists, returns `exactModelId`, and leaves the current stateless `ReadStepFile` surface unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the wasm-side exact-model store and root exact open/release APIs</name>
  <read_first>CMakeLists.txt, src/importer.hpp, src/js-interface.cpp, src/importer-utils.cpp, src/importer-step.cpp, src/importer-iges.cpp, src/importer-brep.cpp, src/importer-xde.cpp, test/exact_model_lifecycle_contract.test.mjs, .planning/phases/05-exact-model-lifecycle-contract/05-RESEARCH.md</read_first>
  <files>CMakeLists.txt, src/importer.hpp, src/js-interface.cpp, src/importer-step.cpp, src/importer-iges.cpp, src/importer-brep.cpp, src/importer-xde.cpp, src/exact-model-store.hpp, src/exact-model-store.cpp, test/exact_model_lifecycle_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports `OpenExactModel`, `OpenExactStepModel`, `OpenExactIgesModel`, `OpenExactBrepModel`, `RetainExactModel`, and `ReleaseExactModel`.
    - Test 2: exact open reuses the current import pipeline enough to preserve the existing scene payload while also returning a numeric `exactModelId`.
    - Test 3: `RetainExactModel` increments the internal reference count for a live handle and `ReleaseExactModel` decrements it, returning `{ ok: true }` while the handle is still valid.
    - Test 4: `ReadFile`, `ReadStepFile`, `ReadIgesFile`, and `ReadBrepFile` keep their current signatures and payload behavior.
  </behavior>
  <action>Add `src/exact-model-store.hpp` and `src/exact-model-store.cpp` implementing a wasm-side exact-model registry with integer handle allocation, retained model entries, and reference counting. Extend `src/importer.hpp` with concrete lifecycle structs for exact open results and lifecycle operation results so `js-interface.cpp` can serialize them. Update `src/importer-step.cpp`, `src/importer-iges.cpp`, `src/importer-brep.cpp`, and `src/importer-xde.cpp` so the exact lane can capture retained exact import state while still producing the existing `OcctSceneData` payload. In `src/js-interface.cpp`, add root bindings named exactly `OpenExactModel`, `OpenExactStepModel`, `OpenExactIgesModel`, `OpenExactBrepModel`, `RetainExactModel`, and `ReleaseExactModel`. Make each exact open binding return the current import payload plus `exactModelId`; make `RetainExactModel` and `ReleaseExactModel` return an object with `ok`, and for error cases reserve explicit `code` / `message` fields even if Phase 5 Plan 02 finalizes them. Keep all current `Read*` and `ReadFile` signatures untouched. Update `CMakeLists.txt` to compile the new exact-model store translation unit.</action>
  <acceptance_criteria>
    - `rg -n "OpenExactModel|OpenExactStepModel|OpenExactIgesModel|OpenExactBrepModel|RetainExactModel|ReleaseExactModel" src/js-interface.cpp` returns matches.
    - `rg -n "class ExactModelStore|struct ExactModelEntry|Retain|Release" src/exact-model-store.hpp src/exact-model-store.cpp` returns matches.
    - `rg -n "exactModelId" src/importer.hpp src/js-interface.cpp test/exact_model_lifecycle_contract.test.mjs` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_model_lifecycle_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_model_lifecycle_contract.test.mjs"</automated>
  </verify>
  <done>The root wasm carrier has a functioning exact-model store and exact open/release API lane that returns `exactModelId` without changing the current stateless import contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS caller -> wasm exact-model store | Exact-model handles cross from untrusted JS input into retained native state here. |
| import pipeline -> retained exact registry | Imported OCCT shapes become long-lived exact state rather than request-scoped import data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | T/R | exact-model handle registry | mitigate | Allocate handles inside `ExactModelStore` only, reject unknown handles, and prove handle-driven lifecycle calls through automated tests. |
| T-05-01-02 | D | retained exact-model memory | mitigate | Add explicit retain/release semantics with deterministic refcount decrement instead of relying on JS GC or hidden global retention. |
| T-05-01-03 | S/T | stateless import lane | mitigate | Keep current `Read*` and `ReadFile` signatures untouched and assert their unchanged behavior in `test/exact_model_lifecycle_contract.test.mjs`. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_model_lifecycle_contract.test.mjs`.
</verification>

<success_criteria>
The root module exposes a separate exact-model open/release lane backed by a retained native store, exact opens return `exactModelId` plus the current mesh payload, and stateless imports continue to behave exactly as before.
</success_criteria>

<output>
After completion, create `.planning/phases/05-exact-model-lifecycle-contract/05-01-SUMMARY.md`
</output>
