---
phase: 05-exact-model-lifecycle-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - dist/occt-js.d.ts
  - package.json
  - packages/occt-core/src/index.js
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - test/exact_model_lifecycle_contract.test.mjs
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
must_haves:
  truths:
    - "The public lifecycle typing surface matches the implemented exact-model open/retain/release APIs."
    - "Invalid, unknown, and released handles fail with explicit lifecycle DTOs instead of silent no-ops."
    - "Root and occt-core verification lock invalid-after-release behavior and prove downstream runtime consumption."
  artifacts:
    - "dist/occt-js.d.ts"
    - "package.json"
    - "packages/occt-core/src/occt-core.js"
    - "packages/occt-core/test/core.test.mjs"
    - "packages/occt-core/test/live-root-integration.test.mjs"
    - "test/exact_model_lifecycle_contract.test.mjs"
  key_links:
    - "dist/occt-js.d.ts -> OpenExact* / RetainExactModel / ReleaseExactModel"
    - "packages/occt-core/src/occt-core.js -> root exact lifecycle bindings"
    - "package.json scripts.test -> test/exact_model_lifecycle_contract.test.mjs"
---

<objective>
Lock lifecycle typings, failure DTOs, and root verification for invalid-after-release behavior.

Purpose: finish LIFE-02 and harden the public contract so downstream JS can rely on explicit lifecycle semantics through the root package and the thin `occt-core` adapter.
Output: typed lifecycle DTOs, minimal `occt-core` exact-session wrappers, invalid-after-release contract tests, and root test-script coverage for the new lifecycle lane.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-exact-model-lifecycle-contract/05-RESEARCH.md
@.planning/phases/05-exact-model-lifecycle-contract/05-VALIDATION.md
@AGENTS.md
@package.json
@dist/occt-js.d.ts
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_model_lifecycle_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the public lifecycle typing surface and thin occt-core adapter</name>
  <read_first>dist/occt-js.d.ts, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, test/exact_model_lifecycle_contract.test.mjs, .planning/phases/05-exact-model-lifecycle-contract/05-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs</files>
  <behavior>
    - Test 1: `dist/occt-js.d.ts` exposes `OpenExactModel`, `OpenExactStepModel`, `OpenExactIgesModel`, `OpenExactBrepModel`, `RetainExactModel`, and `ReleaseExactModel` on `OcctJSModule`.
    - Test 2: lifecycle operation results are typed as explicit success/failure DTOs with `ok`, and failure cases include `code` and `message`.
    - Test 3: `OcctCoreClient` exposes thin lifecycle helpers named `openExactModel`, `openExactStep`, `openExactIges`, `openExactBrep`, `retainExactModel`, and `releaseExactModel` that forward the root contract without hiding lifecycle errors.
    - Test 4: `packages/occt-core/test/core.test.mjs` covers the new wrapper methods using a stub factory and explicit lifecycle DTO assertions.
  </behavior>
  <action>Update `dist/occt-js.d.ts` with concrete public types named `OcctJSExactOpenResult`, `OcctJSLifecycleSuccess`, `OcctJSLifecycleFailure`, and `OcctJSLifecycleResult`, and add the exact lifecycle method signatures to `OcctJSModule`. In `packages/occt-core/src/occt-core.js`, add thin methods `openExactModel`, `openExactStep`, `openExactIges`, `openExactBrep`, `retainExactModel`, and `releaseExactModel`; make them call the corresponding root module methods directly, normalize the format argument the same way `importModel` already does, throw only for exact open import failures where `success` is false, and otherwise return lifecycle DTOs unchanged. Re-export any needed wrapper surface from `packages/occt-core/src/index.js`. Extend `packages/occt-core/test/core.test.mjs` with stub-based tests that assert the exact method names, format dispatch, `exactModelId` propagation, and explicit failure DTO propagation for invalid or released handles.</action>
  <acceptance_criteria>
    - `rg -n "OcctJSExactOpenResult|OcctJSLifecycleSuccess|OcctJSLifecycleFailure|OcctJSLifecycleResult|OpenExactModel|RetainExactModel|ReleaseExactModel" dist/occt-js.d.ts` returns matches.
    - `rg -n "openExactModel|openExactStep|openExactIges|openExactBrep|retainExactModel|releaseExactModel" packages/occt-core/src/occt-core.js packages/occt-core/test/core.test.mjs` returns matches.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix packages/occt-core test</automated>
  </verify>
  <done>The root lifecycle API is fully typed and `occt-core` exposes a thin exact-session wrapper layer that preserves explicit lifecycle success and failure DTOs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock invalid-after-release behavior in root and live downstream verification</name>
  <read_first>package.json, test/exact_model_lifecycle_contract.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, dist/occt-js.d.ts, packages/occt-core/src/occt-core.js, AGENTS.md, .planning/phases/05-exact-model-lifecycle-contract/05-VALIDATION.md</read_first>
  <files>package.json, test/exact_model_lifecycle_contract.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: `test/exact_model_lifecycle_contract.test.mjs` adds subtests named `retainExactModel keeps a live handle valid until the final release`, `released and unknown handles fail with explicit lifecycle codes`, and `stateless ReadFile remains exact-handle-free after exact lane changes`.
    - Test 2: the root lifecycle test proves that a retained handle survives one release, a final release invalidates it, and subsequent `RetainExactModel` / `ReleaseExactModel` calls on that handle fail with explicit codes `invalid-handle` or `released-handle`.
    - Test 3: `packages/occt-core/test/live-root-integration.test.mjs` exercises the built root carrier through `createOcctCore(...)`, opens an exact model, and disposes it successfully.
    - Test 4: `package.json` adds `node --test test/exact_model_lifecycle_contract.test.mjs` to the root `test` script so lifecycle regressions run with the normal root runtime suite.
  </behavior>
  <action>Extend `test/exact_model_lifecycle_contract.test.mjs` with the exact subtest names from `<behavior>` and assert the complete invalid-after-release contract: a fresh exact handle can be retained, one release leaves it live, the final release invalidates it, and later lifecycle calls return `{ ok: false, code: "released-handle" | "invalid-handle", message: ... }`. Add a companion smoke test to `packages/occt-core/test/live-root-integration.test.mjs` that loads the built root carrier, calls `openExactModel` or `openExactStep`, verifies `exactModelId`, then disposes the handle through the adapter. Update `package.json` so the root `test` script includes `node --test test/exact_model_lifecycle_contract.test.mjs` before the existing integration scripts, keeping all current test commands intact. Do not add measurement tests in this phase.</action>
  <acceptance_criteria>
    - `rg -n "retainExactModel keeps a live handle valid until the final release|released and unknown handles fail with explicit lifecycle codes|stateless ReadFile remains exact-handle-free after exact lane changes" test/exact_model_lifecycle_contract.test.mjs` returns matches.
    - `rg -n "openExactModel|openExactStep|releaseExactModel|exactModelId" packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `rg -n "test/exact_model_lifecycle_contract.test.mjs" package.json` returns a match inside the `test` script.
    - `npm --prefix packages/occt-core test` passes.
    - `npm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test"</automated>
  </verify>
  <done>The public lifecycle contract is locked by typings, root tests, and live downstream adapter verification, including deterministic invalid-after-release behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root lifecycle DTOs -> downstream JS callers | Type declarations and adapter methods define how consumers interpret handle validity and failure behavior. |
| live root carrier -> occt-core wrapper | Adapter helpers must not hide or weaken invalid-handle and released-handle semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T/R | lifecycle DTO typing surface | mitigate | Type explicit success/failure DTOs with `ok`, `code`, and `message`, then lock them in root and stub-based tests. |
| T-05-02-02 | S/T | occt-core lifecycle adapter | mitigate | Keep adapter methods thin, forward root lifecycle DTOs directly, and prove behavior with both stub and live-root tests. |
| T-05-02-03 | D | invalid-after-release semantics | mitigate | Add automated tests that retain, partially release, fully release, and then re-call lifecycle APIs to prove deterministic failure codes. |
</threat_model>

<verification>
- Run `npm --prefix packages/occt-core test`.
- Run `npm test`.
</verification>

<success_criteria>
The exact-model lifecycle lane is fully typed, explicit lifecycle failures are stable and testable, `occt-core` can consume the root exact lane without hiding it, and invalid-after-release behavior is part of the normal root verification suite.
</success_criteria>

<output>
After completion, create `.planning/phases/05-exact-model-lifecycle-contract/05-02-SUMMARY.md`
</output>
