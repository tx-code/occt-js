---
phase: 09-root-import-appearance-mode
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/js-interface.cpp
  - src/importer-xde.cpp
  - src/importer-brep.cpp
  - test/import_appearance_contract.test.mjs
autonomous: true
requirements:
  - APPR-01
  - APPR-02
must_haves:
  truths:
    - "Root import APIs accept an explicit `colorMode` that lets callers choose source colors or the built-in default CAD color."
    - "Built-in default mode returns one documented RGB color `[0.9, 0.91, 0.93]` in root scene colors and materials."
    - "The built-in default color is applied during root import, not invented later by viewer or package repaint logic."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/importer.hpp"
    - "src/js-interface.cpp"
    - "src/importer-xde.cpp"
    - "src/importer-brep.cpp"
    - "test/import_appearance_contract.test.mjs"
  key_links:
    - "ParseImportParams -> ImportParams appearance mode"
    - "importer-xde/importer-brep -> built-in default color application"
    - "root import contract test -> built-in color and source/default behavior"
---

<objective>
Add explicit root appearance mode parsing and the built-in default CAD color.

Purpose: satisfy APPR-01 and APPR-02 by making import-time color behavior explicit at the root Wasm boundary instead of leaving default coloring to downstream repaint logic.
Output: root import params with `colorMode`, a built-in default CAD color aligned to the current `occt-core` fallback, and contract tests that prove source/default behavior across supported formats.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-root-import-appearance-mode/09-RESEARCH.md
@.planning/phases/09-root-import-appearance-mode/09-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/js-interface.cpp
@src/importer-xde.cpp
@src/importer-brep.cpp
@packages/occt-core/src/model-normalizer.js
@test/test_multi_format_exports.mjs
@test/ANC101_colored.stp
@test/bearing.igs
@test/ANC101_isolated_components.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract tests for explicit source/default import appearance modes</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, src/importer-xde.cpp, src/importer-brep.cpp, packages/occt-core/src/model-normalizer.js, .planning/phases/09-root-import-appearance-mode/09-RESEARCH.md, .planning/phases/09-root-import-appearance-mode/09-VALIDATION.md</read_first>
  <files>test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `colorMode default forces the built-in CAD color on colored STEP imports` fails until `ANC101_colored.stp` imported with `{ colorMode: "default" }` returns only the built-in RGB default across root `materials` and geometry/face colors.
    - Test 2: a subtest named `colorMode source preserves imported source colors on colored STEP imports` fails until the same fixture imported with `{ colorMode: "source" }` still exposes at least one non-default source color.
    - Test 3: a subtest named `colorMode default assigns the built-in CAD color to colorless IGES and BREP imports` fails until `bearing.igs` and `ANC101_isolated_components.brep` return one built-in default material and default-colored geometries in `default` mode.
    - Test 4: a subtest named `ReadFile and format-specific entry points stay in parity for appearance-mode output` fails until generic and format-specific read entry points return the same appearance-mode colors/materials for STEP and BREP fixtures.
  </behavior>
  <action>Create `test/import_appearance_contract.test.mjs` as a Node built-in test file that loads the built root carrier via `test/load_occt_factory.mjs`. Define the built-in default RGB constant exactly as `{ r: 0.9, g: 0.91, b: 0.93 }`. Add the exact subtest names from `<behavior>`. Use `ANC101_colored.stp` to prove source-color preservation versus forced-default collapse. Use `bearing.igs` and `ANC101_isolated_components.brep` to prove colorless imports gain one built-in default material only in `default` mode. For parity, compare `ReadFile("step", ...)` with `ReadStepFile(...)`, and `ReadFile("brep", ...)` with `ReadBrepFile(...)`, asserting the same appearance-mode material/color outcomes.</action>
  <acceptance_criteria>
    - `rg -n "colorMode default forces the built-in CAD color on colored STEP imports|colorMode source preserves imported source colors on colored STEP imports|colorMode default assigns the built-in CAD color to colorless IGES and BREP imports|ReadFile and format-specific entry points stay in parity for appearance-mode output" test/import_appearance_contract.test.mjs` returns matches.
    - `rg -n "colorMode|0\\.9|0\\.91|0\\.93|ANC101_colored\\.stp|bearing\\.igs|ANC101_isolated_components\\.brep" test/import_appearance_contract.test.mjs` returns matches.
    - `node --test test/import_appearance_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/import_appearance_contract.test.mjs</automated>
  </verify>
  <done>The root contract tests prove the intended source/default appearance behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement root colorMode parsing and built-in default CAD color application</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, src/importer-xde.cpp, src/importer-brep.cpp, packages/occt-core/src/model-normalizer.js, test/import_appearance_contract.test.mjs, .planning/phases/09-root-import-appearance-mode/09-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, src/importer-xde.cpp, src/importer-brep.cpp, test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: root import params accept `colorMode: "source" | "default"` alongside existing import options.
    - Test 2: `colorMode: "default"` returns exactly one built-in default color `{ r: 0.9, g: 0.91, b: 0.93 }` across root `materials` and any geometry/face color fields for STEP, IGES, and BREP imports.
    - Test 3: `colorMode: "source"` preserves current source-color behavior for XDE-backed formats.
    - Test 4: generic and format-specific read entry points continue to return parity for the same appearance-mode inputs.
  </behavior>
  <action>Add an appearance-mode field to `ImportParams` in `src/importer.hpp`, using an internal enum that can distinguish legacy behavior from explicit `source` and `default` modes. In `src/js-interface.cpp`, extend `ParseImportParams` to parse a JS string property named exactly `colorMode`, keeping `readColors` untouched for legacy callers. In `dist/occt-js.d.ts`, add `colorMode?: "source" | "default"` to `OcctJSReadParams`. In `src/importer-xde.cpp`, introduce helper logic that uses the built-in default `OcctColor(0.9, 0.91, 0.93)` when the explicit mode is `default`, preserves extracted XDE colors when the explicit mode is `source`, and leaves omitted-`colorMode` behavior unchanged for now. Stamp both `meshData.color` and fallback face colors through that policy so `BuildResult` derives the right root `materials`. In `src/importer-brep.cpp`, thread `params` into the local mesh extraction helper and stamp the same built-in default color only when `colorMode` is `default`; otherwise keep current colorless behavior. Do not introduce `defaultColor` yet in this phase.</action>
  <acceptance_criteria>
    - `rg -n "colorMode" dist/occt-js.d.ts src/importer.hpp src/js-interface.cpp` returns matches.
    - `rg -n "0\\.9|0\\.91|0\\.93|OcctColor" src/importer-xde.cpp src/importer-brep.cpp` returns matches showing the built-in default CAD color path.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/import_appearance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/import_appearance_contract.test.mjs"</automated>
  </verify>
  <done>The root Wasm carrier now exposes explicit `source`/`default` appearance mode behavior with one documented built-in default CAD color.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS import params -> C++ import policy | Incorrect precedence here would silently change downstream color behavior. |
| importer scene colors -> root materials array | If geometry/face colors are not stamped consistently, materials and scene colors will drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | T/R | root appearance-mode contract | mitigate | Lock explicit source/default behavior with dedicated root contract tests before implementation. |
| T-09-01-02 | T/I | built-in default color application | mitigate | Force one built-in RGB value at import time and assert it across colored and colorless fixtures. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/import_appearance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root read APIs now expose explicit source/default appearance mode behavior with one built-in default CAD color shared across supported formats
  </success_criteria>

<output>
After completion, create `.planning/phases/09-root-import-appearance-mode/09-01-SUMMARY.md`
</output>
