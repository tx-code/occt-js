---
phase: 13-appearance-preset-adapter-parity
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/src/model-normalizer.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
autonomous: true
requirements:
  - APPR-07
  - APPR-08
  - ADAPT-05
must_haves:
  truths:
    - "`@tx-code/occt-core` normalizes and forwards preset/defaultOpacity input without inventing a separate appearance contract."
    - "Normalized adapter output preserves root raw `opacity` instead of dropping it during color normalization."
    - "Live adapter imports through the built root carrier keep preset semantics aligned end-to-end."
  artifacts:
    - "packages/occt-core/src/occt-core.js"
    - "packages/occt-core/src/model-normalizer.js"
    - "packages/occt-core/test/core.test.mjs"
    - "packages/occt-core/test/live-root-integration.test.mjs"
  key_links:
    - "caller-friendly preset/defaultOpacity input -> canonical root params"
    - "root raw opacity -> normalized baseColor alpha"
    - "live adapter import -> built root carrier preset parity"
---

<objective>
Normalize presets and opacity through `@tx-code/occt-core`.

Purpose: satisfy ADAPT-05 and finish Phase 13 by making the adapter package accept caller-friendly preset/opacity input while preserving the underlying root runtime contract and raw opacity semantics.
Output: adapter normalization, raw opacity preservation in normalized output, and live parity tests against the built root carrier.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-appearance-preset-adapter-parity/13-RESEARCH.md
@.planning/phases/13-appearance-preset-adapter-parity/13-VALIDATION.md
@AGENTS.md
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/model-normalizer.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/import_appearance_contract.test.mjs
@dist/occt-js.d.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing adapter tests for preset forwarding and opacity preservation</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/src/model-normalizer.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/13-appearance-preset-adapter-parity/13-RESEARCH.md</read_first>
  <files>packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a unit test named `normalizes and forwards appearancePreset and defaultOpacity before calling the root carrier` fails until adapter input is canonicalized before forwarding.
    - Test 2: a unit test named `preserves raw root opacity when normalizing default appearance output` fails until object-form `opacity` survives normalization into RGBA base colors.
    - Test 3: a live test named `createOcctCore imports named presets through the built root carrier` fails until built-root adapter imports preserve preset semantics end-to-end.
  </behavior>
  <action>Extend `packages/occt-core/test/core.test.mjs` and `packages/occt-core/test/live-root-integration.test.mjs` with the exact test names from `<behavior>`. Cover canonical preset/defaultOpacity forwarding, caller-friendly alpha promotion where appropriate, and normalized output that keeps root raw opacity instead of dropping it.</action>
  <acceptance_criteria>
    - `rg -n "normalizes and forwards appearancePreset and defaultOpacity before calling the root carrier|preserves raw root opacity when normalizing default appearance output|createOcctCore imports named presets through the built root carrier" packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs</automated>
  </verify>
  <done>The adapter tests prove preset/defaultOpacity normalization and raw opacity preservation before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement occt-core preset normalization and live parity with the root runtime</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/src/model-normalizer.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/13-appearance-preset-adapter-parity/13-RESEARCH.md</read_first>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/src/model-normalizer.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: adapter import/openExact calls forward canonical preset/defaultOpacity params to the root carrier.
    - Test 2: normalized adapter output preserves root raw opacity on colors/materials.
    - Test 3: live adapter imports through the built root carrier match the same preset semantics as direct root imports.
  </behavior>
  <action>Extend `packages/occt-core/src/occt-core.js` so import params normalize preset/defaultOpacity and promote alpha from caller-friendly colors when it should become root `defaultOpacity`. Extend `packages/occt-core/src/model-normalizer.js` so object-form `opacity` is treated as alpha during normalization and fallback-material synthesis respects the resolved appearance context. Keep the adapter package convenience-focused, but do not hide or rename the underlying root preset/defaultOpacity contract.</action>
  <acceptance_criteria>
    - `rg -n "appearancePreset|defaultOpacity|opacity" packages/occt-core/src/occt-core.js packages/occt-core/src/model-normalizer.js` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `npm --prefix packages/occt-core test` passes.
    - `node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs"</automated>
  </verify>
  <done>`@tx-code/occt-core` now preserves the expanded root appearance semantics instead of dropping preset or opacity information.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-friendly preset/defaultOpacity input -> canonical forwarded root params | Drift here would make adapter and direct root imports disagree. |
| root raw `opacity` -> normalized adapter baseColor alpha | Losing opacity here would hide runtime behavior from downstream callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | T/R | adapter param forwarding | mitigate | Lock canonical preset/defaultOpacity forwarding with unit tests before implementation. |
| T-13-02-02 | T/I | normalized opacity preservation | mitigate | Add unit and live tests that prove object `opacity` survives adapter normalization. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `npm --prefix packages/occt-core test`.
- Run `node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `@tx-code/occt-core` keeps preset/defaultOpacity behavior aligned with the root carrier end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/13-appearance-preset-adapter-parity/13-02-SUMMARY.md`
</output>
