---
phase: 16-exact-relation-classifier-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - test/exact_relation_contract.test.mjs
autonomous: true
requirements:
  - ADAPT-08
  - REL-03
must_haves:
  truths:
    - "`@tx-code/occt-core` exposes package-first exact relation classification for pairwise refs without inventing viewer ids or policy."
    - "Package wrappers preserve occurrence transforms and keep `kind: \"none\"` distinct from explicit failures."
    - "Live package tests prove repeated-geometry relation behavior through the built root carrier."
  artifacts:
    - "packages/occt-core/src/occt-core.js"
    - "packages/occt-core/test/core.test.mjs"
    - "packages/occt-core/test/live-root-integration.test.mjs"
    - "test/exact_relation_contract.test.mjs"
  key_links:
    - "root relation classifier -> package-first classifyExactRelation(refA, refB)"
    - "local relation support geometry -> occurrence-space package normalization"
    - "repeated-geometry live tests -> adapter parity and none/failure correctness"
---

<objective>
Normalize relation classifier adapters and failure parity in `occt-core`.

Purpose: satisfy ADAPT-08 and the package-facing part of REL-03 by exposing occurrence-safe relation classification through `@tx-code/occt-core`.
Output: package-first `classifyExactRelation(refA, refB)` wrapper plus unit and live tests that prove transform preservation, `none` semantics, and failure parity.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-exact-relation-classifier-contract/16-CONTEXT.md
@.planning/phases/16-exact-relation-classifier-contract/16-RESEARCH.md
@.planning/phases/16-exact-relation-classifier-contract/16-VALIDATION.md
@packages/occt-core/src/occt-core.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_relation_contract.test.mjs
@test/assembly.step
@test/simple_part.step
@test/bearing.igs
@test/as1_pe_203.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing package tests for exact relation wrapper parity</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_relation_contract.test.mjs, .planning/phases/16-exact-relation-classifier-contract/16-RESEARCH.md</read_first>
  <files>packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `occt-core exposes package-first exact relation classification for pairwise refs` fails until `classifyExactRelation(refA, refB)` forwards the expected exact-ref identity and transforms to the root carrier.
    - Test 2: a subtest named `occt-core relation classification preserves none and explicit failure payloads` fails until wrapper results keep `kind: "none"` as success and preserve explicit failure DTOs unchanged.
    - Test 3: a subtest named `occt-core relation wrappers honor occurrence transforms for repeated geometry` fails until a live repeated-geometry or transformed-occurrence case proves the wrapper preserves relation correctness through instance transforms.
  </behavior>
  <action>Extend `packages/occt-core/test/core.test.mjs` with the exact subtest names from `<behavior>` for wrapper shape and none/failure parity. Extend `packages/occt-core/test/live-root-integration.test.mjs` with one live relation smoke test that uses the built root carrier and repeated geometry or transformed occurrences to prove relation results remain stable in occurrence space.</action>
  <acceptance_criteria>
    - `rg -n "occt-core exposes package-first exact relation classification for pairwise refs|occt-core relation classification preserves none and explicit failure payloads|occt-core relation wrappers honor occurrence transforms for repeated geometry" packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `npm --prefix packages/occt-core test` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix packages/occt-core test</automated>
  </verify>
  <done>The package test surface now locks relation wrapper parity and repeated-geometry behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement package-first relation wrappers and parity behavior</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_relation_contract.test.mjs, .planning/phases/16-exact-relation-classifier-contract/16-RESEARCH.md</read_first>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: `@tx-code/occt-core` exports a package-first wrapper method named `classifyExactRelation(refA, refB)`.
    - Test 2: wrappers validate exact refs, forward occurrence transforms unchanged, and attach `refA` / `refB` on successful relation results, including `kind: "none"`.
    - Test 3: when the root relation result includes local support geometry, wrappers normalize that geometry into occurrence space without hiding root semantics.
    - Test 4: repeated-geometry live tests remain green against the built root carrier.
  </behavior>
  <action>Add `classifyExactRelation(refA, refB)` to `packages/occt-core/src/occt-core.js`. Keep the existing pairwise adapter pattern: validate refs, ensure the root module exposes `ClassifyExactRelation`, forward transforms unchanged, preserve explicit failures, and attach `refA` / `refB` on success. Normalize any local support geometry only if the root contract exposes it in local space. Do not invent viewer-only abstractions or collapse `kind: "none"` into an error path.</action>
  <acceptance_criteria>
    - `rg -n "classifyExactRelation|ClassifyExactRelation|none" packages/occt-core/src/occt-core.js packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `node --test test/exact_relation_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/exact_relation_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>`occt-core` now exposes occurrence-safe relation classification with explicit none/failure parity and live repeated-geometry proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root relation result -> package wrapper | If wrapper semantics collapse `none` into a failure or mutate support geometry incorrectly, downstream consumers lose the classifier contract. |
| local support geometry -> occurrence-space package result | Incorrect transform handling would corrupt relation presentation hints for reused geometry instances. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | T/R | package wrapper parity | mitigate | Lock wrapper shape, `none`, and failure preservation in unit tests before implementation. |
| T-16-02-02 | T/R | occurrence-space relation behavior | mitigate | Prove repeated-geometry or transformed-occurrence relation behavior in live package integration tests. |
</threat_model>

<verification>
- Run `node --test test/exact_relation_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
Downstream JS consumers can classify exact relations through `@tx-code/occt-core` with occurrence-safe wrapper behavior, explicit `none`, and stable parity with the root carrier.
</success_criteria>

<output>
After completion, create `.planning/phases/16-exact-relation-classifier-contract/16-02-SUMMARY.md`
</output>
