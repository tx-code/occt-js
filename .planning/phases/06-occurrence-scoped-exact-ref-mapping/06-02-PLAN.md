---
phase: 06-occurrence-scoped-exact-ref-mapping
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - package.json
  - packages/occt-core/src/index.js
  - packages/occt-core/src/exact-model-normalizer.js
  - packages/occt-core/src/exact-ref-resolver.js
  - packages/occt-core/test/exact-ref-resolver.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - test/exact_ref_mapping_contract.test.mjs
autonomous: true
requirements:
  - REF-02
must_haves:
  truths:
    - "`occt-core` resolves existing node/geometry/topology ids into occurrence-scoped exact refs without introducing a second viewer-specific id system."
    - "Repeated geometry instances share one exact definition handle but keep distinct occurrence context."
    - "Invalid handle, invalid topology id, and occurrence mismatch cases fail explicitly instead of silently resolving the wrong element."
  artifacts:
    - "packages/occt-core/src/exact-model-normalizer.js"
    - "packages/occt-core/src/exact-ref-resolver.js"
    - "packages/occt-core/test/exact-ref-resolver.test.mjs"
    - "packages/occt-core/test/live-root-integration.test.mjs"
    - "test/exact_ref_mapping_contract.test.mjs"
  key_links:
    - "exactGeometryBindings -> normalizeExactOpenResult"
    - "normalizeExactOpenResult -> resolveExactElementRef"
    - "resolveExactElementRef -> existing nodeId / geometryId / elementId data"
---

<objective>
Expose core-side ref resolution from normalized model data and repeated-call validation.

Purpose: finish Phase 6 by turning the root exact-definition binding payload into occurrence-scoped exact refs that downstream JS can reuse across repeated measurement calls without inventing a viewer-specific id system.
Output: `occt-core` exact-model normalization, occurrence-scoped exact-ref resolution helpers, explicit failure DTOs, and repeated-geometry validation through unit and live integration tests.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-occurrence-scoped-exact-ref-mapping/06-RESEARCH.md
@.planning/phases/06-occurrence-scoped-exact-ref-mapping/06-VALIDATION.md
@AGENTS.md
@package.json
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/model-normalizer.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_ref_mapping_contract.test.mjs
@test/assembly.step
@E:/Coding/imos-app/packages/viewer-occt/src/loadOcctModel.ts
@E:/Coding/imos-app/packages/viewer-babylon/src/web-runtime.ts
@E:/Coding/imos-app/packages/viewer-babylon/src/bindOcctMeshSelection.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing occt-core tests for occurrence-scoped exact-ref resolution</name>
  <read_first>packages/occt-core/src/model-normalizer.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_ref_mapping_contract.test.mjs, .planning/phases/06-occurrence-scoped-exact-ref-mapping/06-RESEARCH.md</read_first>
  <files>packages/occt-core/test/exact-ref-resolver.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `resolveExactElementRef returns an occurrence-scoped face ref from normalized model data` fails until `occt-core` can map `nodeId + geometryId + faceId` to a success DTO containing `exactModelId`, `exactShapeHandle`, `nodeId`, `geometryId`, `kind`, `elementId`, and a 16-number `transform`.
    - Test 2: a subtest named `reused geometry instances share exactShapeHandle but keep distinct occurrence context` fails until two different assembly nodes that reuse one geometry resolve to refs with the same `exactShapeHandle` but different `nodeId` and/or `transform`.
    - Test 3: a subtest named `invalid geometry or topology ids fail with explicit ref-resolution codes` fails until missing geometry ids, invalid face/edge/vertex ids, and node/geometry mismatches return explicit `{ ok: false, code: "invalid-handle" | "invalid-id" | "occurrence-mismatch", message: ... }` DTOs.
  </behavior>
  <action>Create `packages/occt-core/test/exact-ref-resolver.test.mjs` as a Node built-in test file using stub exact-open payloads and normalized model fixtures. Add the exact subtest names from `<behavior>`. Cover success for a face ref, success for reused geometry under two nodes, and failures for invalid geometry ids, invalid topology ids, released/unknown exact-model handles, and node/geometry mismatches. Extend `packages/occt-core/test/live-root-integration.test.mjs` with a live `assembly.step` smoke test that opens the root exact lane, normalizes the exact-open payload, resolves two occurrence-scoped refs on reused geometry, and asserts shared definition handle plus distinct occurrence context.</action>
  <acceptance_criteria>
    - `rg -n "resolveExactElementRef returns an occurrence-scoped face ref from normalized model data|reused geometry instances share exactShapeHandle but keep distinct occurrence context|invalid geometry or topology ids fail with explicit ref-resolution codes" packages/occt-core/test/exact-ref-resolver.test.mjs` returns matches.
    - `rg -n "exactShapeHandle|occurrence-mismatch|invalid-id|assembly.step" packages/occt-core/test/exact-ref-resolver.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `npm --prefix packages/occt-core test` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix packages/occt-core test</automated>
  </verify>
  <done>`occt-core` now has a failing test surface that defines the expected occurrence-scoped ref DTOs, failure codes, and reused-geometry behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact-open normalization and explicit ref resolution in occt-core</name>
  <read_first>packages/occt-core/src/index.js, packages/occt-core/src/model-normalizer.js, packages/occt-core/test/exact-ref-resolver.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_ref_mapping_contract.test.mjs, E:/Coding/imos-app/packages/viewer-occt/src/loadOcctModel.ts, E:/Coding/imos-app/packages/viewer-babylon/src/web-runtime.ts, E:/Coding/imos-app/packages/viewer-babylon/src/bindOcctMeshSelection.ts, .planning/phases/06-occurrence-scoped-exact-ref-mapping/06-RESEARCH.md</read_first>
  <files>package.json, packages/occt-core/src/index.js, packages/occt-core/src/exact-model-normalizer.js, packages/occt-core/src/exact-ref-resolver.js, packages/occt-core/test/exact-ref-resolver.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_ref_mapping_contract.test.mjs</files>
  <behavior>
    - Test 1: `normalizeExactOpenResult(...)` preserves the current normalized model shape while attaching geometry-id keyed exact-definition bindings and `exactModelId`.
    - Test 2: `resolveExactElementRef(...)` accepts existing `nodeId`, `geometryId`, `kind`, and `elementId` values and returns an occurrence-scoped exact ref DTO without requiring viewer-specific ids.
    - Test 3: repeated geometry instances resolve to the same definition handle but keep distinct occurrence context.
    - Test 4: `package.json` adds `node --test test/exact_ref_mapping_contract.test.mjs` to the normal root test chain if it is not already present.
  </behavior>
  <action>Add `packages/occt-core/src/exact-model-normalizer.js` to adapt a raw exact-open result into a normalized exact-model object keyed by the existing normalized `geometryId` strings and carrying `exactModelId` plus exact-definition bindings. Add `packages/occt-core/src/exact-ref-resolver.js` with a generic `resolveExactElementRef(...)` helper and optional face/edge/vertex convenience helpers that validate `exactModelId`, `nodeId`, `geometryId`, `kind`, and `elementId`, then return either a success DTO or an explicit failure DTO with `invalid-handle`, `invalid-id`, or `occurrence-mismatch`. Export the new helpers from `packages/occt-core/src/index.js`. Use the existing normalized node tree to compute occurrence-scoped transforms; do not add viewer-specific selection ids. Extend live-root integration and root contract coverage as needed, and wire `test/exact_ref_mapping_contract.test.mjs` into the root `npm test` script if Phase 6 adds new root assertions not yet covered by the default suite.</action>
  <acceptance_criteria>
    - `rg -n "normalizeExactOpenResult|resolveExactElementRef|occurrence-mismatch|invalid-id" packages/occt-core/src/index.js packages/occt-core/src/exact-model-normalizer.js packages/occt-core/src/exact-ref-resolver.js` returns matches.
    - `rg -n "exact_ref_mapping_contract.test.mjs" package.json` returns a match inside the `test` script.
    - `npm --prefix packages/occt-core test` passes.
    - `npm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test"</automated>
  </verify>
  <done>Downstream JS can resolve occurrence-scoped exact refs from the existing normalized model ids through `occt-core`, with explicit failures for invalid handles, ids, and node/geometry mismatches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root exact-definition bindings -> `occt-core` normalization | `occt-core` must preserve definition identity while adding occurrence context. |
| normalized model ids -> exact ref resolver | Existing scene-graph ids cross into measurement-facing ref DTOs here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T/R | exact ref resolver | mitigate | Validate every `nodeId`, `geometryId`, `kind`, and `elementId` input and return explicit failure DTOs instead of silent fallback. |
| T-06-02-02 | S/T | reused geometry instances | mitigate | Keep definition identity (`exactShapeHandle`) separate from occurrence context (`nodeId` + `transform`) and prove both in live tests. |
| T-06-02-03 | D | released exact-model handles | mitigate | Preserve `invalid-handle` behavior when ref resolution receives a released or unknown `exactModelId`. |
</threat_model>

<verification>
- Run `npm --prefix packages/occt-core test`.
- Run `npm test`.
</verification>

<success_criteria>
`occt-core` can resolve the existing exported scene ids into occurrence-scoped exact refs with explicit failure semantics, and repeated geometry instances preserve shared definition identity plus distinct occurrence context.
</success_criteria>

<output>
After completion, create `.planning/phases/06-occurrence-scoped-exact-ref-mapping/06-02-SUMMARY.md`
</output>
