---
phase: 04-release-governance-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - DIST-01
must_haves:
  truths:
    - "Maintainers have one canonical root release verification command they can run before a root release lands."
    - "The canonical root release command covers the Windows Wasm build, governance contracts, `occt-core`, and the full root runtime suite without requiring demo, Babylon, or Tauri checks."
    - "The authoritative release command surface is locked by an automated governance contract test."
  artifacts:
    - "package.json"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "package.json -> scripts.test:release:root"
    - "test/release_governance_contract.test.mjs -> package.json scripts"
    - "scripts.test:release:root -> build:wasm:win / contract tests / occt-core test / npm test"
---

<objective>
Establish the authoritative root release verification command surface and the first governance contract that enforces it.

Purpose: Satisfy DIST-01 by giving maintainers one runtime-first command that future docs, skills, and planning artifacts can reference without ambiguity.
Output: A new governance contract test plus a canonical `package.json` release verification script for the root Wasm carrier.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-governance-flow/04-RESEARCH.md
@.planning/phases/04-release-governance-flow/04-VALIDATION.md
@.planning/phases/01-wasm-build-dist-baseline/01-01-SUMMARY.md
@.planning/phases/03-downstream-consumption-contract/03-01-SUMMARY.md
@AGENTS.md
@package.json
@packages/occt-core/package.json
@test/wasm_build_contract.test.mjs
@test/package_tarball_contract.test.mjs
@test/dist_contract_consumers.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the governance contract test for the authoritative root release command</name>
  <read_first>test/release_governance_contract.test.mjs, package.json, test/wasm_build_contract.test.mjs, test/package_tarball_contract.test.mjs, packages/occt-core/package.json, AGENTS.md, .planning/phases/04-release-governance-flow/04-RESEARCH.md</read_first>
  <files>test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: A subtest named `authoritative root release command surface stays runtime-first` fails unless `package.json` exposes `scripts["test:release:root"]`.
    - Test 2: The subtest requires the exact runtime-first chain `npm run build:wasm:win && node --test test/wasm_build_contract.test.mjs test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs && npm --prefix packages/occt-core test && npm test`.
    - Test 3: A subtest named `authoritative root release command surface excludes unconditional secondary-surface gates` fails if the command string contains `demo`, `playwright`, `tauri`, or `occt-babylon`.
  </behavior>
  <action>Create `test/release_governance_contract.test.mjs` as a Node built-in test file that reads `package.json` and asserts the authoritative root release command surface. Use the exact subtest names from `<behavior>` so later plans can extend the same file. Lock the command string to `npm run build:wasm:win && node --test test/wasm_build_contract.test.mjs test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs && npm --prefix packages/occt-core test && npm test`. Keep Plan 01 limited to the root command surface only; do not add README, skill, or `.planning` assertions yet.</action>
  <acceptance_criteria>
    - `rg -n "authoritative root release command surface stays runtime-first|authoritative root release command surface excludes unconditional secondary-surface gates|test:release:root|npm run build:wasm:win|npm --prefix packages/occt-core test|npm test" test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs` passes after Task 2 completes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The repository has a dedicated governance contract test that defines the authoritative root release command surface and rejects unconditional secondary-surface gates.</done>
</task>

<task type="auto">
  <name>Task 2: Add the canonical `test:release:root` command to `package.json`</name>
  <read_first>package.json, test/release_governance_contract.test.mjs, .planning/phases/04-release-governance-flow/04-VALIDATION.md, AGENTS.md</read_first>
  <files>package.json</files>
  <action>Add a new root script named `test:release:root` to `package.json` with the exact command `npm run build:wasm:win && node --test test/wasm_build_contract.test.mjs test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs && npm --prefix packages/occt-core test && npm test`. Leave `test:wasm:preflight` and `test` intact as the existing fast/full maintainer entrypoints. Do not add unconditional `demo`, `playwright`, `tauri`, or Babylon-package commands to this release script.</action>
  <acceptance_criteria>
    - `npm pkg get scripts.test:release:root` prints `"npm run build:wasm:win && node --test test/wasm_build_contract.test.mjs test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs && npm --prefix packages/occt-core test && npm test"`.
    - `rg -n "\"test:release:root\": \"npm run build:wasm:win && node --test test/wasm_build_contract.test.mjs test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs && npm --prefix packages/occt-core test && npm test\"" package.json` returns a match.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$script = npm pkg get scripts.test:release:root; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/release_governance_contract.test.mjs"</automated>
  </verify>
  <done>`package.json` exposes one authoritative runtime-first root release verification command and the governance test enforces that contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> package.json scripts | Release verification intent crosses into executable command strings here. |
| package.json -> test/release_governance_contract.test.mjs | Static governance checks must detect drift in the authoritative release command surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | T/R | `package.json` release scripts | mitigate | Lock `scripts["test:release:root"]` to one exact chain in `test/release_governance_contract.test.mjs`. |
| T-04-01-02 | D | Root release verification flow | mitigate | Make the governance test fail if unconditional `demo`, `playwright`, `tauri`, or Babylon checks creep into the canonical root release command. |
</threat_model>

<verification>
- Run `npm pkg get scripts.test:release:root`.
- Run `node --test test/release_governance_contract.test.mjs`.
</verification>

<success_criteria>
`package.json` contains one canonical root release verification command, and the new governance contract test proves that the command stays runtime-first and free of unconditional secondary-surface gates.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-governance-flow/04-01-SUMMARY.md`
</output>
