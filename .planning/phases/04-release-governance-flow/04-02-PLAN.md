---
phase: 04-release-governance-flow
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - README.md
  - packages/occt-core/README.md
  - AGENTS.md
  - .codex/skills/releasing-occt-js/SKILL.md
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - CONS-03
  - DIST-02
must_haves:
  truths:
    - "Public docs and the repo release skill point to `npm run test:release:root` as the authoritative root release gate."
    - "Babylon, demo, and Tauri verification stays conditional on touched secondary surfaces rather than becoming a blanket root release prerequisite."
    - "The root Wasm carrier and `occt-core` remain the documented primary product surfaces."
  artifacts:
    - "README.md"
    - "packages/occt-core/README.md"
    - "AGENTS.md"
    - ".codex/skills/releasing-occt-js/SKILL.md"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "README.md -> npm run test:release:root"
    - "AGENTS.md -> root tests first / secondary-surface checks conditional"
    - ".codex/skills/releasing-occt-js/SKILL.md -> release workflow and selective secondary-surface verification"
---

<objective>
Reconcile runtime-first docs and the repository release skill with the authoritative root release command surface.

Purpose: Satisfy CONS-03 and the public-doc portion of DIST-02 by removing unconditional secondary-surface release gates and making the root Wasm carrier the clear authority in docs and release workflow guidance.
Output: Governance test coverage for doc/skill wording plus aligned updates to README, `occt-core` README, AGENTS, and the release skill.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-governance-flow/04-RESEARCH.md
@.planning/phases/03-downstream-consumption-contract/03-03-SUMMARY.md
@README.md
@packages/occt-core/README.md
@AGENTS.md
@.codex/skills/releasing-occt-js/SKILL.md
@package.json
@test/release_governance_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the governance contract test to cover runtime-first docs and release skill rules</name>
  <read_first>test/release_governance_contract.test.mjs, README.md, packages/occt-core/README.md, AGENTS.md, .codex/skills/releasing-occt-js/SKILL.md, package.json, .planning/phases/04-release-governance-flow/04-RESEARCH.md</read_first>
  <files>test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: A subtest named `release docs keep the root Wasm carrier authoritative` requires `README.md`, `packages/occt-core/README.md`, and `AGENTS.md` to mention `npm run test:release:root` and preserve the root-carrier-first framing.
    - Test 2: A subtest named `release skill keeps secondary surfaces conditional` requires `.codex/skills/releasing-occt-js/SKILL.md` to use `npm run test:release:root` as the unconditional release verification command.
    - Test 3: The same release-skill subtest fails if the skill still requires unconditional `cd demo; npm run build`, `npx playwright test`, `demo/src/hooks/useOcct.js` release scope, or mandatory Babylon/Tauri verification for root-only releases.
  </behavior>
  <action>Extend `test/release_governance_contract.test.mjs` with the exact subtest names from `<behavior>`. Read `README.md`, `packages/occt-core/README.md`, `AGENTS.md`, and `.codex/skills/releasing-occt-js/SKILL.md`, then assert the runtime-first contract: `npm run test:release:root` is the authoritative root release gate, the root Wasm carrier remains authoritative, and secondary-surface checks are conditional. Add explicit negative assertions for unconditional `cd demo; npm run build`, `npx playwright test`, mandatory `demo/src/hooks/useOcct.js` release scope, and blanket Babylon/Tauri gates.</action>
  <acceptance_criteria>
    - `rg -n "release docs keep the root Wasm carrier authoritative|release skill keeps secondary surfaces conditional|npm run test:release:root|cd demo; npm run build|npx playwright test|demo/src/hooks/useOcct.js" test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs` passes after Task 2 completes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The governance contract test now enforces runtime-first public docs and a release skill that keeps secondary surfaces conditional.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs and the release skill around the runtime-first release boundary</name>
  <read_first>README.md, packages/occt-core/README.md, AGENTS.md, .codex/skills/releasing-occt-js/SKILL.md, package.json, test/release_governance_contract.test.mjs</read_first>
  <files>README.md, packages/occt-core/README.md, AGENTS.md, .codex/skills/releasing-occt-js/SKILL.md</files>
  <action>Update `README.md`, `packages/occt-core/README.md`, `AGENTS.md`, and `.codex/skills/releasing-occt-js/SKILL.md` so they all point to `npm run test:release:root` as the canonical root release verification command. In `AGENTS.md`, keep `npm run test:wasm:preflight` and `npm test` as day-to-day root entrypoints but add `npm run test:release:root` as the release gate, and restate that demo/viewer checks stay conditional on touched secondary surfaces. In the root README and `packages/occt-core` README, add concise release-boundary language stating that Babylon/demo/Tauri remain optional secondaries. In the release skill, remove unconditional demo/Babylon/Tauri release gates, remove hard-coded mandatory `demo/src/hooks/useOcct.js` scope for every root release, and replace the verification section with unconditional `npm run test:release:root` plus a separate conditional section that only runs demo/browser/Tauri checks when files under `demo/`, `demo/src-tauri/`, `packages/occt-babylon-loader/`, `packages/occt-babylon-viewer/`, or `packages/occt-babylon-widgets/` are touched.</action>
  <acceptance_criteria>
    - `rg -n "npm run test:release:root" README.md packages/occt-core/README.md AGENTS.md .codex/skills/releasing-occt-js/SKILL.md` returns matches in all four files.
    - `rg -n "secondary surfaces|optional secondaries|conditional" README.md packages/occt-core/README.md AGENTS.md .codex/skills/releasing-occt-js/SKILL.md` returns matches that describe the conditional boundary.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>Docs and the repository release skill consistently present the root Wasm carrier as authoritative and treat demo/Babylon/Tauri verification as conditional secondary-surface work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> release docs and skill | Humans rely on docs and skill wording to decide what is required before publishing. |
| release skill -> release execution | Incorrect mandatory steps here can block or misdirect root-only releases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | D | `.codex/skills/releasing-occt-js/SKILL.md` | mitigate | Require `npm run test:release:root` as the only unconditional root release gate and forbid unconditional demo/Babylon/Tauri checks in the governance test. |
| T-04-02-02 | S/R | `README.md`, `AGENTS.md`, `packages/occt-core/README.md` | mitigate | Lock the root-carrier-first wording and conditional secondary-surface guidance in `test/release_governance_contract.test.mjs`. |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs`.
- Run `rg -n "npm run test:release:root" README.md packages/occt-core/README.md AGENTS.md .codex/skills/releasing-occt-js/SKILL.md`.
</verification>

<success_criteria>
The repo’s public docs and release skill all point at the same canonical root release command and no longer make demo, Babylon, or Tauri checks unconditional for root-only releases.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-governance-flow/04-02-SUMMARY.md`
</output>
