---
phase: 04-release-governance-flow
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - DIST-02
must_haves:
  truths:
    - "Planning artifacts identify the root Wasm carrier as the authoritative product surface and keep future GSD routing aligned to that boundary."
    - "Requirement traceability reflects completed Phase 1-4 work instead of leaving prior completed requirements marked pending."
    - "Project and state wording stay aligned to the same runtime-first core value."
  artifacts:
    - ".planning/PROJECT.md"
    - ".planning/REQUIREMENTS.md"
    - ".planning/ROADMAP.md"
    - ".planning/STATE.md"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - ".planning/REQUIREMENTS.md -> traceability table"
    - ".planning/ROADMAP.md -> Phase 4 canonical refs and success criteria"
    - ".planning/STATE.md -> core value and current focus"
---

<objective>
Reconcile planning traceability and state so future GSD work sees the same runtime-first contract that the repo docs and release skill enforce.

Purpose: Finish the planning-artifact portion of DIST-02 by removing traceability drift across `.planning` files and aligning state language with the root Wasm carrier.
Output: Governance test coverage for planning files plus synchronized updates to PROJECT, REQUIREMENTS, ROADMAP, and STATE.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-governance-flow/04-RESEARCH.md
@.planning/phases/04-release-governance-flow/04-VALIDATION.md
@test/release_governance_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the governance contract test to cover planning traceability and state alignment</name>
  <read_first>test/release_governance_contract.test.mjs, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/04-release-governance-flow/04-RESEARCH.md</read_first>
  <files>test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: A subtest named `planning artifacts keep requirement traceability current` requires `.planning/REQUIREMENTS.md` to mark `CORE-01` through `DIST-02` as `Completed`.
    - Test 2: A subtest named `planning state stays aligned to the root Wasm carrier` requires `.planning/PROJECT.md` and `.planning/STATE.md` to share the exact core value string `Downstream applications can reliably consume the OCCT Wasm runtime and its root API contract without build drift or packaging surprises.`.
    - Test 3: The planning-state subtest requires `.planning/ROADMAP.md` Phase 4 refs or success criteria to mention `test/release_governance_contract.test.mjs` and `npm run test:release:root`.
  </behavior>
  <action>Extend `test/release_governance_contract.test.mjs` with the exact subtest names from `<behavior>`. Read the four `.planning` files and add strict assertions for completed requirement statuses, the shared runtime-first core value string, and Phase 4 roadmap references to both `test/release_governance_contract.test.mjs` and `npm run test:release:root`. Keep the assertions precise enough to fail on traceability drift, but limited to stable governance strings rather than volatile progress counters.</action>
  <acceptance_criteria>
    - `rg -n "planning artifacts keep requirement traceability current|planning state stays aligned to the root Wasm carrier|Downstream applications can reliably consume the OCCT Wasm runtime and its root API contract without build drift or packaging surprises.|npm run test:release:root|test/release_governance_contract.test.mjs" test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs` passes after Task 2 completes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The governance contract test now enforces planning traceability, roadmap references, and shared runtime-first state language.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize PROJECT, REQUIREMENTS, ROADMAP, and STATE with the finalized Phase 4 contract</name>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, test/release_governance_contract.test.mjs, README.md, AGENTS.md</read_first>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Update `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` together. In `.planning/REQUIREMENTS.md`, change the traceability table status for `CORE-01`, `CORE-02`, `CORE-03`, `CORE-04`, `CONS-01`, `CONS-02`, `CONS-03`, `DIST-01`, and `DIST-02` from `Pending` to `Completed`. In `.planning/PROJECT.md`, add or update decision/outcome language so the runtime-first root release command `npm run test:release:root` and conditional secondary-surface verification are explicit project decisions, and remove stale `— Pending` outcomes for already-established workflow decisions. In `.planning/ROADMAP.md`, update the Phase 4 canonical refs or success criteria to include `test/release_governance_contract.test.mjs` and `npm run test:release:root` while preserving Phase 4 ownership of `CONS-03`, `DIST-01`, and `DIST-02`. In `.planning/STATE.md`, replace the current core-value wording with the exact string `Downstream applications can reliably consume the OCCT Wasm runtime and its root API contract without build drift or packaging surprises.` and update the current-focus sentence so it refers to the root Wasm carrier and `npm run test:release:root`, not a shared viewer/runtime stack.</action>
  <acceptance_criteria>
    - `rg -n "\\| CORE-01 \\| Phase 1 \\| Completed \\||\\| CONS-02 \\| Phase 3 \\| Completed \\||\\| DIST-02 \\| Phase 4 \\| Completed \\|" .planning/REQUIREMENTS.md` returns matches.
    - `rg -n "npm run test:release:root|test/release_governance_contract.test.mjs" .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md` returns matches.
    - `rg -n "Downstream applications can reliably consume the OCCT Wasm runtime and its root API contract without build drift or packaging surprises\\." .planning/PROJECT.md .planning/STATE.md` returns matches in both files.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The planning corpus reflects completed requirement coverage, shared runtime-first product framing, and Phase 4’s canonical governance references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifacts -> future GSD planners/executors | Future automation relies on `.planning` files as the source of truth for requirements, phase routing, and core project framing. |
| repository state -> human maintainers | Humans use `.planning/STATE.md` and `.planning/ROADMAP.md` to decide what is complete and what to run next. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | T/R | `.planning/REQUIREMENTS.md` traceability table | mitigate | Require exact `Completed` status strings for `CORE-01` through `DIST-02` in the governance contract test. |
| T-04-03-02 | S/R | `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` | mitigate | Lock the shared runtime-first core value plus Phase 4 references to `npm run test:release:root` and `test/release_governance_contract.test.mjs` in the governance contract test. |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs`.
- Run `rg -n "Completed" .planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
The `.planning` corpus no longer drifts from the implemented repository contract: completed requirements are marked completed, the roadmap references the new governance surfaces, and state/project language matches the root Wasm carrier mission.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-governance-flow/04-03-SUMMARY.md`
</output>
