---
phase: 11-appearance-governance-downstream-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/occt-core/README.md
  - dist/occt-js.d.ts
  - AGENTS.md
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - ADAPT-04
must_haves:
  truths:
    - "Root/package docs describe `colorMode`, `defaultColor`, and the built-in CAD fallback as part of the runtime contract."
    - "Docs and agent guidance keep user settings persistence and viewer overrides explicitly downstream."
    - "Public typing comments and governance tests agree on the finalized appearance option shape."
  artifacts:
    - "README.md"
    - "packages/occt-core/README.md"
    - "dist/occt-js.d.ts"
    - "AGENTS.md"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "root README -> packaged appearance contract"
    - "occt-core README -> adapter normalization plus downstream boundary"
    - "AGENTS + d.ts comments -> runtime-first scope guidance"
---

<objective>
Finalize the appearance contract docs and type-facing guidance.

Purpose: satisfy the docs-and-typings half of ADAPT-04 by making the shipped import appearance contract visible, package-first, and explicit about downstream boundaries.
Output: root/package docs, agent guidance, type comments, and governance tests that describe the finalized appearance option shape.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-appearance-governance-downstream-contract/11-RESEARCH.md
@.planning/phases/11-appearance-governance-downstream-contract/11-VALIDATION.md
@README.md
@packages/occt-core/README.md
@dist/occt-js.d.ts
@AGENTS.md
@test/release_governance_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing governance tests for appearance docs and finalized typing guidance</name>
  <read_first>README.md, packages/occt-core/README.md, dist/occt-js.d.ts, AGENTS.md, test/release_governance_contract.test.mjs, .planning/phases/11-appearance-governance-downstream-contract/11-RESEARCH.md</read_first>
  <files>test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `release docs describe the import appearance contract and downstream settings boundary` fails until the root README, `occt-core` README, and AGENTS guidance all mention `colorMode`, `defaultColor`, and that app-side settings persistence or viewer overrides remain downstream.
    - Test 2: a subtest named `published typings document the finalized import appearance option shape` fails until the public `dist/occt-js.d.ts` comments and type surface clearly expose `colorMode`, `defaultColor`, the built-in fallback color, and legacy `readColors` precedence.
  </behavior>
  <action>Extend `test/release_governance_contract.test.mjs` with the exact subtest names from `<behavior>`. Assert that README/package docs mention `colorMode`, `defaultColor`, the built-in default CAD color `[0.9, 0.91, 0.93]`, and that app settings persistence or viewer overrides are downstream concerns. Assert that `dist/occt-js.d.ts` documents `colorMode`, `defaultColor`, and legacy `readColors` precedence on the public type surface.</action>
  <acceptance_criteria>
    - `rg -n "release docs describe the import appearance contract and downstream settings boundary|published typings document the finalized import appearance option shape" test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The governance tests define the required docs and public typing guidance before any prose is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update root/package docs and type comments to match the shipped appearance contract</name>
  <read_first>README.md, packages/occt-core/README.md, dist/occt-js.d.ts, AGENTS.md, test/release_governance_contract.test.mjs, .planning/phases/11-appearance-governance-downstream-contract/11-RESEARCH.md</read_first>
  <files>README.md, packages/occt-core/README.md, dist/occt-js.d.ts, AGENTS.md, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: root/package examples prefer `colorMode` and `defaultColor` over boolean-only `readColors`.
    - Test 2: docs keep the built-in fallback `[0.9, 0.91, 0.93]` and legacy `readColors` precedence explicit.
    - Test 3: docs and AGENTS guidance state that apps own settings persistence and viewers own repaint/display behavior.
    - Test 4: the public typing comments remain concise but now describe the finalized appearance option shape and precedence clearly.
  </behavior>
  <action>Update `README.md` and `packages/occt-core/README.md` so their import examples show `colorMode` and `defaultColor` in package-first form, note the built-in fallback color when `defaultColor` is omitted, and state that app-side settings persistence plus viewer overrides stay downstream. Add a short repo-level note in `AGENTS.md` that appearance options are part of the root runtime contract while settings persistence and post-import presentation remain outside scope. Tighten `dist/occt-js.d.ts` comments only as needed to align with the finalized docs contract. Do not expand runtime behavior in this phase.</action>
  <acceptance_criteria>
    - `rg -n "colorMode|defaultColor|0\\.9, 0\\.91, 0\\.93|settings persistence|viewer overrides|readColors" README.md packages/occt-core/README.md dist/occt-js.d.ts AGENTS.md` returns matches.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The root/package docs and type comments now describe the shipped appearance contract and its boundaries clearly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped runtime contract -> published docs | Doc drift here misleads downstream consumers about how appearance options actually work. |
| repo-level agent guidance -> future maintenance behavior | If agent guidance omits the appearance boundary, later phases may reintroduce viewer-scope drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | R/I | docs contract | mitigate | Add failing governance tests before editing README/package docs. |
| T-11-01-02 | R/T | type comments / agent guidance | mitigate | Lock `dist/occt-js.d.ts` and `AGENTS.md` wording to the shipped runtime boundary. |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root/package docs and typing comments describe the shipped import appearance contract without widening scope
</success_criteria>

<output>
After completion, create `.planning/phases/11-appearance-governance-downstream-contract/11-01-SUMMARY.md`
</output>
