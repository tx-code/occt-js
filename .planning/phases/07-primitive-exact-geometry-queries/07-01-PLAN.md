---
phase: 07-primitive-exact-geometry-queries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-model-store.hpp
  - src/exact-model-store.cpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - packages/occt-core/src/index.js
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/live-root-integration.test.mjs
  - test/exact_primitive_queries_contract.test.mjs
autonomous: true
requirements:
  - REF-03
  - MEAS-03
must_haves:
  truths:
    - "Root exact queries classify analytic edge and face refs in definition space without learning occurrence ids."
    - "Radius and center primitives come from retained exact geometry definitions, not mesh-derived approximations."
    - "`occt-core` adapts occurrence-scoped exact refs into world-space classification/radius/center results without hiding the root exact contract."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/exact-query.hpp"
    - "src/exact-query.cpp"
    - "src/js-interface.cpp"
    - "packages/occt-core/src/occt-core.js"
    - "test/exact_primitive_queries_contract.test.mjs"
  key_links:
    - "exactModelId + exactShapeHandle -> exact-query helper"
    - "exact-query helper -> js-interface exact primitive bindings"
    - "resolveExactElementRef -> occt-core primitive-query wrappers"
---

<objective>
Add geometry classification plus exact radius and center primitives.

Purpose: satisfy REF-03 and the analytic-family half of MEAS-03 by turning retained exact refs into definition-scoped primitive geometry queries, while keeping occurrence adaptation in `occt-core`.
Output: root exact query bindings for classification, radius, and center; typings for their success/failure DTOs; and thin `occt-core` wrappers that consume occurrence-scoped exact refs and return world-space primitive results.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-primitive-exact-geometry-queries/07-RESEARCH.md
@.planning/phases/07-primitive-exact-geometry-queries/07-VALIDATION.md
@AGENTS.md
@CMakeLists.txt
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-model-store.hpp
@src/exact-model-store.cpp
@src/js-interface.cpp
@src/orientation.cpp
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/exact-ref-resolver.js
@packages/occt-core/test/live-root-integration.test.mjs
@test/load_occt_factory.mjs
@test/cube_10x10.igs
@test/bearing.igs
@test/simple_part.step
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract tests for exact geometry classification, radius, and center</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, src/orientation.cpp, packages/occt-core/src/occt-core.js, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/07-primitive-exact-geometry-queries/07-RESEARCH.md, .planning/phases/07-primitive-exact-geometry-queries/07-VALIDATION.md</read_first>
  <files>test/exact_primitive_queries_contract.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact queries classify analytic plane and line refs from retained exact geometry` fails until a simple analytic fixture produces `ok === true` classifications for at least one planar face and one linear edge.
    - Test 2: a subtest named `exact queries return radius and center for circular or cylindrical refs` fails until an analytic fixture produces `ok === true` radius and center results with numeric values and 3D anchors.
    - Test 3: a subtest named `unsupported exact primitive requests fail explicitly` fails until non-applicable radius/center requests return `{ ok: false, code: "unsupported-geometry", message: ... }`.
    - Test 4: a subtest named `occt-core adapts primitive query points and directions into occurrence space` fails until a live exact ref resolved through `occt-core` can classify and return world-space center/axis data without exposing occurrence ids to wasm.
  </behavior>
  <action>Create `test/exact_primitive_queries_contract.test.mjs` as a Node built-in test file that loads the built root carrier via `test/load_occt_factory.mjs`, opens one line/plane-friendly fixture such as `test/cube_10x10.igs` or `test/simple_part.step` plus one circle/cylinder-friendly fixture such as `test/bearing.igs`, and adds the exact subtest names from `<behavior>`. Resolve a face id and edge id from the returned topology payload, use `exactGeometryBindings` to address the retained exact definition, and assert classification plus radius/center success and explicit unsupported failures. Extend `packages/occt-core/test/live-root-integration.test.mjs` with a live adapter smoke test that opens an exact model, resolves an occurrence-scoped exact ref, calls the new `occt-core` primitive-query wrappers, and asserts returned points/directions are 3D arrays in occurrence space.</action>
  <acceptance_criteria>
    - `rg -n "exact queries classify analytic plane and line refs from retained exact geometry|exact queries return radius and center for circular or cylindrical refs|unsupported exact primitive requests fail explicitly|occt-core adapts primitive query points and directions into occurrence space" test/exact_primitive_queries_contract.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `rg -n "exactGeometryBindings|MeasureExactRadius|MeasureExactCenter|unsupported-geometry" test/exact_primitive_queries_contract.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `node --test test/exact_primitive_queries_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_primitive_queries_contract.test.mjs</automated>
  </verify>
  <done>The root and `occt-core` contract tests prove the expected classification/radius/center surface and explicit unsupported-geometry failures before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement root exact classification, radius, and center queries plus thin occt-core wrappers</name>
  <read_first>CMakeLists.txt, dist/occt-js.d.ts, src/importer.hpp, src/exact-model-store.hpp, src/exact-model-store.cpp, src/js-interface.cpp, src/orientation.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, packages/occt-core/src/exact-ref-resolver.js, test/exact_primitive_queries_contract.test.mjs, .planning/phases/07-primitive-exact-geometry-queries/07-RESEARCH.md</read_first>
  <files>CMakeLists.txt, dist/occt-js.d.ts, src/importer.hpp, src/exact-model-store.hpp, src/exact-model-store.cpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, packages/occt-core/test/live-root-integration.test.mjs, test/exact_primitive_queries_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports exact classification, radius, and center query functions that accept retained exact definition identity and return structured `ok` success/failure DTOs, including released-handle failures from the retained store.
    - Test 2: analytic families map to string enums such as `line`, `circle`, `plane`, `cylinder`, `sphere`, `cone`, `torus`, or `unknown`.
    - Test 3: radius and center queries use OCCT analytic geometry, not mesh topology, and return numeric primitives plus 3D anchor or axis data.
    - Test 4: `occt-core` wrappers accept occurrence-scoped exact refs, forward definition identity to wasm, and transform returned points/directions into occurrence space where applicable.
  </behavior>
  <action>Add an isolated helper layer such as `src/exact-query.hpp` / `src/exact-query.cpp` that resolves retained exact geometry definitions from `exactModelId + exactShapeHandle`, maps definition-local topology ids back to exact `TopoDS_Face` or `TopoDS_Edge` elements, and uses `BRepAdaptor_Surface`, `BRepAdaptor_Curve`, `BRep_Tool`, and related analytic helpers to classify element families and compute radius/center primitives. Extend `src/exact-model-store.hpp` / `.cpp` as needed to expose validated retained exact geometry lookup without leaking occurrence concerns. In `src/js-interface.cpp`, add exact query bindings and structured DTO serialization, and update `dist/occt-js.d.ts` accordingly. In `packages/occt-core/src/occt-core.js`, add thin primitive-query wrapper methods that accept an occurrence-scoped exact ref, call the new root bindings, and transform returned points/directions into occurrence space using the ref transform. Export the new wrappers from `packages/occt-core/src/index.js`. Keep pairwise measurement composition and viewer/session semantics out of scope.</action>
  <acceptance_criteria>
    - `rg -n "MeasureExactRadius|MeasureExactCenter|geometryType" src/js-interface.cpp dist/occt-js.d.ts packages/occt-core/src/occt-core.js` returns matches.
    - `rg -n "BRepAdaptor_Curve|BRepAdaptor_Surface|GeomAbs_|radius|center" src/exact-query.hpp src/exact-query.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_primitive_queries_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_primitive_queries_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The root carrier and `occt-core` now expose exact geometry classification, radius, and center primitives on retained exact refs without widening into pairwise measurement semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| occurrence-scoped exact ref -> definition-scoped wasm query | `occt-core` strips occurrence state here and forwards only validated retained exact definition identity. |
| retained exact definition -> analytic geometry classification | Incorrect exact-shape resolution would cause valid ids to classify or measure the wrong element. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | T/R | retained exact-query lookup | mitigate | Resolve queries through the exact-model store and explicit exact-shape-handle validation, then lock it with root contract tests. |
| T-07-01-02 | S/T | wasm/public boundary | mitigate | Keep root queries definition-scoped and add `occt-core` wrappers for occurrence adaptation instead of teaching wasm about viewer ids. |
| T-07-01-03 | I | unsupported geometry cases | mitigate | Return explicit `unsupported-geometry` failures instead of mesh approximations or guessed analytic families. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_primitive_queries_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
Downstream exact refs can be classified into primitive analytic families and can retrieve exact radius/center primitives through the retained exact-model lane, while occurrence-space adaptation remains in `occt-core`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-primitive-exact-geometry-queries/07-01-SUMMARY.md`
</output>
