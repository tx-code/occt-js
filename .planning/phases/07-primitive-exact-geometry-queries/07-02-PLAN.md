---
phase: 07-primitive-exact-geometry-queries
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-model-store.hpp
  - src/exact-model-store.cpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - packages/occt-core/src/index.js
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/live-root-integration.test.mjs
  - test/exact_primitive_queries_contract.test.mjs
autonomous: true
requirements:
  - MEAS-03
must_haves:
  truths:
    - "Root exact queries compute edge length and face area from retained exact definitions with unit-consistent results."
    - "Face-normal evaluation stays exact, accepts a query point, and fails explicitly when the point or geometry is unsupported."
    - "`occt-core` handles world/local query-point transforms for face-normal evaluation instead of teaching wasm about occurrence state."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/exact-query.hpp"
    - "src/exact-query.cpp"
    - "src/js-interface.cpp"
    - "packages/occt-core/src/occt-core.js"
    - "test/exact_primitive_queries_contract.test.mjs"
  key_links:
    - "exact ref transform -> occt-core local/world conversion"
    - "exact-query helper -> edge-length / face-area / face-normal bindings"
    - "live-root integration -> transformed repeated-geometry validation"
---

<objective>
Add exact edge length, face area, and face-normal evaluation contracts.

Purpose: finish the single-entity half of MEAS-03 by exposing exact length, area, and normal queries on retained exact refs, while keeping transform-aware adaptation in `occt-core`.
Output: root exact query bindings for edge length, face area, and face-normal evaluation; typed success/failure DTOs; and `occt-core` wrappers that convert query points and returned vectors between local definition space and world occurrence space.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-primitive-exact-geometry-queries/07-RESEARCH.md
@.planning/phases/07-primitive-exact-geometry-queries/07-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-model-store.hpp
@src/exact-model-store.cpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/exact-ref-resolver.js
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_primitive_queries_contract.test.mjs
@test/cube_10x10.igs
@test/bearing.igs
@test/assembly.step
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract tests for exact edge length, face area, and face-normal evaluation</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, packages/occt-core/src/occt-core.js, packages/occt-core/test/live-root-integration.test.mjs, test/exact_primitive_queries_contract.test.mjs, .planning/phases/07-primitive-exact-geometry-queries/07-RESEARCH.md</read_first>
  <files>test/exact_primitive_queries_contract.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact queries return edge length and face area from retained exact refs` fails until at least one edge and one face return `ok === true` numeric results from the retained exact lane.
    - Test 2: a subtest named `exact face normal evaluation accepts a query point and returns a stable normal` fails until a face-normal call with an explicit query point returns an `ok === true` result containing a 3D normal.
    - Test 3: a subtest named `exact face normal failures stay explicit for invalid points or unsupported geometry` fails until invalid query points or non-face targets return explicit `query-out-of-range` or `unsupported-geometry` failures.
    - Test 4: a subtest named `occt-core face normal wrappers invert occurrence transforms for repeated geometry` fails until a live repeated-geometry exact ref can evaluate a face normal from a world-space query point without losing occurrence context.
  </behavior>
  <action>Extend `test/exact_primitive_queries_contract.test.mjs` with the exact subtest names from `<behavior>`, using a plane/line-friendly fixture for length/area plus an analytic face fixture for normal evaluation. Assert edge-length and face-area success DTOs, explicit face-normal failures for invalid or unsupported cases, and stable vector output for a valid face query. Extend `packages/occt-core/test/live-root-integration.test.mjs` with a repeated-geometry smoke test that resolves an occurrence-scoped face ref, passes a world-space query point into the new `occt-core` face-normal wrapper, and asserts the returned normal remains a 3D vector in occurrence space.</action>
  <acceptance_criteria>
    - `rg -n "exact queries return edge length and face area from retained exact refs|exact face normal evaluation accepts a query point and returns a stable normal|exact face normal failures stay explicit for invalid points or unsupported geometry|occt-core face normal wrappers invert occurrence transforms for repeated geometry" test/exact_primitive_queries_contract.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `rg -n "MeasureExactEdgeLength|MeasureExactFaceArea|EvaluateExactFaceNormal|query-out-of-range|unsupported-geometry" test/exact_primitive_queries_contract.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `node --test test/exact_primitive_queries_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_primitive_queries_contract.test.mjs</automated>
  </verify>
  <done>The root and adapter test surface now proves the intended edge-length/area/normal contract and explicit face-normal failure behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact edge length, face area, and face-normal query bindings plus transform-aware occt-core wrappers</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/exact-model-store.hpp, src/exact-model-store.cpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, test/exact_primitive_queries_contract.test.mjs, .planning/phases/07-primitive-exact-geometry-queries/07-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/exact-model-store.hpp, src/exact-model-store.cpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, packages/occt-core/test/live-root-integration.test.mjs, test/exact_primitive_queries_contract.test.mjs</files>
  <behavior>
    - Test 1: root exact query bindings return structured success/failure DTOs for edge length, face area, and face-normal evaluation.
    - Test 2: edge length and face area use exact OCCT properties rather than mesh approximations.
    - Test 3: face-normal evaluation accepts a point in definition space, returns a normalized vector, and fails explicitly when the point cannot be evaluated on the trimmed face.
    - Test 4: `occt-core` wrappers accept world-space query points, convert them to definition space, and transform returned normals back into occurrence space for repeated geometry instances.
  </behavior>
  <action>Extend the exact query helper layer to measure edge length and face area via exact OCCT property APIs and to evaluate face normals on exact faces given a definition-space query point. Prefer projection plus trimmed-face validation rather than evaluating on the infinite carrier surface. Add root bindings and DTO serialization in `src/js-interface.cpp`, update `dist/occt-js.d.ts`, and preserve the current lifecycle/ref-mapping contracts. In `packages/occt-core/src/occt-core.js`, add transform-aware wrappers for edge length, face area, and especially face-normal evaluation, including local/world point conversion and world-space normal transformation for occurrence-scoped exact refs. Export the new wrappers from `packages/occt-core/src/index.js`, and validate them through live repeated-geometry integration without expanding into pairwise attach geometry or measurement-session behavior.</action>
  <acceptance_criteria>
    - `rg -n "MeasureExactEdgeLength|MeasureExactFaceArea|EvaluateExactFaceNormal|normal|centroid|value" src/js-interface.cpp dist/occt-js.d.ts packages/occt-core/src/occt-core.js` returns matches.
    - `rg -n "BRepGProp|LinearProperties|SurfaceProperties|normal|query point" src/exact-query.hpp src/exact-query.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_primitive_queries_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
    - `npm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_primitive_queries_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test"</automated>
  </verify>
  <done>The exact-measurement foundation now includes exact edge-length, face-area, and face-normal query contracts on retained exact refs, with transform-aware `occt-core` wrappers for downstream app use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| world-space query point -> definition-space exact face evaluation | `occt-core` converts downstream world coordinates into exact local coordinates here. |
| exact face evaluation -> world-space normal result | Returned exact normals must be transformed back into occurrence space without mixing in translation or stale transforms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T/R | exact property queries | mitigate | Use retained exact definitions and OCCT property APIs, then lock length/area results with root contract tests. |
| T-07-02-02 | T/I | face-normal evaluation | mitigate | Return explicit `query-out-of-range` and `unsupported-geometry` failures instead of silently snapping to mesh normals. |
| T-07-02-03 | S/T | occurrence transform adaptation | mitigate | Keep local/world transform logic in `occt-core` and prove it with repeated-geometry integration tests. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_primitive_queries_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
- Run `npm test`.
</verification>

<success_criteria>
Downstream exact refs can retrieve exact edge length, face area, and evaluated face normals through the retained exact-model lane, and `occt-core` can adapt world/local coordinates without teaching wasm about occurrence state.
</success_criteria>

<output>
After completion, create `.planning/phases/07-primitive-exact-geometry-queries/07-02-SUMMARY.md`
</output>
