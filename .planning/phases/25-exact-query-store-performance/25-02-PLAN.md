---
phase: 25-exact-query-store-performance
plan: "02"
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/importer-iges-staging.hpp
  - src/importer-iges-staging.cpp
  - src/importer-xde.cpp
  - src/orientation.cpp
  - test/test_iges_degenerated_edges.mjs
  - test/test_optimal_orientation_api.mjs
  - test/test_perf_exact_workflows.mjs
  - package.json
autonomous: true
requirements:
  - PERF-01
  - PERF-02
must_haves:
  truths:
    - "IGES temporary-file staging behavior is implemented once and reused by both import and orientation paths."
    - "Shared IGES staging keeps file-based fallback behavior while improving collision safety and cleanup determinism."
    - "Phase 25 publishes a repeatable large-model performance visibility lane without adding brittle timing thresholds to default release gates."
  artifacts:
    - path: src/importer-iges-staging.hpp
      provides: shared IGES staging contract
      contains: helper declarations for temporary-file write/read/cleanup flow and error shaping
    - path: src/importer-iges-staging.cpp
      provides: single staging implementation
      contains: collision-safe temporary file naming, write handling, and guaranteed cleanup behavior
    - path: src/importer-xde.cpp
      provides: import XDE IGES integration
      contains: shared staging helper usage replacing duplicated inline temp-file logic
    - path: src/orientation.cpp
      provides: orientation IGES integration
      contains: shared staging helper usage with behavior parity and deterministic cleanup
    - path: test/test_perf_exact_workflows.mjs
      provides: explicit performance visibility lane
      contains: repeatable query/store and IGES staging benchmark reporting with stable metric schema
    - path: package.json
      provides: maintainer command routing
      contains: discoverable perf command wiring kept outside authoritative root release gate
  key_links:
    - from: src/importer-xde.cpp
      to: src/importer-iges-staging.hpp
      via: shared IGES ReadFile staging path
      pattern: ReadAndTransferXde|ReadIgesFromMemoryViaTempFile
    - from: src/orientation.cpp
      to: src/importer-iges-staging.hpp
      via: shared IGES ReadFile staging path
      pattern: ReadAndTransferXde|ReadIgesFromMemoryViaTempFile
    - from: test/test_perf_exact_workflows.mjs
      to: src/importer-xde.cpp
      via: repeatable IGES staging performance lane
      pattern: ImportModel|ImportStepFile|ImportIgesFile
---

<objective>
Reduce IGES import-staging overhead and drift while adding repeatable large-model performance visibility.

Purpose: complete the import-staging half of `PERF-01` and the benchmark visibility half of `PERF-02` by converging duplicate IGES temp-file logic and shipping an explicit perf lane.
Output: one shared IGES staging implementation used by both runtime paths, deterministic behavior parity tests, and a maintainer-facing perf command with stable reporting.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-exact-query-store-performance/25-CONTEXT.md
@.planning/phases/25-exact-query-store-performance/25-PATTERNS.md
@.planning/phases/25-exact-query-store-performance/25-01-PLAN.md
@AGENTS.md
@src/importer-xde.cpp
@src/orientation.cpp
@src/importer-iges.cpp
@test/test_iges_degenerated_edges.mjs
@test/test_optimal_orientation_api.mjs
@package.json

<interfaces>
From `src/importer-xde.cpp` (current duplicated IGES staging):
```cpp
std::string tempFileName = fileName.empty() ? "temp_input.igs" : (fileName + ".tmp.igs");
std::ofstream tmpFile(tempFileName, std::ios::binary);
IFSelect_ReturnStatus readStatus = cafReader.ReadFile(tempFileName.c_str());
std::remove(tempFileName.c_str());
```

From `src/orientation.cpp` (same duplicated branch):
```cpp
const std::string tempFileName = fileName.empty() ? "temp_input.igs" : (fileName + ".tmp.igs");
std::ofstream tmpFile(tempFileName, std::ios::binary);
const IFSelect_ReturnStatus readStatus = cafReader.ReadFile(tempFileName.c_str());
std::remove(tempFileName.c_str());
```

From `package.json` (no explicit perf lane yet):
```json
"scripts": {
  "test": "npm run test:wasm:preflight && ...",
  "test:release:root": "npm run build:wasm:win && ..."
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for shared IGES staging parity and explicit perf-lane contract</name>
  <read_first>test/test_iges_degenerated_edges.mjs, test/test_optimal_orientation_api.mjs, package.json, .planning/phases/25-exact-query-store-performance/25-CONTEXT.md</read_first>
  <files>test/test_iges_degenerated_edges.mjs, test/test_optimal_orientation_api.mjs, test/test_perf_exact_workflows.mjs, package.json</files>
  <behavior>
    - Test 1: extend IGES import/orientation script coverage to assert deterministic behavior parity for representative IGES fixtures after staging refactor.
    - Test 2: add a dedicated perf script (`test/test_perf_exact_workflows.mjs`) that emits stable JSON metrics for exact-query/store loops and IGES staging loops.
    - Test 3: wire a discoverable perf command in `package.json` (for example `test:perf:exact`) and ensure the script can run without becoming part of unconditional release gates.
  </behavior>
  <action>Create the contract first: deterministic behavioral checks in existing IGES-focused scripts plus an explicit perf lane with stable output schema (`scenario`, `iterations`, `elapsedMs`, `opsPerSecond`, and metadata). Keep this lane repeatable and informative, not threshold-gated by default.</action>
  <acceptance_criteria>
    - `rg -n "test:perf:exact" package.json` returns matches.
    - `node test/test_perf_exact_workflows.mjs` fails before implementation wiring and passes after Task 2.
    - `node test/test_iges_degenerated_edges.mjs` and `node test/test_optimal_orientation_api.mjs` keep passing after refactor.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node test/test_iges_degenerated_edges.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_optimal_orientation_api.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_perf_exact_workflows.mjs"</automated>
  </verify>
  <done>IGES staging parity and large-model perf visibility requirements are locked before staging internals are changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared IGES staging utility and integrate both import/orientation paths</name>
  <read_first>src/importer-xde.cpp, src/orientation.cpp, src/importer-iges.cpp, test/test_iges_degenerated_edges.mjs, test/test_optimal_orientation_api.mjs, test/test_perf_exact_workflows.mjs</read_first>
  <files>src/importer-iges-staging.hpp, src/importer-iges-staging.cpp, src/importer-xde.cpp, src/orientation.cpp, test/test_iges_degenerated_edges.mjs, test/test_optimal_orientation_api.mjs, test/test_perf_exact_workflows.mjs, package.json</files>
  <behavior>
    - Test 1: both XDE import and orientation IGES paths call one shared helper for temp-file staging and cleanup.
    - Test 2: helper uses collision-safe temporary file names and guarantees cleanup on both success and failure paths.
    - Test 3: file-based IGES fallback remains authoritative in Phase 25; no `ReadStream` switch is introduced.
    - Test 4: perf lane reports stable metrics for maintainers while preserving default root test stability.
  </behavior>
  <action>Extract duplicated IGES staging logic into one internal utility used by both `ReadAndTransferXde` implementations. Preserve existing error strings and transfer semantics unless a targeted wording adjustment is required for shared helper parity. Add any minimal script updates needed so maintainers can run the perf lane independently (`npm run test:perf:exact`).</action>
  <acceptance_criteria>
    - `rg -n "ReadIgesFromMemoryViaTempFile|importer-iges-staging" src/importer-xde.cpp src/orientation.cpp src/importer-iges-staging.hpp src/importer-iges-staging.cpp` returns matches.
    - `npm run test:wasm:preflight` passes.
    - `node test/test_iges_degenerated_edges.mjs` passes.
    - `node test/test_optimal_orientation_api.mjs` passes.
    - `node test/test_perf_exact_workflows.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run test:wasm:preflight; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_iges_degenerated_edges.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_optimal_orientation_api.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_perf_exact_workflows.mjs"</automated>
  </verify>
  <done>IGES staging behavior is centralized, deterministic, and benchmark-visible for large-model workflows without widening release gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory IGES payload -> temp-file staging helper | Poor staging hygiene can cause collisions, leftover files, or failed imports in concurrent/large workflows |
| staging helper -> importer/orientation consumers | Divergent integration can produce behavior drift between import and orientation for the same source data |
| perf-lane reporting -> maintainer release decisions | Unstable metrics schemas or hidden command drift can hide regressions until downstream integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | T/R | shared IGES staging helper | mitigate | Centralize helper logic, preserve deterministic cleanup and error handling, and verify parity across both callers |
| T-25-02-02 | D | large-model IGES workflows | mitigate | Use collision-safe temp naming and explicit perf lane metrics to surface throughput regressions |
| T-25-02-03 | R/I | perf-lane contract visibility | mitigate | Keep a stable output schema and explicit command wiring outside unconditional release gates |
</threat_model>

<verification>
- Run `npm run test:wasm:preflight`.
- Run `node test/test_iges_degenerated_edges.mjs`.
- Run `node test/test_optimal_orientation_api.mjs`.
- Run `node test/test_perf_exact_workflows.mjs`.
</verification>

<success_criteria>
Import and orientation IGES paths share one temp-file staging implementation with deterministic behavior, and maintainers can run a repeatable perf lane that makes large-model staging/query regressions visible.
</success_criteria>

<output>
After completion, create `.planning/phases/25-exact-query-store-performance/25-02-SUMMARY.md`
</output>
