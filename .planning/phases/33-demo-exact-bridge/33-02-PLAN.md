---
phase: 33-demo-exact-bridge
plan: "02"
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - demo/src/hooks/usePicking.js
  - demo/src/store/viewerStore.js
  - demo/src/components/SelectionPanel.jsx
  - demo/src/lib/exact-selection-bridge.js
  - demo/tests/demo.spec.mjs
  - demo/tests/viewer-store.test.mjs
  - demo/tests/exact-selection-bridge.test.mjs
autonomous: true
requirements:
  - DEMO-02
must_haves:
  truths:
    - "Face, edge, and vertex picks are enriched into measurement-ready selection records that resolve to occurrence-safe `OcctExactRef` data without leaking Babylon ids into the contract."
    - "The bridge uses stable OCCT-side identifiers such as `geometryId`, exact bindings, element kind/id, and rendered occurrence transform rather than viewer-local ids."
    - "Selection-derived exact refs are invalidated immediately on session disposal, model replacement, or incompatible presentation changes."
  artifacts:
    - path: demo/src/hooks/usePicking.js
      provides: selection enrichment seam
      contains: exact-ref resolution, stale-state invalidation, and measurement-ready selection details
    - path: demo/src/lib/exact-selection-bridge.js
      provides: bridge logic isolated from Babylon event plumbing
      contains: geometry/exact binding lookup, occurrence-transform handling, and ref builders for face/edge/vertex picks
    - path: demo/src/store/viewerStore.js
      provides: measurement-ready selection state
      contains: selection bridge payloads and clear/invalidation actions
    - path: demo/src/components/SelectionPanel.jsx
      provides: current selection consumer
      contains: any minimal additive detail needed to debug or preserve enriched selection data without turning the panel into a measurement UI
    - path: demo/tests/exact-selection-bridge.test.mjs
      provides: focused bridge unit coverage
      contains: geometryId/exact-binding mapping, transform handling, and invalidation behavior
    - path: demo/tests/demo.spec.mjs
      provides: browser selection regression coverage
      contains: face/edge/vertex pick smoke paths that prove enriched selection state is produced safely
  key_links:
    - from: demo/src/hooks/usePicking.js
      to: demo/src/hooks/useViewer.js
      via: rendered mesh metadata already carries `geometryId` and the viewer already exposes `meshGeoMapRef`
      pattern: meshGeoMapRef|metadata.occt.geometryId
    - from: demo/src/lib/exact-selection-bridge.js
      to: packages/occt-core/src/occt-core.js
      via: bridge output must match the occurrence-scoped exact-ref contract expected by measurement helpers
      pattern: exactModelId|exactShapeHandle|kind|elementId|transform
    - from: demo/tests/exact-selection-bridge.test.mjs
      to: demo/src/lib/exact-selection-bridge.js
      via: unit tests lock geometry/exact binding lookup without depending on Babylon scene setup
      pattern: geometryId|exactGeometryBindings|transform
---

<objective>
Resolve Babylon selections into occurrence-safe exact refs and store them as measurement-ready demo state.

Purpose: satisfy `DEMO-02` by bridging current face/edge/vertex picking into the exact-ref contract that Phase 34 measurement commands will consume, while keeping Babylon ids and highlight tokens out of the downstream API.
Output: exact-selection bridge logic + enriched selection state + regression coverage.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-demo-exact-bridge/33-CONTEXT.md
@.planning/phases/33-demo-exact-bridge/33-01-PLAN.md
@AGENTS.md
@docs/sdk/measurement.md
@dist/occt-js.d.ts
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/model-normalizer.js
@packages/occt-babylon-loader/src/occt-scene-builder.js
@demo/src/hooks/useViewer.js
@demo/src/hooks/usePicking.js
@demo/src/store/viewerStore.js
@demo/src/components/SelectionPanel.jsx
@demo/tests/demo.spec.mjs
@demo/tests/viewer-store.test.mjs

<interfaces>
From `demo/src/hooks/usePicking.js` (current selection detail contract):
```js
return {
  mode: "face" | "edge" | "vertex",
  id,
  meshUniqueId,
  info,
};
```

From `packages/occt-core/src/occt-core.js` (exact-ref contract downstream measurements expect):
```js
{
  exactModelId,
  exactShapeHandle,
  kind,
  elementId,
  transform,
}
```

From `packages/occt-babylon-loader/src/occt-scene-builder.js` (render metadata already available):
```js
mesh.metadata = {
  occt: { nodeId, kind, geometryId, source: "occt-core" }
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for exact-ref selection enrichment and invalidation</name>
  <read_first>demo/src/hooks/usePicking.js, demo/src/store/viewerStore.js, packages/occt-babylon-loader/src/occt-scene-builder.js, packages/occt-core/src/model-normalizer.js, demo/tests/demo.spec.mjs, demo/tests/viewer-store.test.mjs, .planning/phases/33-demo-exact-bridge/33-CONTEXT.md</read_first>
  <files>demo/tests/exact-selection-bridge.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: a focused bridge unit test fails until `geometryId` plus exact bindings can resolve face/edge/vertex selections into valid exact-ref payloads.
    - Test 2: store-level tests fail until model replacement or exact-session disposal invalidates enriched selection state.
    - Test 3: browser-level selection tests fail until face/edge/vertex picks produce enriched selection records without leaking Babylon-local ids as the only bridge key.
  </behavior>
  <action>Create one dedicated unit suite for the exact-selection bridge so mapping logic is testable without a live Babylon scene. Extend existing demo/store tests only where needed for smoke behavior and invalidation.</action>
  <acceptance_criteria>
    - `rg -n "exact ref|exact-selection|geometryId|exactGeometryBindings|invalidate" demo/tests/exact-selection-bridge.test.mjs demo/tests/viewer-store.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The repo now has red tests that lock selection enrichment and invalidation behavior before the bridge implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement a generic exact-selection bridge and wire it into picking/state flow</name>
  <read_first>demo/src/hooks/usePicking.js, demo/src/store/viewerStore.js, demo/src/hooks/useViewer.js, packages/occt-core/src/occt-core.js, packages/occt-core/src/model-normalizer.js, packages/occt-babylon-loader/src/occt-scene-builder.js, demo/tests/exact-selection-bridge.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</read_first>
  <files>demo/src/hooks/usePicking.js, demo/src/store/viewerStore.js, demo/src/lib/exact-selection-bridge.js, demo/src/components/SelectionPanel.jsx, demo/tests/exact-selection-bridge.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: face/edge/vertex picks resolve to measurement-ready selection items carrying occurrence-safe exact refs or exact-ref-ready payloads.
    - Test 2: the bridge logic stays generic across imported CAD and generated shapes and does not encode tool-library semantics.
    - Test 3: model replacement, exact-session disposal, and selection clearing invalidate enriched refs immediately.
    - Test 4: any additive `SelectionPanel` output remains debug-level or structural only; measurement actions and overlay UI are still deferred to Phase 34.
  </behavior>
  <action>Introduce a small bridge module that maps normalized geometry ids and exact bindings into `OcctExactRef` payloads, wire it into `usePicking`, and store the enriched selection detail in Zustand so Phase 34 can execute measurements without reaching back into Babylon internals.</action>
  <acceptance_criteria>
    - `rg -n "exactRef|exact-selection|geometryId|exactGeometryBindings|selectionBridge" demo/src/hooks/usePicking.js demo/src/store/viewerStore.js demo/src/lib/exact-selection-bridge.js` returns matches.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
    - `npm --prefix demo run test:e2e` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run test:e2e"</automated>
  </verify>
  <done>The demo selection pipeline now emits measurement-ready exact refs that survive only as long as the underlying exact session remains valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Babylon pick result -> exact-ref bridge | If selection mapping depends on viewer-local ids rather than OCCT-side identifiers, measurement actions will be brittle and non-portable |
| exact session lifecycle -> enriched selection state | If invalidation is incomplete, Phase 34 will consume released or stale exact refs and render invalid measurements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | T/I | selection bridge mapping | mitigate | Add focused unit tests for `geometryId`/binding/transform resolution before wiring it into picking |
| T-33-02-02 | R | stale selection invalidation | mitigate | Extend store/browser tests so replacement, reset, and exact-session disposal clear enriched refs immediately |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
</verification>

<success_criteria>
The demo picks faces, edges, and vertices into measurement-ready occurrence-safe exact refs without leaking Babylon-local ids into the downstream contract, and those refs clear deterministically when the exact session becomes invalid.
</success_criteria>

<output>
After completion, create `.planning/phases/33-demo-exact-bridge/33-02-SUMMARY.md`
</output>
