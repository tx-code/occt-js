---
phase: 20-conditional-secondary-surface-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/package.json
  - demo/tests/auto-orient.test.mjs
  - demo/src/lib/auto-orient.js
autonomous: true
requirements:
  - SURF-01
must_haves:
  truths:
    - "`demo/package.json` exposes explicit non-Tauri verification commands so maintainers can discover a stable demo check surface without reaching for Tauri or repo-root-only trivia."
    - "The demo Node-style verification lane is runnable from the manifest instead of failing immediately on the current missing `src/lib/auto-orient.js` drift."
  artifacts:
    - path: demo/package.json
      provides: demo-local verification command surface
      contains: separate `test` and `test:e2e` commands distinct from `tauri:*`
    - path: demo/src/lib/auto-orient.js
      provides: stable auto-orient helper boundary for demo node tests
      contains: exports consumed by `demo/tests/auto-orient.test.mjs` or an equivalent explicit replacement
    - path: demo/tests/auto-orient.test.mjs
      provides: explicit guard on the chosen demo node-test lane
      contains: imports that resolve inside the checked-in demo source tree
  key_links:
    - from: demo/package.json
      to: playwright.config.mjs
      via: manifest-driven E2E command delegation
      pattern: test:e2e
    - from: demo/tests/auto-orient.test.mjs
      to: demo/src/lib/auto-orient.js
      via: node test import that currently drifts
      pattern: auto-orient\.js
---

<objective>
Expose an intentional non-Tauri demo verification surface from `demo/package.json` and make the default demo test lane actually runnable from the current repo state.

Purpose: Close the biggest `SURF-01` hole first by giving the demo the same manifest-first test discoverability the Babylon packages already have, while fixing or intentionally narrowing the broken node-test entrypoint that currently fails on a missing helper import.
Output: Explicit `demo` test commands, a stable node-test target, and a browser E2E entrypoint that reuses the existing root Playwright harness without dragging Tauri into the default demo test path.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-conditional-secondary-surface-verification/20-CONTEXT.md
@.planning/phases/20-conditional-secondary-surface-verification/20-PATTERNS.md
@AGENTS.md
@README.md
@demo/package.json
@demo/tests/auto-orient.test.mjs
@demo/tests/use-occt-runtime-contract.test.mjs
@demo/src/hooks/useOcct.js
@playwright.config.mjs

<interfaces>
From `demo/package.json` (current missing manifest-first verification surface):
```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "tauri:dev": "tauri dev",
  "tauri:build": "tauri build"
}
```

From `playwright.config.mjs` (existing browser E2E harness to reuse rather than duplicate):
```javascript
export default defineConfig({
  testDir: "demo/tests",
  webServer: {
    command: "cd demo && npx vite --port 5173",
    port: 5173,
  },
});
```

From `demo/tests/auto-orient.test.mjs` (current node-lane drift that blocks a simple `node --test demo/tests/*.test.mjs` run):
```javascript
import { applyOrientationToResult, getOcctFormatFromFileName, resolveAutoOrientedResult } from "../src/lib/auto-orient.js";
```

From `demo/tests/use-occt-runtime-contract.test.mjs` (existing narrow node-test style already valid for a manifest-driven demo lane):
```javascript
test("dev local dist lookup targets concrete repo-root runtime files instead of a directory base", () => {
  assert.match(useOcctSource, /new URL\(\s*"\.\.\/\.\.\/\.\.\/dist\/occt-js\.js",\s*import\.meta\.url\s*\)/);
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit non-Tauri demo verification commands and stabilize the selected node lane</name>
  <files>demo/package.json, demo/tests/auto-orient.test.mjs, demo/src/lib/auto-orient.js</files>
  <action>Add a manifest-first demo verification surface with explicit non-Tauri commands such as `test` for the maintained node-style lane and `test:e2e` for browser E2E. Reuse the existing root `playwright.config.mjs` rather than cloning browser config into `demo/`. Because the current direct node invocation fails on `demo/tests/auto-orient.test.mjs` importing a missing `demo/src/lib/auto-orient.js`, repair that drift before exposing the command surface: either restore a real shared helper around the current auto-orient logic or intentionally narrow the node lane to the maintained subset so `npm --prefix demo test` becomes a stable contract instead of a broken alias. Keep `tauri:dev` and `tauri:build` separate, and do not make desktop packaging part of the default demo test command.</action>
  <verify>
    <automated>npm --prefix demo test</automated>
    <automated>npm --prefix demo run test:e2e</automated>
    <automated>npm --prefix demo run build</automated>
  </verify>
  <done>`demo/package.json` exposes explicit non-Tauri verification commands, `npm --prefix demo test` no longer dies on the current missing-helper drift, and browser E2E remains discoverable without pulling Tauri into the default path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo manifest -> maintainer behavior | If `demo/package.json` still lacks test commands, maintainers will keep relying on ad-hoc repo knowledge and may skip demo verification entirely |
| demo node-test lane -> checked-in demo helpers | A manifest command that points at a known broken import is worse than no command because it encodes drift as the "official" path |
| demo verification -> desktop/Tauri surface | Collapsing browser checks and Tauri packaging into one default command would accidentally promote desktop work into the default demo path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T | `demo/package.json` | mitigate | Add explicit non-Tauri `test` and `test:e2e` commands so manifest discovery replaces ad-hoc maintainer knowledge |
| T-20-02 | T | `demo/tests/auto-orient.test.mjs` | mitigate | Repair or intentionally narrow the node-test lane before wiring it to `npm --prefix demo test` |
| T-20-03 | E | demo verification surface | mitigate | Keep browser/node verification separate from `tauri:*` so desktop work does not become the default demo test path |
</threat_model>

<verification>
- `npm --prefix demo test`
- `npm --prefix demo run test:e2e`
- `npm --prefix demo run build`
</verification>

<success_criteria>
- `demo/package.json` exposes explicit non-Tauri verification commands instead of only `dev`, `build`, and `tauri:*`.
- The chosen demo node-test lane is runnable from the manifest and no longer fails immediately on the current missing `src/lib/auto-orient.js` drift.
- Browser E2E remains a distinct manifest command that reuses the existing repo-root Playwright harness.
- Tauri packaging remains a separate conditional surface outside the default demo test path.
</success_criteria>

<output>
After completion, create `.planning/phases/20-conditional-secondary-surface-verification/20-01-SUMMARY.md`
</output>
