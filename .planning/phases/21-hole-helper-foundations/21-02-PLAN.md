---
phase: 21-hole-helper-foundations
plan: "02"
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - test/exact_hole_contract.test.mjs
  - test/simple_hole.step
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/live-root-integration.test.mjs
autonomous: true
requirements:
  - FEAT-03
  - ADAPT-09
must_haves:
  truths:
    - "The root carrier exposes one narrow additive `DescribeExactHole` query keyed by a selected retained exact ref rather than broad topology or discovery APIs."
    - "Supported circular edge refs and cylindrical face refs for cylindrical holes return stable semantic DTOs with explicit unsupported failures for out-of-scope geometry."
    - "`@tx-code/occt-core` live integration proves that `describeExactHole(ref)` stays occurrence-safe against the built root carrier."
  artifacts:
    - path: src/importer.hpp
      provides: additive root hole DTO structs
      contains: success and failure-capable result data reusing frame and anchor vocabulary where appropriate
    - path: dist/occt-js.d.ts
      provides: public carrier typings
      contains: `OcctJSExactHoleResult` and a `DescribeExactHole(...)` module signature
    - path: src/exact-query.hpp
      provides: carrier declaration surface
      contains: `DescribeExactHole(int exactModelId, int exactShapeHandle, const std::string& kind, int elementId)`
    - path: src/exact-query.cpp
      provides: minimal selected-ref hole classification logic
      contains: supported cylindrical-hole normalization, robust semantic extraction, and explicit unsupported/failure handling
    - path: src/js-interface.cpp
      provides: JS binding surface
      contains: DTO serialization and the new Embind binding
    - path: test/exact_hole_contract.test.mjs
      provides: retained-model root contract coverage
      contains: supported edge/face success cases plus unsupported failure assertions
    - path: packages/occt-core/test/live-root-integration.test.mjs
      provides: package-to-carrier parity proof
      contains: one transformed-occurrence smoke test for `describeExactHole(ref)`
  key_links:
    - from: src/exact-query.cpp
      to: src/importer.hpp
      via: additive exact-hole DTO population
      pattern: DescribeExactHole|frame|anchors
    - from: src/js-interface.cpp
      to: dist/occt-js.d.ts
      via: carrier method and DTO parity
      pattern: DescribeExactHole|OcctJSExactHoleResult
    - from: test/exact_hole_contract.test.mjs
      to: test/simple_hole.step
      via: deterministic supported cylindrical hole fixture
      pattern: simple_hole.step
---

<objective>
Add the minimal root carrier support required for supported hole helper cases and prove package parity against the built runtime.

Purpose: finish `FEAT-03` and `ADAPT-09` by implementing one selected-ref root query that can robustly describe supported cylindrical holes without reopening whole-model feature recognition or viewer policy.
Output: additive `DescribeExactHole(...)` carrier bindings and typings, a deterministic root contract test plus stable fixture, and a package live-integration test proving occurrence-space parity for `describeExactHole(ref)`.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-hole-helper-foundations/21-CONTEXT.md
@.planning/phases/21-hole-helper-foundations/21-PATTERNS.md
@.planning/phases/21-hole-helper-foundations/21-01-PLAN.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@packages/occt-core/src/occt-core.js
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_placement_contract.test.mjs
@test/simple_part.step
@test/bearing.igs

<interfaces>
From `src/js-interface.cpp` (existing single-ref binding pattern to mirror):
```cpp
val SuggestExactRadiusPlacementBinding(int exactModelId, int exactShapeHandle, const std::string& kind, int elementId)
{
    return SerializeExactPlacementResult(
        SuggestExactRadiusPlacement(exactModelId, exactShapeHandle, kind, elementId)
    );
}
```

From `dist/occt-js.d.ts` (current single-ref exact query shape):
```ts
export interface OcctJSExactRadiusSuccess {
    ok: true;
    family: "circle" | "cylinder" | "sphere";
    radius: number;
    diameter: number;
    localCenter: [number, number, number];
    localAnchorPoint: [number, number, number];
    localAxisDirection: [number, number, number];
}
```

From `src/exact-query.cpp` (failure style to preserve):
```cpp
return MakeFailure<OcctExactRadiusResult>("unsupported-geometry", "Exact radius is not supported for the requested element.");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root contract and live-integration tests for `DescribeExactHole`</name>
  <read_first>src/exact-query.cpp, src/js-interface.cpp, dist/occt-js.d.ts, packages/occt-core/test/live-root-integration.test.mjs, test/exact_placement_contract.test.mjs, .planning/phases/21-hole-helper-foundations/21-CONTEXT.md</read_first>
  <files>test/exact_hole_contract.test.mjs, test/simple_hole.step, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact hole helper describes a cylindrical hole from a circular edge ref` fails until a supported circular edge ref returns `ok === true`, `kind === "hole"`, `profile === "cylindrical"`, scalar `radius`, scalar `diameter`, `frame`, `anchors`, and `axisDirection`.
    - Test 2: a subtest named `exact hole helper accepts a cylindrical face ref for the same supported hole` fails until a cylindrical face ref on the same feature returns the same semantic profile and stable supporting geometry.
    - Test 3: a subtest named `exact hole helper failures stay explicit for unsupported or non-hole geometry` fails until unsupported selections return `{ ok: false, code, message }` rather than guessed hole payloads.
    - Test 4: a subtest named `occt-core describeExactHole normalizes live hole semantics into occurrence space` fails until the package wrapper returns transformed frame and anchor geometry from the built root carrier for a repeated or transformed occurrence.
  </behavior>
  <action>Create `test/exact_hole_contract.test.mjs` as a Node built-in contract test that loads the built carrier via `test/load_occt_factory.mjs`, opens a deterministic supported-hole fixture named `test/simple_hole.step`, resolves one supported circular edge ref and one supported cylindrical face ref, and calls a root binding named exactly `DescribeExactHole`. Assert the success/failure behaviors from `<behavior>`. Extend `packages/occt-core/test/live-root-integration.test.mjs` with a transformed-occurrence smoke test for `describeExactHole(ref)` using the same built root carrier. Introduce the dedicated fixture only if current retained-model fixtures are not deterministic enough for supported hole cases.</action>
  <acceptance_criteria>
    - `rg -n "exact hole helper describes a cylindrical hole from a circular edge ref|exact hole helper accepts a cylindrical face ref for the same supported hole|exact hole helper failures stay explicit for unsupported or non-hole geometry" test/exact_hole_contract.test.mjs` returns matches.
    - `rg -n "occt-core describeExactHole normalizes live hole semantics into occurrence space|describeExactHole|DescribeExactHole" packages/occt-core/test/live-root-integration.test.mjs test/exact_hole_contract.test.mjs` returns matches.
    - `node --test test/exact_hole_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_hole_contract.test.mjs</automated>
  </verify>
  <done>The root and live package test surfaces now lock the supported-hole carrier contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive root `DescribeExactHole` support and pass live package parity</name>
  <read_first>src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, dist/occt-js.d.ts, test/exact_hole_contract.test.mjs, packages/occt-core/src/occt-core.js, packages/occt-core/test/live-root-integration.test.mjs</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, test/exact_hole_contract.test.mjs, test/simple_hole.step, packages/occt-core/src/occt-core.js, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: the root module exports `DescribeExactHole(exactModelId, exactShapeHandle, kind, elementId)` as an additive selected-ref query.
    - Test 2: supported circular edge refs and cylindrical face refs describing a cylindrical hole return semantic hole data with `kind`, `profile`, `radius`, `diameter`, `frame`, `anchors`, `axisDirection`, and optional `depth` / `isThrough` only when robustly derivable.
    - Test 3: unsupported geometry, non-hole circular geometry, or out-of-scope variants return explicit failures instead of guessed hole payloads.
    - Test 4: `@tx-code/occt-core` live integration proves the wrapper remains occurrence-safe against the built carrier.
  </behavior>
  <action>Add additive exact-hole DTO structs to `src/importer.hpp` and corresponding TypeScript declarations to `dist/occt-js.d.ts`. In `src/exact-query.hpp` and `src/exact-query.cpp`, implement `DescribeExactHole` as a narrow selected-ref semantic query: normalize a supported circular edge or cylindrical face to the underlying cylindrical-hole topology, derive semantic data only when stable, and return explicit failures for out-of-scope geometry. Reuse the shipped placement frame and anchor vocabulary where it fits; do not add generic topology traversal or batch discovery APIs. In `src/js-interface.cpp`, add DTO serialization and the new Embind binding. Adjust `packages/occt-core/src/occt-core.js` only as needed to match the finalized live DTO shape and keep `describeExactHole(ref)` occurrence-safe.</action>
  <acceptance_criteria>
    - `rg -n "DescribeExactHole|OcctJSExactHoleResult|profile|isThrough|depth" src/importer.hpp src/exact-query.hpp src/exact-query.cpp src/js-interface.cpp dist/occt-js.d.ts packages/occt-core/src/occt-core.js` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_hole_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_hole_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The root carrier and `@tx-code/occt-core` now expose supported cylindrical-hole semantics from a single selected ref without widening the runtime boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected retained exact ref -> root semantic classification | Over-broad topology inference could silently turn arbitrary circular geometry into false hole positives |
| root local-space semantic DTO -> package occurrence-space DTO | Incorrect transformation would make live helper outputs wrong for repeated or transformed occurrences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | T/R | `src/exact-query.cpp` selected-ref classification | mitigate | Lock supported edge/face success cases and unsupported failures in a dedicated root contract test with a deterministic fixture |
| T-21-02-02 | T/I | package live occurrence transforms | mitigate | Add a built-carrier integration test proving `describeExactHole(ref)` returns transformed frame and anchor geometry |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_hole_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
The repository exposes supported cylindrical-hole semantics through one additive root query and a package-first `describeExactHole(ref)` wrapper, with explicit failures for unsupported cases and occurrence-safe live integration behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/21-hole-helper-foundations/21-02-SUMMARY.md`
</output>
