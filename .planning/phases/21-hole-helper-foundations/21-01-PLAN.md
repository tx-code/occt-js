---
phase: 21-hole-helper-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/core.test.mjs
autonomous: true
requirements:
  - FEAT-03
  - ADAPT-09
must_haves:
  truths:
    - "`@tx-code/occt-core` exposes a package-first `describeExactHole(ref)` helper over one occurrence-scoped exact ref instead of requiring caller-side ref orchestration or viewer session state."
    - "The helper contract is semantic and occurrence-safe: it forwards one selected ref to a lower-level carrier method, transforms local-space geometry into occurrence space, and preserves explicit typed failures."
    - "Package unit tests lock the helper DTO shape and failure propagation before live carrier work lands."
  artifacts:
    - path: packages/occt-core/src/occt-core.js
      provides: package-first hole helper API
      contains: `describeExactHole(ref)` wrapper that validates refs, calls `DescribeExactHole`, transforms geometric fields into occurrence space, and returns `ref` on success
    - path: packages/occt-core/test/core.test.mjs
      provides: wrapper contract coverage
      contains: mocked tests for success DTO shape, transformed geometry, and explicit unsupported/failure propagation
  key_links:
    - from: packages/occt-core/src/occt-core.js
      to: packages/occt-core/src/exact-ref-resolver.js
      via: occurrence-scoped exact ref contract
      pattern: validateExactRef|resolveExactElementRef
    - from: packages/occt-core/src/occt-core.js
      to: dist/occt-js.d.ts
      via: typed failure and exact DTO conventions
      pattern: ok|code|unsupported-geometry
    - from: packages/occt-core/test/core.test.mjs
      to: packages/occt-core/src/occt-core.js
      via: mocked wrapper parity
      pattern: describeExactHole
---

<objective>
Define the package-first hole helper API and lock its wrapper behavior in `@tx-code/occt-core`.

Purpose: satisfy the package side of `FEAT-03` and `ADAPT-09` first by introducing a semantic helper surface that is occurrence-safe, additive, and explicit about the lower-level carrier dependency.
Output: a package-first `describeExactHole(ref)` wrapper plus mocked package tests that fix the method name, DTO shape, transformed geometry, and failure behavior before any root carrier work lands.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-hole-helper-foundations/21-CONTEXT.md
@.planning/phases/21-hole-helper-foundations/21-PATTERNS.md
@AGENTS.md
@dist/occt-js.d.ts
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/exact-ref-resolver.js
@packages/occt-core/test/core.test.mjs
@docs/sdk/measurement.md

<interfaces>
From `packages/occt-core/src/occt-core.js` (single-ref wrapper pattern to extend):
```javascript
async suggestExactRadiusPlacement(ref) {
  const module = await this._ensureModule();
  const exactRef = validateExactRef(ref, "suggestExactRadiusPlacement");
  ...
  return {
    ...result,
    frame: transformPlacementFrame(exactRef.transform, result.frame),
    anchors: transformPlacementAnchors(exactRef.transform, result.anchors),
    axisDirection: result.axisDirection ? transformDirection(exactRef.transform, result.axisDirection) : result.axisDirection,
    ref: exactRef,
  };
}
```

From `packages/occt-core/test/core.test.mjs` (mocked wrapper-contract style to reuse):
```javascript
const core = createOcctCore({
  factory: async () => ({
    SuggestExactRadiusPlacement: () => ({ ok: true, ... }),
  }),
});
```

From `docs/sdk/measurement.md` (existing boundary to cross narrowly):
```markdown
Downstream concerns include:
- feature semantics such as hole or chamfer recognition
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing mocked wrapper tests for `describeExactHole(ref)`</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, dist/occt-js.d.ts, .planning/phases/21-hole-helper-foundations/21-CONTEXT.md</read_first>
  <files>packages/occt-core/test/core.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `wraps describeExactHole(ref) through occurrence-scoped exact refs` fails until the wrapper validates one exact ref, calls a carrier method named exactly `DescribeExactHole`, and appends `ref` on success.
    - Test 2: a subtest named `transforms describeExactHole semantic geometry into occurrence space` fails until local-space frame vectors, anchors, and any point-valued semantic fields returned by the carrier are transformed into occurrence space.
    - Test 3: a subtest named `describeExactHole preserves explicit unsupported and failure results` fails until `{ ok: false, code, message }` results from the carrier pass through unchanged instead of being normalized into empty success payloads.
    - Test 4: a subtest named `describeExactHole requires an occurrence-scoped exact ref object` fails until invalid wrapper inputs raise the same validation-style errors as the existing exact helper methods.
  </behavior>
  <action>Extend `packages/occt-core/test/core.test.mjs` with the exact subtest names from `<behavior>`. Use mocked module factories, not the live root carrier. Lock the package DTO around a semantic result shape that includes `kind: "hole"`, `profile: "cylindrical"`, scalar `radius` and `diameter`, `frame`, `anchors`, `axisDirection`, optional `depth`, optional `isThrough`, and the validated `ref` object on success. Assert that transformed point-valued fields and frame/anchor geometry are returned in occurrence space when the input ref carries a non-identity transform.</action>
  <acceptance_criteria>
    - `rg -n "wraps describeExactHole\\(ref\\) through occurrence-scoped exact refs|transforms describeExactHole semantic geometry into occurrence space|describeExactHole preserves explicit unsupported and failure results|describeExactHole requires an occurrence-scoped exact ref object" packages/occt-core/test/core.test.mjs` returns matches.
    - `rg -n "DescribeExactHole|profile: \\\"cylindrical\\\"|isThrough|depth" packages/occt-core/test/core.test.mjs` returns matches.
    - `node --test packages/occt-core/test/core.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/occt-core/test/core.test.mjs</automated>
  </verify>
  <done>The package test surface now fixes the public helper name and wrapper-level DTO behavior before carrier work begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `describeExactHole(ref)` as an occurrence-safe package wrapper</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, dist/occt-js.d.ts, .planning/phases/21-hole-helper-foundations/21-PATTERNS.md</read_first>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs</files>
  <behavior>
    - Test 1: `OcctCoreClient` exposes an async method named exactly `describeExactHole(ref)`.
    - Test 2: the wrapper uses the existing single-ref validation path and throws the same class of validation errors for malformed refs.
    - Test 3: success DTOs preserve semantic fields such as `kind`, `profile`, `radius`, `diameter`, `axisDirection`, `frame`, `anchors`, optional `depth`, and optional `isThrough`, while transforming any local-space geometry into occurrence space and appending `ref`.
    - Test 4: explicit carrier failures remain explicit and unchanged.
  </behavior>
  <action>Add `describeExactHole(ref)` to `packages/occt-core/src/occt-core.js`. Follow the existing single-ref exact-helper rhythm: validate the ref, check that the loaded module exposes `DescribeExactHole`, call that method with `exactModelId`, `exactShapeHandle`, `kind`, and `elementId`, return explicit failures unchanged, and transform any local-space semantic geometry into occurrence space on success. Reuse the existing transform helpers for `frame`, `anchors`, vector directions, and point-valued metadata rather than inventing a second transformation path.</action>
  <acceptance_criteria>
    - `rg -n "async describeExactHole\\(ref\\)|DescribeExactHole|isThrough|depth" packages/occt-core/src/occt-core.js` returns matches.
    - `node --test packages/occt-core/test/core.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/occt-core/test/core.test.mjs</automated>
  </verify>
  <done>`@tx-code/occt-core` now publishes the package-first hole helper wrapper with the locked DTO and occurrence-transform behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| occurrence-scoped exact refs -> package helper wrapper | Incorrect validation or transform handling would corrupt downstream semantic geometry before the carrier is even involved |
| carrier DTO -> occurrence-space helper DTO | A wrapper that forgets to transform point- or frame-valued fields would silently return local-space geometry to downstream callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | T/R | `packages/occt-core/src/occt-core.js` | mitigate | Lock the helper name and DTO shape in mocked wrapper tests before live carrier work |
| T-21-01-02 | T/I | occurrence-space wrapper transforms | mitigate | Assert transformed frames, anchors, and point metadata in package tests with a non-identity ref transform |
</threat_model>

<verification>
- Run `node --test packages/occt-core/test/core.test.mjs`.
</verification>

<success_criteria>
Downstream JS can call a package-first `describeExactHole(ref)` helper and rely on a stable semantic DTO plus explicit typed failures, with occurrence transforms preserved and no viewer/session state introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/21-hole-helper-foundations/21-01-SUMMARY.md`
</output>
