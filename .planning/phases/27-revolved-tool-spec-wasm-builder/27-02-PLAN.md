---
phase: 27-revolved-tool-spec-wasm-builder
plan: "02"
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/revolved-tool.hpp
  - src/revolved-tool.cpp
  - src/js-interface.cpp
  - package.json
  - test/revolved_tool_spec_contract.test.mjs
  - test/generated_revolved_tool_contract.test.mjs
autonomous: true
requirements:
  - TOOL-02
  - TOOL-03
  - GEOM-01
must_haves:
  truths:
    - "The root Wasm carrier exposes one additive `BuildRevolvedTool(spec, options)` entrypoint that builds supported strict specs into canonical root scene payloads."
    - "Supported full and partial revolves for representative endmill-like and drill-like specs reuse the shipped scene topology contract (`rootNodes`, `geometries`, `materials`, `warnings`, `stats`) instead of inventing a second mesh/result shape."
    - "Generated tools remain renderable in Phase 27 through a deterministic neutral material fallback and a small additive top-level `generatedTool` metadata block, while stable face-binding semantics stay deferred to Phase 28."
  artifacts:
    - path: dist/occt-js.d.ts
      provides: public build API and result typings
      contains: `BuildRevolvedTool(...)`, build options, build result types, and additive `generatedTool` metadata typing
    - path: src/revolved-tool.hpp
      provides: generated-tool build declarations
      contains: OCCT profile/build entrypoints and generated-tool metadata structs
    - path: src/revolved-tool.cpp
      provides: OCCT generated-tool implementation
      contains: profile edge construction, closure normalization, revolve execution, triangulation reuse, and build diagnostics shaping
    - path: src/js-interface.cpp
      provides: Embind build surface
      contains: JS build-option parsing, build-result serialization, and additive export registration
    - path: test/generated_revolved_tool_contract.test.mjs
      provides: built-runtime build contract coverage
      contains: representative tool-build success cases, canonical scene assertions, partial-revolve coverage, and explicit build-failure behavior
    - path: package.json
      provides: authoritative root command routing
      contains: generated-tool contract tests wired into root test commands without widening secondary-surface gates
  key_links:
    - from: src/revolved-tool.cpp
      to: src/importer-utils.cpp
      via: generated shape meshing and topology extraction reuse
      pattern: TriangulateShape|ExtractMeshFromShape
    - from: src/revolved-tool.cpp
      to: occt/src/BRepPrimAPI/BRepPrimAPI_MakeRevol.hxx
      via: OCCT revolve construction with future history-aware extension path
      pattern: BRepPrimAPI_MakeRevol
    - from: test/generated_revolved_tool_contract.test.mjs
      to: test/test_multi_format_exports.mjs
      via: canonical scene/topology assertions reused for generated models
      pattern: rootNodes|geometries|materials|stats
---

<objective>
Implement the OCCT revolve builder and emit generated tools as canonical root scene payloads.

Purpose: satisfy the build half of `TOOL-03`, finish `TOOL-02`, and satisfy `GEOM-01` by turning the strict validated revolved-tool spec into a rendered root scene result while keeping exact-open/history semantics out of scope until Phase 28.
Output: additive root build API + OCCT revolve pipeline + built-runtime contract tests + root command routing.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-CONTEXT.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-RESEARCH.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-PATTERNS.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-VALIDATION.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-01-PLAN.md
@AGENTS.md
@CMakeLists.txt
@package.json
@dist/occt-js.d.ts
@src/importer.hpp
@src/importer-utils.hpp
@src/importer-utils.cpp
@src/js-interface.cpp
@test/load_occt_factory.mjs
@test/test_multi_format_exports.mjs
@test/revolved_tool_spec_contract.test.mjs
@occt/src/GC/GC_MakeSegment.hxx
@occt/src/GC/GC_MakeArcOfCircle.hxx
@occt/src/BRepBuilderAPI/BRepBuilderAPI_MakeEdge.hxx
@occt/src/BRepBuilderAPI/BRepBuilderAPI_MakeWire.hxx
@occt/src/BRepBuilderAPI/BRepBuilderAPI_MakeFace.hxx
@occt/src/BRepPrimAPI/BRepPrimAPI_MakeRevol.hxx

<interfaces>
From `src/importer-utils.hpp` (scene-meshing seam to reuse):
```cpp
bool TriangulateShape(TopoDS_Shape& shape, const ImportParams& params);
OcctMeshData ExtractMeshFromShape(const TopoDS_Shape& shape);
```

From `src/js-interface.cpp` (canonical scene payload shape to preserve):
```cpp
val result = val::object();
result.set("sourceFormat", sourceFormat);
result.set("rootNodes", rootNodes);
result.set("geometries", geometries);
result.set("materials", materials);
result.set("warnings", val::array());
result.set("stats", stats);
```

From `occt/src/BRepPrimAPI/BRepPrimAPI_MakeRevol.hxx` (kernel and future extension path):
```cpp
Standard_EXPORT BRepPrimAPI_MakeRevol(const TopoDS_Shape& S, const gp_Ax1& A, const Standard_Real D, const Standard_Boolean Copy = Standard_False);
Standard_EXPORT virtual const TopTools_ListOfShape& Generated(const TopoDS_Shape& S) Standard_OVERRIDE;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing built-runtime contract tests for generated revolved-tool scene builds</name>
  <read_first>test/load_occt_factory.mjs, test/test_multi_format_exports.mjs, dist/occt-js.d.ts, src/js-interface.cpp, .planning/phases/27-revolved-tool-spec-wasm-builder/27-RESEARCH.md</read_first>
  <files>test/generated_revolved_tool_contract.test.mjs, test/revolved_tool_spec_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `BuildRevolvedTool builds an endmill-like spec into a canonical generated scene payload` fails until a representative strict full-revolve spec returns `success === true`, `sourceFormat === "generated-revolved-tool"`, non-empty `rootNodes/geometries/materials`, and valid scene stats.
    - Test 2: a subtest named `BuildRevolvedTool builds a drill-like partial revolve and keeps topology invariants intact` fails until a representative partial-revolve spec returns a valid topology payload with consistent `triangleToFaceMap`, face ranges, edge ownership, and non-empty geometry buffers.
    - Test 3: a subtest named `BuildRevolvedTool emits additive generatedTool metadata without claiming stable face bindings yet` fails until the result carries a top-level `generatedTool` block that echoes normalized build facts but does not rely on face order or claim Phase 28 semantics.
    - Test 4: a subtest named `BuildRevolvedTool preserves explicit diagnostics for validation-passing specs that OCCT cannot build` fails until kernel-level build failures remain explicit and typed.
  </behavior>
  <action>Create `test/generated_revolved_tool_contract.test.mjs` using inline spec objects and the built root runtime via `test/load_occt_factory.mjs`. Reuse the canonical scene/topology assertion style from `test/test_multi_format_exports.mjs` rather than snapshotting raw buffers. Adjust `test/revolved_tool_spec_contract.test.mjs` only if shared helper fixtures need to distinguish validation from build-path expectations.</action>
  <acceptance_criteria>
    - `rg -n "BuildRevolvedTool builds an endmill-like spec into a canonical generated scene payload|BuildRevolvedTool builds a drill-like partial revolve and keeps topology invariants intact|BuildRevolvedTool emits additive generatedTool metadata without claiming stable face bindings yet|BuildRevolvedTool preserves explicit diagnostics for validation-passing specs that OCCT cannot build" test/generated_revolved_tool_contract.test.mjs` returns matches.
    - `rg -n "BuildRevolvedTool|generatedTool|generated-revolved-tool|triangleToFaceMap" test/generated_revolved_tool_contract.test.mjs` returns matches.
    - `npm run build:wasm:win && node --test test/generated_revolved_tool_contract.test.mjs` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/generated_revolved_tool_contract.test.mjs"</automated>
  </verify>
  <done>The built root runtime now has a locked scene-build contract for generated revolved tools.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement OCCT revolve construction scene export and root test routing for generated tools</name>
  <read_first>src/revolved-tool.hpp, src/revolved-tool.cpp, src/importer-utils.hpp, src/importer-utils.cpp, src/js-interface.cpp, dist/occt-js.d.ts, package.json, test/generated_revolved_tool_contract.test.mjs, .planning/phases/27-revolved-tool-spec-wasm-builder/27-PATTERNS.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/revolved-tool.hpp, src/revolved-tool.cpp, src/js-interface.cpp, package.json, test/revolved_tool_spec_contract.test.mjs, test/generated_revolved_tool_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports `BuildRevolvedTool(spec, options)` as an additive scene-build API.
    - Test 2: supported strict specs build profile edges with `line`, `arc_center`, and `arc_3pt`, close deterministically with `explicit` or `auto_axis`, revolve around the `Z` axis, and serialize into the canonical root scene payload.
    - Test 3: generated tools remain renderable in Phase 27 via deterministic neutral material fallback and a small additive `generatedTool` metadata block.
    - Test 4: root command routing includes the generated-tool contract tests in `npm test` and `test:release:root` without widening conditional secondary-surface gates.
  </behavior>
  <action>Extend `src/revolved-tool.hpp/.cpp` from validation-only into the full OCCT builder seam: construct profile edges with `GC_MakeSegment` and `GC_MakeArcOfCircle`, normalize `auto_axis` closure deterministically, make a closed wire/face, revolve with `BRepPrimAPI_MakeRevol`, and reuse `TriangulateShape(...)` plus `ExtractMeshFromShape(...)` for the scene output. Serialize the result through `src/js-interface.cpp` into the canonical root scene DTO with `sourceFormat: "generated-revolved-tool"` and a minimal top-level `generatedTool` block. Update `dist/occt-js.d.ts` and route the new root contract tests through `package.json` once both test files exist. Keep exact-retained history and stable face bindings out of this plan.</action>
  <acceptance_criteria>
    - `rg -n "BuildRevolvedTool|generatedTool|generated-revolved-tool|OcctJSRevolvedToolBuildResult" dist/occt-js.d.ts src/revolved-tool.hpp src/revolved-tool.cpp src/js-interface.cpp` returns matches.
    - `rg -n "revolved_tool_spec_contract|generated_revolved_tool_contract" package.json` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/generated_revolved_tool_contract.test.mjs` passes.
    - `npm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/generated_revolved_tool_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test"</automated>
  </verify>
  <done>The repository now builds generated revolved tools into canonical root scene payloads and routes the new root contract tests through the authoritative runtime test surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated normalized spec -> OCCT builder seam | Even validation-passing specs can still trigger null shapes, invalid wires, or unstable solids if OCCT build errors are not handled explicitly |
| generated OCCT shape -> canonical scene payload | A generated-tool builder that bypasses the existing scene/topology invariants can silently break downstream root consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | T/R | OCCT generated-tool build seam | mitigate | Lock representative success cases and explicit build-failure behavior in built-runtime contract tests before implementation |
| T-27-02-02 | T/I | generated scene/result routing | mitigate | Reuse the shipped scene/topology extraction seam and route the new contract tests through root npm commands once implementation lands |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/generated_revolved_tool_contract.test.mjs`.
- Run `npm test`.
</verification>

<success_criteria>
Downstream JS can build representative strict revolved-tool specs through the root Wasm carrier and receive canonical scene data plus explicit build diagnostics, without introducing package-layer adapters or future face-binding claims into Phase 27.
</success_criteria>

<output>
After completion, create `.planning/phases/27-revolved-tool-spec-wasm-builder/27-02-SUMMARY.md`
</output>
