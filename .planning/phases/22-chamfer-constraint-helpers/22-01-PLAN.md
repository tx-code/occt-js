---
phase: 22-chamfer-constraint-helpers
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - test/exact_chamfer_contract.test.mjs
autonomous: true
requirements:
  - FEAT-04
must_haves:
  truths:
    - "`@tx-code/occt-core` exposes a package-first `describeExactChamfer(ref)` helper over one occurrence-scoped exact ref instead of requiring caller-side topology orchestration."
    - "The root carrier exposes one narrow additive `DescribeExactChamfer(...)` query keyed by a selected retained exact ref rather than generic topology or discovery APIs."
    - "Supported planar chamfer face refs return stable semantic DTOs with explicit typed failures for unsupported geometry or topology."
  artifacts:
    - path: packages/occt-core/src/occt-core.js
      provides: package-first chamfer helper API
      contains: `describeExactChamfer(ref)` wrapper that validates refs, calls `DescribeExactChamfer`, transforms local-space geometry into occurrence space, and returns `ref` on success
    - path: src/importer.hpp
      provides: additive root chamfer DTO structs
      contains: success and failure-capable result data reusing frame and anchor vocabulary where appropriate
    - path: dist/occt-js.d.ts
      provides: public carrier typings
      contains: `OcctJSExactChamferResult` and a `DescribeExactChamfer(...)` module signature
    - path: src/exact-query.hpp
      provides: carrier declaration surface
      contains: `DescribeExactChamfer(int exactModelId, int exactShapeHandle, const std::string& kind, int elementId)`
    - path: src/exact-query.cpp
      provides: minimal selected-ref chamfer classification logic
      contains: supported planar chamfer normalization, support-face derivation, semantic extraction, and explicit unsupported/failure handling
    - path: src/js-interface.cpp
      provides: JS binding surface
      contains: DTO serialization and the new Embind binding
    - path: test/exact_chamfer_contract.test.mjs
      provides: retained-model root contract coverage
      contains: supported face-ref success cases plus unsupported failure assertions against shipped fixture geometry
    - path: packages/occt-core/test/core.test.mjs
      provides: mocked wrapper contract coverage
      contains: success DTO shape, transformed geometry, and explicit unsupported/failure propagation for `describeExactChamfer(ref)`
    - path: packages/occt-core/test/live-root-integration.test.mjs
      provides: package-to-carrier parity proof
      contains: one transformed-occurrence smoke test for `describeExactChamfer(ref)`
  key_links:
    - from: src/exact-query.cpp
      to: src/importer.hpp
      via: additive exact-chamfer DTO population
      pattern: DescribeExactChamfer|variant|frame|anchors
    - from: src/js-interface.cpp
      to: dist/occt-js.d.ts
      via: carrier method and DTO parity
      pattern: DescribeExactChamfer|OcctJSExactChamferResult
    - from: test/exact_chamfer_contract.test.mjs
      to: test/ANC101.stp
      via: deterministic planar chamfer candidate reuse
      pattern: ANC101.stp
---

<objective>
Add the minimal root and package support required for selected-ref planar chamfer semantics.

Purpose: satisfy `FEAT-04` by extending the Phase 21 helper pattern to one supported chamfer family without widening the runtime into generic topology discovery or viewer policy.
Output: additive `DescribeExactChamfer(...)` carrier bindings and typings, a package-first `describeExactChamfer(ref)` wrapper, and focused root/package tests proving stable semantic behavior on shipped fixture geometry.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-chamfer-constraint-helpers/22-CONTEXT.md
@.planning/phases/22-chamfer-constraint-helpers/22-PATTERNS.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@packages/occt-core/src/occt-core.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_hole_contract.test.mjs
@test/ANC101.stp

<interfaces>
From `packages/occt-core/src/occt-core.js` (single-ref semantic wrapper pattern to extend):
```javascript
async describeExactHole(ref) {
  const module = await this._ensureModule();
  const exactRef = validateExactRef(ref, "describeExactHole");
  ...
  return {
    ...transformExactHoleResult(exactRef.transform, result),
    ref: exactRef,
  };
}
```

From `src/js-interface.cpp` (single-ref binding pattern to mirror):
```cpp
val DescribeExactHoleBinding(int exactModelId, int exactShapeHandle, const std::string& kind, int elementId)
{
    return SerializeExactHoleResult(
        DescribeExactHole(exactModelId, exactShapeHandle, kind, elementId)
    );
}
```

From `src/exact-query.cpp` (support-face and plane helpers to reuse conceptually):
```cpp
const gp_Pln planeA = surfaceA.Plane();
const gp_Pln planeB = surfaceB.Plane();
result.value = CanonicalAngle(planeA.Axis().Direction().Angle(planeB.Axis().Direction()));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root and package tests for `describeExactChamfer(ref)`</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_hole_contract.test.mjs, .planning/phases/22-chamfer-constraint-helpers/22-CONTEXT.md</read_first>
  <files>packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_chamfer_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `wraps describeExactChamfer(ref) through occurrence-scoped exact refs` fails until the wrapper validates one exact ref, calls a carrier method named exactly `DescribeExactChamfer`, and appends `ref` on success.
    - Test 2: a subtest named `transforms describeExactChamfer semantic geometry into occurrence space` fails until local-space frame vectors, anchors, and any point-valued semantic fields returned by the carrier are transformed into occurrence space.
    - Test 3: a root subtest named `exact chamfer helper describes a supported planar chamfer face ref` fails until a supported face ref returns `ok === true`, `kind === "chamfer"`, `profile === "planar"`, scalar support distances, scalar support angle, `variant`, `frame`, and anchors.
    - Test 4: a root subtest named `exact chamfer helper failures stay explicit for unsupported or non-chamfer planar faces` fails until unsupported selections return `{ ok: false, code, message }` instead of guessed chamfer payloads.
    - Test 5: a live package subtest named `occt-core describeExactChamfer normalizes live chamfer semantics into occurrence space` fails until the package wrapper returns transformed frame and anchor geometry from the built root carrier.
  </behavior>
  <action>Extend `packages/occt-core/test/core.test.mjs` with mocked wrapper tests for `describeExactChamfer(ref)` using the exact subtest names from `<behavior>`. Create `test/exact_chamfer_contract.test.mjs` as a retained-model root contract suite that loads the built carrier, opens `test/ANC101.stp`, finds a stable planar chamfer-face candidate, and asserts supported success plus explicit unsupported behavior. Extend `packages/occt-core/test/live-root-integration.test.mjs` with a transformed-occurrence smoke test for `describeExactChamfer(ref)` against the same built carrier.</action>
  <acceptance_criteria>
    - `rg -n "wraps describeExactChamfer\\(ref\\) through occurrence-scoped exact refs|transforms describeExactChamfer semantic geometry into occurrence space" packages/occt-core/test/core.test.mjs` returns matches.
    - `rg -n "exact chamfer helper describes a supported planar chamfer face ref|exact chamfer helper failures stay explicit for unsupported or non-chamfer planar faces" test/exact_chamfer_contract.test.mjs` returns matches.
    - `rg -n "occt-core describeExactChamfer normalizes live chamfer semantics into occurrence space" packages/occt-core/test/live-root-integration.test.mjs` returns a match.
    - `node --test test/exact_chamfer_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_chamfer_contract.test.mjs</automated>
  </verify>
  <done>The root and package test surfaces now lock the selected-ref chamfer contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive root `DescribeExactChamfer` support and package wrapper parity</name>
  <read_first>src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, dist/occt-js.d.ts, test/exact_chamfer_contract.test.mjs, packages/occt-core/src/occt-core.js, packages/occt-core/test/live-root-integration.test.mjs</read_first>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, test/exact_chamfer_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports `DescribeExactChamfer(exactModelId, exactShapeHandle, kind, elementId)` as an additive selected-ref query.
    - Test 2: supported planar chamfer face refs return semantic chamfer data with `kind`, `profile`, `variant`, scalar support distances, scalar support angle, `frame`, anchors, and any stable direction fields needed by downstream overlays.
    - Test 3: unsupported geometry, unsupported topology, or non-chamfer planar faces return explicit typed failures instead of guessed chamfer payloads.
    - Test 4: `@tx-code/occt-core` live integration proves the wrapper remains occurrence-safe against the built carrier.
  </behavior>
  <action>Add additive exact-chamfer DTO structs to `src/importer.hpp` and corresponding TypeScript declarations to `dist/occt-js.d.ts`. In `src/exact-query.hpp` and `src/exact-query.cpp`, implement `DescribeExactChamfer` as a narrow selected-ref semantic query over planar face refs: resolve the selected face, derive the two planar support faces only when the topology is stable, compute semantic distances and support angle, and return explicit failures for everything else. Reuse the shipped placement frame and anchor vocabulary where it fits; do not add generic topology traversal or batch discovery APIs. In `src/js-interface.cpp`, add DTO serialization and the new Embind binding. In `packages/occt-core/src/occt-core.js`, add `describeExactChamfer(ref)` using the existing one-ref wrapper rhythm and occurrence transforms.</action>
  <acceptance_criteria>
    - `rg -n "DescribeExactChamfer|OcctJSExactChamferResult|variant|distanceA|distanceB|supportAngle" src/importer.hpp src/exact-query.hpp src/exact-query.cpp src/js-interface.cpp dist/occt-js.d.ts packages/occt-core/src/occt-core.js` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_chamfer_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_chamfer_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The root carrier and `@tx-code/occt-core` now expose supported planar chamfer semantics from one selected ref without widening the runtime boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected retained exact ref -> root chamfer classification | Over-broad topology inference could silently turn arbitrary planar faces into false chamfer positives |
| root local-space chamfer DTO -> package occurrence-space DTO | Incorrect transformation would make live chamfer outputs wrong for transformed or repeated occurrences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | T/R | `src/exact-query.cpp` selected-ref chamfer classification | mitigate | Lock supported face success and unsupported planar failures in a focused root contract test against a shipped fixture |
| T-22-01-02 | T/I | package live occurrence transforms | mitigate | Add a built-carrier integration test proving `describeExactChamfer(ref)` returns transformed frame and anchor geometry |
</threat_model>

<verification>
- Run `node --test packages/occt-core/test/core.test.mjs`.
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_chamfer_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
Downstream JS can call a package-first `describeExactChamfer(ref)` helper and rely on a stable semantic DTO plus explicit typed failures, with occurrence transforms preserved and the root carrier widened only by one narrow selected-ref descriptor.
</success_criteria>

<output>
After completion, create `.planning/phases/22-chamfer-constraint-helpers/22-01-SUMMARY.md`
</output>
