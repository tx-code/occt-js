---
phase: 24-exact-model-lifecycle-governance
plan: "02"
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/src/index.d.ts
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
autonomous: true
requirements:
  - LIFE-01
  - ADAPT-10
must_haves:
  truths:
    - "`@tx-code/occt-core` exposes additive lifecycle ergonomics without replacing `openExactModel()` or the root numeric-handle contract."
    - "Managed exact-model disposal is explicit and idempotent for callers, while stale refs and stale handles still rely on authoritative root lifecycle failures."
    - "Any `FinalizationRegistry` usage remains best-effort only and never becomes the contractual cleanup guarantee."
  artifacts:
    - path: packages/occt-core/src/occt-core.js
      provides: package-first lifecycle helper surface
      contains: additive managed exact-model open/dispose helpers plus diagnostics pass-through
    - path: packages/occt-core/src/index.d.ts
      provides: typed package lifecycle contract
      contains: managed exact-model helper types and additive `OcctCoreClient` method signatures aligned with the JS surface
    - path: packages/occt-core/test/core.test.mjs
      provides: mocked package contract coverage
      contains: managed helper semantics, diagnostics pass-through, and finalizer/idempotent-dispose expectations
    - path: packages/occt-core/test/live-root-integration.test.mjs
      provides: live carrier parity
      contains: managed helper behavior against the built root runtime, including explicit dispose and released-handle outcomes
  key_links:
    - from: packages/occt-core/src/occt-core.js
      to: packages/occt-core/src/index.d.ts
      via: managed lifecycle helper API parity
      pattern: openManagedExactModel|OcctManagedExactModel|getExactModelDiagnostics
    - from: packages/occt-core/test/core.test.mjs
      to: packages/occt-core/src/occt-core.js
      via: mocked lifecycle helper semantics
      pattern: openManagedExactModel|dispose|FinalizationRegistry|getExactModelDiagnostics
    - from: packages/occt-core/test/live-root-integration.test.mjs
      to: dist/occt-js.d.ts
      via: built runtime lifecycle parity
      pattern: GetExactModelDiagnostics|ReleaseExactModel
---

<objective>
Add package-first lifecycle ergonomics on top of the root runtime contract from Plan 24-01.

Purpose: finish the package/adaptor half of `LIFE-01` and `ADAPT-10` by exposing opt-in managed exact-model helpers in `@tx-code/occt-core`, surfacing root diagnostics to downstream JS, and keeping any finalizer behavior clearly secondary to explicit dispose/release.
Output: additive managed exact-model helpers in `occt-core`, aligned typings, and mocked/live contract coverage.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-exact-model-lifecycle-governance/24-CONTEXT.md
@.planning/phases/24-exact-model-lifecycle-governance/24-PATTERNS.md
@.planning/phases/24-exact-model-lifecycle-governance/24-01-PLAN.md
@AGENTS.md
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/index.d.ts
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@packages/occt-core/src/exact-model-normalizer.js
@dist/occt-js.d.ts

<interfaces>
From `packages/occt-core/src/occt-core.js` (existing lifecycle pass-through to preserve):
```javascript
async retainExactModel(exactModelId) {
  const module = await this._ensureModule();
  return module.RetainExactModel(exactModelId);
}
```

From `packages/occt-core/src/index.d.ts` (current additive client surface to extend rather than replace):
```typescript
openExactModel(content: OcctBinaryInput, options?: OcctImportModelOptions): Promise<OcctJSExactOpenResult>;
retainExactModel(exactModelId: number): Promise<OcctJSLifecycleResult>;
releaseExactModel(exactModelId: number): Promise<OcctJSLifecycleResult>;
```

From `.planning/phases/24-exact-model-lifecycle-governance/24-CONTEXT.md` (locked package-boundary rule):
```markdown
Add any safer lifecycle surface package-first in `@tx-code/occt-core` as an opt-in managed wrapper/helper around the existing exact open + release flow.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing package tests for managed exact-model helpers and diagnostics pass-through</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/24-exact-model-lifecycle-governance/24-CONTEXT.md</read_first>
  <files>packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a unit test named `openManagedExactModel wraps exact open results with explicit dispose semantics` fails until `OcctCoreClient` exposes an additive managed exact-model helper surface.
    - Test 2: a unit test named `managed exact-model helpers keep FinalizationRegistry best-effort and dispose idempotent` fails until the wrapper behavior is explicit about deterministic dispose and non-authoritative finalizer semantics.
    - Test 3: a live test named `managed exact-model helpers release real retained handles while preserving root lifecycle failures` fails until the built root carrier and the package helper cooperate correctly.
    - Test 4: a unit or live assertion named `getExactModelDiagnostics surfaces root lifecycle snapshots package-first` fails until `occt-core` forwards the root diagnostics API to downstream JS.
  </behavior>
  <action>Extend the package test suite first. Use mocked runtime coverage in `core.test.mjs` to pin helper semantics and optional finalizer behavior without needing GC guarantees, then add one focused live-root integration test that opens a real exact model, disposes it through the managed helper, and proves stale follow-up behavior still comes from the root runtime.</action>
  <acceptance_criteria>
    - `rg -n "openManagedExactModel wraps exact open results with explicit dispose semantics|managed exact-model helpers keep FinalizationRegistry best-effort and dispose idempotent|getExactModelDiagnostics surfaces root lifecycle snapshots package-first" packages/occt-core/test/core.test.mjs` returns matches.
    - `rg -n "managed exact-model helpers release real retained handles while preserving root lifecycle failures" packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` fails before implementation and passes after the remaining task in this plan lands.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs</automated>
  </verify>
  <done>The package test surface now fixes the managed-lifecycle and diagnostics expectations before helper code lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive managed exact-model helpers in `@tx-code/occt-core`</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, dist/occt-js.d.ts, .planning/phases/24-exact-model-lifecycle-governance/24-PATTERNS.md</read_first>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: `OcctCoreClient` gains additive `openManagedExactModel` plus format-specific convenience variants only if they reuse the same managed-wrapper core and leave the existing `openExact*` methods untouched.
    - Test 2: the managed wrapper exposes explicit `dispose()` semantics, a stable reference to the underlying exact open result, and package-first access to root lifecycle diagnostics.
    - Test 3: `FinalizationRegistry`, if available, is used only as a best-effort fallback behind the helper layer; explicit dispose remains the documented and tested cleanup path.
    - Test 4: the typed package surface remains additive and compatible with existing exact helper APIs.
  </behavior>
  <action>Implement an additive managed exact-model helper layer in `packages/occt-core/src/occt-core.js`. The recommended shape is `openManagedExactModel` plus format-specific variants returning an `OcctManagedExactModel` wrapper with idempotent `dispose()` and a stable `exactModel` result property; `OcctCoreClient` should also expose `getExactModelDiagnostics()` as a package-first pass-through to the root snapshot API from Plan 24-01. If `FinalizationRegistry` is available, use it only to reduce forgotten-handle leaks after explicit helper adoption, never as the primary cleanup contract. Mirror the finalized helper surface in `packages/occt-core/src/index.d.ts`.</action>
  <acceptance_criteria>
    - `rg -n "openManagedExactModel|openManagedExactStep|openManagedExactIges|openManagedExactBrep|getExactModelDiagnostics" packages/occt-core/src/occt-core.js packages/occt-core/src/index.d.ts` returns matches.
    - `node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>Downstream JS consumers now have an additive package-first lifecycle helper path that improves disposal ergonomics without replacing the shipped exact open/release contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root lifecycle contract -> package lifecycle helper | If the package invents separate ownership truth, callers will see mismatched disposed/released behavior |
| explicit dispose semantics -> best-effort finalizer fallback | If finalizer behavior is over-promised, downstream callers will treat nondeterministic GC as the cleanup contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | T/R | managed package wrapper | mitigate | Keep the wrapper additive, expose root diagnostics package-first, and verify live parity against the built carrier |
| T-24-02-02 | R/I | finalizer semantics | mitigate | Lock explicit dispose behavior in mocked tests and treat `FinalizationRegistry` as best-effort only |
</threat_model>

<verification>
- Run `node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
`@tx-code/occt-core` now offers safer exact-model disposal ergonomics and lifecycle diagnostics package-first, while the root numeric-handle contract and deterministic released-handle behavior remain the source of truth.
</success_criteria>

<output>
After completion, create `.planning/phases/24-exact-model-lifecycle-governance/24-02-SUMMARY.md`
</output>
