---
phase: 24-exact-model-lifecycle-governance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/importer.hpp
  - src/exact-model-store.hpp
  - src/exact-model-store.cpp
  - src/js-interface.cpp
  - dist/occt-js.d.ts
  - test/exact_model_lifecycle_contract.test.mjs
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
must_haves:
  truths:
    - "The root Wasm carrier keeps integer `exactModelId` handles plus explicit `RetainExactModel` / `ReleaseExactModel` as the authoritative lifecycle contract."
    - "Representative retained exact-query paths still fail deterministically with typed lifecycle codes after final release instead of degrading into ambiguous runtime failures."
    - "The root runtime exposes an additive pull-based diagnostics snapshot for unreleased exact handles without adding viewer policy or Phase 25 performance work."
  artifacts:
    - path: src/exact-model-store.hpp
      provides: lifecycle state API
      contains: additive diagnostics/snapshot declarations alongside the existing retain/release contract
    - path: src/exact-model-store.cpp
      provides: authoritative lifecycle bookkeeping
      contains: live/released handle diagnostics plus deterministic lifecycle failure behavior
    - path: src/js-interface.cpp
      provides: JS binding surface
      contains: additive diagnostics binding(s) that preserve the current lifecycle API shape
    - path: dist/occt-js.d.ts
      provides: public runtime typings
      contains: typed diagnostics snapshot DTOs and runtime method signatures aligned with the binding surface
    - path: test/exact_model_lifecycle_contract.test.mjs
      provides: real-carrier lifecycle contract coverage
      contains: diagnostics assertions and released-handle propagation checks for representative exact-query usage
  key_links:
    - from: src/exact-model-store.cpp
      to: src/js-interface.cpp
      via: diagnostics snapshot binding parity
      pattern: GetExactModelDiagnostics|LifecycleResultToVal
    - from: dist/occt-js.d.ts
      to: src/js-interface.cpp
      via: public runtime lifecycle API parity
      pattern: GetExactModelDiagnostics|OcctJSExactModelDiagnostics
    - from: test/exact_model_lifecycle_contract.test.mjs
      to: src/exact-model-store.cpp
      via: real runtime released-handle and diagnostics verification
      pattern: released-handle|invalid-handle|GetExactModelDiagnostics
---

<objective>
Tighten the root/runtime lifecycle contract before package ergonomics land.

Purpose: satisfy the root-runtime half of `LIFE-01` and `LIFE-02` by making retained-handle diagnostics explicit, keeping released-handle failures deterministic across representative exact-query use, and exposing that lifecycle snapshot through the public Wasm carrier without widening scope into package policy or performance refactors.
Output: additive lifecycle diagnostics in the root runtime, aligned public typings, and focused real-carrier contract coverage.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-exact-model-lifecycle-governance/24-CONTEXT.md
@.planning/phases/24-exact-model-lifecycle-governance/24-PATTERNS.md
@AGENTS.md
@src/importer.hpp
@src/exact-model-store.hpp
@src/exact-model-store.cpp
@src/js-interface.cpp
@src/exact-query.cpp
@dist/occt-js.d.ts
@test/exact_model_lifecycle_contract.test.mjs

<interfaces>
From `src/exact-model-store.cpp` (current authoritative lifecycle semantics to preserve):
```cpp
if (state.releasedEntries.count(exactModelId) > 0) {
    return MakeFailure("released-handle", "Exact model handle has already been released.");
}
```

From `src/js-interface.cpp` (existing binding rhythm to extend additively):
```cpp
val RetainExactModel(int exactModelId)
{
    return LifecycleResultToVal(ExactModelStore::Instance().Retain(exactModelId));
}
```

From `test/exact_model_lifecycle_contract.test.mjs` (current contract baseline to deepen rather than replace):
```javascript
assert.deepEqual(module.ReleaseExactModel(result.exactModelId), { ok: true });
assert.deepEqual(module.RetainExactModel(result.exactModelId), {
  ok: false,
  code: "released-handle",
  message: "Exact model handle has already been released.",
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root contract tests for diagnostics snapshots and released-query behavior</name>
  <read_first>test/exact_model_lifecycle_contract.test.mjs, dist/occt-js.d.ts, src/js-interface.cpp, .planning/phases/24-exact-model-lifecycle-governance/24-CONTEXT.md</read_first>
  <files>test/exact_model_lifecycle_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `GetExactModelDiagnostics reports live retained exact handles with stable metadata` fails until the real runtime returns a diagnostics snapshot containing at least `liveExactModelCount`, `releasedHandleCount`, and per-handle metadata such as `exactModelId`, `refCount`, `sourceFormat`, and retained geometry count.
    - Test 2: a subtest named `GetExactModelDiagnostics shrinks after final release and retains released-handle history` fails until the snapshot reflects handle release transitions deterministically.
    - Test 3: a subtest named `representative exact queries return released-handle after final release` fails until at least one representative retained-query path uses the same `released-handle` lifecycle code after the model is released.
  </behavior>
  <action>Extend `test/exact_model_lifecycle_contract.test.mjs` into the authoritative root lifecycle suite for Phase 24. Reuse real fixture bytes and the built Wasm carrier. Keep the coverage focused: lifecycle retain/release, the new diagnostics snapshot, and one or two representative retained-query paths that prove stale-handle behavior stays typed after release.</action>
  <acceptance_criteria>
    - `rg -n "GetExactModelDiagnostics reports live retained exact handles|GetExactModelDiagnostics shrinks after final release|representative exact queries return released-handle after final release" test/exact_model_lifecycle_contract.test.mjs` returns matches.
    - `node --test test/exact_model_lifecycle_contract.test.mjs` fails before implementation and passes after the remaining task in this plan lands.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_model_lifecycle_contract.test.mjs</automated>
  </verify>
  <done>The root lifecycle suite now locks the additive diagnostics contract and representative released-handle query behavior before runtime changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive root diagnostics and preserve deterministic lifecycle failures</name>
  <read_first>src/importer.hpp, src/exact-model-store.hpp, src/exact-model-store.cpp, src/js-interface.cpp, dist/occt-js.d.ts, test/exact_model_lifecycle_contract.test.mjs, .planning/phases/24-exact-model-lifecycle-governance/24-PATTERNS.md</read_first>
  <files>src/importer.hpp, src/exact-model-store.hpp, src/exact-model-store.cpp, src/js-interface.cpp, dist/occt-js.d.ts, test/exact_model_lifecycle_contract.test.mjs</files>
  <behavior>
    - Test 1: the root runtime exports one additive diagnostics snapshot API named `GetExactModelDiagnostics()` and types it in `dist/occt-js.d.ts`.
    - Test 2: the diagnostics snapshot is explicit and pull-based; it does not log implicitly or introduce viewer/session policy.
    - Test 3: released, missing, or stale retained-handle queries continue to surface deterministic lifecycle result codes, and the new diagnostics path does not regress the existing retain/release contract.
  </behavior>
  <action>Extend the exact-model store with an additive diagnostics snapshot that reports live retained handles and aggregate released-handle counts without changing the existing numeric-handle contract. Bind that snapshot through `src/js-interface.cpp`, type it in `dist/occt-js.d.ts`, and make any targeted root fixes needed so representative exact-query code paths preserve `released-handle` behavior after final release. Keep `GetEntry` copy optimization and other hot-path work out of this plan unless a tiny lifecycle bug fix is required for correctness.</action>
  <acceptance_criteria>
    - `rg -n "GetExactModelDiagnostics|OcctJSExactModelDiagnostics" src/js-interface.cpp dist/occt-js.d.ts` returns matches.
    - `npm run build:wasm:win` passes.
    - `node --test test/exact_model_lifecycle_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_model_lifecycle_contract.test.mjs"</automated>
  </verify>
  <done>The root Wasm carrier now exposes authoritative lifecycle diagnostics and keeps released-handle behavior deterministic across the retained exact-model surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained native state -> JS-facing lifecycle contract | If diagnostics or release behavior drift, downstream callers can leak native geometry or mis-handle stale refs |
| exact-query paths -> lifecycle failure codes | If released handles degrade into generic invalid-id/internal errors, callers lose deterministic cleanup semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | T/R | lifecycle diagnostics | mitigate | Add a real-carrier diagnostics contract test and bind a typed additive snapshot through the public Wasm surface |
| T-24-01-02 | T/R | released-handle query behavior | mitigate | Add representative released-query tests and route stale-handle failures through the existing lifecycle codes |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_model_lifecycle_contract.test.mjs`.
</verification>

<success_criteria>
Downstream callers can inspect unreleased exact handles through the root Wasm carrier and still rely on deterministic typed lifecycle failures after final release, all without changing the shipped numeric-handle ownership model.
</success_criteria>

<output>
After completion, create `.planning/phases/24-exact-model-lifecycle-governance/24-01-SUMMARY.md`
</output>
