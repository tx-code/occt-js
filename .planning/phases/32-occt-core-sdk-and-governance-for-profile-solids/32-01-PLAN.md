---
phase: 32-occt-core-sdk-and-governance-for-profile-solids
plan: "01"
type: execute
wave: 1
depends_on:
  - 31-02
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/src/index.d.ts
  - packages/occt-core/src/model-normalizer.js
  - packages/occt-core/test/core.test.mjs
autonomous: true
requirements:
  - SDK-02
must_haves:
  truths:
    - "`@tx-code/occt-core` exposes additive package-first wrappers for shared profile validation and extruded-shape validate/build/exact-open flows."
    - "Published package typings mirror the shipped root `Profile2D` and extruded-shape DTOs instead of inventing a second schema."
    - "Package normalization preserves additive `generated-extruded-shape` metadata and package tests fail on wrapper or normalizer drift."
---

# Phase 32 Plan 01

<objective>
Ship the shared-profile and extruded-shape contract package-first through `@tx-code/occt-core`.
</objective>

<implementation_notes>

- Add `validateProfile2DSpec`, `validateExtrudedShapeSpec`, `buildExtrudedShape`, and `openExactExtrudedShape` wrappers to `OcctCoreClient`.
- Extend `packages/occt-core/src/index.d.ts` with shared-profile and extruded-shape package typings plus additive normalized extruded metadata.
- Teach `normalizeOcctResult(...)` to preserve `generated-extruded-shape` metadata and source format without disturbing the existing revolved path.
- Cover success and missing-method failure paths in package tests.

</implementation_notes>

<verification>
- `npm --prefix packages/occt-core test`
</verification>

<output>
After completion, create `.planning/phases/32-occt-core-sdk-and-governance-for-profile-solids/32-01-SUMMARY.md`
</output>
