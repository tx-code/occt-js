---
phase: 02-root-runtime-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/js-interface.cpp
  - test/test_multi_format_exports.mjs
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "STEP, IGES, and BREP imports expose the canonical top-level payload shape through both direct and generic root APIs."
    - "Generic ReadFile(format, ...) dispatch and direct format-specific entrypoints stay behaviorally aligned for supported formats."
  artifacts:
    - "src/js-interface.cpp"
    - "test/test_multi_format_exports.mjs"
  key_links:
    - "src/js-interface.cpp -> test/test_multi_format_exports.mjs"
---

<objective>
Tighten the canonical import/result-shape contract for the root Wasm entrypoints.

Purpose: Make the root API contract explicit at the top-level payload and direct-vs-generic dispatch layer without widening into downstream package concerns.
Output: Strengthened root API contract coverage plus any minimum Embind/result-shape fixes required to satisfy it.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-root-runtime-contract/02-RESEARCH.md
@AGENTS.md
@src/js-interface.cpp
@src/importer.hpp
@test/test_multi_format_exports.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand contract coverage for direct and generic root import entrypoints</name>
  <files>test/test_multi_format_exports.mjs</files>
  <action>Extend `test/test_multi_format_exports.mjs` so STEP, IGES, and BREP imports assert the canonical top-level payload contract on successful imports and verify that generic `ReadFile(format, ...)` remains aligned with the direct format-specific entrypoints. Make the assertions explicit for `sourceFormat`, `rootNodes`, `geometries`, `materials`, `warnings`, `stats`, and unit metadata where available. Keep this test focused on root API contract behavior, not viewer or package-layer concerns.</action>
  <acceptance_criteria>
    - `node test/test_multi_format_exports.mjs` passes.
    - `rg -n "sourceFormat|rootNodes|geometries|materials|warnings|stats|sourceUnit|unitScaleToMeters|ReadFile" test/test_multi_format_exports.mjs` shows explicit contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>node test/test_multi_format_exports.mjs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile js-interface result shaping with the hardened root import contract</name>
  <files>src/js-interface.cpp, test/test_multi_format_exports.mjs</files>
  <action>Update `src/js-interface.cpp` only as needed so the direct and generic import entrypoints satisfy the hardened contract from Task 1. Preserve the existing API surface, keep supported formats limited to STEP/IGES/BREP, and avoid widening into package or viewer concerns. If the result contract changes, keep the change minimal and test-led.</action>
  <acceptance_criteria>
    - `npm run build:wasm:win && node test/test_multi_format_exports.mjs` passes.
    - `rg -n "ReadFile|ReadStepFile|ReadIgesFile|ReadBrepFile|sourceFormat|sourceUnit|unitScaleToMeters|warnings|stats" src/js-interface.cpp` shows the finalized result-shape contract.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:wasm:win && node test/test_multi_format_exports.mjs</automated>
  </verify>
</task>

</tasks>

<verification>
Run `node test/test_multi_format_exports.mjs` during test-only tightening. Before closing the plan, run `npm run build:wasm:win && node test/test_multi_format_exports.mjs`.
</verification>

<success_criteria>
The root import entrypoints expose one explicit contract for successful imports across supported formats, and generic format dispatch stays aligned with the direct APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-root-runtime-contract/02-01-SUMMARY.md`
</output>
