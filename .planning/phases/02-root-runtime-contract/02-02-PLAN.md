---
phase: 02-root-runtime-contract
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/importer-xde.cpp
  - src/importer-brep.cpp
  - src/importer.hpp
  - test/test_step_iges_root_mode.mjs
  - test/test_brep_root_mode.mjs
autonomous: true
requirements:
  - CORE-03
must_haves:
  truths:
    - "Supported root-mode semantics are explicit and verified per format."
    - "Unit metadata expectations remain stable on the root Wasm import path."
  artifacts:
    - "src/importer-xde.cpp"
    - "src/importer-brep.cpp"
    - "src/importer.hpp"
    - "test/test_step_iges_root_mode.mjs"
    - "test/test_brep_root_mode.mjs"
  key_links:
    - "src/importer-xde.cpp -> test/test_step_iges_root_mode.mjs"
    - "src/importer-brep.cpp -> test/test_brep_root_mode.mjs"
---

<objective>
Lock root-shape and unit-metadata semantics for supported formats.

Purpose: Make `one-shape` / `multiple-shapes` behavior explicit and regression-resistant across the supported root runtime formats without over-promising unsupported semantics.
Output: Tightened root-mode/unit tests plus minimum importer changes needed to satisfy the explicit contract.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-root-runtime-contract/02-RESEARCH.md
@AGENTS.md
@src/importer-xde.cpp
@src/importer-brep.cpp
@src/importer.hpp
@test/test_step_iges_root_mode.mjs
@test/test_brep_root_mode.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make per-format root-mode and unit metadata expectations explicit in tests</name>
  <files>test/test_step_iges_root_mode.mjs, test/test_brep_root_mode.mjs</files>
  <action>Extend the STEP/IGES/BREP root-mode tests so they explicitly define supported semantics for `one-shape` and `multiple-shapes` per format, including realistic fixtures where they already exist. Tighten unit metadata assertions where the runtime already exposes them, and keep direct-vs-generic path expectations aligned.</action>
  <acceptance_criteria>
    - `node test/test_step_iges_root_mode.mjs && node test/test_brep_root_mode.mjs` passes.
    - `rg -n "rootMode|multiple-shapes|one-shape|sourceUnit|unitScaleToMeters|ReadFile" test/test_step_iges_root_mode.mjs test/test_brep_root_mode.mjs` shows explicit format-level contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>node test/test_step_iges_root_mode.mjs && node test/test_brep_root_mode.mjs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile importer behavior with the explicit root-mode contract</name>
  <files>src/importer-xde.cpp, src/importer-brep.cpp, src/importer.hpp, test/test_step_iges_root_mode.mjs, test/test_brep_root_mode.mjs</files>
  <action>Update the root import implementation only as needed so STEP, IGES, and BREP satisfy the explicit root-mode semantics from Task 1. Preserve the existing public API and avoid inventing unsupported format parity where the importer does not provide it today; instead, lock the actual supported behavior into the test contract. Keep any unit-metadata fixes minimal and tied to the test expectations.</action>
  <acceptance_criteria>
    - `npm run build:wasm:win && node test/test_step_iges_root_mode.mjs && node test/test_brep_root_mode.mjs` passes.
    - `rg -n "RootMode|MultipleShapes|OneShape|sourceUnit|unitScaleToMeters" src/importer-xde.cpp src/importer-brep.cpp src/importer.hpp` shows the finalized contract hooks.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:wasm:win && node test/test_step_iges_root_mode.mjs && node test/test_brep_root_mode.mjs</automated>
  </verify>
</task>

</tasks>

<verification>
Run the targeted root-mode tests during test tightening. Before closing the plan, run `npm run build:wasm:win && node test/test_step_iges_root_mode.mjs && node test/test_brep_root_mode.mjs`.
</verification>

<success_criteria>
Supported root-mode behavior is explicit and regression-resistant for the root Wasm runtime, and unit metadata expectations remain stable on the verified import paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-root-runtime-contract/02-02-SUMMARY.md`
</output>
