---
phase: 02-root-runtime-contract
plan: "03"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/orientation.cpp
  - src/js-interface.cpp
  - test/test_optimal_orientation_api.mjs
  - test/test_optimal_orientation_reference.mjs
autonomous: true
requirements:
  - CORE-04
must_haves:
  truths:
    - "Manufacturing-oriented optimal orientation remains supported for STEP, IGES, and BREP single-part inputs."
    - "Orientation results expose a stable diagnostics shape that downstream code can rely on."
  artifacts:
    - "src/orientation.cpp"
    - "src/js-interface.cpp"
    - "test/test_optimal_orientation_api.mjs"
    - "test/test_optimal_orientation_reference.mjs"
  key_links:
    - "src/orientation.cpp -> test/test_optimal_orientation_reference.mjs"
    - "src/js-interface.cpp -> test/test_optimal_orientation_api.mjs"
---

<objective>
Lock the optimal-orientation diagnostics contract for supported root runtime formats.

Purpose: Harden the orientation API so downstream consumers can rely on a stable result shape and meaningful diagnostics across supported single-part inputs.
Output: Expanded orientation contract tests plus minimum runtime changes required to satisfy them.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-root-runtime-contract/02-RESEARCH.md
@AGENTS.md
@src/orientation.cpp
@src/js-interface.cpp
@test/test_optimal_orientation_api.mjs
@test/test_optimal_orientation_reference.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand orientation contract tests for supported formats and diagnostics shape</name>
  <files>test/test_optimal_orientation_api.mjs, test/test_optimal_orientation_reference.mjs</files>
  <action>Extend the orientation tests so the contract is explicit for supported formats, unsupported-format failures, diagnostics presence, and reference-fixture expectations. Make the assertions clear around `transform`, `localFrame`, `bbox`, `strategy`, `stage1`, `stage2`, `confidence`, and unit metadata when available. Keep the tests focused on contract-level guarantees rather than overfitting the current heuristic internals.</action>
  <acceptance_criteria>
    - `node test/test_optimal_orientation_api.mjs && node test/test_optimal_orientation_reference.mjs` passes.
    - `rg -n "transform|localFrame|bbox|strategy|stage1|stage2|confidence|sourceUnit|unitScaleToMeters|Unsupported format" test/test_optimal_orientation_api.mjs test/test_optimal_orientation_reference.mjs` shows explicit diagnostics assertions.
  </acceptance_criteria>
  <verify>
    <automated>node test/test_optimal_orientation_api.mjs && node test/test_optimal_orientation_reference.mjs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile orientation result shaping with the hardened diagnostics contract</name>
  <files>src/orientation.cpp, src/js-interface.cpp, test/test_optimal_orientation_api.mjs, test/test_optimal_orientation_reference.mjs</files>
  <action>Update the orientation runtime only as needed so supported STEP, IGES, and BREP single-part inputs satisfy the explicit diagnostics contract from Task 1. Preserve the manufacturing-oriented API surface and avoid widening Phase 02 into new analytics features. Keep any result-shape changes minimal and bound to the tested contract.</action>
  <acceptance_criteria>
    - `npm run build:wasm:win && node test/test_optimal_orientation_api.mjs && node test/test_optimal_orientation_reference.mjs` passes.
    - `rg -n "AnalyzeOptimalOrientation|sourceUnit|unitScaleToMeters|stage1|stage2|confidence|strategy|bbox|localFrame" src/orientation.cpp src/js-interface.cpp` shows the finalized diagnostics contract hooks.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:wasm:win && node test/test_optimal_orientation_api.mjs && node test/test_optimal_orientation_reference.mjs</automated>
  </verify>
</task>

</tasks>

<verification>
Run the targeted orientation tests during contract tightening. Before closing the wave, run `npm run build:wasm:win && node test/test_optimal_orientation_api.mjs && node test/test_optimal_orientation_reference.mjs`. Before closing Phase 02, run `npm run build:wasm:win && npm test`.
</verification>

<success_criteria>
The optimal-orientation API remains supported for the current root runtime formats and exposes a stable diagnostics contract that is locked by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-root-runtime-contract/02-03-SUMMARY.md`
</output>
