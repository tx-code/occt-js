---
phase: 02-root-runtime-contract
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_optimal_orientation_api.mjs
  - test/test_optimal_orientation_reference.mjs
  - test/orientation_reference_golden.json
  - src/orientation.cpp
autonomous: true
requirements:
  - CORE-04
must_haves:
  truths:
    - "Supported STEP, IGES, and BREP inputs can request manufacturing-oriented optimal orientation and get a transform plus meaningful diagnostics."
    - "Successful orientation results expose `transform`, `localFrame`, `bbox`, `strategy`, `stage1`, `stage2`, `confidence`, and unit metadata when the loaded source provides it."
    - "Unsupported formats and unsupported orientation modes fail explicitly instead of returning misleading partial success."
  artifacts:
    - "test/test_optimal_orientation_api.mjs"
    - "test/test_optimal_orientation_reference.mjs"
    - "test/orientation_reference_golden.json"
    - "src/orientation.cpp"
  key_links:
    - "src/js-interface.cpp:AnalyzeOptimalOrientation -> OrientationResultToVal"
    - "src/orientation.cpp:AnalyzeOptimalOrientationFromMemory -> BuildOrientationResult"
    - "test/test_optimal_orientation_reference.mjs -> test/orientation_reference_golden.json"
---

<objective>
Stabilize the optimal-orientation diagnostics contract for supported root Wasm formats.

Purpose: Make CORE-04 concrete by separating the orientation API contract from the current heuristic implementation details and proving the returned diagnostics stay meaningful for STEP, IGES, and BREP.
Output: Stronger API/reference tests plus the minimum `src/orientation.cpp` updates needed to keep the orientation diagnostics stable.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-root-runtime-contract/02-RESEARCH.md
@AGENTS.md
@src/js-interface.cpp
@src/orientation.cpp
@test/test_optimal_orientation_api.mjs
@test/test_optimal_orientation_reference.mjs
@test/orientation_reference_golden.json

<interfaces>
From `src/js-interface.cpp`:
```cpp
val AnalyzeOptimalOrientation(const std::string& format, const val& content, const val& jsParams);
```

From `src/orientation.cpp`:
```cpp
OrientationResult AnalyzeOptimalOrientationFromMemory(
    const std::string& format,
    const uint8_t* data,
    size_t size,
    const OrientationParams& params
);

OrientationResult BuildOrientationResult(const LoadedShape& loaded,
                                         const Stage1Computation& stage1,
                                         const Stage2Computation& stage2);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten the orientation API and golden-fixture contract tests</name>
  <files>test/test_optimal_orientation_api.mjs, test/test_optimal_orientation_reference.mjs</files>
  <read_first>
    .planning/phases/02-root-runtime-contract/02-RESEARCH.md
    src/js-interface.cpp
    src/orientation.cpp
    test/test_optimal_orientation_api.mjs
    test/test_optimal_orientation_reference.mjs
    test/orientation_reference_golden.json
  </read_first>
  <behavior>
    - Test 1: Unsupported formats and unsupported orientation modes fail with explicit error text.
    - Test 2: Successful `AnalyzeOptimalOrientation(...)` calls for STEP, IGES, and BREP expose the full contract fields and stable container shapes.
    - Test 3: Golden fixtures keep meaningful diagnostics for `strategy`, `confidence`, `stage1`, `stage2`, `transform`, `localFrame`, `bbox`, and unit metadata where present.
    - Test 4: Preset-axis requests still succeed and preserve stage diagnostics without adding new public API surface.
  </behavior>
  <action>Expand `test/test_optimal_orientation_api.mjs` into a real contract test. Add a helper such as `assertOrientationContract(result, label)` that checks `success`, a 16-number `transform`, `localFrame.origin/xDir/yDir/zDir`, `bbox.dx/dy/dz`, `strategy`, `confidence`, `stage1.detectedAxis`, `stage1.usedCylinderSupport`, and `stage2.rotationAroundZDeg`. Keep the current unsupported-format assertion, add an explicit unsupported-mode case such as `{ mode: "presentation" }`, and cover successful calls for `step`, `iges`, and `brep` using the fixtures named in `test/orientation_reference_golden.json`. In `test/test_optimal_orientation_reference.mjs`, keep the existing numeric tolerance helpers, then tighten the contract by checking `stage1.baseFaceId` presence only when the chosen strategy implies a planar base, asserting unit metadata on STEP and IGES when the golden file provides it, asserting unit metadata is absent for BREP, and requiring preset-axis results to keep `stage1.detectedAxis` plus a `strategy` that begins with `preset-axis`. This task is the tests-first half of CORE-04.</action>
  <acceptance_criteria>
    - `powershell -NoProfile -Command "node test/test_optimal_orientation_api.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_optimal_orientation_reference.mjs; exit $LASTEXITCODE"` passes.
    - `rg -n "assertOrientationContract|Unsupported orientation mode|stage1|stage2|baseFaceId|sourceUnit|unitScaleToMeters|preset-axis" test/test_optimal_orientation_api.mjs test/test_optimal_orientation_reference.mjs` shows the tightened contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node test/test_optimal_orientation_api.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_optimal_orientation_reference.mjs; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The orientation test suite clearly distinguishes API-shape guarantees from heuristic numeric expectations and fails when meaningful diagnostics regress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep orientation diagnostics and golden references aligned with the supported-format contract</name>
  <files>src/orientation.cpp, test/orientation_reference_golden.json</files>
  <read_first>
    src/orientation.cpp
    src/js-interface.cpp
    test/orientation_reference_golden.json
    test/test_optimal_orientation_api.mjs
    test/test_optimal_orientation_reference.mjs
    AGENTS.md
  </read_first>
  <behavior>
    - Test 1: `AnalyzeOptimalOrientationFromMemory(...)` keeps rejecting unsupported formats and unsupported modes before any heuristic work begins.
    - Test 2: `BuildOrientationResult(...)` returns stable `strategy`, `confidence`, `stage1`, `stage2`, `transform`, `localFrame`, and `bbox` fields for supported fixtures.
    - Test 3: STEP and IGES propagate `sourceUnit` / `unitScaleToMeters`, while BREP continues to omit those fields.
    - Test 4: Golden numeric references only change when verified implementation fixes require them.
  </behavior>
  <action>Modify `src/orientation.cpp` only as required to satisfy Task 1 while keeping the public API unchanged. Preserve the current supported formats (`step`, `iges`, `brep`), the existing manufacturing-mode gate, and the strategy strings already emitted by `ComputeStage1(...)` plus the optional `+projected-min-area-rect` suffix from `BuildOrientationResult(...)`. Make sure `stage1.detectedAxis` and `stage1.usedCylinderSupport` remain stable for all successful results, keep `stage1.baseFaceId` populated only when a planar base is actually selected, and continue propagating STEP/IGES unit metadata from `FillUnitMetadata(...)` into `BuildOrientationResult(...)`. If a verified implementation fix changes any golden numbers, update `test/orientation_reference_golden.json` to the new measured values while keeping the fixture set limited to `ANC101.stp`, `bearing.igs`, and `ANC101_isolated_components.brep`. Do not add new orientation modes, package-layer APIs, or Babylon/demo behavior.</action>
  <acceptance_criteria>
    - `powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_optimal_orientation_api.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_optimal_orientation_reference.mjs; exit $LASTEXITCODE"` passes.
    - `rg -n "Unsupported format|Unsupported orientation mode|preset-axis|planar-base-with-cylinder-support|principal-inertia-fallback|result\\.sourceUnit = loaded\\.sourceUnit|result\\.unitScaleToMeters = loaded\\.unitScaleToMeters" src/orientation.cpp` shows the contract-critical diagnostics and metadata flow.
    - `rg -n "\"step\"|\"iges\"|\"brep\"" test/orientation_reference_golden.json` shows the golden reference remains scoped to the supported formats only.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_optimal_orientation_api.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_optimal_orientation_reference.mjs; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The orientation analysis contract is explicit, the golden diagnostics stay verified against regenerated Wasm output, and supported-format callers still receive transform plus meaningful diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS caller -> orientation API | Caller-supplied format strings, bytes, and mode values must not yield misleading success states |
| loaded CAD shape -> diagnostic payload | Heuristic regressions can silently distort transform/frame/bbox outputs that downstream manufacturing logic depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | `src/orientation.cpp` | mitigate | Keep explicit unsupported-format and unsupported-mode failures plus golden-fixture assertions for supported formats |
| T-02-06 | I | `test/test_optimal_orientation_reference.mjs`, `test/orientation_reference_golden.json` | mitigate | Lock the diagnostics contract with numeric golden checks, orthonormal-frame assertions, and unit-metadata expectations per supported format |
</threat_model>

<verification>
Run the two orientation tests while expanding coverage. Before closing the plan, rebuild `dist/` with `npm run build:wasm:win` and rerun both orientation tests because this plan changes `src/orientation.cpp`.
</verification>

<success_criteria>
Phase 02 has explicit orientation contract coverage for supported formats, explicit rejection coverage for unsupported formats and modes, and regenerated Wasm output that still matches the verified golden diagnostics for STEP, IGES, and BREP.
</success_criteria>

<output>
After completion, create `.planning/phases/02-root-runtime-contract/02-03-SUMMARY.md`
</output>
