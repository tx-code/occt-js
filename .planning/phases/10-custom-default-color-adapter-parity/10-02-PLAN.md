---
phase: 10-custom-default-color-adapter-parity
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/src/model-normalizer.js
  - packages/occt-core/src/exact-model-normalizer.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
autonomous: true
requirements:
  - APPR-03
  - APPR-04
  - ADAPT-03
must_haves:
  truths:
    - "`@tx-code/occt-core` normalizes and forwards appearance options instead of relying on caller-specific parameter shapes."
    - "Adapter normalization no longer invents fallback default materials unless explicit default appearance was requested."
    - "Live core imports through the built root carrier preserve the same custom default-color semantics as the root runtime."
  artifacts:
    - "packages/occt-core/src/occt-core.js"
    - "packages/occt-core/src/model-normalizer.js"
    - "packages/occt-core/src/exact-model-normalizer.js"
    - "packages/occt-core/test/core.test.mjs"
    - "packages/occt-core/test/live-root-integration.test.mjs"
  key_links:
    - "appearance param normalization -> root method forwarding"
    - "normalized materials fallback policy -> explicit default appearance only"
    - "live core integration -> built root carrier custom-default parity"
---

<objective>
Normalize and forward the appearance contract through `@tx-code/occt-core`.

Purpose: satisfy ADAPT-03 and finish Phase 10 by making the adapter package accept user-friendly appearance inputs without hiding the root runtime contract or reintroducing viewer-side fallback drift.
Output: adapter-side appearance param normalization, conditional fallback-material behavior, and live parity tests against the built root carrier.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-custom-default-color-adapter-parity/10-RESEARCH.md
@.planning/phases/10-custom-default-color-adapter-parity/10-VALIDATION.md
@AGENTS.md
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/model-normalizer.js
@packages/occt-core/src/exact-model-normalizer.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/import_appearance_contract.test.mjs
@dist/occt-js.d.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing adapter tests for appearance param normalization and conditional fallback behavior</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/src/model-normalizer.js, packages/occt-core/src/exact-model-normalizer.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/10-custom-default-color-adapter-parity/10-RESEARCH.md</read_first>
  <files>packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a unit test named `normalizes and forwards defaultColor appearance params before calling the root carrier` fails until `createOcctCore(...).importModel/openExactModel` convert tuple/object user input into a canonical forwarded root shape.
    - Test 2: a unit test named `does not synthesize fallback default materials when explicit default appearance is not requested` fails until `normalizeOcctResult(...)` preserves colorless output for legacy colorless imports.
    - Test 3: a unit test named `synthesizes the caller requested default color only for explicit default appearance` fails until adapter normalization produces the caller-provided default color when raw payloads are colorless under explicit default appearance.
    - Test 4: a live integration test named `createOcctCore imports custom defaultColor through the built root carrier` fails until the built root runtime and adapter agree on the imported custom default color.
  </behavior>
  <action>Extend `packages/occt-core/test/core.test.mjs` with the exact unit-test names from `<behavior>`, covering both tuple/object caller inputs and the fallback-material policy. Use stubbed root carrier methods to assert the forwarded params shape. Add one or more normalization tests that distinguish explicit default appearance from legacy `readColors: false` colorless behavior. Extend `packages/occt-core/test/live-root-integration.test.mjs` with the exact live-test name from `<behavior>`, importing a real fixture through `createOcctCore` against the built root carrier and asserting the resulting normalized material/base color matches the requested custom RGB value.</action>
  <acceptance_criteria>
    - `rg -n "normalizes and forwards defaultColor appearance params before calling the root carrier|does not synthesize fallback default materials when explicit default appearance is not requested|synthesizes the caller requested default color only for explicit default appearance|createOcctCore imports custom defaultColor through the built root carrier" packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs</automated>
  </verify>
  <done>The adapter tests prove the intended forwarding and normalization policy before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement occt-core appearance normalization and live parity with the root runtime</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/src/model-normalizer.js, packages/occt-core/src/exact-model-normalizer.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/10-custom-default-color-adapter-parity/10-RESEARCH.md</read_first>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/src/model-normalizer.js, packages/occt-core/src/exact-model-normalizer.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: `createOcctCore` forwards canonical appearance params to both stateless and exact-open root entry points.
    - Test 2: normalized colorless results keep zero synthesized materials unless default appearance is explicit.
    - Test 3: explicit default appearance still produces a normalized fallback material/color when the raw payload has no explicit colors.
    - Test 4: live adapter imports through the built root carrier match the requested custom default color end-to-end.
  </behavior>
  <action>Add a helper in `packages/occt-core/src/occt-core.js` that normalizes `importParams` appearance fields before forwarding them to root methods, converting array/object caller input into the root contract's object-form `defaultColor`. Extend `packages/occt-core/src/model-normalizer.js` so fallback-material synthesis depends on explicit appearance context instead of always using `DEFAULT_CAD_BASE_COLOR`; preserve old fallback only where backward compatibility is still required for legacy raw payloads with no appearance context. Thread `options.importParams` through `normalizeOcctResult(...)`, and make `packages/occt-core/src/exact-model-normalizer.js` pass that context along as well. Keep `core.openExactModel(...)` returning the raw runtime result so the adapter still does not hide the exact runtime contract.</action>
  <acceptance_criteria>
    - `rg -n "defaultColor|colorMode|readColors" packages/occt-core/src/occt-core.js packages/occt-core/src/model-normalizer.js packages/occt-core/src/exact-model-normalizer.js` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `npm --prefix packages/occt-core test` passes.
    - `node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs"</automated>
  </verify>
  <done>`@tx-code/occt-core` now forwards and normalizes the appearance contract without drifting from the root runtime semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-friendly appearance params -> canonical forwarded root params | Shape drift here would make `occt-core` and direct root imports disagree. |
| raw root payload -> normalized adapter materials | Unconditional fallback here would hide colorless runtime semantics from downstream callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | T/R | adapter param forwarding | mitigate | Lock canonical forwarding with stubbed adapter tests before implementation. |
| T-10-02-02 | T/I | fallback-material normalization | mitigate | Add unit and live tests that distinguish explicit default appearance from legacy colorless behavior. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `npm --prefix packages/occt-core test`.
- Run `node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `@tx-code/occt-core` forwards and normalizes the custom default-color contract without hiding the root runtime appearance semantics
</success_criteria>

<output>
After completion, create `.planning/phases/10-custom-default-color-adapter-parity/10-02-SUMMARY.md`
</output>
