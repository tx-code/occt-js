---
phase: 08-pairwise-measurement-contract-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - packages/occt-core/src/index.js
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - test/exact_pairwise_measurement_contract.test.mjs
autonomous: true
requirements:
  - MEAS-01
  - MEAS-02
  - ADAPT-01
must_haves:
  truths:
    - "Root exact distance queries return exact numeric distance plus attach points and working-plane data from retained exact refs."
    - "Root exact angle queries return exact angle plus origin, directions, and working-plane data for supported planar-face and linear-edge pairs."
    - "`occt-core` forwards occurrence transforms into pairwise root queries so repeated geometry can be measured in occurrence space without viewer-only ids."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/exact-query.hpp"
    - "src/exact-query.cpp"
    - "src/js-interface.cpp"
    - "packages/occt-core/src/occt-core.js"
    - "test/exact_pairwise_measurement_contract.test.mjs"
  key_links:
    - "occurrence-scoped exact refs -> transform-aware pairwise root bindings"
    - "exact-query helper -> distance / angle bindings"
    - "occt-core wrappers -> repeated-geometry pairwise integration"
---

<objective>
Add exact distance and angle primitives with overlay-ready attach geometry.

Purpose: satisfy MEAS-01 and MEAS-02 by exposing transform-aware pairwise exact measurements on retained refs, while keeping the public adapter surface in `occt-core`.
Output: root exact distance and angle bindings, typed DTOs, and `occt-core` wrappers that accept occurrence-scoped refs and return package-friendly pairwise measurement results.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-pairwise-measurement-contract-hardening/08-RESEARCH.md
@.planning/phases/08-pairwise-measurement-contract-hardening/08-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/exact-ref-resolver.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/simple_part.step
@test/assembly.step
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract tests for exact pairwise distance and angle queries</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/08-pairwise-measurement-contract-hardening/08-RESEARCH.md, .planning/phases/08-pairwise-measurement-contract-hardening/08-VALIDATION.md</read_first>
  <files>test/exact_pairwise_measurement_contract.test.mjs, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact distance queries return attach points and working plane from retained exact refs` fails until a same-model fixture returns `ok === true`, a numeric `value`, `pointA`, `pointB`, `workingPlaneOrigin`, and `workingPlaneNormal`.
    - Test 2: a subtest named `exact angle queries return origin directions and working plane for linear edges or planar faces` fails until a same-model fixture returns `ok === true`, a numeric `value`, `origin`, `directionA`, `directionB`, `pointA`, `pointB`, `workingPlaneOrigin`, and `workingPlaneNormal`.
    - Test 3: a subtest named `exact angle failures stay explicit for parallel or unsupported geometry` fails until parallel face pairs return `{ ok: false, code: "parallel-geometry", message: ... }` and unsupported pair kinds return `{ ok: false, code: "unsupported-geometry", message: ... }`.
    - Test 4: a subtest named `occt-core pairwise wrappers honor occurrence transforms for repeated geometry` fails until a repeated-geometry pair resolved from `assembly.step` can produce a nonzero exact distance through the `occt-core` wrapper while preserving occurrence placement.
  </behavior>
  <action>Create `test/exact_pairwise_measurement_contract.test.mjs` as a Node built-in test file that loads the built root carrier via `test/load_occt_factory.mjs`, opens `test/simple_part.step`, and resolves planar faces plus linear edges from the exported topology payload. Add the exact subtest names from `<behavior>`. Call root bindings named exactly `MeasureExactDistance` and `MeasureExactAngle`, passing both exact refs and both 16-value column-major transforms. Assert the distance success DTO exposes `value`, `pointA`, `pointB`, `workingPlaneOrigin`, and `workingPlaneNormal`. Assert the angle success DTO exposes `value`, `origin`, `directionA`, `directionB`, `pointA`, `pointB`, `workingPlaneOrigin`, and `workingPlaneNormal`. Extend `packages/occt-core/test/core.test.mjs` with wrapper-shape tests for `measureExactDistance(refA, refB)` and `measureExactAngle(refA, refB)`. Extend `packages/occt-core/test/live-root-integration.test.mjs` with a repeated-geometry distance smoke test using `test/assembly.step` and occurrence-scoped exact refs from two different nodes.</action>
  <acceptance_criteria>
    - `rg -n "exact distance queries return attach points and working plane from retained exact refs|exact angle queries return origin directions and working plane for linear edges or planar faces|exact angle failures stay explicit for parallel or unsupported geometry|occt-core pairwise wrappers honor occurrence transforms for repeated geometry" test/exact_pairwise_measurement_contract.test.mjs packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `rg -n "MeasureExactDistance|MeasureExactAngle|parallel-geometry|unsupported-geometry" test/exact_pairwise_measurement_contract.test.mjs packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `node --test test/exact_pairwise_measurement_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_pairwise_measurement_contract.test.mjs</automated>
  </verify>
  <done>The root and adapter contract tests prove the intended pairwise distance/angle surface and transform-aware repeated-geometry behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transform-aware exact distance and angle root bindings plus occt-core wrappers</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, test/exact_pairwise_measurement_contract.test.mjs, .planning/phases/08-pairwise-measurement-contract-hardening/08-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_pairwise_measurement_contract.test.mjs</files>
  <behavior>
    - Test 1: root exact distance and angle bindings accept one `exactModelId`, two exact refs, and two 16-value column-major transforms and return structured success/failure DTOs.
    - Test 2: exact distance uses OCCT exact distance APIs and returns overlay-ready attach points plus working-plane origin/normal.
    - Test 3: exact angle supports `edge`/`edge` when both curves are `line` and `face`/`face` when both surfaces are `plane`, returning explicit `parallel-geometry`, `coincident-geometry`, or `unsupported-geometry` failures otherwise.
    - Test 4: `occt-core` exposes wrapper methods `measureExactDistance(refA, refB)` and `measureExactAngle(refA, refB)` that validate occurrence-scoped refs, forward transforms to wasm, and attach `refA` / `refB` to successful results.
  </behavior>
  <action>Add exact query methods named exactly `MeasureExactDistance` and `MeasureExactAngle` to `src/exact-query.hpp`, `src/exact-query.cpp`, `src/js-interface.cpp`, and `dist/occt-js.d.ts`. Use the flattened same-model pairwise signature `exactModelId, exactShapeHandleA, kindA, elementIdA, exactShapeHandleB, kindB, elementIdB, transformA, transformB`, where each transform is a 16-number column-major array. In `src/js-interface.cpp`, add matrix parsing helpers and DTO serializers. In `src/exact-query.cpp`, resolve both retained exact subshapes from the same exact model entry, apply the supplied transforms to temporary shapes or primitive frames, compute exact distance through exact OCCT distance APIs, and compute exact angle for planar-face and linear-edge pairs. Return distance success DTO fields `ok`, `value`, `pointA`, `pointB`, `workingPlaneOrigin`, and `workingPlaneNormal`. Return angle success DTO fields `ok`, `value`, `origin`, `directionA`, `directionB`, `pointA`, `pointB`, `workingPlaneOrigin`, and `workingPlaneNormal`. Return explicit failure codes `invalid-handle`, `released-handle`, `invalid-id`, `unsupported-geometry`, `parallel-geometry`, `coincident-geometry`, and `internal-error` as applicable. In `packages/occt-core/src/occt-core.js`, add wrappers `measureExactDistance(refA, refB)` and `measureExactAngle(refA, refB)` that validate both refs, require `refA.exactModelId === refB.exactModelId` in v1.1, forward both transforms unchanged, and append `refA` / `refB` to successful results. Export both wrappers from `packages/occt-core/src/index.js`.</action>
  <acceptance_criteria>
    - `rg -n "MeasureExactDistance|MeasureExactAngle|workingPlaneOrigin|workingPlaneNormal|directionA|directionB" src/js-interface.cpp dist/occt-js.d.ts packages/occt-core/src/occt-core.js` returns matches.
    - `rg -n "parallel-geometry|coincident-geometry|transformA|transformB" src/exact-query.hpp src/exact-query.cpp src/js-interface.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_pairwise_measurement_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_pairwise_measurement_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The root carrier and `occt-core` now expose transform-aware exact distance and angle primitives on occurrence-scoped refs without widening into app-side measurement sessions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| occurrence-scoped exact refs -> pairwise root query | Numeric transforms cross from JS into retained exact-query execution here. |
| transformed exact shapes -> attach geometry DTOs | Incorrect transform application would return valid-looking but wrong measurement points and planes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T/R | transform-aware pairwise root queries | mitigate | Lock the flattened pairwise signature and DTO shape in root contract tests before implementation. |
| T-08-01-02 | S/T | repeated-geometry occurrence adaptation | mitigate | Prove repeated-geometry pairwise results through live `assembly.step` integration in `occt-core`. |
| T-08-01-03 | I | unsupported or parallel angle queries | mitigate | Return explicit `parallel-geometry`, `coincident-geometry`, or `unsupported-geometry` failures instead of guessing from mesh data. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_pairwise_measurement_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
Downstream occurrence-scoped exact refs can retrieve exact distance and supported exact angle results, including overlay-ready attach geometry, through the root wasm carrier and the `occt-core` package wrapper surface.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pairwise-measurement-contract-hardening/08-01-SUMMARY.md`
</output>
