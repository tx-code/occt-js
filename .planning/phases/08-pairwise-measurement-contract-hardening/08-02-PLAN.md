---
phase: 08-pairwise-measurement-contract-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - packages/occt-core/src/index.js
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - test/exact_pairwise_measurement_contract.test.mjs
autonomous: true
requirements:
  - MEAS-04
  - MEAS-05
must_haves:
  truths:
    - "Root exact thickness queries measure planar offset for parallel planar face pairs rather than collapsing into generic face-face min distance."
    - "Pairwise exact failures stay explicit and stable across distance, angle, and thickness root bindings."
    - "`occt-core` preserves pairwise failure codes and exposes a single package-friendly wrapper for exact thickness."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/exact-query.hpp"
    - "src/exact-query.cpp"
    - "src/js-interface.cpp"
    - "packages/occt-core/src/occt-core.js"
    - "test/exact_pairwise_measurement_contract.test.mjs"
  key_links:
    - "pairwise exact ref transforms -> thickness root binding"
    - "exact-query helper -> structured pairwise failure DTOs"
    - "occt-core wrapper -> repeated parallel occurrence validation"
---

<objective>
Add exact thickness and structured pairwise failure hardening.

Purpose: satisfy MEAS-04 and the failure-contract half of MEAS-05 by exposing planar thickness plus stable pairwise failure semantics on the new exact pairwise surface.
Output: root exact thickness binding, hardened pairwise failure DTOs, and `occt-core` wrappers/tests that preserve those semantics for downstream app consumption.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-pairwise-measurement-contract-hardening/08-RESEARCH.md
@.planning/phases/08-pairwise-measurement-contract-hardening/08-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/exact-ref-resolver.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_pairwise_measurement_contract.test.mjs
@test/simple_part.step
@test/assembly.step
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract tests for exact thickness and stable pairwise failures</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, packages/occt-core/src/occt-core.js, packages/occt-core/test/live-root-integration.test.mjs, test/exact_pairwise_measurement_contract.test.mjs, .planning/phases/08-pairwise-measurement-contract-hardening/08-RESEARCH.md</read_first>
  <files>test/exact_pairwise_measurement_contract.test.mjs, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact thickness queries use plane distance for parallel planar face pairs` fails until a planar pair returns `ok === true`, a numeric `value`, `pointA`, `pointB`, `workingPlaneOrigin`, and `workingPlaneNormal`.
    - Test 2: a subtest named `exact thickness failures stay explicit for nonparallel or unsupported geometry` fails until nonparallel or nonface thickness requests return `{ ok: false, code: "unsupported-geometry", message: ... }`.
    - Test 3: a subtest named `exact pairwise failures preserve stable codes and messages` fails until invalid ids, parallel angle queries, and released handles all return explicit `code` / `message` fields.
    - Test 4: a subtest named `occt-core exact thickness wrappers honor repeated parallel occurrences` fails until a repeated-geometry planar face pair from `assembly.step` can produce a nonzero thickness through the `occt-core` wrapper using occurrence transforms.
  </behavior>
  <action>Extend `test/exact_pairwise_measurement_contract.test.mjs` with the exact subtest names from `<behavior>`. Use `test/simple_part.step` for same-model planar thickness and nonparallel failure coverage, and assert `MeasureExactThickness` returns the same success DTO shape as distance. Reuse the same file to assert `MeasureExactAngle` parallel pairs and released exact handles return explicit `code` and `message` fields. Extend `packages/occt-core/test/core.test.mjs` with wrapper tests for `measureExactThickness(refA, refB)` failure preservation. Extend `packages/occt-core/test/live-root-integration.test.mjs` with a repeated-parallel-occurrence smoke test using two planar face refs from `test/assembly.step`.</action>
  <acceptance_criteria>
    - `rg -n "exact thickness queries use plane distance for parallel planar face pairs|exact thickness failures stay explicit for nonparallel or unsupported geometry|exact pairwise failures preserve stable codes and messages|occt-core exact thickness wrappers honor repeated parallel occurrences" test/exact_pairwise_measurement_contract.test.mjs packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `rg -n "MeasureExactThickness|parallel-geometry|released-handle|unsupported-geometry" test/exact_pairwise_measurement_contract.test.mjs packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `node --test test/exact_pairwise_measurement_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_pairwise_measurement_contract.test.mjs</automated>
  </verify>
  <done>The root and adapter contract tests prove the intended exact thickness behavior and stable pairwise failure semantics before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact thickness and shared pairwise failure/result hardening</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, test/exact_pairwise_measurement_contract.test.mjs, .planning/phases/08-pairwise-measurement-contract-hardening/08-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_pairwise_measurement_contract.test.mjs</files>
  <behavior>
    - Test 1: root exact thickness bindings accept one `exactModelId`, two face refs, and two transforms and return the distance-style success DTO shape.
    - Test 2: exact thickness computes plane offset for parallel planar face pairs and does not regress into boundary min-distance semantics.
    - Test 3: pairwise exact results use a shared explicit failure family with `invalid-handle`, `released-handle`, `invalid-id`, `unsupported-geometry`, `parallel-geometry`, `coincident-geometry`, `insufficient-precision`, and `internal-error`.
    - Test 4: `occt-core` exposes `measureExactThickness(refA, refB)` and preserves root success/failure payloads while attaching `refA` / `refB` on success.
  </behavior>
  <action>Add a root exact query method named exactly `MeasureExactThickness` in `src/exact-query.hpp`, `src/exact-query.cpp`, `src/js-interface.cpp`, and `dist/occt-js.d.ts` using the same flattened same-model pairwise signature as `MeasureExactDistance` and `MeasureExactAngle`: `exactModelId, exactShapeHandleA, kindA, elementIdA, exactShapeHandleB, kindB, elementIdB, transformA, transformB`. Limit support to `face` / `face` queries where both transformed exact faces are planar and parallel. Compute thickness as plane distance between the transformed planar faces, not by delegating to generic face-face minimum distance. Return success DTO fields `ok`, `value`, `pointA`, `pointB`, `workingPlaneOrigin`, and `workingPlaneNormal`. At the same time, harden all pairwise root serializers and typings so distance, angle, and thickness share the explicit failure-code family `invalid-handle`, `released-handle`, `invalid-id`, `unsupported-geometry`, `parallel-geometry`, `coincident-geometry`, `insufficient-precision`, and `internal-error`. In `packages/occt-core/src/occt-core.js`, add `measureExactThickness(refA, refB)` using the same wrapper pattern as the new distance/angle wrappers, requiring `refA.exactModelId === refB.exactModelId` in v1.1, preserving root failures unchanged, and attaching `refA` / `refB` to successful results. Export it from `packages/occt-core/src/index.js`.</action>
  <acceptance_criteria>
    - `rg -n "MeasureExactThickness|parallel-geometry|coincident-geometry|insufficient-precision" src/js-interface.cpp dist/occt-js.d.ts packages/occt-core/src/occt-core.js` returns matches.
    - `rg -n "MeasureExactThickness|plane distance|parallel planar" src/exact-query.hpp src/exact-query.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_pairwise_measurement_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_pairwise_measurement_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The exact pairwise contract now includes planar thickness plus stable structured failure semantics across all pairwise measurements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pairwise exact failure serialization -> downstream adapters | Unstable failure codes here would break app-side measurement handling and tests. |
| transformed planar faces -> thickness result | Using boundary min distance instead of plane distance would return plausible but contract-breaking thickness values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | T/R | exact thickness kernel | mitigate | Lock plane-distance semantics and repeated-parallel-occurrence behavior in root and live adapter tests. |
| T-08-02-02 | I | pairwise failure DTOs | mitigate | Harden and test the shared explicit failure-code family across root and `occt-core`. |
| T-08-02-03 | S/T | unsupported geometry routing | mitigate | Reject nonface thickness and nonparallel cases with explicit `unsupported-geometry` instead of guessing from mesh or min-distance behavior. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_pairwise_measurement_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
Downstream occurrence-scoped exact refs can measure planar thickness with exact plane-distance semantics and can rely on stable structured pairwise failure DTOs across root wasm and `occt-core`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pairwise-measurement-contract-hardening/08-02-SUMMARY.md`
</output>
