---
phase: 01-wasm-build-dist-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - tools/build_wasm_win.bat
  - tools/build_wasm_win_dist.bat
  - test/wasm_build_contract.test.mjs
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - "A clean Windows build has one unambiguous path from local toolchain activation to fresh dist/occt-js.js and dist/occt-js.wasm, and that path is proven by `npm run build:wasm:win` followed by root tests per D-02, D-03, and D-05."
    - "Windows build failures still leave build/wasm-build.log and a BUILD_JOBS=1 retry path per D-07 and D-08."
  artifacts:
    - "CMakeLists.txt"
    - "tools/build_wasm_win.bat"
    - "tools/build_wasm_win_dist.bat"
    - "test/wasm_build_contract.test.mjs"
  key_links:
    - "tools/build_wasm_win_dist.bat -> tools/build_wasm_win_release.bat -> tools/build_wasm_win.bat"
    - "tools/build_wasm_win.bat -> build/wasm/emsdk/emsdk_env.bat"
---

<objective>
Make the Windows build scripts and CMake output contract agree on one authoritative runtime-artifact path.

Purpose: Eliminate the current ambiguity between `CMakeLists.txt` output settings and `tools/build_wasm_win_dist.bat` copy assumptions without widening Phase 01 beyond the Windows-first baseline.
Output: Reconciled build scripts plus a fast regression test that locks the script/CMake contract.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wasm-build-dist-baseline/01-CONTEXT.md
@.planning/phases/01-wasm-build-dist-baseline/01-RESEARCH.md
@AGENTS.md
@README.md
@CMakeLists.txt
@tools/build_wasm_win.bat
@tools/build_wasm_win_dist.bat
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the Windows build-path invariants in a fast contract test</name>
  <files>test/wasm_build_contract.test.mjs</files>
  <read_first>
    .planning/phases/01-wasm-build-dist-baseline/01-CONTEXT.md
    .planning/phases/01-wasm-build-dist-baseline/01-RESEARCH.md
    CMakeLists.txt
    tools/build_wasm_win.bat
    tools/build_wasm_win_dist.bat
    test/wasm_build_prereqs.test.mjs
  </read_first>
  <behavior>
    - Test 1: `CMakeLists.txt` and the Windows batch entrypoints agree that `dist/occt-js.js` and `dist/occt-js.wasm` are the post-build runtime artifacts.
    - Test 2: `tools/build_wasm_win.bat` still writes `build/wasm-build.log` and prints `BUILD_JOBS=1` retry guidance.
    - Test 3: `tools/build_wasm_win_dist.bat` no longer passes only because of a stale `build/wasm` copy assumption.
  </behavior>
  <action>Create `test/wasm_build_contract.test.mjs` as a static regression test that reads `CMakeLists.txt`, `tools/build_wasm_win.bat`, and `tools/build_wasm_win_dist.bat` and fails until the scripts encode one authoritative runtime-artifact path. Make the assertions explicit around `dist/occt-js.js`, `dist/occt-js.wasm`, `build/wasm-build.log`, and `BUILD_JOBS=1` so later script edits cannot reintroduce the current ambiguity. This task implements D-02, D-03, D-07, and D-08.</action>
  <acceptance_criteria>
    - `node --test test/wasm_build_contract.test.mjs` passes.
    - `rg -n "dist\\\\occt-js\\.(js|wasm)|wasm-build.log|BUILD_JOBS=1" test/wasm_build_contract.test.mjs` shows the asserted contract strings.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/wasm_build_contract.test.mjs</automated>
  </verify>
  <done>The repository has a dedicated fast test that fails if Windows build scripts drift away from the canonical `dist/` runtime-artifact contract or drop retained-log/retry expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile CMake and Windows batch scripts to generate the canonical dist artifacts directly</name>
  <files>CMakeLists.txt, tools/build_wasm_win.bat, tools/build_wasm_win_dist.bat</files>
  <read_first>
    CMakeLists.txt
    tools/build_wasm_win.bat
    tools/build_wasm_win_release.bat
    tools/build_wasm_win_dist.bat
    test/wasm_build_contract.test.mjs
    README.md
    AGENTS.md
  </read_first>
  <behavior>
    - Test 1: The Windows build wrapper validates `dist/occt-js.js` and `dist/occt-js.wasm` as the authoritative outputs after the release build.
    - Test 2: The build script still logs to `build/wasm-build.log` and preserves the `BUILD_JOBS=1` retry hint.
    - Test 3: No batch script depends on `build/wasm/occt-js.js` or `build/wasm/occt-js.wasm` as a competing runtime-artifact origin.
    - Test 4: The documented clean-Windows proof succeeds by running `npm run build:wasm:win`, confirming `dist/occt-js.js` and `dist/occt-js.wasm` exist, and then running `npm test`.
  </behavior>
  <action>Update `CMakeLists.txt` and the Windows batch scripts so the clean Windows flow produces and validates `dist/occt-js.js` and `dist/occt-js.wasm` directly per D-02 and D-03. Remove the conditional copy-from-`build\\wasm` fallback in `tools/build_wasm_win_dist.bat`, keep `build/wasm/emsdk` as the only local toolchain path, and preserve the retained `build/wasm-build.log` diagnostics plus `BUILD_JOBS=1` retry guidance per D-07 and D-08. Before closing the task, prove the documented clean-Windows path end to end by rebuilding with `npm run build:wasm:win`, checking both canonical `dist/` artifacts, and then running `npm test` per D-05. Do not change the root package’s published artifact names or introduce any demo/desktop-specific behavior.</action>
  <acceptance_criteria>
    - `node --test test/wasm_build_contract.test.mjs` passes after the script changes.
    - `rg -n "RUNTIME_OUTPUT_DIRECTORY.+dist|dist\\\\occt-js\\.(js|wasm)|wasm-build.log|BUILD_JOBS=1" CMakeLists.txt tools/build_wasm_win.bat tools/build_wasm_win_dist.bat` shows the aligned contract.
    - `rg -n "build\\\\wasm\\\\occt-js\\.(js|wasm)" tools/build_wasm_win_dist.bat` returns no matches.
    - `powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if (!(Test-Path 'dist/occt-js.js')) { throw 'Missing dist/occt-js.js' }; if (!(Test-Path 'dist/occt-js.wasm')) { throw 'Missing dist/occt-js.wasm' }; npm test; exit $LASTEXITCODE"` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/wasm_build_contract.test.mjs</automated>
  </verify>
  <done>The Windows build path has one authoritative runtime-artifact destination, the scripts still retain the expected log/retry behavior without relying on stale intermediary copies, and the clean-Windows rebuild regenerates the canonical `dist/` artifacts before the root runtime suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer shell -> Windows batch scripts | Local commands and env vars drive the build entrypoints and can accidentally route outputs to the wrong location |
| build scripts -> dist runtime artifacts | Incorrect copy/validation logic can publish stale or missing runtime artifacts as if they were healthy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `tools/build_wasm_win_dist.bat` | mitigate | Remove the competing `build\\wasm` runtime-artifact assumption and assert the canonical `dist/` outputs via `test/wasm_build_contract.test.mjs` |
| T-01-02 | D | `tools/build_wasm_win.bat` | mitigate | Preserve `build/wasm-build.log` and the `BUILD_JOBS=1` retry guidance so configure/build failures remain diagnosable |
</threat_model>

<verification>
Run `node --test test/wasm_build_contract.test.mjs` after each script edit. Before closing the wave, run `powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if (!(Test-Path 'dist/occt-js.js')) { throw 'Missing dist/occt-js.js' }; if (!(Test-Path 'dist/occt-js.wasm')) { throw 'Missing dist/occt-js.wasm' }; npm test; exit $LASTEXITCODE"` and use the `rg` checks to confirm `build\\wasm` is no longer treated as a competing runtime-artifact origin.
</verification>

<success_criteria>
The Windows batch wrappers and `CMakeLists.txt` agree on the canonical runtime-artifact contract, the retained log path and retry guidance still exist, a fast automated test blocks regressions in that contract, and the documented clean-Windows rebuild path succeeds through `npm run build:wasm:win`, explicit `dist/` artifact checks, and `npm test`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-wasm-build-dist-baseline/01-01-SUMMARY.md`
</output>
