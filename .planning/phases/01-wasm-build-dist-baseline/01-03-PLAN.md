---
phase: 01-wasm-build-dist-baseline
plan: "03"
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - package.json
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - "Maintainers can see one documented clean-Windows sequence centered on `build/wasm/emsdk` and `npm run build:wasm:win` per D-01 and D-02."
    - "Maintainers can run a fast preflight command separately from the full build-dependent runtime verification per D-05 and D-06."
    - "Docs and command surfaces keep `dist/occt-js.js` and `dist/occt-js.wasm` as the only canonical runtime artifacts, while `dist/occt-js.d.ts` remains tracked per D-03 and D-04."
  artifacts:
    - "package.json"
    - "README.md"
    - "AGENTS.md"
  key_links:
    - "package.json:test:wasm:preflight -> test/wasm_build_prereqs.test.mjs + test/load_occt_factory.test.mjs + test/dist_contract_consumers.test.mjs"
    - "README.md + AGENTS.md -> tools/setup_emscripten_win.bat + npm run build:wasm:win + build/wasm-build.log"
---

<objective>
Reconcile the public command surface and documentation with the finalized Phase 01 build and preflight contract.

Purpose: Make the repository’s user-facing instructions and npm scripts match the hardened Windows build path and fast verification split from the earlier plans.
Output: Updated root npm scripts plus aligned README and AGENTS guidance.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wasm-build-dist-baseline/01-CONTEXT.md
@.planning/phases/01-wasm-build-dist-baseline/01-RESEARCH.md
@AGENTS.md
@README.md
@package.json
@tools/setup_emscripten_win.bat
@tools/build_wasm_win.bat
@tools/build_wasm_win_dist.bat
@test/wasm_build_prereqs.test.mjs
@test/load_occt_factory.test.mjs
@test/dist_contract_consumers.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit fast preflight npm command without changing the full runtime gate</name>
  <files>package.json</files>
  <read_first>
    package.json
    test/wasm_build_prereqs.test.mjs
    test/load_occt_factory.test.mjs
    test/dist_contract_consumers.test.mjs
    README.md
    AGENTS.md
  </read_first>
  <action>Add a dedicated root npm script such as `test:wasm:preflight` that runs only the fast Phase 01 checks from Plan 01-02, then update `npm test` to invoke that preflight command before the existing full runtime suite. Keep `npm run build:wasm:win` as the canonical Windows build entrypoint, and do not add demo/Tauri build commands to the root release gate. This task implements D-05 and D-06 while preserving the existing root package boundary.</action>
  <acceptance_criteria>
    - `npm pkg get scripts.test scripts.test:wasm:preflight` returns both scripts.
    - `rg -n "test:wasm:preflight|build:wasm:win" package.json` shows the split command surface.
  </acceptance_criteria>
  <verify>
    <automated>npm pkg get scripts.test scripts.test:wasm:preflight</automated>
  </verify>
  <done>`package.json` exposes one explicit fast preflight command and keeps `npm test` as the full build-dependent verification gate.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the root docs around the finalized Windows build baseline</name>
  <files>README.md, AGENTS.md</files>
  <read_first>
    README.md
    AGENTS.md
    package.json
    tools/setup_emscripten_win.bat
    tools/build_wasm_win.bat
    tools/build_wasm_win_dist.bat
    tools/check_wasm_prereqs.mjs
    test/wasm_build_prereqs.test.mjs
    test/load_occt_factory.test.mjs
    test/dist_contract_consumers.test.mjs
  </read_first>
  <action>Update `README.md` and `AGENTS.md` so both documents describe the same Phase 01 contract: `build/wasm/emsdk` is the only Windows toolchain path per D-01 and D-02; `npm run build:wasm:win` produces the canonical `dist/occt-js.js` and `dist/occt-js.wasm` artifacts per D-03; `dist/occt-js.d.ts` is tracked and restored from git per D-04; `npm run test:wasm:preflight` is the fast prerequisite/error-path check; `npm test` remains the full build-dependent runtime gate; `build/wasm-build.log` and the `BUILD_JOBS=1` retry path remain part of the documented troubleshooting flow per D-07 and D-08. Keep the docs scoped to Phase 01 and do not add CI, release automation, or Linux/macOS parity work.</action>
  <acceptance_criteria>
    - `rg -n "build/wasm/emsdk|npm run build:wasm:win|test:wasm:preflight|npm test|dist/occt-js\\.(js|wasm|d\\.ts)|build/wasm-build.log|BUILD_JOBS=1" README.md AGENTS.md` shows the reconciled contract.
    - `npm run test:wasm:preflight` is the command referenced by the docs for fast preflight verification.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "build/wasm/emsdk|npm run build:wasm:win|test:wasm:preflight|npm test|dist/occt-js\\.(js|wasm|d\\.ts)|build/wasm-build.log|BUILD_JOBS=1" README.md AGENTS.md package.json</automated>
  </verify>
  <done>The repo’s public docs and root command surface tell the same Windows-first story, with a distinct fast preflight command and no ambiguity about canonical `dist/` artifacts or tracked type definitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> maintainer actions | Incorrect or outdated docs can cause maintainers to use the wrong toolchain path or wrong verification command |
| package.json scripts -> release verification | Ambiguous command naming can blur the fast preflight gate with the full build-dependent suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | `README.md`, `AGENTS.md` | mitigate | Rewrite both docs to the same Windows-first contract and point them at the finalized scripts/tests from Plans 01-01 and 01-02 |
| T-01-06 | D | `package.json` | mitigate | Add a dedicated `test:wasm:preflight` script so clean-worktree validation does not require the full runtime suite before artifacts exist |
</threat_model>

<verification>
After the doc/script updates, run `npm pkg get scripts.test scripts.test:wasm:preflight` and the documented `rg` check to prove the public command surface and root docs are aligned.
</verification>

<success_criteria>
Maintainers can follow one documented Windows build path, distinguish fast preflight from full runtime verification, and see the same canonical `dist/` artifact policy in both root docs and npm scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-wasm-build-dist-baseline/01-03-SUMMARY.md`
</output>
