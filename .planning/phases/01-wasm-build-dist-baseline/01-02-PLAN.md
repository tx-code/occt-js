---
phase: 01-wasm-build-dist-baseline
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/check_wasm_prereqs.mjs
  - test/wasm_build_prereqs.test.mjs
  - test/load_occt_factory.mjs
  - test/load_occt_factory.test.mjs
  - test/dist_contract_consumers.test.mjs
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - "Fast prerequisite checks stay separate from the full build-dependent runtime suite per D-05 and D-06."
    - "Missing local prerequisites or missing canonical dist artifacts fail with actionable messages before a maintainer assumes the build is healthy."
    - "Root tests, local web dev, and desktop packaging remain anchored to `dist/occt-js.js` and `dist/occt-js.wasm` per D-03 and D-04."
  artifacts:
    - "tools/check_wasm_prereqs.mjs"
    - "test/wasm_build_prereqs.test.mjs"
    - "test/load_occt_factory.mjs"
    - "test/load_occt_factory.test.mjs"
    - "test/dist_contract_consumers.test.mjs"
  key_links:
    - "tools/check_wasm_prereqs.mjs -> test/wasm_build_prereqs.test.mjs"
    - "test/load_occt_factory.mjs -> test/load_occt_factory.test.mjs"
    - "package.json + demo/src/hooks/useOcct.js + demo/src-tauri/tauri.conf.json -> test/dist_contract_consumers.test.mjs"
---

<objective>
Strengthen the fast preflight and diagnostics surface around missing prerequisites and canonical `dist/` consumers.

Purpose: Give Phase 01 a quick, build-independent verification layer that catches missing markers and dist-contract drift before the full runtime suite is attempted.
Output: Expanded preflight helpers/tests and a static contract test for every current `dist/` consumer.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wasm-build-dist-baseline/01-CONTEXT.md
@.planning/phases/01-wasm-build-dist-baseline/01-RESEARCH.md
@tools/check_wasm_prereqs.mjs
@test/wasm_build_prereqs.test.mjs
@test/load_occt_factory.mjs
@test/load_occt_factory.test.mjs
@demo/src/hooks/useOcct.js
@demo/src-tauri/tauri.conf.json

<interfaces>
From `tools/check_wasm_prereqs.mjs`:
```js
export function resolveRepoRoot(defaultPath);
export function resolveOcctSourceMarker(repoRoot);
export function resolveWindowsEmsdkMarker(repoRoot);
export function resolveTypeDefinitionsMarker(repoRoot);
export function createMissingOcctSubmoduleError(markerPath);
export function createMissingWindowsEmsdkError(markerPath);
export function createMissingTypeDefinitionsError(markerPath);
export function assertOcctSubmodulePresent(repoRoot);
export function assertWindowsEmsdkPresent(repoRoot);
export function assertTypeDefinitionsPresent(repoRoot);
```

From `test/load_occt_factory.mjs`:
```js
export function resolveFactoryPath(defaultPath);
export function createOcctRequireError(factoryPath);
export function loadOcctFactory(factoryPath);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand fast prereq and dist-preflight helpers without pulling in the full runtime suite</name>
  <files>tools/check_wasm_prereqs.mjs, test/wasm_build_prereqs.test.mjs, test/load_occt_factory.mjs, test/load_occt_factory.test.mjs</files>
  <read_first>
    .planning/phases/01-wasm-build-dist-baseline/01-CONTEXT.md
    .planning/phases/01-wasm-build-dist-baseline/01-RESEARCH.md
    tools/check_wasm_prereqs.mjs
    test/wasm_build_prereqs.test.mjs
    test/load_occt_factory.mjs
    test/load_occt_factory.test.mjs
    README.md
  </read_first>
  <behavior>
    - Test 1: Missing `dist/occt-js.js` or `dist/occt-js.wasm` produces actionable guidance that points back to `npm run build:wasm:win`.
    - Test 2: Missing `dist/occt-js.d.ts` still explains that the tracked file must be restored from git rather than regenerated.
    - Test 3: The fast preflight helpers can be run by themselves without requiring a completed Wasm build.
  </behavior>
  <action>Extend the existing helper/test surface so the quick preflight layer covers both prerequisite markers and missing canonical runtime artifacts. Keep `tools/check_wasm_prereqs.mjs` responsible for submodule, local emsdk, and tracked type-definition checks per D-04, and evolve `test/load_occt_factory.mjs` into a small dist-runtime preflight helper that can assert both `dist/occt-js.js` and `dist/occt-js.wasm` before loading the factory. Update the paired test files so this layer stays fast and independent from `npm test`, implementing D-04, D-05, and D-06.</action>
  <acceptance_criteria>
    - `node --test test/wasm_build_prereqs.test.mjs test/load_occt_factory.test.mjs` passes.
    - `rg -n "dist/occt-js\\.(js|wasm)|git restore --source=HEAD -- dist/occt-js.d.ts|npm run build:wasm:win" test/load_occt_factory.mjs test/load_occt_factory.test.mjs test/wasm_build_prereqs.test.mjs` shows the actionable messaging strings.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/wasm_build_prereqs.test.mjs test/load_occt_factory.test.mjs</automated>
  </verify>
  <done>The repository has a fast, build-independent verification layer for missing prerequisites and missing canonical `dist/` artifacts, and it still treats `dist/occt-js.d.ts` as a tracked file restored from git.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a static consumer-contract test for every current `dist/` runtime consumer</name>
  <files>test/dist_contract_consumers.test.mjs</files>
  <read_first>
    package.json
    demo/src/hooks/useOcct.js
    demo/src-tauri/tauri.conf.json
    AGENTS.md
    README.md
    test/load_occt_factory.test.mjs
  </read_first>
  <behavior>
    - Test 1: `package.json` continues to publish `dist/occt-js.js`, `dist/occt-js.wasm`, and tracked `dist/occt-js.d.ts`.
    - Test 2: `demo/src/hooks/useOcct.js` loads the runtime from `dist/occt-js.js` and `dist/occt-js.wasm`, not from `build/wasm`.
    - Test 3: `demo/src-tauri/tauri.conf.json` bundles only the canonical `dist/` runtime artifacts for desktop packaging.
  </behavior>
  <action>Create `test/dist_contract_consumers.test.mjs` as a fast static guard that reads `package.json`, `demo/src/hooks/useOcct.js`, and `demo/src-tauri/tauri.conf.json` and fails if any of them drift away from the Phase 01 artifact policy in D-03 and D-04. Keep the assertions narrow to contract paths and published files; do not turn this into viewer or Tauri behavior testing.</action>
  <acceptance_criteria>
    - `node --test test/wasm_build_prereqs.test.mjs test/load_occt_factory.test.mjs test/dist_contract_consumers.test.mjs` passes.
    - `rg -n "dist/occt-js\\.(js|wasm|d\\.ts)|build/wasm" test/dist_contract_consumers.test.mjs` shows explicit assertions about allowed and disallowed paths.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/wasm_build_prereqs.test.mjs test/load_occt_factory.test.mjs test/dist_contract_consumers.test.mjs</automated>
  </verify>
  <done>There is automated coverage proving the root package, local web path, and Tauri bundle resources all stay attached to the same canonical `dist/` runtime contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer verification commands -> preflight helpers | Fast checks must distinguish missing prerequisites from real runtime failures without requiring a full Wasm build |
| package/demo/Tauri config -> canonical dist artifacts | Static path drift can quietly break downstream consumption while leaving unrelated code untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | D | `tools/check_wasm_prereqs.mjs`, `test/load_occt_factory.mjs` | mitigate | Keep fast helper coverage for missing markers and missing `dist/` artifacts with actionable, tested error messages |
| T-01-04 | T | `package.json`, `demo/src/hooks/useOcct.js`, `demo/src-tauri/tauri.conf.json` | mitigate | Add `test/dist_contract_consumers.test.mjs` to block drift away from canonical `dist/occt-js.js` and `dist/occt-js.wasm` references |
</threat_model>

<verification>
Use `node --test test/wasm_build_prereqs.test.mjs test/load_occt_factory.test.mjs test/dist_contract_consumers.test.mjs` as the fast Phase 01 preflight gate before any full Wasm build attempt.
</verification>

<success_criteria>
Phase 01 has a dedicated fast verification layer that covers missing prerequisites, missing canonical `dist/` artifacts, and consumer-path drift without depending on a freshly built Wasm runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/01-wasm-build-dist-baseline/01-02-SUMMARY.md`
</output>
