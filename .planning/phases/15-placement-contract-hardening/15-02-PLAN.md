---
phase: 15-placement-contract-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - packages/occt-core/src/index.js
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - test/exact_placement_contract.test.mjs
autonomous: true
requirements:
  - PLCT-03
  - ADAPT-07
must_haves:
  truths:
    - "Root circular placement helpers return stable center, anchor, axis, and frame data for supported circular geometry without inventing viewer policy."
    - "`@tx-code/occt-core` exposes package-first placement helpers for both pairwise and circular flows while preserving occurrence transforms."
    - "Package wrappers normalize local circular placement data into occurrence space and keep root semantics visible rather than hiding them behind viewer abstractions."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/importer.hpp"
    - "src/exact-query.hpp"
    - "src/exact-query.cpp"
    - "src/js-interface.cpp"
    - "packages/occt-core/src/index.js"
    - "packages/occt-core/src/occt-core.js"
    - "packages/occt-core/test/core.test.mjs"
    - "packages/occt-core/test/live-root-integration.test.mjs"
    - "test/exact_placement_contract.test.mjs"
  key_links:
    - "existing MeasureExactRadius/Center -> additive SuggestExactRadius/DiameterPlacement helpers"
    - "local circular placement DTOs -> occurrence-space package wrappers"
    - "root placement helpers -> package-first suggestExact*Placement API surface"
---

<objective>
Add circular placement helpers and `occt-core` placement adapter parity.

Purpose: satisfy PLCT-03 and ADAPT-07 by extending the root placement contract to circular geometry and exposing package-first placement helpers that preserve occurrence transforms.
Output: `SuggestExactRadiusPlacement` and `SuggestExactDiameterPlacement` bindings, package-first `occt-core` placement wrappers for all Phase 15 helpers, and package tests proving occurrence-space placement parity.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-placement-contract-hardening/15-CONTEXT.md
@.planning/phases/15-placement-contract-hardening/15-RESEARCH.md
@.planning/phases/15-placement-contract-hardening/15-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_primitive_queries_contract.test.mjs
@test/exact_placement_contract.test.mjs
@test/bearing.igs
@test/assembly.step
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract tests for circular placement and package wrapper parity</name>
  <read_first>dist/occt-js.d.ts, packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_primitive_queries_contract.test.mjs, .planning/phases/15-placement-contract-hardening/15-RESEARCH.md</read_first>
  <files>test/exact_placement_contract.test.mjs, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact radius placement returns center anchor axis and frame for supported circular geometry` fails until a supported retained exact ref returns `ok === true`, `kind === "radius"`, scalar `value`, anchor array, `axisDirection`, and full frame data.
    - Test 2: a subtest named `exact diameter placement returns presentation-oriented circular anchors and frame` fails until a supported retained exact ref returns `ok === true`, `kind === "diameter"`, scalar `value`, anchor array, `axisDirection`, and full frame data.
    - Test 3: a subtest named `occt-core placement wrappers normalize local circular placement into occurrence space` fails until a live exact ref resolved through `occt-core` returns transformed center, anchor, and frame data in occurrence space.
    - Test 4: a subtest named `occt-core exposes package-first placement helpers for pairwise and circular flows` fails until wrapper-shape tests cover `suggestExactDistancePlacement`, `suggestExactAnglePlacement`, `suggestExactThicknessPlacement`, `suggestExactRadiusPlacement`, and `suggestExactDiameterPlacement`.
  </behavior>
  <action>Extend `test/exact_placement_contract.test.mjs` to load `test/bearing.igs` and add the exact subtest names from `<behavior>` for circular placement. Extend `packages/occt-core/test/core.test.mjs` with wrapper-shape coverage for all placement helpers. Extend `packages/occt-core/test/live-root-integration.test.mjs` with one transformed-occurrence smoke test proving circular placement results are normalized into occurrence space and one package-level pairwise placement smoke test proving parity with the root APIs.</action>
  <acceptance_criteria>
    - `rg -n "exact radius placement returns center anchor axis and frame for supported circular geometry|exact diameter placement returns presentation-oriented circular anchors and frame|occt-core placement wrappers normalize local circular placement into occurrence space|occt-core exposes package-first placement helpers for pairwise and circular flows" test/exact_placement_contract.test.mjs packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `rg -n "SuggestExactRadiusPlacement|SuggestExactDiameterPlacement|suggestExactDistancePlacement|suggestExactRadiusPlacement|suggestExactDiameterPlacement" test/exact_placement_contract.test.mjs packages/occt-core/test/core.test.mjs packages/occt-core/test/live-root-integration.test.mjs` returns matches.
    - `npm --prefix packages/occt-core test` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix packages/occt-core test</automated>
  </verify>
  <done>The root and package test surfaces now lock circular placement behavior plus package-first placement wrapper parity before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement circular placement helpers and package-first placement wrappers</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, test/exact_placement_contract.test.mjs, .planning/phases/15-placement-contract-hardening/15-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/index.js, packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_placement_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports `SuggestExactRadiusPlacement` and `SuggestExactDiameterPlacement` as additive single-entity placement helpers on retained exact geometry.
    - Test 2: circular placement helpers return scalar `value`, `axisDirection`, stable anchor arrays, and a full frame for supported circular or cylindrical geometry, with explicit failures for unsupported geometry.
    - Test 3: `@tx-code/occt-core` exports package-first wrapper methods `suggestExactDistancePlacement`, `suggestExactAnglePlacement`, `suggestExactThicknessPlacement`, `suggestExactRadiusPlacement`, and `suggestExactDiameterPlacement`.
    - Test 4: package wrappers preserve pairwise occurrence transforms and normalize local circular placement results into occurrence space while keeping the root placement contract visible.
  </behavior>
  <action>Add root helpers named exactly `SuggestExactRadiusPlacement` and `SuggestExactDiameterPlacement` to `src/exact-query.hpp`, `src/exact-query.cpp`, `src/js-interface.cpp`, and `dist/occt-js.d.ts`. Reuse retained exact circular-geometry identity and the existing radius and center analytic seam to produce circular placement anchors, axis direction, and full frame data. Keep diameter placement presentation-oriented but still geometry-support only. In `packages/occt-core/src/occt-core.js`, add package-first wrapper methods for all placement helpers. Pairwise wrappers should validate both refs, forward transforms unchanged, and append `refA` and `refB` on success. Circular wrappers should validate one ref, call the root helper, and transform centers, anchors, axes, and frame vectors into occurrence space before returning package DTOs. Export the new wrappers from `packages/occt-core/src/index.js`.</action>
  <acceptance_criteria>
    - `rg -n "SuggestExactRadiusPlacement|SuggestExactDiameterPlacement|suggestExactDistancePlacement|suggestExactAnglePlacement|suggestExactThicknessPlacement|suggestExactRadiusPlacement|suggestExactDiameterPlacement" src/exact-query.hpp src/exact-query.cpp src/js-interface.cpp dist/occt-js.d.ts packages/occt-core/src/index.js packages/occt-core/src/occt-core.js` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_placement_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_placement_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The root carrier and `occt-core` now expose the full Phase 15 placement surface, including circular placement and occurrence-safe package wrappers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained exact circular geometry -> placement anchors and frames | Incorrect circular support logic would return valid-looking but unstable placement geometry. |
| local circular placement DTOs -> occurrence-space package wrappers | Incorrect vector or point transformation would corrupt app-facing placement results for reused geometry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | T/R | root circular placement helpers | mitigate | Lock circular placement DTO shape and explicit unsupported-geometry failures in the new root contract test. |
| T-15-02-02 | T/R | occurrence-space package placement wrappers | mitigate | Prove transformed placement points, axes, and frame vectors through `occt-core` unit and live integration tests. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_placement_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
Downstream JS consumers can request both pairwise and circular exact placement support through root Wasm and `@tx-code/occt-core`, with occurrence-safe package DTOs and no spillover into viewer-specific abstractions.
</success_criteria>

<output>
After completion, create `.planning/phases/15-placement-contract-hardening/15-02-SUMMARY.md`
</output>
