---
phase: 15-placement-contract-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - test/exact_placement_contract.test.mjs
autonomous: true
requirements:
  - PLCT-01
  - PLCT-02
  - PLCT-04
must_haves:
  truths:
    - "Root placement helpers stay additive beside the shipped MeasureExact* APIs and do not reshape existing exact measurement DTOs."
    - "Root pairwise placement helpers return stable anchors plus full frame data for supported distance, angle, and thickness flows."
    - "Pairwise placement helpers keep the existing same-model transform-aware signature so repeated geometry remains exact through occurrence transforms."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/importer.hpp"
    - "src/exact-query.hpp"
    - "src/exact-query.cpp"
    - "src/js-interface.cpp"
    - "test/exact_placement_contract.test.mjs"
  key_links:
    - "existing MeasureExactDistance/Angle/Thickness -> additive SuggestExactDistance/Angle/ThicknessPlacement helpers"
    - "exact-query frame helpers -> placement DTO serializers"
    - "root placement contract tests -> stable frame and anchor surface"
---

<objective>
Add root placement helper DTOs and pairwise placement bindings.

Purpose: satisfy PLCT-01, PLCT-02, and PLCT-04 by exposing additive pairwise placement helpers for exact distance, angle, and thickness without changing the shipped exact measurement API shapes.
Output: root placement DTO structs and typings, `SuggestExactDistancePlacement`, `SuggestExactAnglePlacement`, and `SuggestExactThicknessPlacement` bindings, plus root contract tests for supported geometry and explicit failure behavior.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-placement-contract-hardening/15-CONTEXT.md
@.planning/phases/15-placement-contract-hardening/15-RESEARCH.md
@.planning/phases/15-placement-contract-hardening/15-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@test/exact_pairwise_measurement_contract.test.mjs
@test/simple_part.step
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root contract tests for pairwise placement helpers</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, src/exact-query.cpp, test/exact_pairwise_measurement_contract.test.mjs, .planning/phases/15-placement-contract-hardening/15-RESEARCH.md, .planning/phases/15-placement-contract-hardening/15-VALIDATION.md</read_first>
  <files>test/exact_placement_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact distance placement returns anchors and full frame from retained exact refs` fails until a supported same-model pair returns `ok === true`, `kind === "distance"`, scalar `value`, at least two anchors, and frame fields `origin`, `normal`, `xDir`, `yDir`.
    - Test 2: a subtest named `exact angle placement returns origin directions anchors and frame for supported exact pairs` fails until a supported line-line or plane-plane pair returns `ok === true`, `kind === "angle"`, `directionA`, `directionB`, anchor points, and full frame data.
    - Test 3: a subtest named `exact thickness placement returns attach anchors and frame for supported planar pairs` fails until a supported planar pair returns `ok === true`, `kind === "thickness"`, scalar `value`, anchor points, and full frame data.
    - Test 4: a subtest named `pairwise placement failures stay explicit for unsupported or degenerate geometry` fails until unsupported or degenerate cases return `{ ok: false, code: ... }` instead of empty placement payloads.
  </behavior>
  <action>Create `test/exact_placement_contract.test.mjs` as a Node built-in test file that loads the built root carrier via `test/load_occt_factory.mjs`, opens `test/simple_part.step`, resolves retained exact refs from `exactGeometryBindings`, and adds the exact subtest names from `<behavior>`. Call root bindings named exactly `SuggestExactDistancePlacement`, `SuggestExactAnglePlacement`, and `SuggestExactThicknessPlacement` using the same pairwise exact-ref identity plus two 16-value transforms. Assert success DTOs include `kind`, `frame.origin`, `frame.normal`, `frame.xDir`, `frame.yDir`, and stable anchor arrays. Assert failure DTOs preserve explicit codes for unsupported or degenerate cases.</action>
  <acceptance_criteria>
    - `rg -n "exact distance placement returns anchors and full frame from retained exact refs|exact angle placement returns origin directions anchors and frame for supported exact pairs|exact thickness placement returns attach anchors and frame for supported planar pairs|pairwise placement failures stay explicit for unsupported or degenerate geometry" test/exact_placement_contract.test.mjs` returns matches.
    - `rg -n "SuggestExactDistancePlacement|SuggestExactAnglePlacement|SuggestExactThicknessPlacement|frame|anchors" test/exact_placement_contract.test.mjs` returns matches.
    - `node --test test/exact_placement_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_placement_contract.test.mjs</automated>
  </verify>
  <done>The root contract test locks the intended pairwise placement DTO shape and failure behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive pairwise placement DTOs and root bindings</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, test/exact_placement_contract.test.mjs, .planning/phases/15-placement-contract-hardening/15-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, test/exact_placement_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports `SuggestExactDistancePlacement`, `SuggestExactAnglePlacement`, and `SuggestExactThicknessPlacement` with the same flattened same-model pairwise signature as the current pairwise `MeasureExact*` APIs.
    - Test 2: each placement success DTO includes additive placement metadata: `kind`, `frame.origin`, `frame.normal`, `frame.xDir`, `frame.yDir`, and stable `anchors`.
    - Test 3: supported pairwise placement flows reuse the current exact measurement scope: line-line or plane-plane for angle, planar face pairs for thickness, and supported retained exact pairs for distance.
    - Test 4: unsupported or degenerate cases return explicit failures instead of guessed frames or zero vectors.
  </behavior>
  <action>Add placement DTO structs to `src/importer.hpp` and corresponding TypeScript declarations to `dist/occt-js.d.ts`. In `src/exact-query.hpp` and `src/exact-query.cpp`, add root helpers named exactly `SuggestExactDistancePlacement`, `SuggestExactAnglePlacement`, and `SuggestExactThicknessPlacement`. Keep the existing pairwise signature shape `exactModelId, exactShapeHandleA, kindA, elementIdA, exactShapeHandleB, kindB, elementIdB, transformA, transformB`. Reuse current retained exact lookup and exact measurement kernels to compute anchors and derive a normalized right-handed frame with `origin`, `normal`, `xDir`, and `yDir`. In `src/js-interface.cpp`, add the new Embind bindings plus DTO serialization for placement frames and anchors. Preserve the existing `MeasureExactDistance`, `MeasureExactAngle`, and `MeasureExactThickness` APIs unchanged.</action>
  <acceptance_criteria>
    - `rg -n "SuggestExactDistancePlacement|SuggestExactAnglePlacement|SuggestExactThicknessPlacement|xDir|yDir|anchors" src/importer.hpp src/exact-query.hpp src/exact-query.cpp src/js-interface.cpp dist/occt-js.d.ts` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_placement_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_placement_contract.test.mjs"</automated>
  </verify>
  <done>The root carrier now exposes additive pairwise placement helpers with stable placement DTOs while preserving the shipped exact measurement contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| occurrence-scoped exact refs -> root pairwise placement bindings | Numeric transforms cross from JS into exact-placement helper execution here. |
| exact pairwise measurement kernels -> placement DTO frame construction | Incorrect frame derivation would return valid-looking but unstable overlay support geometry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | T/R | root pairwise placement DTO construction | mitigate | Lock the placement DTO shape and failure behavior in a root contract test before implementation. |
| T-15-01-02 | T/I | frame derivation helpers | mitigate | Require explicit failures for unsupported or degenerate geometry instead of guessed frame normals or zero vectors. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_placement_contract.test.mjs`.
</verification>

<success_criteria>
Downstream callers can request pairwise exact placement support for distance, angle, and thickness through additive root APIs that return stable anchors and full frame DTOs without changing the shipped exact measurement result contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/15-placement-contract-hardening/15-01-SUMMARY.md`
</output>
