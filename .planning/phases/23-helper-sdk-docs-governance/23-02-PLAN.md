---
phase: 23-helper-sdk-docs-governance
plan: "02"
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - package.json
  - test/dist_contract_consumers.test.mjs
  - test/package_tarball_contract.test.mjs
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - GOV-04
  - DOCS-03
must_haves:
  truths:
    - "The authoritative root release gate treats exact helper semantics as part of the shipped root/package contract."
    - "Root tarball and governance assertions lock helper docs, helper typing references, and release-boundary wording without widening into secondary surfaces."
    - "Phase 23 keeps demo/Babylon/Tauri verification conditional even while helper governance expands."
  artifacts:
    - path: package.json
      provides: authoritative root release script surface
      contains: helper root contract tests added to `test:release:root` while keeping secondary surfaces out
    - path: test/release_governance_contract.test.mjs
      provides: release wording and script lock
      contains: helper-surface release gate, docs wording, and boundary assertions
    - path: test/package_tarball_contract.test.mjs
      provides: packed root contract coverage
      contains: helper README / typing references and root package boundary assertions
    - path: test/dist_contract_consumers.test.mjs
      provides: downstream consumer contract
      contains: helper-doc wording and packaged consumer-path expectations
  key_links:
    - from: package.json
      to: test/release_governance_contract.test.mjs
      via: authoritative release command parity
      pattern: test:release:root|exact_hole_contract|exact_chamfer_contract
    - from: test/package_tarball_contract.test.mjs
      to: README.md
      via: root tarball helper docs coverage
      pattern: Exact Measurement|Helper|describeExactHole|describeExactChamfer
    - from: test/dist_contract_consumers.test.mjs
      to: packages/occt-core/README.md
      via: package-first helper docs consumer contract
      pattern: describeExactHole|describeExactChamfer|suggestExactMidpointPlacement
---

<objective>
Lock the helper SDK into the authoritative root governance path.

Purpose: finish `GOV-04` by extending the current root contract suites and release script surface so helper docs, helper contract tests, and release-boundary wording drift are caught automatically.
Output: updated root release scripts plus governance/tarball/consumer tests that treat the shipped helper family as part of the authoritative root/package contract while keeping secondary surfaces conditional.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-helper-sdk-docs-governance/23-CONTEXT.md
@.planning/phases/23-helper-sdk-docs-governance/23-PATTERNS.md
@.planning/phases/23-helper-sdk-docs-governance/23-01-PLAN.md
@AGENTS.md
@package.json
@README.md
@packages/occt-core/README.md
@docs/sdk/measurement.md
@dist/occt-js.d.ts
@test/dist_contract_consumers.test.mjs
@test/package_tarball_contract.test.mjs
@test/release_governance_contract.test.mjs
@test/exact_hole_contract.test.mjs
@test/exact_chamfer_contract.test.mjs

<interfaces>
From `package.json` (current authoritative release gate surface to extend):
```json
"test:release:root": "npm run build:wasm:win && node --test test/wasm_build_contract.test.mjs test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs test/exact_pairwise_measurement_contract.test.mjs test/exact_placement_contract.test.mjs test/exact_relation_contract.test.mjs && npm --prefix packages/occt-core test && npm test"
```

From `test/release_governance_contract.test.mjs` (current governance style to reuse):
```javascript
assert.match(readme, /## Exact Measurement SDK/);
assert.match(typesSource, /SuggestExactDistancePlacement/);
```

From `AGENTS.md` (release-boundary rule to preserve):
```markdown
demo, Babylon, and Tauri checks are conditional secondary-surface verification, not unconditional root release gates
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root-governance tests for helper release coverage</name>
  <read_first>package.json, test/release_governance_contract.test.mjs, test/package_tarball_contract.test.mjs, test/dist_contract_consumers.test.mjs, .planning/phases/23-helper-sdk-docs-governance/23-CONTEXT.md</read_first>
  <files>test/release_governance_contract.test.mjs, test/package_tarball_contract.test.mjs, test/dist_contract_consumers.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `authoritative root release command surface includes exact helper contract coverage` fails until `test:release:root` explicitly includes `test/exact_hole_contract.test.mjs` and `test/exact_chamfer_contract.test.mjs`.
    - Test 2: a subtest named `release docs describe the helper SDK package-first while keeping deeper feature discovery downstream` fails until the root/package docs and SDK guide name the shipped helper family and keep only richer feature discovery/viewer UX downstream.
    - Test 3: a subtest named `packed root package helper docs and typings stay aligned with the shipped helper family` fails until the tarball contract suite asserts helper-doc references and root helper typing symbols.
    - Test 4: a subtest named `consumer-facing docs describe the helper SDK without widening secondary-surface requirements` fails until consumer-path docs mention the helper family and retain the conditional-secondary-surface split.
  </behavior>
  <action>Extend the three root governance suites with focused helper assertions using the exact subtest names above. Reuse the current regex/assertion style. Keep the checks on root/package docs, release scripts, and public typing symbols only.</action>
  <acceptance_criteria>
    - `rg -n "includes exact helper contract coverage|describe the helper SDK package-first|helper docs and typings stay aligned|describe the helper SDK without widening secondary-surface requirements" test/release_governance_contract.test.mjs test/package_tarball_contract.test.mjs test/dist_contract_consumers.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs test/package_tarball_contract.test.mjs test/dist_contract_consumers.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs test/package_tarball_contract.test.mjs test/dist_contract_consumers.test.mjs</automated>
  </verify>
  <done>The root-governance test surface now fixes the exact helper release expectations before script and docs changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the authoritative release gate and governance suites for helpers</name>
  <read_first>package.json, test/release_governance_contract.test.mjs, test/package_tarball_contract.test.mjs, test/dist_contract_consumers.test.mjs, README.md, packages/occt-core/README.md, docs/sdk/measurement.md</read_first>
  <files>package.json, test/release_governance_contract.test.mjs, test/package_tarball_contract.test.mjs, test/dist_contract_consumers.test.mjs</files>
  <behavior>
    - Test 1: `npm run test:release:root` now treats hole and chamfer contract suites as part of the authoritative helper release surface.
    - Test 2: governance/tarball/consumer tests assert the helper SDK wording and release-boundary language introduced in Plan 23-01.
    - Test 3: no demo, Babylon, or Tauri checks are added to the authoritative root release gate.
  </behavior>
  <action>Update `package.json` so the authoritative root release gate includes the helper root contract suites in the same runtime-first lane as the existing exact SDK tests. Extend the three root governance suites so helper docs, helper typing references, and package-first boundary wording are treated as release-critical. Keep the gate root/package-only; do not add secondary-surface commands or assertions beyond the existing conditional-boundary wording.</action>
  <acceptance_criteria>
    - `rg -n "exact_hole_contract|exact_chamfer_contract" package.json test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs test/package_tarball_contract.test.mjs test/dist_contract_consumers.test.mjs` passes.
    - `npm run test:release:root` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/release_governance_contract.test.mjs test/package_tarball_contract.test.mjs test/dist_contract_consumers.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:release:root"</automated>
  </verify>
  <done>The helper SDK is now part of the authoritative release-governance path, and drift in scripts/docs/typing references fails automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped helper contract -> authoritative release scripts | If helper contract tests stay outside the release gate, future releases can drift silently |
| helper docs / typing references -> root governance assertions | Missing assertions would let stale or contradictory helper wording ship even after Phase 23 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | T/R | authoritative release script | mitigate | Assert exact helper root contract suites are explicitly included in `test:release:root` |
| T-23-02-02 | T/I | root governance assertions | mitigate | Extend tarball, release-governance, and consumer-path tests to cover helper docs and typing references |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs test/package_tarball_contract.test.mjs test/dist_contract_consumers.test.mjs`.
- Run `npm run test:release:root`.
</verification>

<success_criteria>
The authoritative root release path now fails if the shipped helper family drifts in docs, contract tests, release scripts, or public typing references, while demo/Babylon/Tauri checks remain conditional.
</success_criteria>

<output>
After completion, create `.planning/phases/23-helper-sdk-docs-governance/23-02-SUMMARY.md`
</output>
