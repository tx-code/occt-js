---
phase: 03-downstream-consumption-contract
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - README.md
  - packages/occt-core/README.md
  - test/dist_contract_consumers.test.mjs
autonomous: true
requirements:
  - CONS-01
  - CONS-02
must_haves:
  truths:
    - "Root and package docs describe the verified downstream consumption path clearly and consistently."
    - "The repository docs make the root Wasm carrier and `occt-core` adapter more prominent than repo-local Babylon/demo convenience layers."
    - "Docs and repo-level contract tests agree on the package names, Wasm resolution story, and vendored/tarballed usage expectations."
  artifacts:
    - "README.md"
    - "packages/occt-core/README.md"
    - "test/dist_contract_consumers.test.mjs"
  key_links:
    - "README.md -> root package install/init contract"
    - "packages/occt-core/README.md -> adapter usage contract"
    - "test/dist_contract_consumers.test.mjs -> docs/consumer guardrails"
---

<objective>
Reconcile repository docs with the verified downstream package-consumption contract.

Purpose: Finish Phase 03 by making the documented downstream path match the package and adapter behavior verified in 03-01 and 03-02.
Output: Root/package README updates plus repo-level guards that keep docs aligned with the real downstream contract.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-downstream-consumption-contract/03-RESEARCH.md
@README.md
@packages/occt-core/README.md
@test/dist_contract_consumers.test.mjs
@package.json
@packages/occt-core/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add docs contract checks for the packaged downstream story</name>
  <files>test/dist_contract_consumers.test.mjs</files>
  <behavior>
    - Test 1: Root docs use the current scoped package names and describe the canonical root-package consumption path.
    - Test 2: Root docs mention the supported Wasm resolution hooks (`locateFile` / `wasmBinary`) or an equivalent verified packaged-consumption pattern.
    - Test 3: `occt-core` docs show the engine-agnostic adapter usage path and do not make Babylon/demo layers a prerequisite.
  </behavior>
  <action>Expand `test/dist_contract_consumers.test.mjs` so it guards the new downstream documentation contract in addition to the current `dist/` path checks. Add narrow, explicit assertions for the scoped package names, root package consumption guidance, and `occt-core` package-first usage language. Keep the tests focused on real downstream contract statements, not on cosmetic prose.</action>
  <acceptance_criteria>
    - `node --test test/dist_contract_consumers.test.mjs` passes.
    - `rg -n "@tx-code/occt-js|@tx-code/occt-core|locateFile|wasmBinary|createOcctCore|dist/occt-js.wasm" test/dist_contract_consumers.test.mjs` shows the docs contract checks.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/dist_contract_consumers.test.mjs</automated>
  </verify>
  <done>The repo has automated guardrails that fail when docs stop matching the verified downstream package-consumption story.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite root and `occt-core` docs around the verified downstream package contract</name>
  <files>README.md, packages/occt-core/README.md</files>
  <behavior>
    - Test 1: Root README documents the packaged `@tx-code/occt-js` usage path clearly, including how downstreams locate or provide `occt-js.wasm`.
    - Test 2: `occt-core` README documents the adapter layer as the canonical engine-agnostic consumer surface on top of the root Wasm carrier.
    - Test 3: Docs make clear that Babylon/demo layers are optional secondary surfaces rather than required downstream dependencies.
  </behavior>
  <action>Update `README.md` and `packages/occt-core/README.md` to reflect the final verified Phase 03 contract. Add a package-first usage example for `@tx-code/occt-js`, document the supported Wasm resolution patterns (`locateFile` and/or `wasmBinary`), add a package-first `createOcctCore(...)` example using the root Wasm carrier, and make the secondary nature of repo-local Babylon/demo layers explicit. Keep the docs accurate to the verified contract; do not introduce speculative or future-only guidance.</action>
  <acceptance_criteria>
    - `node --test test/dist_contract_consumers.test.mjs` passes after the doc changes.
    - `rg -n "@tx-code/occt-js|@tx-code/occt-core|locateFile|wasmBinary|createOcctCore|Babylon" README.md packages/occt-core/README.md` shows the package-first downstream guidance.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/dist_contract_consumers.test.mjs</automated>
  </verify>
  <done>The published docs describe one clear downstream contract centered on the root Wasm carrier and `occt-core`, with repo-local viewer layers clearly secondary.</done>
</task>

</tasks>

<success_criteria>
Root/package docs and repo-level guardrails describe one consistent downstream consumption story for `@tx-code/occt-js` and `@tx-code/occt-core`, without making repo-local Babylon/demo layers part of the required contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-downstream-consumption-contract/03-03-SUMMARY.md`
</output>
