---
phase: 03-downstream-consumption-contract
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/src/model-normalizer.js
  - packages/occt-core/src/orientation.js
autonomous: true
requirements:
  - CONS-02
must_haves:
  truths:
    - "`@tx-code/occt-core` remains the canonical engine-agnostic adapter over the root Wasm package."
    - "`createOcctCore(...)` supports predictable loader configuration and explicit import semantics for downstream consumers."
    - "Normalized `occt-core` results are internally consistent for colors, unit metadata, warnings, and stats, without requiring repo-local Babylon/demo layers."
  artifacts:
    - "packages/occt-core/test/core.test.mjs"
    - "packages/occt-core/src/occt-core.js"
    - "packages/occt-core/src/model-normalizer.js"
    - "packages/occt-core/src/orientation.js"
  key_links:
    - "packages/occt-core/src/occt-core.js -> createOcctCore / importModel / loader options"
    - "packages/occt-core/src/model-normalizer.js -> normalized DTO contract"
    - "packages/occt-core/src/orientation.js -> convenience orientation wrapper"
---

<objective>
Tighten the `@tx-code/occt-core` adapter and normalization contract for downstream consumers.

Purpose: Close CONS-02 by turning `occt-core` from an implicitly useful helper into an explicitly verified engine-agnostic adapter on top of the root Wasm carrier.
Output: Stronger package-level contract tests plus the minimum adapter/normalizer fixes required to make downstream use stable.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-downstream-consumption-contract/03-RESEARCH.md
@AGENTS.md
@packages/occt-core/package.json
@packages/occt-core/src/index.js
@packages/occt-core/src/formats.js
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/model-normalizer.js
@packages/occt-core/src/orientation.js
@packages/occt-core/test/core.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Turn the `occt-core` adapter and normalized DTO into an explicit package contract</name>
  <files>packages/occt-core/test/core.test.mjs</files>
  <behavior>
    - Test 1: `createOcctCore(...)` supports `factory`, `factoryGlobalName`, `wasmBinary`, and `wasmBinaryLoader` with explicit, predictable behavior.
    - Test 2: `importModel(...)` uses explicit format selection or file-name inference, and missing/unsupported format cases fail clearly instead of silently guessing unsafe defaults.
    - Test 3: `normalizeOcctResult(...)` returns internally consistent normalized output for colors, warnings, unit metadata, materials, and stats.
    - Test 4: `resolveAutoOrientedModel(...)` remains a Babylon-independent convenience wrapper and does not become a hidden dependency on repo-local viewer code.
  </behavior>
  <action>Expand `packages/occt-core/test/core.test.mjs` into a true downstream contract suite. Add targeted cases for `factoryGlobalName`, `wasmBinaryLoader`, file-name inference, explicit missing-format error boundaries, warning normalization, unit metadata presence/absence, consistent fallback stats/material behavior, and 0-255 color normalization. Add at least one regression case that proves legacy payloads normalize into a single canonical DTO shape rather than leaking mixed historical shapes. Keep the suite focused on `occt-core` as the engine-agnostic adapter, not on Babylon scene-building behavior.</action>
  <acceptance_criteria>
    - `node --test packages/occt-core/test/core.test.mjs` passes.
    - `rg -n "factoryGlobalName|wasmBinaryLoader|sourceUnit|unitScaleToMeters|warnings|missing format|fileName|normalizeOcctResult" packages/occt-core/test/core.test.mjs` shows the explicit contract coverage.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/occt-core/test/core.test.mjs</automated>
  </verify>
  <done>`occt-core` has an automated package-level contract suite that fails when loader setup or normalized DTO behavior drifts in ways downstream consumers would notice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the adapter and normalizer behavior internally consistent for downstream use</name>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/src/model-normalizer.js, packages/occt-core/src/orientation.js</files>
  <behavior>
    - Test 1: Loader configuration and import semantics match the explicit contract from Task 1.
    - Test 2: Color normalization, warnings, unit metadata, and stats are consistent with the DTO that `normalizeOcctResult(...)` actually returns.
    - Test 3: Legacy payload compatibility still works, but normalizes into one canonical shape instead of mixed historical structures.
    - Test 4: The orientation helper remains a convenience layer and does not widen the package into Babylon/demo-specific coupling.
  </behavior>
  <action>Modify `packages/occt-core/src/occt-core.js`, `packages/occt-core/src/model-normalizer.js`, and `packages/occt-core/src/orientation.js` only as needed to satisfy Task 1. Keep `occt-core` package-first and engine-agnostic. Fix any verified normalization bugs that block stable downstream use, especially color normalization and metadata/stat consistency. Make missing-format semantics explicit if the tests prove the current silent default is unsafe. Preserve the current package purpose and do not widen scope into Babylon scene builders, viewer widgets, or repo-local demo concerns.</action>
  <acceptance_criteria>
    - `node --test packages/occt-core/test/core.test.mjs` passes after the code changes.
    - `rg -n "factoryGlobalName|wasmBinaryLoader|sourceUnit|unitScaleToMeters|normalizeColor|normalizeWarnings|stats" packages/occt-core/src/occt-core.js packages/occt-core/src/model-normalizer.js packages/occt-core/src/orientation.js` shows the contract-critical paths.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/occt-core/test/core.test.mjs</automated>
  </verify>
  <done>`@tx-code/occt-core` behaves as a stable, package-level OCCT adapter whose normalized DTO is consistent enough for downstream consumers to build on directly.</done>
</task>

</tasks>

<success_criteria>
`@tx-code/occt-core` is explicitly verified as the canonical engine-agnostic adapter over the root Wasm carrier, with stable loader configuration and normalized DTO behavior for downstream consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-downstream-consumption-contract/03-02-SUMMARY.md`
</output>
