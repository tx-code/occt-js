---
phase: 03-downstream-consumption-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/package_tarball_contract.test.mjs
  - package.json
  - dist/occt-js.d.ts
autonomous: true
requirements:
  - CONS-01
must_haves:
  truths:
    - "A packed `@tx-code/occt-js` tarball exposes only the canonical root runtime surface needed by downstream consumers."
    - "Downstream consumers can resolve `occt-js.wasm` either adjacent to `occt-js.js` or through explicit `locateFile` / `wasmBinary` hooks."
    - "Package metadata and published types make the root package entry and Wasm initialization contract explicit without widening the public surface."
  artifacts:
    - "test/package_tarball_contract.test.mjs"
    - "package.json"
    - "dist/occt-js.d.ts"
  key_links:
    - "package.json -> main/types/files/(optional) exports"
    - "dist/occt-js.js -> locateFile(...) / wasmBinary / adjacent occt-js.wasm"
    - "dist/occt-js.d.ts -> published initialization options"
---

<objective>
Tighten the packaged root Wasm carrier contract for tarballed and vendored downstream consumers.

Purpose: Close the packaged-consumption portion of CONS-01 by proving the packed `@tx-code/occt-js` surface is minimal, explicit, and stable for downstream Wasm resolution.
Output: A tarball-focused contract test plus the minimum package metadata / typing updates needed to keep the root package consumption contract stable.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-downstream-consumption-contract/03-RESEARCH.md
@AGENTS.md
@package.json
@dist/occt-js.d.ts
@dist/occt-js.js
@test/dist_contract_consumers.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the packed root package surface and Wasm initialization contract with a tarball test</name>
  <files>test/package_tarball_contract.test.mjs</files>
  <behavior>
    - Test 1: `npm pack --dry-run --json` reports only the canonical root package surface: `package.json`, `README.md`, `LICENSE`, `dist/occt-js.js`, `dist/occt-js.wasm`, and `dist/occt-js.d.ts`.
    - Test 2: The packed contract never references repo-local `build/wasm` or demo-only asset paths.
    - Test 3: The published typings expose the supported initialization hooks for downstream Wasm resolution and match the actual runtime callback shape closely enough to be relied on.
    - Test 4: A temp consumer view of the packed package can identify the package root entry plus adjacent `occt-js.wasm` without depending on repo-local paths.
  </behavior>
  <action>Create `test/package_tarball_contract.test.mjs` as the tests-first contract for root package consumption. Use `npm pack --dry-run --json` to inspect the packed surface, parse the packed file list, and assert the root tarball only publishes the canonical runtime artifacts and metadata. Also inspect the published `dist/occt-js.d.ts` and generated loader text closely enough to lock the supported `locateFile` / `wasmBinary` contract and the adjacent-wasm assumption. Keep the test focused on the packed root runtime boundary rather than repo-local demo behavior.</action>
  <acceptance_criteria>
    - `node --test test/package_tarball_contract.test.mjs` passes.
    - `rg -n "npm pack|locateFile|wasmBinary|occt-js\\.wasm|README\\.md|LICENSE" test/package_tarball_contract.test.mjs` shows the tarball-focused assertions.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/package_tarball_contract.test.mjs</automated>
  </verify>
  <done>The repository has an automated contract test that fails when the packed `@tx-code/occt-js` surface or its Wasm initialization contract drifts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make package metadata and published types explicit for the supported tarball contract</name>
  <files>package.json, dist/occt-js.d.ts</files>
  <behavior>
    - Test 1: `package.json` makes the intended root package entry contract explicit without reintroducing build-intermediate paths or deep repo-local assumptions.
    - Test 2: The published type declarations match the supported initialization hooks, including the effective `locateFile` callback shape.
    - Test 3: The packed root contract remains limited to the root Wasm carrier and does not expand into viewer/demo package expectations.
  </behavior>
  <action>Modify `package.json` and `dist/occt-js.d.ts` only as needed to satisfy Task 1. Preserve the canonical runtime files and root package purpose. If the tarball test shows the root package surface is still too implicit, add the minimum explicit package metadata needed to encode the intended entrypoint contract. Align the published `locateFile` typing with the supported runtime callback shape and keep `wasmBinary` as an explicit supported hook. Do not add new root Wasm APIs, Babylon/demo dependencies, or non-canonical package files.</action>
  <acceptance_criteria>
    - `powershell -NoProfile -Command "node --test test/package_tarball_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm pack --dry-run --json | Out-Null; exit $LASTEXITCODE"` passes.
    - `rg -n "main|types|files|exports" package.json && rg -n "locateFile|wasmBinary" dist/occt-js.d.ts` shows the explicit packaged contract surface.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/package_tarball_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm pack --dry-run --json | Out-Null; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The root package’s packed surface and published initialization types are explicit, minimal, and stable for downstream vendored/tarballed consumption.</done>
</task>

</tasks>

<success_criteria>
The packed `@tx-code/occt-js` surface is explicitly verified and typed as a standalone Wasm carrier whose `occt-js.wasm` resolution story remains stable for downstream consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-downstream-consumption-contract/03-01-SUMMARY.md`
</output>
