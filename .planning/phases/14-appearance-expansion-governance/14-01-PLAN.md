---
phase: 14-appearance-expansion-governance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/occt-core/README.md
  - AGENTS.md
  - dist/occt-js.d.ts
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - ADAPT-06
must_haves:
  truths:
    - "Root and package docs describe `appearancePreset`, `defaultOpacity`, and the downstream settings boundary."
    - "Type commentary stays aligned with the shipped preset/defaultOpacity contract."
    - "Governance tests fail if the finalized docs drift from the implemented runtime behavior."
  artifacts:
    - "README.md"
    - "packages/occt-core/README.md"
    - "AGENTS.md"
    - "dist/occt-js.d.ts"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "final shipped appearance contract -> root/package docs"
    - "preset/defaultOpacity wording -> type comments"
    - "governance tests -> doc drift prevention"
---

<objective>
Finalize docs and type commentary for the appearance expansion contract.

Purpose: satisfy ADAPT-06 by making the shipped preset/defaultOpacity contract explicit in root/package docs and governance tests without expanding scope into viewer behavior.
Output: updated root/package/agent docs plus governance assertions for the finalized appearance wording.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-appearance-expansion-governance/14-RESEARCH.md
@.planning/phases/14-appearance-expansion-governance/14-VALIDATION.md
@AGENTS.md
@README.md
@packages/occt-core/README.md
@dist/occt-js.d.ts
@test/release_governance_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing governance tests for final appearance-expansion docs</name>
  <read_first>README.md, packages/occt-core/README.md, AGENTS.md, dist/occt-js.d.ts, test/release_governance_contract.test.mjs, .planning/phases/14-appearance-expansion-governance/14-RESEARCH.md</read_first>
  <files>test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: governance fails until root/package docs mention `appearancePreset` and `defaultOpacity`.
    - Test 2: governance fails until repo-level agent guidance names the finalized appearance options explicitly.
    - Test 3: governance fails until the current planning state reflects Phase 14 execution readiness.
  </behavior>
  <action>Extend `test/release_governance_contract.test.mjs` so it asserts the root README, `@tx-code/occt-core` README, `AGENTS.md`, and type comments all mention `appearancePreset`, `defaultOpacity`, and the downstream settings boundary. Update the planning-state assertions so Phase 14 must be recognized as planned/ready for execution once this planning step lands.</action>
  <acceptance_criteria>
    - `rg -n "appearancePreset|defaultOpacity|Phase 14" test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs` fails before doc updates and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The governance suite proves the final appearance-expansion docs before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update root/package docs and type comments for the finalized appearance contract</name>
  <read_first>README.md, packages/occt-core/README.md, AGENTS.md, dist/occt-js.d.ts, test/release_governance_contract.test.mjs, .planning/phases/14-appearance-expansion-governance/14-RESEARCH.md</read_first>
  <files>README.md, packages/occt-core/README.md, AGENTS.md, dist/occt-js.d.ts, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: docs explain the built-in presets, default opacity behavior, and explicit override rules.
    - Test 2: docs keep settings persistence and viewer overrides downstream.
    - Test 3: public type comments describe the final preset/defaultOpacity surface without ambiguity.
  </behavior>
  <action>Update the root README, `packages/occt-core/README.md`, `AGENTS.md`, and any needed `dist/occt-js.d.ts` comments so they describe the shipped `appearancePreset` / `defaultOpacity` contract clearly and package-first. Keep examples at import time, not post-import repaint flows. Update the governance assertions alongside the docs.</action>
  <acceptance_criteria>
    - `rg -n "appearancePreset|defaultOpacity|cad-ghosted|settings persistence|viewer overrides" README.md packages/occt-core/README.md AGENTS.md dist/occt-js.d.ts` returns matches.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The shipped docs and type comments now describe the final appearance-expansion contract precisely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implemented runtime contract -> public docs/type comments | Drift here confuses downstream consumers even if the code remains correct. |
| current milestone state -> governance assertions | If state/tests drift, the GSD flow can report the wrong next step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | T/R | final appearance docs | mitigate | Add failing governance tests for preset/defaultOpacity wording before editing docs. |
| T-14-01-02 | T/I | planning-state/governance drift | mitigate | Update and test Phase 14 readiness assertions together with docs. |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Docs and type comments now describe the final appearance-expansion contract clearly and governance catches drift
</success_criteria>

<output>
After completion, create `.planning/phases/14-appearance-expansion-governance/14-01-SUMMARY.md`
</output>
