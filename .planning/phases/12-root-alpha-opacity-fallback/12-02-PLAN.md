---
phase: 12-root-alpha-opacity-fallback
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/js-interface.cpp
  - src/importer-xde.cpp
  - src/importer-brep.cpp
  - test/import_appearance_contract.test.mjs
autonomous: true
requirements:
  - APPR-06
must_haves:
  truths:
    - "`Read*` and `OpenExact*` expose the same opacity-fallback payload because they share one root import path."
    - "STEP, IGES, and BREP all honor explicit default-opacity semantics under `colorMode: \"default\"`."
    - "`defaultOpacity` stays ignored for `source` mode and legacy no-`colorMode` calls, preserving v1.2 compatibility."
  artifacts:
    - "src/js-interface.cpp"
    - "src/importer-xde.cpp"
    - "src/importer-brep.cpp"
    - "test/import_appearance_contract.test.mjs"
  key_links:
    - "shared root import path -> read/openExact opacity parity"
    - "raw material identity -> opacity-aware result payload"
    - "compatibility matrix -> explicit default mode only"
---

<objective>
Lock parity and compatibility for root opacity fallback.

Purpose: finish APPR-06 by proving the new default-opacity contract behaves the same across stateless and exact-open imports and by making precedence explicit across supported formats.
Output: parity/compatibility contract tests plus any remaining root hardening needed for opacity-aware result payloads.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-root-alpha-opacity-fallback/12-RESEARCH.md
@.planning/phases/12-root-alpha-opacity-fallback/12-VALIDATION.md
@AGENTS.md
@src/js-interface.cpp
@src/importer-xde.cpp
@src/importer-brep.cpp
@test/import_appearance_contract.test.mjs
@test/ANC101_colored.stp
@test/bearing.igs
@test/ANC101_isolated_components.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root tests for opacity parity and compatibility across read/exact lanes</name>
  <read_first>src/js-interface.cpp, src/importer-xde.cpp, src/importer-brep.cpp, test/import_appearance_contract.test.mjs, .planning/phases/12-root-alpha-opacity-fallback/12-RESEARCH.md</read_first>
  <files>test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `OpenExact uses the same defaultOpacity appearance payload as ReadFile` fails until direct and generic exact-open APIs match the stateless read-lane opacity payload.
    - Test 2: a subtest named `defaultOpacity parity holds across colored and colorless fixtures` fails until STEP, IGES, and BREP all honor the same default-opacity semantics.
    - Test 3: a subtest named `defaultOpacity compatibility keeps source and legacy lanes deterministic` fails until `colorMode: "source"` and no-`colorMode` calls keep the shipped v1.2 behavior.
  </behavior>
  <action>Extend `test/import_appearance_contract.test.mjs` with the exact subtest names from `<behavior>`. Reuse the real fixtures and assert that `ReadFile`, `OpenExactModel`, and format-specific `OpenExact*` methods expose the same opacity-bearing appearance payload under explicit default appearance. Add explicit assertions that `defaultOpacity` does not leak into source-mode or legacy calls.</action>
  <acceptance_criteria>
    - `rg -n "OpenExact uses the same defaultOpacity appearance payload as ReadFile|defaultOpacity parity holds across colored and colorless fixtures|defaultOpacity compatibility keeps source and legacy lanes deterministic" test/import_appearance_contract.test.mjs` returns matches.
    - `node --test test/import_appearance_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/import_appearance_contract.test.mjs</automated>
  </verify>
  <done>The root contract tests prove read/openExact opacity parity and compatibility behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden shared root serialization and cross-format parity for defaultOpacity</name>
  <read_first>src/js-interface.cpp, src/importer-xde.cpp, src/importer-brep.cpp, test/import_appearance_contract.test.mjs, .planning/phases/12-root-alpha-opacity-fallback/12-RESEARCH.md</read_first>
  <files>src/js-interface.cpp, src/importer-xde.cpp, src/importer-brep.cpp, test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: exact-open and stateless read lanes stay in parity through the shared root importer/binding path.
    - Test 2: opacity-aware raw materials/colors remain deterministic across supported formats.
    - Test 3: compatibility with the shipped v1.2 appearance contract remains explicit and enforced by tests.
  </behavior>
  <action>Review the shared importer/binding path and harden any remaining seams so opacity-aware colors/materials stay deterministic across STEP, IGES, and BREP. If raw material identity still ignores opacity, update the relevant root serialization logic so opacity-bearing payloads cannot collapse incorrectly. Keep the final result strictly root-scoped: no preset work, no adapter parity, and no viewer-facing logic.</action>
  <acceptance_criteria>
    - `rg -n "opacity|defaultOpacity" src/js-interface.cpp src/importer-xde.cpp src/importer-brep.cpp test/import_appearance_contract.test.mjs` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs"</automated>
  </verify>
  <done>The root Wasm carrier now keeps explicit default-opacity semantics aligned across read and exact-open imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared root import path -> stateless/exact result parity | If this forks, exact workflows and stateless workflows drift immediately. |
| opacity-aware raw colors/materials -> material identity | If identity ignores opacity, the raw payload becomes nondeterministic when transparency policy is active. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | T/R | read/openExact parity | mitigate | Lock parity in the same root contract suite across generic and format-specific exact APIs. |
| T-12-02-02 | T/I | opacity-aware raw material serialization | mitigate | Add cross-format compatibility tests and harden any RGB-only material identity seam if it causes collapse or drift. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/import_appearance_contract.test.mjs test/release_governance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root opacity-fallback behavior is explicit, deterministic, and aligned across read/openExact import paths
</success_criteria>

<output>
After completion, create `.planning/phases/12-root-alpha-opacity-fallback/12-02-SUMMARY.md`
</output>
