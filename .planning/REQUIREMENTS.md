# Requirements: occt-js

No active milestone requirements are currently defined.

Most recently shipped requirements:

- [v1.1 Exact BRep Measurement Foundation](./milestones/v1.1-REQUIREMENTS.md)

Next step:

- Run `/gsd-new-milestone` to define the next active requirement set.

Carry-forward ideas:

- Add an explicit import option that ignores source colors and uses the default CAD color.
- Keep app-side measurement semantics, overlays, and interaction flows downstream from the wasm/core contract unless a future milestone explicitly changes that boundary.
