---
phase: 38-boundary-api-pruning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/src/lib/measurement-actions.js
  - demo/src/hooks/useMeasurement.js
  - demo/src/components/SelectionPanel.jsx
  - demo/tests/measurement-actions.test.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - BOUND-01
  - SDK-04
must_haves:
  truths:
    - "Demo-owned action routing remains explicitly downstream and no longer reads like a package-owned analysis layer."
    - "The browser sample still exposes the same supported measurements/helpers and current-result flow after the ownership cleanup."
    - "No retained root or `@tx-code/occt-core` exact primitive/helper behavior changes in this plan."
  artifacts:
    - path: demo/src/lib/measurement-actions.js
      provides: demo-owned action routing seam
      contains: clearly local naming, explicit supported-action matrix, and no stale candidate-analysis wording
    - path: demo/src/hooks/useMeasurement.js
      provides: demo measurement execution bridge
      contains: imports and controller logic aligned with the tightened demo-local seam
    - path: demo/src/components/SelectionPanel.jsx
      provides: browser-visible action copy
      contains: neutral supported-action wording that does not expose internal ownership jargon
    - path: demo/tests/measurement-actions.test.mjs
      provides: demo seam contract coverage
      contains: failing-first expectations for tightened demo-local naming and absence of stale candidate semantics
    - path: demo/tests/demo.spec.mjs
      provides: browser regression coverage
      contains: stable sample-first measurement assertions after copy or seam tightening
  key_links:
    - from: demo/src/lib/measurement-actions.js
      to: demo/src/hooks/useMeasurement.js
      via: helper-seam renames or API tightening must propagate cleanly through the measurement hook
      pattern: deriveMeasurementAvailability|runMeasurementAction|availability|runMeasurement
    - from: demo/src/components/SelectionPanel.jsx
      to: demo/src/lib/measurement-actions.js
      via: user-facing action copy should match the clarified downstream ownership boundary
      pattern: supported actions|measurement-panel|current result
    - from: demo/tests/measurement-actions.test.mjs
      to: demo/src/lib/measurement-actions.js
      via: tests must lock the demo-local seam and remove stale candidate-style wording
      pattern: candidate|availability|actionId|unsupported
---

<objective>
Tighten the demo-owned measurement action seam so it no longer reads like package-owned analysis behavior.

Purpose: satisfy the first half of `BOUND-01` / `SDK-04` by clarifying that action routing and presentation stay inside `demo/`.
Output: a clearer demo-local helper seam, neutral browser copy, and preserved measurement behavior for the simplified sample-first workflow.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-boundary-api-pruning/38-CONTEXT.md
@.planning/phases/38-boundary-api-pruning/38-RESEARCH.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-01-SUMMARY.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-02-SUMMARY.md
@AGENTS.md
@demo/src/lib/measurement-actions.js
@demo/src/hooks/useMeasurement.js
@demo/src/components/SelectionPanel.jsx
@demo/tests/measurement-actions.test.mjs
@demo/tests/demo.spec.mjs

<interfaces>
From `demo/src/lib/measurement-actions.js`:
```js
deriveMeasurementAvailability({ items })
runMeasurementAction({ core, actionId, items, availability })
```

From `demo/src/hooks/useMeasurement.js`:
```js
const availability = deriveMeasurementAvailability({ items });
const result = await runMeasurementAction({ core, actionId, items, availability });
```

From `demo/src/components/SelectionPanel.jsx`:
```jsx
<div>Explicit demo-owned exact actions</div>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for a clarified demo-local action seam</name>
  <read_first>demo/src/lib/measurement-actions.js, demo/src/hooks/useMeasurement.js, demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs, .planning/phases/38-boundary-api-pruning/38-CONTEXT.md</read_first>
  <files>demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: node tests fail until the demo action helper seam is described and imported as explicitly local rather than stale candidate-style analysis.
    - Test 2: node tests fail until stale wording such as "candidate descriptors" is removed from the demo helper contract.
    - Test 3: browser assertions fail until the inspector presents neutral supported-action wording instead of internal ownership jargon.
  </behavior>
  <action>Write the smallest focused tests first. Lock the intended demo-local seam and neutral UI wording before touching implementation, while preserving the current result workflow from Phase 37.</action>
  <acceptance_criteria>
    - `rg -n "candidate|supported actions|demo-owned|derive|runMeasurement" demo/tests/measurement-actions.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The repo has red tests locking the clarified demo-local seam before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the tightened demo-owned action seam</name>
  <read_first>demo/src/lib/measurement-actions.js, demo/src/hooks/useMeasurement.js, demo/src/components/SelectionPanel.jsx, demo/tests/measurement-actions.test.mjs</read_first>
  <files>demo/src/lib/measurement-actions.js, demo/src/hooks/useMeasurement.js, demo/src/components/SelectionPanel.jsx, demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the action helper seam is obviously demo-local through naming and/or module structure.
    - Test 2: user-facing action copy describes supported exact actions without leaking internal ownership jargon.
    - Test 3: supported action behavior and typed unsupported handling remain unchanged.
  </behavior>
  <action>Tighten the helper seam with the smallest truthful implementation. Rename or reframe demo-local exports as needed, update the hook imports, and reduce stale candidate or ownership wording in UI/test surfaces without reopening package contracts.</action>
  <acceptance_criteria>
    - `rg -n "supported exact actions|candidate|deriveMeasurementAvailability|runMeasurementAction" demo/src/lib/measurement-actions.js demo/src/hooks/useMeasurement.js demo/src/components/SelectionPanel.jsx` reflects the reduced boundary.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build"</automated>
  </verify>
  <done>The browser sample now makes the demo-owned action-routing boundary explicit without changing retained measurement behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconfirm browser behavior after seam cleanup</name>
  <read_first>demo/tests/demo.spec.mjs, AGENTS.md</read_first>
  <files>demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: maintained browser flows still cover import, tool generation, selection, measurement, and current-result inspection.
    - Test 2: no stale browser assertion depends on removed candidate-style or internal-ownership wording.
    - Test 3: the sample-first inspector remains usable on the maintained browser lane.
  </behavior>
  <action>Run the maintained browser suite and adjust only the assertions needed to reflect the clarified demo-local seam. Keep the demo focused on proving integration, not on surfacing internal architecture language.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The demo seam cleanup is browser-locked and ready for the package-focused follow-up plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo helper seam -> perceived SDK surface | If demo-local routing keeps generic names and stale candidate wording, downstream readers can infer package-owned workflow semantics that do not actually ship |
| browser copy -> sample-first boundary | If UI copy exposes internal ownership jargon, the demo stops reading like a clean integration sample |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | I | demo helper seam | mitigate | Write red node tests that lock demo-local naming and remove stale candidate wording before implementation |
| T-38-01-02 | T/R | browser action copy | mitigate | Keep browser assertions around supported-action wording and current-result flow while avoiding behavior drift |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
</verification>

<success_criteria>
The browser demo still proves the same exact measurement loop, but its action-routing seam and UI wording now read clearly as downstream demo behavior rather than as package-owned analysis logic.
</success_criteria>

<output>
After completion, create `.planning/phases/38-boundary-api-pruning/38-01-SUMMARY.md`
</output>
