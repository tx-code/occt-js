---
phase: 38-boundary-api-pruning
plan: "02"
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - packages/occt-core/src/index.js
  - packages/occt-core/src/index.d.ts
  - packages/occt-core/test/package-contract.test.mjs
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - BOUND-01
  - SDK-04
must_haves:
  truths:
    - "Published `@tx-code/occt-core` exports and typings describe only retained reusable runtime/helper behavior."
    - "Demo-owned action routing and session semantics remain absent from package exports, typed names, and targeted governance coverage."
    - "The authoritative `npm run test:release:root` boundary stays unchanged."
  artifacts:
    - path: packages/occt-core/src/index.js
      provides: published JS barrel
      contains: only retained package exports with no demo/session helper leakage
    - path: packages/occt-core/src/index.d.ts
      provides: published typing surface
      contains: retained exact measurement/helper APIs and no demo-owned routing/session descriptors
    - path: packages/occt-core/test/package-contract.test.mjs
      provides: package contract audit
      contains: positive coverage for retained helper primitives and negative coverage for leaked demo/session semantics
    - path: test/release_governance_contract.test.mjs
      provides: targeted root-boundary audit
      contains: assertions that Phase 38 hardening does not widen the authoritative root release gate
  key_links:
    - from: packages/occt-core/src/index.js
      to: packages/occt-core/src/index.d.ts
      via: the JS barrel and published typings must stay aligned on the retained package surface
      pattern: createOcctCore|OcctCoreClient|normalizeExactOpenResult|resolveExactElementRef
    - from: packages/occt-core/test/package-contract.test.mjs
      to: packages/occt-core/src/index.d.ts
      via: contract tests should fail if demo-routing/session semantics leak into published types
      pattern: candidate|session|availability|runMeasurement
    - from: test/release_governance_contract.test.mjs
      to: package.json
      via: governance coverage must keep the root release gate unchanged while package contract hardening evolves
      pattern: test:release:root|test:planning:audit|test:secondary:contracts
---

<objective>
Harden the published package contract around the reduced exact measurement/helper surface.

Purpose: satisfy the second half of `BOUND-01` / `SDK-04` by proving that retained `@tx-code/occt-core` exports stay reusable while demo-owned routing/session semantics stay downstream.
Output: aligned JS/type package surface and stronger contract tests that lock the reduced boundary without widening the root release gate.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-boundary-api-pruning/38-CONTEXT.md
@.planning/phases/38-boundary-api-pruning/38-RESEARCH.md
@.planning/phases/38-boundary-api-pruning/38-01-PLAN.md
@AGENTS.md
@packages/occt-core/src/index.js
@packages/occt-core/src/index.d.ts
@packages/occt-core/src/occt-core.js
@packages/occt-core/test/package-contract.test.mjs
@test/release_governance_contract.test.mjs

<interfaces>
From `packages/occt-core/src/index.js`:
```js
export { createOcctCore, OcctCoreClient } from "./occt-core.js";
export { normalizeExactOpenResult } from "./exact-model-normalizer.js";
export { resolveExactElementRef, resolveExactFaceRef, resolveExactEdgeRef, resolveExactVertexRef } from "./exact-ref-resolver.js";
export { normalizeOcctFormat, getReadMethodName, listSupportedFormats } from "./formats.js";
export { normalizeOcctResult } from "./model-normalizer.js";
export { applyOrientationToModel, resolveAutoOrientedModel } from "./orientation.js";
```

From `packages/occt-core/src/index.d.ts`:
```ts
export declare class OcctCoreClient {
  measureExactDistance(...)
  describeExactHole(...)
  suggestExactMidpointPlacement(...)
  describeExactEqualDistance(...)
  suggestExactSymmetryPlacement(...)
}
```

From `packages/occt-core/test/package-contract.test.mjs`:
```js
assert.doesNotMatch(typesSource, /OcctExactMeasurementCandidate/);
assert.doesNotMatch(typesSource, /analyzeExactMeasurementCandidates/);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing contract tests for the reduced published surface</name>
  <read_first>packages/occt-core/src/index.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/package-contract.test.mjs, test/release_governance_contract.test.mjs, .planning/phases/38-boundary-api-pruning/38-CONTEXT.md</read_first>
  <files>packages/occt-core/test/package-contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: package contract tests fail until retained exports/types are explicitly locked against demo-routing/session leakage.
    - Test 2: package contract tests fail until negative checks cover stale demo-local naming that must never publish through `@tx-code/occt-core`.
    - Test 3: targeted governance tests fail until the package hardening still leaves `npm run test:release:root` unchanged.
  </behavior>
  <action>Write the smallest focused red tests first. Favor positive coverage for retained package exports and negative coverage for demo-owned action/session semantics leaking into published types or governance assertions.</action>
  <acceptance_criteria>
    - `rg -n "candidate|session|availability|runMeasurement|test:release:root" packages/occt-core/test/package-contract.test.mjs test/release_governance_contract.test.mjs` returns matches.
    - `npm --prefix packages/occt-core test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The repo has red package/governance tests locking the reduced public surface before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten package exports and typings only where the audit proves it is necessary</name>
  <read_first>packages/occt-core/src/index.js, packages/occt-core/src/index.d.ts, packages/occt-core/src/occt-core.js, packages/occt-core/test/package-contract.test.mjs</read_first>
  <files>packages/occt-core/src/index.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/package-contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: the published barrel stays aligned with the retained package contract and does not re-export demo/session helpers.
    - Test 2: published typings remain complete for retained exact primitives/helpers while excluding demo-local routing/session names.
    - Test 3: any implementation change stays minimal and additive-safe.
  </behavior>
  <action>Implement the smallest truthful fix. If the audit confirms the current barrel/types are already minimal, keep code churn low and let stronger tests be the primary artifact. If an ambiguous export or typed name exists, remove or tighten it without reopening runtime behavior.</action>
  <acceptance_criteria>
    - `rg -n "createOcctCore|OcctCoreClient|normalizeExactOpenResult|runMeasurement|availability|session" packages/occt-core/src/index.js packages/occt-core/src/index.d.ts` reflects the retained boundary.
    - `npm --prefix packages/occt-core test` passes.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/release_governance_contract.test.mjs"</automated>
  </verify>
  <done>The published package surface is now locked around retained reusable behavior and explicit non-goals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconfirm no accidental surface widening and prepare closeout-ready notes</name>
  <read_first>AGENTS.md, packages/occt-core/test/package-contract.test.mjs, test/release_governance_contract.test.mjs</read_first>
  <files>packages/occt-core/test/package-contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: package verification passes without invoking demo, Babylon, or Tauri gates.
    - Test 2: targeted governance checks confirm the authoritative root release route is unchanged.
    - Test 3: closeout notes explicitly record whether `superpowers:requesting-code-review` was skipped under repo rules.
  </behavior>
  <action>Finish the targeted verification story for the pruned package contract. Keep the verification scope honest and limited to the touched package/governance surfaces.</action>
  <acceptance_criteria>
    - `npm --prefix packages/occt-core test` passes.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/release_governance_contract.test.mjs"</automated>
  </verify>
  <done>The package-side boundary cleanup is verified and ready for the docs/governance realignment phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published package surface -> downstream expectations | If `@tx-code/occt-core` publishes ambiguous action/session names, downstream apps may infer workflow/product semantics that do not actually belong to the package |
| package hardening -> root release routing | If contract hardening accidentally widens the authoritative root gate, the repo boundary regresses even if APIs look cleaner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | I | published package exports/types | mitigate | Add red positive/negative contract tests before changing barrel/types |
| T-38-02-02 | T/R | governance routing | mitigate | Add targeted governance assertions so package hardening cannot silently widen `npm run test:release:root` |
</threat_model>

<verification>
- Run `npm --prefix packages/occt-core test`.
- Run `node --test test/release_governance_contract.test.mjs`.
- Apply `superpowers:requesting-code-review` before closeout if the user authorizes delegated review; otherwise record the explicit skip reason in the phase summary per `AGENTS.md`.
</verification>

<success_criteria>
`@tx-code/occt-core` exports and typings now describe only retained reusable exact behavior, while demo-owned routing/session semantics remain explicitly downstream and the authoritative root release boundary stays unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/38-boundary-api-pruning/38-02-SUMMARY.md`
</output>
