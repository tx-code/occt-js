---
phase: 37-demo-simplification-sample-first-workflow
plan: "02"
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - demo/src/store/viewerStore.js
  - demo/src/hooks/useMeasurement.js
  - demo/src/App.jsx
  - demo/src/hooks/useViewerActions.js
  - demo/src/components/DropZone.jsx
  - demo/src/components/Toolbar.jsx
  - demo/tests/viewer-store.test.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - DEMO-05
  - UX-04
must_haves:
  truths:
    - "Demo-owned workflow state is reduced to the minimum needed for one live import or generate-tool or select or measure loop."
    - "Tool and workpiece setup remain explicit and manual, with no default sample autoload and no hidden workflow branch required to reach the demo's primary value."
    - "Workspace invalidation on reset, actor replacement, exact-session replacement, and actor-pose change stays deterministic after subtraction."
  artifacts:
    - path: demo/src/store/viewerStore.js
      provides: reduced demo workflow state
      contains: minimal retained measurement state, preserved workspace invalidation seams, and no leftover compare-oriented session machinery
    - path: demo/src/hooks/useMeasurement.js
      provides: state-aligned measurement bridge
      contains: hook logic matching the reduced store shape and simplified inspector ownership
    - path: demo/src/App.jsx
      provides: simplified shell composition
      contains: explicit sample-first wiring for import, tool generation, and selection or measurement surfaces
    - path: demo/src/hooks/useViewerActions.js
      provides: explicit workflow entrypoint orchestration
      contains: manual import or sample or close behavior with subtraction-oriented browser semantics
    - path: demo/src/components/DropZone.jsx
      provides: empty-state workflow guidance
      contains: clearer import and generated-tool copy without product-style branching
    - path: demo/src/components/Toolbar.jsx
      provides: loaded-state workflow controls
      contains: tightened entrypoints and reduced workflow emphasis aligned with the simplified sample
    - path: demo/tests/viewer-store.test.mjs
      provides: reduced-state and invalidation coverage
      contains: explicit assertions for the simplified measurement state and preserved invalidation boundaries
    - path: demo/tests/demo.spec.mjs
      provides: browser regression coverage
      contains: reduced workflow assertions for import, tool generation, measurement, clear, and invalidation behavior
  key_links:
    - from: demo/src/store/viewerStore.js
      to: demo/src/hooks/useMeasurement.js
      via: store-state subtraction must be reflected exactly in the measurement hook contract
      pattern: measurementRuns|activeMeasurementId|clearMeasurements|reset
    - from: demo/src/hooks/useViewerActions.js
      to: demo/src/components/DropZone.jsx
      via: empty-state and toolbar entrypoints should describe the same simplified workflow
      pattern: importModelFile|openSample|openFile|closeModel
    - from: demo/src/App.jsx
      to: demo/src/lib/sample-autoload.js
      via: the browser app must continue to keep sample loading manual unless explicitly opted in
      pattern: shouldAutoLoadSample|openSample
---

<objective>
Remove unnecessary demo workflow state and tighten tool or workpiece interaction surfaces.

Purpose: finish `DEMO-05` and `UX-04` by aligning demo state and entrypoints with the simplified inspector from `37-01`.
Output: reduced measurement session state, clearer import or generate-tool workflow surfaces, and preserved invalidation behavior without product-style baggage.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-CONTEXT.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-RESEARCH.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-01-PLAN.md
@AGENTS.md
@demo/src/store/viewerStore.js
@demo/src/hooks/useMeasurement.js
@demo/src/App.jsx
@demo/src/hooks/useViewerActions.js
@demo/src/components/DropZone.jsx
@demo/src/components/Toolbar.jsx
@demo/src/lib/sample-autoload.js
@demo/tests/viewer-store.test.mjs
@demo/tests/demo.spec.mjs

<interfaces>
From `demo/src/store/viewerStore.js`:
```js
measurementRuns: []
activeMeasurementId: null
measurementRunSeq: 0
recordMeasurementRun(run)
setActiveMeasurement(measurementId)
clearMeasurements()
```

From `demo/src/hooks/useViewerActions.js`:
```js
return {
  closeModel,
  importModelFile,
  openFile,
  openSample,
}
```

From `demo/src/App.jsx`:
```jsx
<DropZone visible={!model} onFile={importModelFile} onOpenGenerator={() => setGeneratorOpen(true)} />
<Toolbar onOpenFile={openFile} onOpenGenerator={() => setGeneratorOpen(true)} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for reduced demo state and workflow entrypoints</name>
  <read_first>demo/src/store/viewerStore.js, demo/src/hooks/useMeasurement.js, demo/src/App.jsx, demo/src/hooks/useViewerActions.js, demo/src/components/DropZone.jsx, demo/src/components/Toolbar.jsx, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs, .planning/phases/37-demo-simplification-sample-first-workflow/37-CONTEXT.md</read_first>
  <files>demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: store tests fail until unnecessary compare-oriented measurement state is removed or collapsed into the reduced state shape chosen for the simplified demo.
    - Test 2: browser tests fail until the empty state and loaded state reinforce one explicit workflow for import and optional tool generation without default sample assumptions.
    - Test 3: invalidation tests still fail until reset, actor replacement, and actor-pose changes clear the reduced measurement state deterministically.
  </behavior>
  <action>Tighten node and browser expectations before implementation. Remove stale assertions for product-style workflow state, then add focused assertions for the reduced state model and the clearer import-plus-tool setup flow.</action>
  <acceptance_criteria>
    - `rg -n "measurementRuns|activeMeasurementId|reset|actor replacement|pose change|drop-zone|toolbar" demo/tests/viewer-store.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The reduced workflow state and entrypoint expectations are locked in red tests before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reduced state and tighten workflow surfaces</name>
  <read_first>demo/src/store/viewerStore.js, demo/src/hooks/useMeasurement.js, demo/src/App.jsx, demo/src/hooks/useViewerActions.js, demo/src/components/DropZone.jsx, demo/src/components/Toolbar.jsx, demo/tests/viewer-store.test.mjs</read_first>
  <files>demo/src/store/viewerStore.js, demo/src/hooks/useMeasurement.js, demo/src/App.jsx, demo/src/hooks/useViewerActions.js, demo/src/components/DropZone.jsx, demo/src/components/Toolbar.jsx, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the store retains only the measurement state actually needed by the simplified inspector and clear flow.
    - Test 2: import and generated-tool entrypoints remain explicit and browser-first, with sample loading still manual or opt-in only.
    - Test 3: actor-pose and workspace invalidation behavior remains unchanged in meaning even if the underlying state shape is smaller.
  </behavior>
  <action>Collapse leftover measurement session state, then simplify the demo entrypoints and copy so the app reads as an integration sample. Keep the browser empty state honest about what the user should do next and keep generated tools as a supporting path rather than a competing product mode.</action>
  <acceptance_criteria>
    - `rg -n "measurementRuns|activeMeasurementId|measurementRunSeq|openSample|open-generated-tool|Browse Files|Tool MVP" demo/src/store/viewerStore.js demo/src/hooks/useMeasurement.js demo/src/App.jsx demo/src/hooks/useViewerActions.js demo/src/components/DropZone.jsx demo/src/components/Toolbar.jsx` returns matches aligned with the reduced design.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build"</automated>
  </verify>
  <done>The demo's state model and workflow entrypoints now match the smaller sample-first story.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize browser regression and closeout-ready verification notes</name>
  <read_first>demo/tests/demo.spec.mjs, AGENTS.md</read_first>
  <files>demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: browser flows still cover import, tool generation, selection, measurement, clear, and actor-pose invalidation after the workflow reduction.
    - Test 2: maintained mobile assertions still pass with the tightened toolbar and selection-panel behavior.
    - Test 3: closeout notes explicitly record whether the required code-review gate ran or was skipped under repository rules.
  </behavior>
  <action>Finish browser assertions for the reduced workflow and leave execution ready for closeout discipline. Keep the verification surface honest about what the sample still proves.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The reduced workflow is browser-locked and the phase is ready for closeout discipline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reduced measurement state -> invalidation rules | If state subtraction forgets an invalidation seam, stale exact results will survive reset, replacement, or actor motion |
| simplified entrypoints -> sample-first story | If the empty or loaded states still emphasize multiple competing workflows, the demo will continue to read like a half-product |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | T/R | reduced measurement state | mitigate | Add failing store tests before implementation and preserve hard invalidation boundaries in the smaller state model |
| T-37-02-02 | I | workflow entrypoints | mitigate | Add browser assertions that lock the reduced import-plus-tool sample flow and keep sample loading opt-in only |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
- Apply `superpowers:requesting-code-review` before closeout if the user authorizes delegated review; otherwise record the explicit skip reason in the phase summary per `AGENTS.md`.
</verification>

<success_criteria>
The browser demo keeps one explicit import-plus-tool-plus-measure integration loop, backed by a smaller state model and clearer entrypoints, without reintroducing default sample loading or product-style session complexity.
</success_criteria>

<output>
After completion, create `.planning/phases/37-demo-simplification-sample-first-workflow/37-02-SUMMARY.md`
</output>
