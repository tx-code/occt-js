---
phase: 37-demo-simplification-sample-first-workflow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/src/components/SelectionPanel.jsx
  - demo/src/hooks/useMeasurement.js
  - demo/src/lib/measurement-actions.js
  - demo/tests/measurement-actions.test.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - DEMO-05
  - UX-04
must_haves:
  truths:
    - "The selection inspector exposes only explicit supported measurement or helper actions for the current actor-scoped selection and never implies package-owned candidate intelligence."
    - "The measurement surface reads as a sample-first current-result inspector, not a compare-oriented measurement workspace."
    - "The browser demo still proves the import or generate-tool or select or measure loop on both desktop and mobile layouts."
  artifacts:
    - path: demo/src/components/SelectionPanel.jsx
      provides: simplified sample-first inspector
      contains: selection metadata, compact tool-pose controls, explicit supported actions, current-result presentation, and minimal clear behavior
    - path: demo/src/hooks/useMeasurement.js
      provides: inspector-facing measurement controller
      contains: simplified current-result bridge, explicit unsupported handling, and reduced presentation helpers
    - path: demo/src/lib/measurement-actions.js
      provides: explicit action presentation metadata
      contains: supported action ordering, presentation grouping or labeling, and conservative unsupported reasoning
    - path: demo/tests/measurement-actions.test.mjs
      provides: focused action-presentation coverage
      contains: explicit supported action surface and typed unsupported outcomes for sample-first UI expectations
    - path: demo/tests/demo.spec.mjs
      provides: browser workflow regression coverage
      contains: select, run, inspect current result, and clear behavior without product-style history assumptions
  key_links:
    - from: demo/src/components/SelectionPanel.jsx
      to: demo/src/lib/measurement-actions.js
      via: the simplified inspector should stay grounded in the demo-owned explicit action matrix
      pattern: action|category|unsupported|measurement
    - from: demo/src/hooks/useMeasurement.js
      to: demo/src/store/viewerStore.js
      via: inspector simplification must still respect the authoritative workspace invalidation seam
      pattern: measurementRuns|activeMeasurementId|clearMeasurements|setActiveMeasurement
    - from: demo/tests/demo.spec.mjs
      to: demo/src/components/SelectionPanel.jsx
      via: browser verification should assert the reduced inspector workflow, not the removed product-style controls
      pattern: selection-panel|measurement-panel|measurement-action|measurement-clear
---

<objective>
Simplify the selection and measurement inspector into a clearer integration sample.

Purpose: satisfy the first half of `DEMO-05` and `UX-04` by reducing product-style measurement presentation while keeping the exact integration loop explicit and testable.
Output: a smaller selection panel, explicit supported action presentation, and a current-result-centric measurement UI that no longer centers comparison or rerun workflow.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-CONTEXT.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-RESEARCH.md
@.planning/milestones/v1.10-phases/34-measurement-commands-overlay-mvp/34-01-PLAN.md
@.planning/milestones/v1.10-phases/34-measurement-commands-overlay-mvp/34-02-SUMMARY.md
@AGENTS.md
@demo/src/components/SelectionPanel.jsx
@demo/src/hooks/useMeasurement.js
@demo/src/lib/measurement-actions.js
@demo/src/store/viewerStore.js
@demo/tests/measurement-actions.test.mjs
@demo/tests/demo.spec.mjs

<interfaces>
From `demo/src/hooks/useMeasurement.js`:
```js
return {
  actions,
  unsupportedReason,
  runningActionId,
  measurementRuns,
  activeMeasurementId,
  activeMeasurement,
  activeMeasurementOverlay,
  runMeasurement,
  rerunMeasurement,
  clearMeasurements,
  setActiveMeasurement,
};
```

From `demo/src/lib/measurement-actions.js`:
```js
deriveMeasurementAvailability({ items })
runMeasurementAction({ core, actionId, items, availability })
```

From `demo/src/components/SelectionPanel.jsx`:
```jsx
<MeasurementPanel ensureModule={ensureModule} hasSelection={hasSelection} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for the simplified inspector contract</name>
  <read_first>demo/src/components/SelectionPanel.jsx, demo/src/hooks/useMeasurement.js, demo/src/lib/measurement-actions.js, demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs, .planning/phases/37-demo-simplification-sample-first-workflow/37-CONTEXT.md</read_first>
  <files>demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: node tests fail until supported actions are presented in the reduced sample-first shape expected by the inspector.
    - Test 2: node or browser tests fail until unsupported selections still show typed feedback instead of disappearing silently.
    - Test 3: browser smoke fails until users can run one supported measurement, inspect one clear current result, and clear it without relying on compare or rerun workflow.
  </behavior>
  <action>Write the smallest focused tests first around explicit action presentation and reduced measurement-panel expectations, then add one browser smoke that proves the simplified import-plus-tool-plus-measure loop. Remove assertions that lock product-style history controls if they no longer fit the reduced surface.</action>
  <acceptance_criteria>
    - `rg -n "measurement-action|unsupported|current result|clear" demo/tests/measurement-actions.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The repo has red tests locking the simplified inspector behavior before UI changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the sample-first inspector and current-result presentation</name>
  <read_first>demo/src/components/SelectionPanel.jsx, demo/src/hooks/useMeasurement.js, demo/src/lib/measurement-actions.js, demo/tests/measurement-actions.test.mjs</read_first>
  <files>demo/src/components/SelectionPanel.jsx, demo/src/hooks/useMeasurement.js, demo/src/lib/measurement-actions.js, demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the inspector shows only the explicit supported actions relevant to the current selection.
    - Test 2: the measurement panel emphasizes the current result and explicit clear behavior instead of compare-oriented row switching or rerun-first workflow.
    - Test 3: overlay status or panel-only fallback messaging remains explicit but compact.
  </behavior>
  <action>Simplify the measurement section inside the existing selection panel. Keep the inspector compact, selection-driven, and scan-friendly; reduce measurement chrome that makes the demo read like a product workspace. Preserve typed statuses and supported action clarity.</action>
  <acceptance_criteria>
    - `rg -n "measurement-panel|unsupportedReason|activeMeasurement|clearMeasurements" demo/src/components/SelectionPanel.jsx demo/src/hooks/useMeasurement.js demo/src/lib/measurement-actions.js` returns matches.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build"</automated>
  </verify>
  <done>The demo inspector now presents measurement as a narrower sample-first workflow centered on the current result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finish browser-visible inspector simplification for desktop and mobile</name>
  <read_first>demo/src/components/SelectionPanel.jsx, demo/tests/demo.spec.mjs</read_first>
  <files>demo/src/components/SelectionPanel.jsx, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: desktop measurement flows remain readable and deterministic with the reduced inspector.
    - Test 2: mobile selection or measurement layout still avoids overlapping critical utility panels after simplification.
    - Test 3: browser assertions no longer depend on removed history-management controls.
  </behavior>
  <action>Finish the visible panel reduction and update browser expectations so the sample-first workflow is locked on both desktop and mobile. Keep the demo usable, but avoid reintroducing product-style workflow affordances just to satisfy old tests.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The browser demo now exposes a smaller inspector that still proves the import-plus-tool-plus-measure loop across maintained layouts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| explicit action matrix -> inspector UI | If the simplified UI hides or renames supported actions ambiguously, the sample will misrepresent package capabilities |
| measurement state -> reduced inspector | If product-style controls are removed without a clear replacement, users may lose the visible proof of successful measurement integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | T/R | simplified inspector | mitigate | Add failing node and browser tests for explicit action presentation and one clear current-result flow before implementation |
| T-37-01-02 | I | reduced measurement UX | mitigate | Keep typed unsupported messaging and browser-visible clear behavior while removing compare-oriented chrome |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
</verification>

<success_criteria>
The browser demo shows measurement as an explicit, current-result-centric integration sample instead of a product-style inspector workspace, while preserving the supported workpiece-plus-tool measurement loop.
</success_criteria>

<output>
After completion, create `.planning/phases/37-demo-simplification-sample-first-workflow/37-01-SUMMARY.md`
</output>
