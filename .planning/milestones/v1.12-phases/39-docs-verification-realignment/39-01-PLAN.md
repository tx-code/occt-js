---
phase: 39-docs-verification-realignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/sdk/measurement.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DOCS-07
must_haves:
  truths:
    - "Root and SDK docs describe the browser demo as a simplified integration sample with explicit supported actions and current-result inspection."
    - "Active planning truth no longer advertises removed product-style demo workflow states as current behavior."
    - "The documentation rewrite does not recreate dedicated demo workflow docs or promise richer shipped measurement UX."
  artifacts:
    - path: README.md
      provides: top-level measurement/helper surface explanation
      contains: reduced sample-first wording aligned with the retained package/runtime boundary
    - path: docs/sdk/measurement.md
      provides: package-first measurement/helper guide
      contains: reduced downstream ownership wording aligned with current demo behavior
    - path: .planning/PROJECT.md
      provides: active milestone truth
      contains: current validated/current-state wording that matches the reduced demo surface
  key_links:
    - from: README.md
      to: docs/sdk/measurement.md
      via: both docs must use the same reduced wording for what remains downstream app behavior
      pattern: supported exact actions|current-result session behavior|integration sample
    - from: .planning/PROJECT.md
      to: .planning/phases/37-demo-simplification-sample-first-workflow/37-01-SUMMARY.md
      via: active planning truth should reflect the sample-first/current-result demo behavior already accepted in Phase 37
      pattern: current result|sample|rerun|compare|clear
---

<objective>
Rewrite the live documentation around the reduced measurement surface and integration-sample boundary.

Purpose: satisfy `DOCS-07` by making root docs, the SDK guide, and active planning truth describe only the retained sample-first/current-result workflow.
Output: aligned docs in `README.md`, `docs/sdk/measurement.md`, and `.planning/PROJECT.md`.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-docs-verification-realignment/39-CONTEXT.md
@.planning/phases/39-docs-verification-realignment/39-RESEARCH.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-01-SUMMARY.md
@.planning/phases/37-demo-simplification-sample-first-workflow/37-02-SUMMARY.md
@.planning/phases/38-boundary-api-pruning/38-02-SUMMARY.md
@AGENTS.md
@README.md
@docs/sdk/measurement.md

<interfaces>
Current README wording:
```md
The shipped browser demo keeps selection-to-measure mapping, action presentation, overlay rendering, and transient run history in app code.
```

Current SDK-guide wording:
```md
The shipped browser demo consumes the same measurement and helper methods, but selection-to-measure mapping, action presentation, overlay rendering, and transient run history remain downstream app concerns rather than package APIs.
```

Current active planning drift:
```md
- ✓ The browser demo now supports typed exact measurement runs, rerun/compare/clear flows, and placement-backed overlay inspection without widening the root DTO boundary. — Phase 34
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing docs/governance expectations for the reduced wording</name>
  <read_first>README.md, docs/sdk/measurement.md, .planning/PROJECT.md, test/secondary_surface_contract.test.mjs, .planning/phases/39-docs-verification-realignment/39-CONTEXT.md</read_first>
  <files>test/secondary_surface_contract.test.mjs, npm run test:planning:audit</files>
  <behavior>
    - Test 1: secondary-surface contract fails until README and SDK docs stop advertising stale `selection-to-measure mapping` / `transient run history` semantics.
    - Test 2: planning audit or focused planning truth checks expose the remaining active `.planning/PROJECT.md` drift.
    - Test 3: red tests lock the reduced wording before the doc rewrite starts.
  </behavior>
  <action>Write the smallest focused failing expectations first so Phase 39 rewrites live docs against explicit red evidence instead of editing prose speculatively.</action>
  <acceptance_criteria>
    - `rg -n "supported exact action routing|current-result session behavior|selection-to-measure mapping|transient run history|rerun/compare/clear flows" README.md docs/sdk/measurement.md .planning/PROJECT.md test/secondary_surface_contract.test.mjs` returns the targeted drift/evidence lines.
    - `node --test test/secondary_surface_contract.test.mjs` fails before implementation.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/secondary_surface_contract.test.mjs"</automated>
  </verify>
  <done>Focused red expectations now pin the reduced wording that the live docs and active planning truth must adopt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the minimal truthful docs and active-truth rewrite</name>
  <read_first>README.md, docs/sdk/measurement.md, .planning/PROJECT.md, packages/occt-core/README.md</read_first>
  <files>README.md, docs/sdk/measurement.md, .planning/PROJECT.md</files>
  <behavior>
    - Test 1: root and SDK docs now describe the browser demo as an integration sample with supported exact actions and current-result inspection.
    - Test 2: active planning truth no longer states removed `rerun/compare/clear` browser workflow as current behavior.
    - Test 3: no new package/runtime promises or dedicated demo workflow docs are introduced.
  </behavior>
  <action>Use the Phase 38 package README wording as the phrasing anchor, then make the smallest consistent edits across the live root docs and active planning truth.</action>
  <acceptance_criteria>
    - `rg -n "supported exact action routing|current-result session behavior|integration sample|rerun/compare/clear flows" README.md docs/sdk/measurement.md .planning/PROJECT.md` reflects the reduced surface.
    - `node --test test/secondary_surface_contract.test.mjs` passes.
    - `npm run test:planning:audit` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/secondary_surface_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:planning:audit"</automated>
  </verify>
  <done>The live docs and active planning truth now describe the same reduced measurement/demo boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live docs -> downstream expectations | If README or SDK docs keep old workflow wording, downstream users infer a richer measurement product surface than the repo now intentionally ships |
| active `.planning/` truth -> future milestone decisions | If current planning docs still advertise removed workflow behavior, later phases may plan against false current-state assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | I | live docs wording | mitigate | Write red contract expectations first, then update README/SDK docs to the reduced sample-first/current-result wording |
| T-39-01-02 | I/T | active planning truth | mitigate | Correct the active `.planning/PROJECT.md` wording so future milestones do not inherit stale browser-surface assumptions |
</threat_model>

<verification>
- Run `node --test test/secondary_surface_contract.test.mjs`.
- Run `npm run test:planning:audit`.
</verification>

<success_criteria>
The surviving live docs and active planning truth now all describe the same reduced boundary: package-first exact primitives/helpers plus a browser integration sample with explicit supported actions and one current-result flow.
</success_criteria>

<output>
After completion, create `.planning/phases/39-docs-verification-realignment/39-01-SUMMARY.md`
</output>
