---
phase: 39-docs-verification-realignment
plan: "02"
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - test/secondary_surface_contract.test.mjs
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - GOV-05
must_haves:
  truths:
    - "Conditional docs and browser verification fail on drift from the reduced measurement/demo surface."
    - "The maintained browser lane remains tied to the simplified measurement loop and absence of dropped productivity behavior."
    - "The authoritative `npm run test:release:root` command stays unchanged."
  artifacts:
    - path: test/secondary_surface_contract.test.mjs
      provides: conditional docs/manifest/browser audit
      contains: reduced wording assertions and maintained-browser-lane checks aligned to the simplified sample
    - path: test/release_governance_contract.test.mjs
      provides: targeted governance routing audit
      contains: confirmation that the reduced conditional verification remains outside the authoritative root release gate
  key_links:
    - from: test/secondary_surface_contract.test.mjs
      to: README.md
      via: conditional docs assertions must match the live reduced wording landed in 39-01
      pattern: supported exact action routing|current-result session behavior|integration sample
    - from: test/secondary_surface_contract.test.mjs
      to: demo/tests/demo.spec.mjs
      via: the maintained browser lane should still prove the simplified measurement loop without dropped productivity surface
      pattern: measurement-action-midpoint|tool-pose-nudge|measurement-pin|measurement-export
    - from: test/release_governance_contract.test.mjs
      to: package.json
      via: governance coverage must continue to prove that `test:secondary:contracts` stays outside `test:release:root`
      pattern: test:release:root|test:secondary:contracts
---

<objective>
Realign conditional verification and governance around the reduced docs and browser sample.

Purpose: satisfy `GOV-05` by making the secondary-surface audit fail on docs/behavior drift while preserving the established root release boundary.
Output: updated secondary/governance contract tests and reconfirmed browser lane coverage.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-docs-verification-realignment/39-CONTEXT.md
@.planning/phases/39-docs-verification-realignment/39-RESEARCH.md
@.planning/phases/39-docs-verification-realignment/39-01-PLAN.md
@AGENTS.md
@package.json
@README.md
@packages/occt-core/README.md
@docs/sdk/measurement.md
@test/secondary_surface_contract.test.mjs
@test/release_governance_contract.test.mjs
@demo/tests/demo.spec.mjs

<interfaces>
Current secondary-surface assertions include stale wording:
```js
assert.match(readme, /selection-to-measure mapping/i);
assert.match(occtCoreReadme, /selection-to-measure mapping/i);
assert.match(sdkGuide, /selection-to-measure mapping/i);
assert.match(readme, /transient run history/i);
assert.match(occtCoreReadme, /transient run history/i);
```

Current release-routing guard:
```js
assert.equal(contractCommand, "node --test test/secondary_surface_contract.test.mjs");
assert.equal(releaseCommand.includes("secondary_surface_contract"), false);
assert.equal(releaseCommand.includes("test:secondary:contracts"), false);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing secondary/governance assertions for the reduced surface</name>
  <read_first>test/secondary_surface_contract.test.mjs, test/release_governance_contract.test.mjs, README.md, docs/sdk/measurement.md, packages/occt-core/README.md</read_first>
  <files>test/secondary_surface_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: the secondary-surface contract fails until it expects reduced wording instead of the stale workflow terms.
    - Test 2: the secondary-surface contract fails until it keeps the maintained browser lane centered on the simplified demo loop and absence of removed productivity flows.
    - Test 3: governance assertions continue to fail if `test:secondary:contracts` ever leaks into `test:release:root`.
  </behavior>
  <action>Replace the stale contract expectations with focused reduced-surface assertions before making any implementation-side test cleanup.</action>
  <acceptance_criteria>
    - `rg -n "selection-to-measure mapping|transient run history|supported exact action routing|current-result session behavior|measurement-pin|measurement-export" test/secondary_surface_contract.test.mjs test/release_governance_contract.test.mjs` returns targeted expectation lines.
    - `node --test test/secondary_surface_contract.test.mjs` fails before implementation if docs/tests are not aligned.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/secondary_surface_contract.test.mjs"</automated>
  </verify>
  <done>Red conditional/governance tests now encode the reduced browser/docs surface before final alignment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the minimal verification realignment and reconfirm the maintained browser lane</name>
  <read_first>test/secondary_surface_contract.test.mjs, test/release_governance_contract.test.mjs, demo/tests/demo.spec.mjs, AGENTS.md</read_first>
  <files>test/secondary_surface_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: conditional docs assertions now lock the reduced wording from Phase 39-01.
    - Test 2: maintained browser-lane assertions remain focused on the simplified measurement loop and absence of dropped productivity surface.
    - Test 3: release-route guards remain unchanged and explicit.
  </behavior>
  <action>Implement the smallest truthful verification update, then rerun the maintained secondary lanes without widening the root release contract.</action>
  <acceptance_criteria>
    - `node --test test/secondary_surface_contract.test.mjs` passes.
    - `node --test test/release_governance_contract.test.mjs` passes.
    - `npm --prefix demo run test:e2e` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/secondary_surface_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/release_governance_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run test:e2e"</automated>
  </verify>
  <done>The reduced docs/browser surface is now locked by conditional verification without touching the authoritative root release gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conditional docs audit -> public boundary understanding | If the audit still expects stale workflow language, the repo encourages future drift instead of preventing it |
| secondary-surface verification -> root release routing | If conditional verification bleeds into the authoritative root release gate, the repo boundary regresses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | I | secondary-surface contract assertions | mitigate | Replace stale wording expectations with reduced-surface assertions tied to Phase 39-01 docs |
| T-39-02-02 | T/R | release routing | mitigate | Keep explicit route-guard assertions proving `test:secondary:contracts` remains outside `test:release:root` |
</threat_model>

<verification>
- Run `node --test test/secondary_surface_contract.test.mjs`.
- Run `node --test test/release_governance_contract.test.mjs`.
- Run `npm --prefix demo run test:e2e`.
- Apply `superpowers:requesting-code-review` before closeout if the user authorizes delegated review; otherwise record the explicit skip reason in the phase summary per `AGENTS.md`.
</verification>

<success_criteria>
The repo now fails fast on drift from the reduced docs/browser surface while preserving the unchanged authoritative root release route.
</success_criteria>

<output>
After completion, create `.planning/phases/39-docs-verification-realignment/39-02-SUMMARY.md`
</output>
