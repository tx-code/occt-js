---
phase: 33.1-multi-actor-exact-workspace
plan: "02"
type: execute
wave: 2
depends_on:
  - 33.1-01
files_modified:
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - dist/occt-js.d.ts
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/src/index.d.ts
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - demo/src/hooks/usePicking.js
  - demo/src/store/viewerStore.js
  - demo/src/components/SelectionPanel.jsx
  - demo/tests/demo.spec.mjs
  - demo/tests/viewer-store.test.mjs
  - demo/tests/exact-selection-bridge.test.mjs
autonomous: true
requirements:
  - DEMO-02
  - MEAS-04
must_haves:
  truths:
    - "Actor-scoped selections resolve to occurrence-safe exact refs using the selected actor's exact model plus the composed actor-pose and occurrence transform."
    - "Existing same-model pairwise exact flows remain source-compatible while `occt-core` gains additive dispatch for refs from different `exactModelId` values."
    - "Babylon mesh ids and instance ids remain UI-local; downstream measurement state carries actor identity and exact refs rather than viewer-only handles."
  artifacts:
    - path: src/exact-query.cpp
      provides: root cross-model pairwise exact kernel seam
      contains: additive pairwise operations that accept distinct exact-model handles while preserving same-model behavior
    - path: src/js-interface.cpp
      provides: root Wasm binding surface
      contains: additive cross-model pairwise bindings exported to JS without rewriting the current same-model entrypoints
    - path: dist/occt-js.d.ts
      provides: canonical root typing contract
      contains: additive cross-model pairwise method signatures aligned with the new Wasm binding surface
    - path: packages/occt-core/src/occt-core.js
      provides: package-first pairwise exact wrappers
      contains: transparent dispatch to same-model or cross-model root entrypoints based on the refs' `exactModelId`
    - path: demo/src/hooks/usePicking.js
      provides: actor-aware selection enrichment seam
      contains: actor-scoped exact-ref resolution and invalidation tied to actor pose and actor lifecycle
    - path: demo/tests/exact-selection-bridge.test.mjs
      provides: focused actor-aware bridge coverage
      contains: per-actor geometry lookup, transform composition, and invalidation behavior
  key_links:
    - from: packages/occt-core/src/occt-core.js
      to: dist/occt-js.d.ts
      via: package wrappers should preserve current public method names while dispatching to additive root cross-model methods when needed
      pattern: measureExactDistance|measureExactAngle|measureExactThickness|ClassifyExactRelation|SuggestExactDistancePlacement
    - from: demo/src/hooks/usePicking.js
      to: packages/occt-core/src/exact-ref-resolver.js
      via: selection bridging should reuse existing exact-ref resolution per actor instead of re-deriving topology from Babylon ids
      pattern: resolveExactElementRef|resolveExactFaceRef|resolveExactEdgeRef|resolveExactVertexRef
    - from: demo/tests/exact-selection-bridge.test.mjs
      to: demo/src/store/viewerStore.js
      via: actor pose and actor invalidation must be observable in the bridge contract before Phase 34 measurement actions depend on it
      pattern: actorId|exactRef|transform|selection
---

<objective>
Add additive cross-model exact pairwise support and re-derive the demo selection bridge around actor-scoped exact refs.

Purpose: satisfy `DEMO-02` and `MEAS-04` by making imported-workpiece plus generated-tool measurement technically correct rather than viewer-local. The root/package seam must preserve same-model callers while enabling cross-model pairwise operations when the demo holds refs from different retained exact models.
Output: additive root cross-model pairwise methods + package-first dispatch + actor-aware demo selection bridge + regression coverage.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33.1-multi-actor-exact-workspace/33.1-CONTEXT.md
@.planning/phases/33.1-multi-actor-exact-workspace/33.1-01-PLAN.md
@.planning/phases/33-demo-exact-bridge/33-02-SUPERSEDED.md
@AGENTS.md
@dist/occt-js.d.ts
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/exact-ref-resolver.js
@packages/occt-core/src/index.d.ts
@demo/src/hooks/usePicking.js
@demo/src/store/viewerStore.js
@demo/src/components/SelectionPanel.jsx
@demo/tests/demo.spec.mjs
@demo/tests/viewer-store.test.mjs

<interfaces>
From `dist/occt-js.d.ts` (current same-model pairwise root contract):
```ts
MeasureExactDistance(
  exactModelId: number,
  exactShapeHandleA: number,
  kindA: OcctJSExactElementKind,
  elementIdA: number,
  exactShapeHandleB: number,
  kindB: OcctJSExactElementKind,
  elementIdB: number,
  transformA: OcctJSMatrix4,
  transformB: OcctJSMatrix4
): OcctJSExactDistanceResult;
```

From `packages/occt-core/src/exact-ref-resolver.js` (existing per-model exact ref seam):
```js
resolveExactElementRef(exactModel, {
  nodeId,
  geometryId,
  kind,
  elementId,
});
```

From `packages/occt-core/src/occt-core.js` (current package wrapper guardrail):
```js
if (exactRefA.exactModelId !== exactRefB.exactModelId) {
  throw new Error(`${operationName} requires refs from the same exactModelId in v1.1.`);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for additive cross-model pairwise dispatch and actor-aware selection bridging</name>
  <read_first>src/exact-query.cpp, src/js-interface.cpp, packages/occt-core/src/occt-core.js, packages/occt-core/src/exact-ref-resolver.js, demo/src/hooks/usePicking.js, demo/src/store/viewerStore.js, demo/tests/demo.spec.mjs, demo/tests/viewer-store.test.mjs, .planning/phases/33.1-multi-actor-exact-workspace/33.1-CONTEXT.md</read_first>
  <files>packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, demo/tests/exact-selection-bridge.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: package tests fail until same-model wrappers keep their current behavior and different-model refs dispatch through additive cross-model root entrypoints.
    - Test 2: actor-aware selection-bridge tests fail until actor pose is composed with occurrence transform and selection records carry actor identity plus exact refs.
    - Test 3: browser smoke fails until workpiece and tool selections can coexist as actor-scoped measurement-ready refs without leaking Babylon ids as the contract.
  </behavior>
  <action>Create the smallest focused root/package and demo tests first. Keep cross-model package tests narrow and explicit, then add one dedicated demo bridge suite so actor-aware transform composition is testable without a live Babylon scene.</action>
  <acceptance_criteria>
    - `rg -n "cross-model|different exactModelId|actorId|exactRef|transform composition" packages/occt-core/test/core.test.mjs demo/tests/exact-selection-bridge.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm --prefix packages/occt-core test` fails before implementation and passes after Task 2.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo test"</automated>
  </verify>
  <done>The repo has red tests that lock additive cross-model pairwise semantics and actor-aware selection bridging before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive cross-model pairwise support and actor-aware exact-ref selection bridging</name>
  <read_first>src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, dist/occt-js.d.ts, packages/occt-core/src/occt-core.js, packages/occt-core/src/exact-ref-resolver.js, demo/src/hooks/usePicking.js, demo/src/store/viewerStore.js, demo/tests/exact-selection-bridge.test.mjs, demo/tests/demo.spec.mjs</read_first>
  <files>src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, dist/occt-js.d.ts, packages/occt-core/src/occt-core.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, demo/src/hooks/usePicking.js, demo/src/store/viewerStore.js, demo/src/components/SelectionPanel.jsx, demo/tests/exact-selection-bridge.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the root Wasm surface adds additive cross-model pairwise methods for distance, angle, thickness, relation classification, and pairwise placement without rewriting current same-model entrypoints.
    - Test 2: `occt-core` keeps its existing public pairwise method names but dispatches to same-model or cross-model root entrypoints based on the refs' `exactModelId`.
    - Test 3: actor-aware demo selections resolve through `resolveExact*Ref(...)` against the selected actor's exact model, then compose `actorPose * occurrenceTransform` into the stored exact ref.
    - Test 4: actor replacement, pose changes, and workspace reset invalidate actor-scoped selection refs immediately.
  </behavior>
  <action>Implement narrow additive root cross-model pairwise entrypoints, update the tracked typings contract in `dist/occt-js.d.ts`, and keep `occt-core` public wrappers source-compatible by dispatching internally. Rebuild the demo selection bridge around actor ids and actor pose while keeping Babylon ids UI-local only.</action>
  <acceptance_criteria>
    - `rg -n "CrossModel|different exactModelId|actorId|resolveExactFaceRef|resolveExactEdgeRef|resolveExactVertexRef" src packages/occt-core demo/src` returns matches.
    - `npm run test:release:root` passes.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run test:release:root; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The root/package pairwise exact surface now supports workpiece-tool measurement additively, and the demo selection pipeline emits actor-scoped measurement-ready exact refs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package pairwise wrapper -> root cross-model methods | If dispatch is wrong, same-model callers can regress or cross-model calls can silently misroute to the old single-model path |
| actor pose -> exact-ref transform | If actor pose is not composed with occurrence transform, displayed tool motion and measured geometry will diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.1-02-01 | T/R | pairwise dispatch compatibility | mitigate | Add focused package tests that lock same-model and different-model dispatch behavior before implementation |
| T-33.1-02-02 | I | actor-aware selection bridge | mitigate | Add dedicated bridge unit coverage and browser smoke so actor identity and transform composition cannot silently drift |
</threat_model>

<verification>
- Run `npm run test:release:root`.
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
</verification>

<success_criteria>
The demo can resolve workpiece and tool picks into actor-scoped exact refs, and the root/package pairwise exact surface can compare refs from different retained exact models without breaking existing same-model callers.
</success_criteria>

<output>
After completion, create `.planning/phases/33.1-multi-actor-exact-workspace/33.1-02-SUMMARY.md`
</output>

