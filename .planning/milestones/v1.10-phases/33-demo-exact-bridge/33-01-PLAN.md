---
phase: 33-demo-exact-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/src/hooks/useOcct.js
  - demo/src/hooks/useViewerActions.js
  - demo/src/store/viewerStore.js
  - demo/tests/use-occt-runtime-contract.test.mjs
  - demo/tests/viewer-store.test.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - DEMO-01
must_haves:
  truths:
    - "The demo owns one authoritative exact session for the currently loaded model and disposes the previous session before any replacement becomes active."
    - "Imported CAD and generated shapes converge on one demo-facing exact-session contract even if generated exact-open results need a demo-local managed wrapper."
    - "Raw vs auto-orient presentation changes do not reopen exact models by default; the exact session stays tied to the underlying source while UI bridge state is invalidated safely."
  artifacts:
    - path: demo/src/hooks/useOcct.js
      provides: demo runtime + exact-session creation seam
      contains: exact-session bootstrap, replacement, generated-shape parity, and explicit lifecycle cleanup
    - path: demo/src/store/viewerStore.js
      provides: shared demo exact-session state
      contains: exact-session metadata, invalidation actions, and reset-safe store contract
    - path: demo/src/hooks/useViewerActions.js
      provides: viewer reset lifecycle entrypoint
      contains: model close/reset path that must clear exact sessions deterministically
    - path: demo/tests/use-occt-runtime-contract.test.mjs
      provides: runtime-hook contract coverage
      contains: exact-session lifecycle expectations in the demo import/generated path
    - path: demo/tests/viewer-store.test.mjs
      provides: store lifecycle coverage
      contains: exact-session reset, replacement, and orientation-safe state invariants
    - path: demo/tests/demo.spec.mjs
      provides: browser lifecycle regression coverage
      contains: close/reload/generated-shape smoke assertions that stale exact sessions do not survive model replacement
  key_links:
    - from: demo/src/hooks/useOcct.js
      to: packages/occt-core/src/occt-core.js
      via: imported CAD already has managed exact-model helpers and generated shapes need parity without package-boundary creep
      pattern: openManagedExactModel|openExactRevolvedShape|openExactExtrudedShape|releaseExactModel
    - from: demo/src/hooks/useViewerActions.js
      to: demo/src/store/viewerStore.js
      via: close/reset path must invalidate exact sessions together with model state
      pattern: reset|setImportedModels|setModel
    - from: demo/tests/viewer-store.test.mjs
      to: demo/src/store/viewerStore.js
      via: store invariants lock exact-session lifecycle semantics
      pattern: reset|orientationMode|exactSession
---

<objective>
Retain and replace the demo exact session deterministically across imported and generated model flows.

Purpose: satisfy `DEMO-01` by giving the browser demo one authoritative exact-session contract that survives normal viewing operations, resets cleanly on model replacement, and does not widen the package/root boundary prematurely.
Output: demo-local exact-session store contract + lifecycle-safe import/generated load path + regression coverage.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-demo-exact-bridge/33-CONTEXT.md
@AGENTS.md
@docs/sdk/measurement.md
@packages/occt-core/README.md
@packages/occt-core/src/occt-core.js
@demo/src/hooks/useOcct.js
@demo/src/hooks/useViewerActions.js
@demo/src/store/viewerStore.js
@demo/src/App.jsx
@demo/src/components/GeneratedToolPanel.jsx
@demo/tests/use-occt-runtime-contract.test.mjs
@demo/tests/viewer-store.test.mjs
@demo/tests/demo.spec.mjs

<interfaces>
From `packages/occt-core/src/occt-core.js` (existing imported-model managed lifecycle):
```js
async openManagedExactModel(content, options = {}) { ... }
async releaseExactModel(exactModelId) { ... }
```

From `demo/src/store/viewerStore.js` (existing reset contract to extend):
```js
reset: () => set({
  model: null,
  rawModel: null,
  autoOrientModel: null,
  ...
})
```

From `demo/src/hooks/useOcct.js` (load paths that need exact-session parity):
```js
const result = await core.importModel(bytes, ...);
const result = occt.BuildRevolvedShape(spec, options);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing demo/store tests for exact-session lifecycle across import, generated-shape load, and reset</name>
  <read_first>demo/src/hooks/useOcct.js, demo/src/store/viewerStore.js, demo/src/hooks/useViewerActions.js, demo/tests/use-occt-runtime-contract.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs, .planning/phases/33-demo-exact-bridge/33-CONTEXT.md</read_first>
  <files>demo/tests/use-occt-runtime-contract.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: demo runtime-hook tests fail until the import/generated load path provisions an exact session alongside the rendered model result.
    - Test 2: viewer-store tests fail until reset and replacement actions clear or replace exact-session state deterministically.
    - Test 3: browser-level demo tests fail until closing or replacing a model no longer leaves stale exact-session residue behind.
  </behavior>
  <action>Extend the smallest existing demo tests first. Prefer store/runtime-hook coverage for lifecycle semantics, then add one Playwright smoke assertion proving exact-session state clears on replacement/reset without surfacing stale-handle regressions.</action>
  <acceptance_criteria>
    - `rg -n "exact session|exactSession|stale exact|reset clears" demo/tests/use-occt-runtime-contract.test.mjs demo/tests/viewer-store.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The demo has failing red tests that lock exact-session lifecycle behavior before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one authoritative demo exact-session contract with explicit replacement and reset semantics</name>
  <read_first>demo/src/hooks/useOcct.js, demo/src/hooks/useViewerActions.js, demo/src/store/viewerStore.js, packages/occt-core/src/occt-core.js, demo/tests/use-occt-runtime-contract.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</read_first>
  <files>demo/src/hooks/useOcct.js, demo/src/hooks/useViewerActions.js, demo/src/store/viewerStore.js, demo/tests/use-occt-runtime-contract.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: imported CAD loads create a managed exact session that is stored beside the rendered model contract.
    - Test 2: generated-shape loads create an equivalent demo-facing exact session without adding a new package boundary unless planning proves it necessary.
    - Test 3: reset, model replacement, and close-model flows dispose the previous session deterministically before clearing state.
    - Test 4: orientation switching keeps the exact source stable while leaving the door open for transform-aware selection mapping in Plan 33-02.
  </behavior>
  <action>Implement a demo-local exact-session contract that is shared across import and generated-shape flows, store it in `viewerStore`, and centralize disposal on reset/replacement paths. Keep the design generic and measurement-ready; Phase 33 must not encode tool-specific semantics into the bridge.</action>
  <acceptance_criteria>
    - `rg -n "exactSession|disposeExact|clearExactSession|setExactSession" demo/src/hooks/useOcct.js demo/src/hooks/useViewerActions.js demo/src/store/viewerStore.js` returns matches.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
    - `npm --prefix demo run test:e2e` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run test:e2e"</automated>
  </verify>
  <done>The demo now has a deterministic, generic exact-session lifecycle that imported CAD and generated shapes can share safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loaded model source -> demo exact session | If replacement/reset paths fail to dispose the previous session, Phase 34 will inherit stale handles and non-deterministic measurement failures |
| generated exact-open result -> demo lifecycle wrapper | If generated shapes are wrapped inconsistently, the demo will bifurcate exact-session handling by source family and reintroduce tool-coupled drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | T/R | demo exact-session replacement | mitigate | Add store/runtime/browser lifecycle tests before implementation and enforce explicit dispose on replacement/reset |
| T-33-01-02 | I | generated-shape exact-session parity | mitigate | Reuse one demo-facing exact-session contract and keep package expansion deferred unless a concrete blocker appears |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
</verification>

<success_criteria>
The browser demo retains one authoritative exact session per loaded model, clears it deterministically on replacement/reset, and does so generically across imported CAD and generated shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/33-demo-exact-bridge/33-01-SUMMARY.md`
</output>
