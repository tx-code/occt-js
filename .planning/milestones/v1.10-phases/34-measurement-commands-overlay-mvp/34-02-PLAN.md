---
phase: 34-measurement-commands-overlay-mvp
plan: "02"
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - demo/src/hooks/useViewer.js
  - demo/src/hooks/useMeasurement.js
  - demo/src/lib/measurement-overlay.js
  - demo/src/components/SelectionPanel.jsx
  - demo/tests/measurement-overlay.test.mjs
  - demo/tests/viewer-store.test.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - MEAS-02
  - MEAS-03
must_haves:
  truths:
    - "Placement-backed measurement results render demo-owned scene overlays through existing line-pass and marker infrastructure instead of text labels or viewer-framework semantics."
    - "Only the active measurement run owns visible overlay state, and overlays dispose on result switch, clear, workspace reset, exact-session replacement, or actor-pose changes."
    - "Measurement families without stable placement DTOs stay panel-only with explicit no-overlay semantics instead of guessed guide geometry."
  artifacts:
    - path: demo/src/lib/measurement-overlay.js
      provides: placement-to-overlay translation seam
      contains: line-pass batches, anchor marker descriptors, overlay roles, and panel-only fallback rules
    - path: demo/src/hooks/useViewer.js
      provides: scene overlay integration
      contains: measurement overlay layer management, active-overlay replacement, and deterministic cleanup beside existing highlight batches
    - path: demo/src/hooks/useMeasurement.js
      provides: active result bridge
      contains: placement payload normalization and synchronization between active result state and scene overlay requests
    - path: demo/tests/measurement-overlay.test.mjs
      provides: focused overlay contract coverage
      contains: translation from placement DTOs to overlay batches, panel-only fallback behavior, and overlay disposal rules
    - path: demo/tests/viewer-store.test.mjs
      provides: overlay invalidation coverage
      contains: active-result clearing on reset, actor replacement, and pose changes
    - path: demo/tests/demo.spec.mjs
      provides: browser overlay regression coverage
      contains: visible guide geometry, active-result switching, and clear-all disposal behavior
  key_links:
    - from: demo/src/hooks/useViewer.js
      to: demo/src/lib/measurement-overlay.js
      via: the existing viewer already owns line-pass batches and highlight cleanup, so measurement overlays should be injected through the same mechanism
      pattern: createViewerLinePass|updateBatches|setEdgeHighlight|clearEdgeHighlight
    - from: packages/occt-core/src/occt-core.js
      to: demo/src/lib/measurement-overlay.js
      via: placement helper results already expose `frame`, `anchors`, and `axisDirection` in occurrence/world space, so the overlay layer should translate those DTOs directly
      pattern: suggestExactDistancePlacement|suggestExactAnglePlacement|suggestExactThicknessPlacement|suggestExactRadiusPlacement|suggestExactDiameterPlacement
    - from: demo/src/store/viewerStore.js
      to: demo/src/hooks/useMeasurement.js
      via: active-result identity and workspace lifecycle must remain the authoritative overlay invalidation seam
      pattern: activeMeasurementId|workspaceActors|setActorPose|reset
---

<objective>
Render placement-backed measurement overlay and inspection state in the demo scene.

Purpose: satisfy `MEAS-02` and the visualization half of `MEAS-03` by turning stored placement payloads into minimal but correct scene guides that follow the active measurement result and dispose cleanly.
Output: placement-to-overlay translator + viewer overlay integration + active-result switching + explicit panel-only fallback for measurements without stable placement geometry.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-measurement-commands-overlay-mvp/34-CONTEXT.md
@.planning/phases/34-measurement-commands-overlay-mvp/34-RESEARCH.md
@.planning/phases/34-measurement-commands-overlay-mvp/34-01-PLAN.md
@AGENTS.md
@docs/sdk/measurement.md
@packages/occt-core/src/occt-core.js
@demo/src/store/viewerStore.js
@demo/src/hooks/useViewer.js
@demo/src/hooks/useMeasurement.js
@demo/src/components/SelectionPanel.jsx
@demo/tests/viewer-store.test.mjs
@demo/tests/demo.spec.mjs

<interfaces>
From `packages/occt-core/src/occt-core.js`:
```js
suggestExactDistancePlacement(refA, refB)
suggestExactAnglePlacement(refA, refB)
suggestExactThicknessPlacement(refA, refB)
suggestExactRadiusPlacement(ref)
suggestExactDiameterPlacement(ref)
```

Placement DTO shape:
```js
{
  ok: true,
  kind: "distance" | "angle" | "thickness" | "radius" | "diameter",
  value,
  frame,
  anchors,
  axisDirection?
}
```

From `demo/src/hooks/useViewer.js`:
```js
linePass.updateBatches(batches)
setEdgeHighlight(kind, token, lines)
clearEdgeHighlight(kind, token)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for placement-backed overlay translation and disposal</name>
  <read_first>demo/src/hooks/useViewer.js, demo/src/store/viewerStore.js, packages/occt-core/src/occt-core.js, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs, .planning/phases/34-measurement-commands-overlay-mvp/34-CONTEXT.md</read_first>
  <files>demo/tests/measurement-overlay.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: overlay tests fail until placement DTOs are translated into demo-owned guide geometry rather than ad-hoc scene objects.
    - Test 2: store or hook tests fail until active-result switches and lifecycle invalidation dispose overlays deterministically.
    - Test 3: browser smoke fails until one active measurement shows guide geometry and clear-all removes it.
  </behavior>
  <action>Write the smallest focused node tests around placement translation first, then add one browser smoke for active overlay visibility and disposal. Keep no-overlay fallbacks explicit when placement data is unavailable.</action>
  <acceptance_criteria>
    - `rg -n "overlay|placement|anchors|active result|clear" demo/tests/measurement-overlay.test.mjs demo/tests/viewer-store.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The repo has red tests locking placement-to-overlay behavior and overlay disposal before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement placement-to-overlay translation and viewer integration</name>
  <read_first>demo/src/hooks/useViewer.js, demo/src/hooks/useMeasurement.js, packages/occt-core/src/occt-core.js, demo/tests/measurement-overlay.test.mjs</read_first>
  <files>demo/src/lib/measurement-overlay.js, demo/src/hooks/useViewer.js, demo/src/hooks/useMeasurement.js, demo/tests/measurement-overlay.test.mjs, demo/tests/viewer-store.test.mjs</files>
  <behavior>
    - Test 1: supported placement results become one active overlay made of guide lines and anchor markers.
    - Test 2: switching the active measurement run swaps overlay geometry rather than accumulating orphaned overlays.
    - Test 3: results without stable placement DTOs remain panel-only with explicit no-overlay semantics.
  </behavior>
  <action>Translate placement DTOs into demo-local overlay descriptors, then integrate them into the existing viewer line-pass lifecycle. Reuse current highlight and depth-safe overlay patterns; do not add 3D text labels or a second viewer framework layer.</action>
  <acceptance_criteria>
    - `rg -n "measurement overlay|measurement-overlay|activeMeasurement|anchors|frame" demo/src` returns matches.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build"</automated>
  </verify>
  <done>The demo can translate placement DTOs into one active scene overlay and dispose it deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize browser-visible inspection output and overlay regression coverage</name>
  <read_first>demo/src/components/SelectionPanel.jsx, demo/src/hooks/useViewer.js, demo/tests/demo.spec.mjs</read_first>
  <files>demo/src/components/SelectionPanel.jsx, demo/src/hooks/useViewer.js, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: browser flows can switch active measurement runs and see overlay changes follow the active result.
    - Test 2: clear-all, reset, and actor-pose change remove stale overlay state.
    - Test 3: panel-only fallback is visible and explicit for measurement families that cannot render stable guide geometry.
  </behavior>
  <action>Finish the MVP inspection output so panel state and scene overlay stay aligned. Keep scene geometry minimal and let the inspector remain the source of numeric truth.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The active measurement result now drives minimal placement-backed scene guides, and browser coverage locks the overlay lifecycle in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| placement DTO -> scene overlay | If placement output is guessed or translated incorrectly, the scene will visualize geometry that does not match the exact result |
| active result state -> viewer resources | If overlay resources do not follow active-result and lifecycle changes, stale batches or markers will leak into later sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | I | placement translation | mitigate | Add focused node tests that lock frame/anchor-to-overlay translation and explicit panel-only fallbacks |
| T-34-02-02 | T/R | overlay lifecycle | mitigate | Bind overlay ownership to active-result identity and add browser/store coverage for switch/clear/reset/pose-change disposal |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
</verification>

<success_criteria>
The browser demo renders minimal placement-backed measurement guides for the active result, keeps panel and scene inspection state aligned, and disposes overlays cleanly across compare, clear, and workspace invalidation flows.
</success_criteria>

<output>
After completion, create `.planning/phases/34-measurement-commands-overlay-mvp/34-02-SUMMARY.md`
</output>
