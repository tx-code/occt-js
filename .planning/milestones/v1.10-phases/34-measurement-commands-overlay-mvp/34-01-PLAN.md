---
phase: 34-measurement-commands-overlay-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/src/store/viewerStore.js
  - demo/src/components/SelectionPanel.jsx
  - demo/src/hooks/useMeasurement.js
  - demo/src/lib/measurement-actions.js
  - demo/src/App.jsx
  - demo/tests/viewer-store.test.mjs
  - demo/tests/measurement-actions.test.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - MEAS-01
  - MEAS-03
must_haves:
  truths:
    - "The demo exposes only the exact measurement actions supported by the current actor-scoped selection and returns typed unsupported feedback for all other combinations."
    - "Measurement execution stays demo-owned and package-first: actions consume `selectedDetail.items[*].exactRef` and call `@tx-code/occt-core` without widening the root or package boundary."
    - "Measurement run history is separate from selection state and clears deterministically on workspace reset, exact-session replacement, or actor-pose changes."
  artifacts:
    - path: demo/src/lib/measurement-actions.js
      provides: measurement command matrix
      contains: supported action definitions, selection arity/kind rules, placement-capable family metadata, and explicit unsupported reasons
    - path: demo/src/hooks/useMeasurement.js
      provides: demo-owned measurement runner seam
      contains: command dispatch, typed success/failure row normalization, rerun/clear helpers, and active-result selection
    - path: demo/src/store/viewerStore.js
      provides: measurement session state
      contains: run history, active measurement id, and invalidation on workspace lifecycle changes
    - path: demo/src/components/SelectionPanel.jsx
      provides: selection-plus-measurement inspector
      contains: supported action buttons, typed result rows, compare affordances, and clear/rerun controls
    - path: demo/tests/measurement-actions.test.mjs
      provides: focused measurement command coverage
      contains: action availability, typed unsupported outcomes, and result normalization for single-ref and pairwise commands
    - path: demo/tests/viewer-store.test.mjs
      provides: measurement invalidation coverage
      contains: clear-on-reset, clear-on-pose-change, and clear-on-actor-replacement invariants
    - path: demo/tests/demo.spec.mjs
      provides: browser workflow regression coverage
      contains: select, run, compare, rerun, and clear flows for workpiece/tool measurement actions
  key_links:
    - from: demo/src/hooks/usePicking.js
      to: packages/occt-core/src/occt-core.js
      via: current selection already carries actor-scoped exact refs, so measurement execution should consume those refs directly instead of re-deriving geometry from Babylon ids
      pattern: selectedDetail|exactRef|actorId|measureExact
    - from: demo/src/store/viewerStore.js
      to: demo/src/hooks/useOcct.js
      via: workspace reset, actor replacement, and actor pose changes are the authoritative invalidation boundaries for measurement state
      pattern: upsertWorkpieceActor|upsertToolActor|setActorPose|clearWorkspaceActors
    - from: demo/src/components/SelectionPanel.jsx
      to: demo/tests/demo.spec.mjs
      via: the existing inspector already hosts tool-pose and selection metadata, so measurement controls should extend the same panel and be browser-verifiable there
      pattern: selection-panel|tool-pose-panel|data-testid
---

<objective>
Add the demo-owned measurement action runner and typed results panel for currently supported exact workflows.

Purpose: satisfy `MEAS-01` and the state-management half of `MEAS-03` by turning the existing actor-scoped exact refs into deliberate, testable measurement commands with explicit success and failure semantics.
Output: selection-driven action matrix + typed measurement runner + run history state + compare/rerun/clear UI in the demo inspector.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-measurement-commands-overlay-mvp/34-CONTEXT.md
@.planning/phases/34-measurement-commands-overlay-mvp/34-RESEARCH.md
@.planning/phases/33-demo-exact-bridge/33-CONTEXT.md
@.planning/phases/33.1-multi-actor-exact-workspace/33.1-CONTEXT.md
@.planning/phases/33.1-multi-actor-exact-workspace/33.1-02-SUMMARY.md
@AGENTS.md
@docs/sdk/measurement.md
@packages/occt-core/src/occt-core.js
@demo/src/store/viewerStore.js
@demo/src/hooks/usePicking.js
@demo/src/hooks/useOcct.js
@demo/src/components/SelectionPanel.jsx
@demo/src/App.jsx
@demo/tests/viewer-store.test.mjs
@demo/tests/exact-selection-bridge.test.mjs
@demo/tests/demo.spec.mjs

<interfaces>
From `demo/src/store/viewerStore.js`:
```js
selectedItems: []
selectedDetail: null
workspaceActors: {}
setActorPose(actorId, patch)
clearWorkspaceActors()
reset()
```

From `packages/occt-core/src/occt-core.js`:
```js
measureExactDistance(refA, refB)
measureExactAngle(refA, refB)
measureExactThickness(refA, refB)
measureExactRadius(ref)
measureExactEdgeLength(ref)
measureExactFaceArea(ref)
suggestExactDistancePlacement(refA, refB)
suggestExactAnglePlacement(refA, refB)
suggestExactThicknessPlacement(refA, refB)
suggestExactRadiusPlacement(ref)
suggestExactDiameterPlacement(ref)
```

From `demo/src/components/SelectionPanel.jsx`:
```jsx
const selectedDetail = useViewerStore((s) => s.selectedDetail);
const toolActor = useViewerStore((s) => s.workspaceActors?.tool ?? null);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for selection-driven measurement actions and typed result state</name>
  <read_first>demo/src/store/viewerStore.js, demo/src/components/SelectionPanel.jsx, demo/src/hooks/usePicking.js, packages/occt-core/src/occt-core.js, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs, .planning/phases/34-measurement-commands-overlay-mvp/34-CONTEXT.md</read_first>
  <files>demo/tests/measurement-actions.test.mjs, demo/tests/viewer-store.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: command-matrix tests fail until supported actions are derived from selection count and element kinds instead of one generic "measure" button.
    - Test 2: store tests fail until measurement runs are stored separately from selection state and clear on reset, actor replacement, and actor-pose changes.
    - Test 3: browser smoke fails until users can run at least one supported measurement, see a typed result row, keep multiple runs for comparison, and clear them explicitly.
  </behavior>
  <action>Create the smallest focused node tests first, then add one browser smoke that covers the happy path for workpiece/tool measurement interaction. Keep failures typed and inspectable rather than toast-driven.</action>
  <acceptance_criteria>
    - `rg -n "measurement|measure|compare|clear|rerun|unsupported" demo/tests/measurement-actions.test.mjs demo/tests/viewer-store.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The repo has red tests locking action availability, typed result rows, and measurement invalidation before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement demo-owned measurement command routing and run history</name>
  <read_first>packages/occt-core/src/occt-core.js, demo/src/store/viewerStore.js, demo/src/components/SelectionPanel.jsx, demo/tests/measurement-actions.test.mjs, demo/tests/viewer-store.test.mjs</read_first>
  <files>demo/src/store/viewerStore.js, demo/src/lib/measurement-actions.js, demo/src/hooks/useMeasurement.js, demo/src/components/SelectionPanel.jsx, demo/src/App.jsx, demo/tests/measurement-actions.test.mjs, demo/tests/viewer-store.test.mjs</files>
  <behavior>
    - Test 1: the current selection exposes only supported command buttons.
    - Test 2: running a command writes a typed result row with stable refs, actor ids, status, numeric values, and placement payload when available.
    - Test 3: unsupported selections or kernel failures become typed result rows, not uncaught UI errors.
    - Test 4: clear-all, rerun-current-selection, and active-result selection all work against one short in-memory run history.
  </behavior>
  <action>Add a demo-local measurement action registry plus a runner hook that uses the shipped `occt-core` exact APIs. Keep the implementation generic over actor role labels and avoid any tool-coupled logic beyond user-facing labeling.</action>
  <acceptance_criteria>
    - `rg -n "measurementRuns|activeMeasurementId|runMeasurement|clearMeasurements|rerunMeasurement" demo/src` returns matches.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build"</automated>
  </verify>
  <done>The demo can route supported exact measurements from current selections and store typed result rows independently of selection state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finish inspector UX and browser workflow coverage for compare, rerun, and clear</name>
  <read_first>demo/src/components/SelectionPanel.jsx, demo/src/hooks/useMeasurement.js, demo/tests/demo.spec.mjs</read_first>
  <files>demo/src/components/SelectionPanel.jsx, demo/src/App.jsx, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the inspector shows supported actions and the active result list without hiding current selection context.
    - Test 2: users can compare multiple runs during one stable workspace session and explicitly choose which result is active.
    - Test 3: clear and rerun flows remain deterministic and do not leak stale result rows after workspace invalidation.
  </behavior>
  <action>Finish the demo-visible result panel behavior inside the existing inspector surface. Keep the UI compact and scan-friendly on both desktop and mobile; numeric data stays panel-first.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The selection inspector now supports practical measurement execution, compare, rerun, and clear flows for the Phase 34 MVP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selection summary -> measurement runner | If unsupported commands are exposed for the current refs, the demo will produce noisy runtime failures instead of deliberate typed feedback |
| workspace lifecycle -> measurement history | If actor replacement, reset, or pose changes do not invalidate runs, stale exact refs and stale result rows will survive past valid geometry state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | T/R | action matrix | mitigate | Add focused node tests that lock selection-to-command mapping and unsupported feedback before implementation |
| T-34-01-02 | I | run-history invalidation | mitigate | Bind measurement runs to workspace lifecycle and add store/browser coverage for pose/reset/replacement clearing behavior |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
</verification>

<success_criteria>
The browser demo can execute supported exact measurements from current actor-scoped selections, record typed result rows, and let users compare, rerun, and clear measurements without leaking stale lifecycle state.
</success_criteria>

<output>
After completion, create `.planning/phases/34-measurement-commands-overlay-mvp/34-01-SUMMARY.md`
</output>
