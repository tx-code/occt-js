---
phase: 35-demo-docs-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "35-01"
files_modified:
  - demo/package.json
  - demo/tests/demo.spec.mjs
  - test/secondary_surface_contract.test.mjs
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - E2E-01
  - GOV-03
must_haves:
  truths:
    - "`npm --prefix demo run test:e2e` becomes the authoritative demo-browser lane for the measurement MVP and includes interaction-level coverage, not only the project-home shell."
    - "Automated demo coverage proves load or generate, actor-scoped selection, measurement execution, and at least one invalidation path caused by reset, replacement, or actor-pose change."
    - "`npm run test:release:root` remains unchanged; measurement demo verification stays enforced only through demo-local commands and `npm run test:secondary:contracts`."
  artifacts:
    - path: demo/package.json
      provides: manifest-routed demo browser verification lane
      contains: an explicit `test:e2e` route that includes both app-home smoke and measurement interaction regression
    - path: demo/tests/demo.spec.mjs
      provides: browser measurement lifecycle regression coverage
      contains: one or more tests that prove measurement state clears on replacement, reset, or actor-pose invalidation in addition to the shipped happy-path workflow
    - path: test/secondary_surface_contract.test.mjs
      provides: governance lock for demo-browser routing
      contains: assertions that the demo manifest includes measurement interaction coverage and keeps the lane outside the authoritative root release gate
    - path: README.md
      provides: top-level verification matrix
      contains: truthful wording for the demo-browser verification lane after the manifest route is tightened
    - path: AGENTS.md
      provides: repo-level agent verification guidance
      contains: matching conditional-demo wording so agent behavior and docs do not drift
  key_links:
    - from: demo/package.json
      to: test/secondary_surface_contract.test.mjs
      via: the contract suite should prove that the manifest-routed demo browser lane actually includes measurement interaction coverage
      pattern: test:e2e|app-home.spec|demo.spec
    - from: demo/tests/demo.spec.mjs
      to: demo/src/hooks/useOcct.js
      via: lifecycle browser tests should exercise real exact-session replacement or invalidation behavior instead of synthetic hooks
      pattern: importFile|clearExactSession|replaceActorExactSession
    - from: README.md
      to: package.json
      via: docs must keep `npm run test:release:root` authoritative while describing the improved demo-browser lane as explicit and conditional
      pattern: test:release:root|test:secondary:contracts|npm --prefix demo run test:e2e
---

<objective>
Add demo verification and conditional governance coverage for the shipped measurement integration.

Purpose: satisfy `E2E-01` and the verification half of `GOV-03` by making the documented demo-browser lane actually cover the measurement MVP and one invalidation path, while keeping the authoritative root gate unchanged.
Output: updated demo manifest route, tighter browser regression coverage, and secondary-surface contract assertions that lock the routing and boundary.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-demo-docs-verification/35-CONTEXT.md
@.planning/phases/35-demo-docs-verification/35-RESEARCH.md
@.planning/phases/35-demo-docs-verification/35-01-PLAN.md
@AGENTS.md
@README.md
@package.json
@demo/package.json
@demo/tests/app-home.spec.mjs
@demo/tests/demo.spec.mjs
@demo/tests/viewer-store.test.mjs
@demo/src/hooks/useOcct.js
@demo/src/hooks/useViewerActions.js
@test/secondary_surface_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract and browser tests for manifest-routed measurement verification</name>
  <read_first>demo/package.json, demo/tests/app-home.spec.mjs, demo/tests/demo.spec.mjs, demo/tests/viewer-store.test.mjs, test/secondary_surface_contract.test.mjs, .planning/phases/35-demo-docs-verification/35-CONTEXT.md</read_first>
  <files>test/secondary_surface_contract.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: secondary-surface governance fails until `demo`'s manifest-routed browser lane includes the measurement interaction suite as well as the app-home smoke.
    - Test 2: browser coverage fails until at least one measurement invalidation path is proven after the happy-path run.
    - Test 3: governance continues to fail if root release scripts absorb any demo-browser checks.
  </behavior>
  <action>Extend `test/secondary_surface_contract.test.mjs` and `demo/tests/demo.spec.mjs` first so the repo goes red until the manifest route and invalidation browser coverage are both in place. Keep the invalidation scenario user-visible: workpiece replacement, tool replacement, reset, or tool-pose change.</action>
  <acceptance_criteria>
    - `rg -n "demo\\.spec|app-home\\.spec|replacement|pose change|reset|test:e2e" test/secondary_surface_contract.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - `npm run test:secondary:contracts` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:secondary:contracts</automated>
  </verify>
  <done>The repo has red tests that lock the manifest-routed measurement lane and one lifecycle invalidation browser case before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten the demo manifest route and secondary-surface docs</name>
  <read_first>demo/package.json, README.md, AGENTS.md, test/secondary_surface_contract.test.mjs, demo/tests/app-home.spec.mjs, demo/tests/demo.spec.mjs</read_first>
  <files>demo/package.json, test/secondary_surface_contract.test.mjs, README.md, AGENTS.md</files>
  <behavior>
    - Test 1: `npm --prefix demo run test:e2e` includes both app-home shell entry and measurement interaction regression.
    - Test 2: root and agent docs still describe this lane as conditional secondary-surface verification, not as an authoritative root gate.
    - Test 3: root release scripts stay free of demo-browser commands.
  </behavior>
  <action>Update the demo manifest and the secondary-surface docs/contract together. Prefer one explicit manifest route over undocumented ad hoc Playwright knowledge. Keep all wording aligned with the repo rule that `npm run test:release:root` stays authoritative for the root Wasm carrier only.</action>
  <acceptance_criteria>
    - `rg -n "demo\\.spec|app-home\\.spec|test:e2e|secondary-surface" demo/package.json README.md AGENTS.md test/secondary_surface_contract.test.mjs` returns matches.
    - `npm run test:secondary:contracts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:secondary:contracts</automated>
  </verify>
  <done>The manifest-routed demo browser lane and the published secondary-surface boundary now agree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finish browser lifecycle coverage for measurement invalidation</name>
  <read_first>demo/tests/demo.spec.mjs, demo/src/hooks/useOcct.js, demo/src/hooks/useViewerActions.js, demo/tests/viewer-store.test.mjs</read_first>
  <files>demo/tests/demo.spec.mjs, demo/package.json</files>
  <behavior>
    - Test 1: browser coverage proves the full measurement loop from load or generate through selection and measurement execution.
    - Test 2: browser coverage proves measurement state is cleared after a real invalidation path such as workpiece replacement, tool replacement, reset, or tool-pose change.
    - Test 3: the demo-browser command surface remains stable and runnable through `npm --prefix demo run test:e2e`.
  </behavior>
  <action>Extend the existing Playwright interaction suite with one stable invalidation scenario and verify it through the manifest-routed `demo` browser lane. Favor real UI flows over internal test-only store mutations.</action>
  <acceptance_criteria>
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
    - `npm --prefix demo run test:e2e` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run test:e2e"</automated>
  </verify>
  <done>The demo-browser verification lane now proves the measurement MVP and a real invalidation path through one documented manifest command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented demo E2E lane -> actual browser coverage | If `demo/package.json` stays app-home-only, users and agents will believe measurement coverage exists when it really requires ad hoc commands |
| browser measurement state -> exact-session ownership | If lifecycle invalidation is not proven in the browser, stale measurements can survive real UI flows even if store tests are green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-01 | T/R | demo manifest routing | mitigate | Add failing contract assertions before changing `demo/package.json` |
| T-35-02-02 | I | browser lifecycle coverage | mitigate | Add a real invalidation Playwright case before updating the manifest-routed lane |
| T-35-02-03 | T | root release boundary | mitigate | Keep `README.md`, `AGENTS.md`, and `test/secondary_surface_contract.test.mjs` aligned that demo-browser checks remain outside `npm run test:release:root` |
</threat_model>

<verification>
- Run `npm run test:secondary:contracts`.
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
</verification>

<success_criteria>
The repo’s documented demo-browser lane now covers the shipped measurement MVP and one invalidation path, while root release verification remains runtime-first and unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/35-demo-docs-verification/35-02-SUMMARY.md`
</output>
