---
phase: 35-demo-docs-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/occt-core/README.md
  - docs/sdk/measurement.md
  - docs/demo/exact-measurement-workflow.md
  - test/secondary_surface_contract.test.mjs
autonomous: true
requirements:
  - DOCS-05
  - GOV-03
must_haves:
  truths:
    - "One canonical downstream demo workflow guide explains the shipped workpiece-plus-tool measurement loop, supported selection-to-measure mappings, and exact-session invalidation rules."
    - "Root and package docs point to that demo workflow while preserving the package-first SDK boundary and the authoritative root release boundary."
    - "Secondary-surface contract tests fail if demo measurement docs or lifecycle-boundary statements drift."
  artifacts:
    - path: docs/demo/exact-measurement-workflow.md
      provides: canonical browser-demo workflow guide
      contains: workpiece-plus-tool setup, actor-scoped selection flow, supported command matrix, panel-only fallback notes, and invalidation rules for reset or replacement or pose changes
    - path: README.md
      provides: top-level discoverability and boundary notes
      contains: link to the demo workflow guide plus a concise statement that demo measurement UX is downstream and conditional
    - path: packages/occt-core/README.md
      provides: package-first downstream guidance
      contains: link to the demo workflow guide plus the rule that package APIs supply refs and helpers but not measurement UI policy
    - path: docs/sdk/measurement.md
      provides: package-first SDK guide
      contains: downstream integration notes connecting exact refs and lifecycle ownership to the demo workflow guide
    - path: test/secondary_surface_contract.test.mjs
      provides: docs/governance lock for the conditional demo surface
      contains: assertions for the measurement workflow guide, supported-mapping terminology, and exact-session invalidation boundary notes
  key_links:
    - from: demo/tests/demo.spec.mjs
      to: docs/demo/exact-measurement-workflow.md
      via: the guide should describe the actual shipped workpiece-plus-tool workflow and action matrix already exercised in the browser suite
      pattern: measurement-action|measurement-clear-all|generated-tool|tool-pose
    - from: demo/tests/viewer-store.test.mjs
      to: docs/demo/exact-measurement-workflow.md
      via: lifecycle docs must match the store-level invalidation contract for replacement, pose changes, and reset
      pattern: measurement runs stay separate|clear on actor replacement|pose changes|reset
    - from: README.md
      to: package.json
      via: docs must keep `npm run test:release:root` authoritative while describing demo measurement verification as conditional
      pattern: test:release:root|test:secondary:contracts
---

<objective>
Document the shipped measurement demo workflow, supported mappings, and lifecycle constraints without blurring the runtime/package boundary.

Purpose: satisfy `DOCS-05` and the docs half of `GOV-03` by making the actual browser-demo loop discoverable and contract-checked.
Output: one dedicated demo workflow guide, linked root/package/sdk docs, and failing-then-green secondary-surface contract assertions that lock the documentation boundary.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-demo-docs-verification/35-CONTEXT.md
@.planning/phases/35-demo-docs-verification/35-RESEARCH.md
@.planning/phases/34-measurement-commands-overlay-mvp/34-CONTEXT.md
@.planning/phases/34-measurement-commands-overlay-mvp/34-01-SUMMARY.md
@.planning/phases/34-measurement-commands-overlay-mvp/34-02-SUMMARY.md
@AGENTS.md
@README.md
@packages/occt-core/README.md
@docs/sdk/measurement.md
@demo/package.json
@demo/tests/demo.spec.mjs
@demo/tests/viewer-store.test.mjs
@test/secondary_surface_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing secondary-surface contract assertions for demo measurement docs</name>
  <read_first>README.md, packages/occt-core/README.md, docs/sdk/measurement.md, demo/tests/demo.spec.mjs, demo/tests/viewer-store.test.mjs, test/secondary_surface_contract.test.mjs, .planning/phases/35-demo-docs-verification/35-CONTEXT.md</read_first>
  <files>test/secondary_surface_contract.test.mjs</files>
  <behavior>
    - Test 1: secondary-surface docs fail until one dedicated demo measurement workflow guide exists.
    - Test 2: docs fail until they describe the supported selection-to-measure matrix actually shipped in the demo.
    - Test 3: docs fail until they state that measurement runs and overlays are invalidated by exact-session replacement, workspace reset, or actor-pose changes.
  </behavior>
  <action>Extend `test/secondary_surface_contract.test.mjs` so it asserts the presence of a dedicated demo workflow guide, conservative selection-to-measure mapping language, and explicit lifecycle invalidation boundary notes in the linked docs.</action>
  <acceptance_criteria>
    - `rg -n "measurement workflow|selection-to-measure|pose change|reset|replacement" test/secondary_surface_contract.test.mjs` returns matches.
    - `npm run test:secondary:contracts` fails before doc updates and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:secondary:contracts</automated>
  </verify>
  <done>The contract suite now proves that the demo measurement docs and lifecycle rules cannot drift silently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the dedicated demo workflow guide and link it from root and package docs</name>
  <read_first>README.md, packages/occt-core/README.md, docs/sdk/measurement.md, demo/tests/demo.spec.mjs, demo/tests/viewer-store.test.mjs, test/secondary_surface_contract.test.mjs, .planning/phases/35-demo-docs-verification/35-RESEARCH.md</read_first>
  <files>README.md, packages/occt-core/README.md, docs/sdk/measurement.md, docs/demo/exact-measurement-workflow.md, test/secondary_surface_contract.test.mjs</files>
  <behavior>
    - Test 1: the new guide walks through importing a workpiece, generating a tool, selecting actor-scoped refs, running supported measurements, and understanding panel-only fallback.
    - Test 2: root/package/sdk docs point to the guide while preserving the rule that command availability, overlay rendering, and UX remain downstream demo behavior.
    - Test 3: lifecycle sections explicitly explain invalidation on workpiece or tool replacement, reset, or tool-pose changes.
  </behavior>
  <action>Create `docs/demo/exact-measurement-workflow.md` and update the existing docs so the measurement MVP is discoverable end to end without collapsing runtime/package guidance into viewer behavior. Keep the command matrix conservative and match Phase 34 exactly.</action>
  <acceptance_criteria>
    - `rg -n "workpiece|generated tool|distance|angle|thickness|face-area|edge-length|panel-only|pose|replacement" README.md packages/occt-core/README.md docs/sdk/measurement.md docs/demo/exact-measurement-workflow.md` returns matches.
    - `npm run test:secondary:contracts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:secondary:contracts</automated>
  </verify>
  <done>The shipped docs now explain the browser-demo measurement loop and keep the runtime/package boundary explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped demo workflow -> published docs | If docs describe unsupported commands or omit invalidation rules, users and maintainers will reason from the wrong behavior |
| package/runtime reference docs -> downstream demo guide | If these layers are not linked clearly, the repo risks either hiding the demo workflow or implying the SDK owns UI policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | T/R | demo workflow docs | mitigate | Add failing secondary-surface contract assertions before publishing the new guide |
| T-35-01-02 | I | boundary clarity | mitigate | Keep root/package docs concise and point to one canonical downstream workflow guide |
</threat_model>

<verification>
- Run `npm run test:secondary:contracts`.
</verification>

<success_criteria>
The repo has one canonical demo measurement workflow guide, linked from root/package docs, and a contract suite that locks the supported mapping and lifecycle boundary without widening the root release gate.
</success_criteria>

<output>
After completion, create `.planning/phases/35-demo-docs-verification/35-01-SUMMARY.md`
</output>
