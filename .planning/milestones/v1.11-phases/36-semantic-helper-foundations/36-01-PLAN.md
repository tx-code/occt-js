---
phase: 36-semantic-helper-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/src/index.d.ts
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - test/exact_compound_hole_contract.test.mjs
autonomous: true
requirements:
  - FEAT-07
must_haves:
  truths:
    - "`@tx-code/occt-core` exposes additive package-first compound-hole helpers for supported `counterbore` and `countersink` selections instead of forcing callers into raw topology or demo-owned policy."
    - "Any new root carrier support is limited to one selected-ref compound-hole descriptor that stays additive and avoids generic topology traversal or discovery APIs."
    - "Supported compound-hole selections return stable semantic DTOs with explicit typed failures for unsupported geometry, unsupported topology, or degenerate inputs."
  artifacts:
    - path: packages/occt-core/src/occt-core.js
      provides: package-first compound-hole helper APIs
      contains: `describeExactCounterbore(ref)` and `describeExactCountersink(ref)` wrappers that validate refs, call a shared compound-hole carrier descriptor when needed, transform local-space geometry into occurrence space, and append `ref` on success
    - path: packages/occt-core/src/index.d.ts
      provides: typed package helper surface
      contains: result types and method signatures for the new compound-hole helpers
    - path: src/importer.hpp
      provides: additive compound-hole DTO structs
      contains: shared success and failure-capable result data for supported `counterbore` and `countersink` semantics
    - path: src/exact-query.hpp
      provides: carrier declaration surface
      contains: one additive selected-ref compound-hole descriptor declaration
    - path: src/exact-query.cpp
      provides: narrow compound-hole classification logic
      contains: supported selected-ref topology normalization, semantic extraction, and explicit unsupported failure handling
    - path: src/js-interface.cpp
      provides: JS binding surface
      contains: DTO serialization and the new Embind binding for the compound-hole descriptor
    - path: dist/occt-js.d.ts
      provides: public carrier typings
      contains: the compound-hole result type and selected-ref method signature
    - path: test/exact_compound_hole_contract.test.mjs
      provides: retained-model root contract coverage
      contains: supported counterbore or countersink success cases plus explicit unsupported behavior
    - path: packages/occt-core/test/core.test.mjs
      provides: mocked wrapper contract coverage
      contains: success DTO shape, occurrence transforms, and explicit unsupported propagation for the new package helpers
    - path: packages/occt-core/test/live-root-integration.test.mjs
      provides: package-to-carrier parity proof
      contains: one transformed-occurrence smoke test for compound-hole helper semantics against the built carrier
  key_links:
    - from: src/exact-query.cpp
      to: src/importer.hpp
      via: additive compound-hole DTO population
      pattern: DescribeExactCompoundHole|counterbore|countersink|frame|anchors
    - from: src/js-interface.cpp
      to: dist/occt-js.d.ts
      via: carrier method and DTO parity
      pattern: DescribeExactCompoundHole|OcctJSExactCompoundHoleResult
    - from: packages/occt-core/src/occt-core.js
      to: packages/occt-core/src/index.d.ts
      via: package-first helper names and typed results must stay aligned
      pattern: describeExactCounterbore|describeExactCountersink
---

<objective>
Add the minimal root and package support required for supported compound-hole semantics.

Purpose: satisfy `FEAT-07` by extending the shipped hole-helper pattern into `counterbore` and `countersink` without widening the carrier into generic feature discovery or viewer policy.
Output: additive compound-hole carrier bindings and typings, package-first helper wrappers, and focused root/package tests proving stable semantic behavior on supported selected refs.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-semantic-helper-foundations/36-CONTEXT.md
@.planning/phases/36-semantic-helper-foundations/36-RESEARCH.md
@.planning/milestones/v1.6-phases/21-hole-helper-foundations/21-CONTEXT.md
@.planning/milestones/v1.6-phases/22-chamfer-constraint-helpers/22-CONTEXT.md
@AGENTS.md
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/index.d.ts
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@test/exact_hole_contract.test.mjs

<interfaces>
From `packages/occt-core/src/occt-core.js` (single-ref semantic wrapper pattern to extend):
```javascript
async describeExactHole(ref) {
  const module = await this._ensureModule();
  const exactRef = validateExactRef(ref, "describeExactHole");
  ...
  return {
    ...transformExactHoleResult(exactRef.transform, result),
    ref: exactRef,
  };
}
```

From `src/exact-query.cpp` (selected-ref hole semantic foundation to reuse conceptually):
```cpp
OcctExactHoleResult DescribeExactHole(
    int exactModelId,
    int exactShapeHandle,
    const std::string& kind,
    int elementId)
{
    ...
    lifecycle = CollectHoleBoundaryInfo(holeFace, cylinder, axisDirection, boundaries);
    ...
}
```

From `packages/occt-core/src/index.d.ts` (typed package helper style to extend):
```typescript
export type OcctExactHoleDescriptionResult = OcctExactHoleSuccess | OcctJSExactQueryFailure;
...
describeExactHole(ref: OcctExactRef): Promise<OcctExactHoleDescriptionResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root and package tests for compound-hole helpers</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_hole_contract.test.mjs, .planning/phases/36-semantic-helper-foundations/36-CONTEXT.md</read_first>
  <files>packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, test/exact_compound_hole_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `wraps describeExactCounterbore(ref) through occurrence-scoped exact refs` fails until the wrapper validates one exact ref, calls the shared compound-hole carrier method, and appends `ref` on success.
    - Test 2: a subtest named `wraps describeExactCountersink(ref) through occurrence-scoped exact refs` fails until the wrapper validates one exact ref, narrows the returned family correctly, and appends `ref` on success.
    - Test 3: a root subtest named `exact compound-hole helper describes a supported counterbore selection` fails until a supported selected ref returns a stable semantic counterbore payload with frame and anchor data.
    - Test 4: a root subtest named `exact compound-hole helper describes a supported countersink selection` fails until a supported selected ref returns a stable semantic countersink payload with included-angle data.
    - Test 5: a root subtest named `exact compound-hole helper failures stay explicit for unsupported or plain cylindrical holes` fails until unsupported selections return typed failures instead of guessed compound-hole payloads.
    - Test 6: a live subtest named `occt-core compound-hole helpers normalize live semantics into occurrence space` fails until the package wrappers return transformed occurrence-space geometry from the built carrier.
  </behavior>
  <action>Extend `packages/occt-core/test/core.test.mjs` with mocked wrapper tests for `describeExactCounterbore(ref)` and `describeExactCountersink(ref)` using the exact subtest names from `<behavior>`. Create `test/exact_compound_hole_contract.test.mjs` as a retained-model root contract suite that loads the built carrier, prefers shipped fixtures for deterministic counterbore and countersink candidates, and asserts supported success plus explicit unsupported behavior. Extend `packages/occt-core/test/live-root-integration.test.mjs` with one transformed-occurrence smoke test for the compound-hole helper family.</action>
  <acceptance_criteria>
    - `rg -n "wraps describeExactCounterbore\\(ref\\) through occurrence-scoped exact refs|wraps describeExactCountersink\\(ref\\) through occurrence-scoped exact refs" packages/occt-core/test/core.test.mjs` returns matches.
    - `rg -n "exact compound-hole helper describes a supported counterbore selection|exact compound-hole helper describes a supported countersink selection|exact compound-hole helper failures stay explicit for unsupported or plain cylindrical holes" test/exact_compound_hole_contract.test.mjs` returns matches.
    - `rg -n "occt-core compound-hole helpers normalize live semantics into occurrence space" packages/occt-core/test/live-root-integration.test.mjs` returns a match.
    - `node --test test/exact_compound_hole_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_compound_hole_contract.test.mjs</automated>
  </verify>
  <done>The root and package test surfaces now lock the compound-hole helper contract before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive compound-hole carrier support and package wrapper parity</name>
  <read_first>src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, dist/occt-js.d.ts, test/exact_compound_hole_contract.test.mjs, packages/occt-core/src/occt-core.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/live-root-integration.test.mjs</read_first>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/src/index.d.ts, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, test/exact_compound_hole_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports one additive selected-ref compound-hole query, such as `DescribeExactCompoundHole(exactModelId, exactShapeHandle, kind, elementId)`.
    - Test 2: supported counterbore and countersink selections return semantic data with base-hole facts plus the supported compound-hole dimensions, stable frame and anchors, and explicit family or variant identification.
    - Test 3: unsupported geometry, unsupported topology, or plain cylindrical holes return explicit typed failures rather than guessed compound-hole payloads.
    - Test 4: `@tx-code/occt-core` exposes additive package-first wrappers that remain occurrence-safe against the built carrier.
  </behavior>
  <action>Add additive compound-hole DTO structs to `src/importer.hpp` and corresponding TypeScript declarations to `dist/occt-js.d.ts`. In `src/exact-query.hpp` and `src/exact-query.cpp`, implement one narrow selected-ref compound-hole descriptor by extending the existing hole-topology foundation only as far as required for supported `counterbore` and `countersink` cases. Reuse the shipped frame and anchor vocabulary where it fits; do not add generic topology traversal or whole-model discovery APIs. In `src/js-interface.cpp`, add DTO serialization and the new Embind binding. In `packages/occt-core/src/occt-core.js` and `packages/occt-core/src/index.d.ts`, add `describeExactCounterbore(ref)` and `describeExactCountersink(ref)` as package-first helpers over the shared carrier result.</action>
  <acceptance_criteria>
    - `rg -n "DescribeExactCompoundHole|counterbore|countersink" src/importer.hpp src/exact-query.hpp src/exact-query.cpp src/js-interface.cpp dist/occt-js.d.ts packages/occt-core/src/occt-core.js packages/occt-core/src/index.d.ts` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_compound_hole_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_compound_hole_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The root carrier and `@tx-code/occt-core` now expose supported compound-hole semantics from one selected ref without widening the runtime boundary into generic discovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected retained exact ref -> root compound-hole classification | Over-broad topology inference could silently turn plain holes or unsupported geometry into false compound-hole positives |
| root local-space compound-hole DTO -> package occurrence-space DTO | Incorrect transformation would make live helper outputs wrong for transformed or repeated occurrences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | T/R | `src/exact-query.cpp` compound-hole classification | mitigate | Lock supported counterbore and countersink cases plus unsupported plain-hole failures in a focused root contract test |
| T-36-01-02 | T/I | package live occurrence transforms | mitigate | Add a built-carrier integration test proving compound-hole helpers return transformed frame and anchor geometry |
</threat_model>

<verification>
- Run `node --test test/exact_compound_hole_contract.test.mjs`.
- Run `npm run build:wasm:win`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
Downstream JS can call package-first compound-hole helpers over one selected exact ref and rely on stable semantic DTOs plus explicit typed failures, with occurrence transforms preserved and the root carrier widened only by one narrow selected-ref descriptor.
</success_criteria>

<output>
After completion, create `.planning/phases/36-semantic-helper-foundations/36-01-SUMMARY.md`
</output>
