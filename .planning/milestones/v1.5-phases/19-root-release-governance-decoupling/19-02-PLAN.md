---
phase: 19-root-release-governance-decoupling
plan: "02"
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - README.md
  - AGENTS.md
  - packages/occt-core/README.md
  - .codex/skills/releasing-occt-js/SKILL.md
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "Root release docs consistently describe one authoritative runtime-first root gate and one separate planning/archive audit path."
    - "Conditional demo, Babylon, and Tauri verification remains downstream of the root release gate in all touched guidance."
  artifacts:
    - path: README.md
      provides: public root release guidance aligned to the new gate split
      contains: npm run test:release:root remains authoritative and the separate planning audit path is documented explicitly
    - path: AGENTS.md
      provides: repository-level release policy aligned to the new gate split
      contains: runtime-first gate, separate planning audit, conditional secondary-surface verification, and semver-vs-milestone guidance
    - path: packages/occt-core/README.md
      provides: package-first downstream release guidance aligned to the new root gate split
      contains: reference to the authoritative root gate and the separate planning audit path when maintainers need process verification
    - path: .codex/skills/releasing-occt-js/SKILL.md
      provides: thin release shim aligned to AGENTS.md
      contains: root gate plus separate planning audit path without pulling in unconditional secondary-surface work
  key_links:
    - from: README.md
      to: package.json
      via: documented root release and planning audit commands
      pattern: npm run test:
    - from: .codex/skills/releasing-occt-js/SKILL.md
      to: AGENTS.md
      via: thin release shim wording
      pattern: AGENTS\.md
---

<objective>
Align release-facing documentation to the updated governance split once the root gate and planning audit commands are separated.

Purpose: Satisfy DOCS-02 by making README, repo policy, package guidance, and the release skill tell the same story: one authoritative runtime-first root release gate and one separate planning/archive audit path.
Output: Updated docs plus governance-contract assertions that lock the new release guidance in place without dragging Phase 20 secondary-surface work into the root gate.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-root-release-governance-decoupling/19-CONTEXT.md
@.planning/phases/19-root-release-governance-decoupling/19-PATTERNS.md
@AGENTS.md
@package.json
@README.md
@packages/occt-core/README.md
@.codex/skills/releasing-occt-js/SKILL.md
@test/release_governance_contract.test.mjs
@docs/sdk/measurement.md

<interfaces>
From `README.md` (current authoritative release-gate section to preserve and refine):
```markdown
## Release Gate

Use `npm run test:release:root` as the authoritative root release verification command:
```

From `packages/occt-core/README.md` (package-first release note to keep concise):
```markdown
- Root release verification is driven by `npm run test:release:root` from the repository root; demo, Babylon, and Tauri checks remain conditional secondary-surface verification.
```

From `.codex/skills/releasing-occt-js/SKILL.md` (thin-skill policy to preserve):
```markdown
Repository-wide policy lives in `AGENTS.md`. Use that file as the source of truth for release boundaries, root-vs-secondary surfaces, and conditional secondary-surface verification.
```

From `AGENTS.md` (semver-vs-milestone guidance to preserve):
```markdown
- GSD milestone versions such as `v1.4` are planning and delivery markers.
- When preparing a real package release, make the semver bump explicit, run `npm run test:release:root`, and update downstream vendor snapshots only as a separate deliberate step.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align release-facing docs and governance assertions to the new gate split</name>
  <files>README.md, AGENTS.md, packages/occt-core/README.md, .codex/skills/releasing-occt-js/SKILL.md, test/release_governance_contract.test.mjs</files>
  <action>Update all touched release-facing docs so they describe the same command split established in Plan 19-01: `npm run test:release:root` remains the authoritative runtime-first root release gate, while the separate planning/archive audit runs only when maintainers intentionally want process verification. Preserve the existing semver-vs-milestone guidance in `AGENTS.md`, keep the release skill thin and pointed back to `AGENTS.md`, and keep demo/Babylon/Tauri checks explicitly conditional secondary-surface verification rather than unconditional root release blockers. Update the doc assertions in `test/release_governance_contract.test.mjs` so the new wording is locked without reintroducing `.planning` archive-state coupling to the root gate.</action>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
    <automated>npm run test:planning:audit</automated>
    <automated>npm run test:release:root</automated>
  </verify>
  <done>README, AGENTS, `packages/occt-core/README.md`, and the release skill all point to the same split between root release verification and planning audit, and the release-governance contract test locks that guidance in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release docs -> maintainer behavior | Drift between README, AGENTS, package README, and the release skill can make maintainers run the wrong gate or mistake planning state for publish readiness |
| release docs -> secondary-surface policy | A sloppy wording change could accidentally promote demo/Babylon/Tauri checks into unconditional root release blockers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | T | README / AGENTS / package README / skill | mitigate | Update all touched docs together around one authoritative root gate plus one separate planning audit path |
| T-19-05 | T | `test/release_governance_contract.test.mjs` doc assertions | mitigate | Lock the updated docs wording with focused release-surface assertions so later doc drift is caught without restoring `.planning` archive-state coupling |
| T-19-06 | E | release guidance | mitigate | Preserve explicit conditional-secondary-surface wording and semver-vs-milestone guidance in AGENTS and the thin release skill |
</threat_model>

<verification>
- `node --test test/release_governance_contract.test.mjs`
- `npm run test:planning:audit`
- `npm run test:release:root`
</verification>

<success_criteria>
- README, AGENTS, package README, and the release skill all document the same root gate vs planning-audit split.
- Semver-vs-milestone guidance remains explicit after the docs update.
- Demo, Babylon, and Tauri checks remain documented as conditional secondary-surface verification.
</success_criteria>

<output>
After completion, create `.planning/phases/19-root-release-governance-decoupling/19-02-SUMMARY.md`
</output>
