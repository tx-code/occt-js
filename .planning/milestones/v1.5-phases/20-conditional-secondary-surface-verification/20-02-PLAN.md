---
phase: 20-conditional-secondary-surface-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - packages/occt-babylon-loader/package.json
  - packages/occt-babylon-loader/README.md
  - packages/occt-babylon-viewer/README.md
  - packages/occt-babylon-widgets/README.md
  - package.json
  - test/secondary_surface_contract.test.mjs
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - SURF-01
  - SURF-02
  - SURF-03
must_haves:
  truths:
    - "`packages/occt-babylon-loader` declares every runtime/test dependency it imports directly instead of relying on repo-hoisted Babylon packages."
    - "Secondary-surface routing is documented and contract-tested through an opt-in path that remains outside `npm run test:release:root`."
  artifacts:
    - path: packages/occt-babylon-loader/package.json
      provides: standalone loader dependency ownership
      contains: `dependencies` entries for `@babylonjs/core` and `@tx-code/occt-babylon-viewer`, with `@tx-code/occt-core` remaining caller-supplied
    - path: README.md
      provides: top-level touched-path secondary-surface verification matrix
      contains: explicit demo, Babylon, and Tauri follow-up commands kept outside the root release gate
    - path: AGENTS.md
      provides: repository policy for conditional secondary-surface verification
      contains: touched-path routing plus continued exclusion from `npm run test:release:root`
    - path: package.json
      provides: opt-in root maintainer entrypoint for secondary-surface contract audit
      contains: separate `test:secondary:contracts` script outside `test:release:root`
    - path: test/secondary_surface_contract.test.mjs
      provides: opt-in contract coverage for secondary-surface manifests and docs
      contains: assertions for demo commands, Babylon package commands and dependencies, and root-gate exclusion
  key_links:
    - from: packages/occt-babylon-loader/package.json
      to: packages/occt-babylon-loader/src/occt-scene-builder.js
      via: direct import ownership
      pattern: '@babylonjs/core|@tx-code/occt-babylon-viewer'
    - from: package.json
      to: test/secondary_surface_contract.test.mjs
      via: opt-in secondary-surface contract audit
      pattern: test:secondary:contracts
    - from: README.md
      to: demo/package.json
      via: documented demo verification commands
      pattern: npm --prefix demo
---

<objective>
Make the Babylon side standalone-correct and publish one explicit conditional secondary-surface routing contract without polluting the authoritative root release gate.

Purpose: Finish `SURF-01`, `SURF-02`, and `SURF-03` after the demo manifest surface exists by fixing loader dependency ownership, documenting the touched-path command matrix across README/AGENTS/package READMEs, and locking that policy behind a separate opt-in contract audit instead of `npm run test:release:root`.
Output: A loader package that owns its direct imports, concise Babylon package verification notes, a top-level conditional verification matrix, and a dedicated `test:secondary:contracts` lane that keeps root release verification unchanged.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-conditional-secondary-surface-verification/20-CONTEXT.md
@.planning/phases/20-conditional-secondary-surface-verification/20-PATTERNS.md
@.planning/phases/20-conditional-secondary-surface-verification/20-01-PLAN.md
@AGENTS.md
@README.md
@package.json
@demo/package.json
@test/release_governance_contract.test.mjs
@packages/occt-babylon-loader/package.json
@packages/occt-babylon-loader/src/occt-scene-builder.js
@packages/occt-babylon-loader/test/occt-scene-builder.test.mjs
@packages/occt-babylon-loader/README.md
@packages/occt-babylon-viewer/package.json
@packages/occt-babylon-viewer/README.md
@packages/occt-babylon-widgets/package.json
@packages/occt-babylon-widgets/README.md

<interfaces>
From `packages/occt-babylon-loader/package.json` (current hoist-only dependency shape to fix):
```json
"peerDependencies": {
  "@tx-code/occt-core": "^0.1.7",
  "@tx-code/occt-babylon-viewer": "^0.1.7",
  "@babylonjs/core": "^8.0.0"
}
```

From `packages/occt-babylon-loader/src/occt-scene-builder.js` (direct imports that define real package ownership):
```javascript
import { TransformNode } from "@babylonjs/core/Meshes/transformNode.js";
...
import {
  createCadPartMaterial,
  resolveShadingNormals,
} from "@tx-code/occt-babylon-viewer";
```

From `README.md` (existing policy wording to extend, not replace):
```markdown
Demo, Babylon, and Tauri surfaces are conditional secondary-surface verification only. Run their checks when your release changes `demo/`, `demo/tests/`, `demo/src-tauri/`, or the Babylon package surfaces.
```

From `test/release_governance_contract.test.mjs` (root-gate exclusion already locked and must stay authoritative):
```javascript
test("authoritative root release command surface excludes unconditional secondary-surface gates", () => {
  assert.equal(releaseCommand.includes("demo"), false);
  assert.equal(releaseCommand.includes("playwright"), false);
  assert.equal(releaseCommand.includes("tauri"), false);
  assert.equal(releaseCommand.includes("occt-babylon"), false);
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make loader dependency ownership explicit and codify conditional secondary-surface routing</name>
  <files>packages/occt-babylon-loader/package.json, packages/occt-babylon-loader/README.md, packages/occt-babylon-viewer/README.md, packages/occt-babylon-widgets/README.md, package.json, test/secondary_surface_contract.test.mjs, README.md, AGENTS.md</files>
  <action>Update `packages/occt-babylon-loader/package.json` so every direct runtime/test import owned by loader source or tests is declared by the loader package itself. Concretely, move `@babylonjs/core` and `@tx-code/occt-babylon-viewer` out of the hoist-only peer-only path and into loader-owned dependencies; keep `@tx-code/occt-core` caller-supplied unless loader source starts importing it directly. After that boundary is correct, update the Babylon package READMEs plus root `README.md` and `AGENTS.md` with one compact touched-path matrix covering demo commands, Babylon package `test` commands, and `demo/src-tauri/` follow-up verification while explicitly keeping all of them outside `npm run test:release:root`. Add a separate opt-in root script such as `test:secondary:contracts` and a new `test/secondary_surface_contract.test.mjs` file to lock manifest discoverability, loader dependency ownership, and continued root-gate exclusion without extending the authoritative release gate.</action>
  <verify>
    <automated>npm --prefix packages/occt-babylon-loader test</automated>
    <automated>npm --prefix packages/occt-babylon-viewer test</automated>
    <automated>npm --prefix packages/occt-babylon-widgets test</automated>
    <automated>node --test test/secondary_surface_contract.test.mjs</automated>
    <automated>npm run test:secondary:contracts</automated>
    <automated>npm run test:release:root</automated>
  </verify>
  <done>The loader package test lane runs from declared dependencies, Babylon/demo/Tauri follow-up commands are easy to find from docs and manifests, and the separate secondary-surface contract audit exists without expanding the authoritative root release gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader package manifest -> package runtime/tests | Direct imports hidden behind peer-only or hoisted installs make standalone package tests unreliable and conceal real ownership |
| root docs/policy -> maintainer verification behavior | If touched-path routing stays vague, maintainers will either skip secondary checks or accidentally promote them into unconditional root release work |
| optional secondary audit -> authoritative root release gate | A new contract test can accidentally creep into `npm run test:release:root` if command wiring is sloppy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | T | `packages/occt-babylon-loader/package.json` | mitigate | Declare direct loader imports in `dependencies` so package tests no longer rely on undeclared hoisted Babylon installs |
| T-20-05 | T | `README.md` / `AGENTS.md` / Babylon package READMEs | mitigate | Publish one compact touched-path command matrix that keeps secondary surfaces explicit and conditional |
| T-20-06 | E | root command wiring | mitigate | Add a separate `test:secondary:contracts` lane and keep it out of both `npm test` and `npm run test:release:root` |
</threat_model>

<verification>
- `npm --prefix packages/occt-babylon-loader test`
- `npm --prefix packages/occt-babylon-viewer test`
- `npm --prefix packages/occt-babylon-widgets test`
- `node --test test/secondary_surface_contract.test.mjs`
- `npm run test:secondary:contracts`
- `npm run test:release:root`
</verification>

<success_criteria>
- `packages/occt-babylon-loader` can run its package-local test surface from declared dependencies instead of repo hoisting.
- README, AGENTS, and the Babylon package READMEs surface a compact touched-path verification matrix that keeps demo/Babylon/Tauri checks conditional.
- An opt-in root `test:secondary:contracts` command locks the secondary-surface manifest/docs contract without becoming part of the authoritative root release gate.
- `npm run test:release:root` stays unchanged as the runtime-first root release command.
</success_criteria>

<output>
After completion, create `.planning/phases/20-conditional-secondary-surface-verification/20-02-SUMMARY.md`
</output>
