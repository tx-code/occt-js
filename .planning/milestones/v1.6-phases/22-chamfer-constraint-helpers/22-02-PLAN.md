---
phase: 22-chamfer-constraint-helpers
plan: "02"
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/test/live-root-integration.test.mjs
autonomous: true
requirements:
  - FEAT-05
must_haves:
  truths:
    - "Midpoint, equal-distance, and symmetry-style helpers stay package-first and are composed from the shipped placement/relation foundation instead of adding new root carrier APIs."
    - "The new helper family remains occurrence-safe and follows the additive DTO and failure conventions established in the existing exact helper surface."
    - "Package tests lock both mocked behavior and live retained-model integration for the helper family."
  artifacts:
    - path: packages/occt-core/src/occt-core.js
      provides: package-only helper APIs
      contains: midpoint, equal-distance, and symmetry-style helpers built on existing measurement, placement, and relation wrappers
    - path: packages/occt-core/test/core.test.mjs
      provides: mocked helper contract coverage
      contains: helper DTO shape, failure propagation, and tolerance behavior assertions without depending on a live carrier
    - path: packages/occt-core/test/live-root-integration.test.mjs
      provides: live retained-model package proof
      contains: smoke coverage that midpoint and symmetry-style helpers compose correctly over the built carrier's placement and relation outputs
  key_links:
    - from: packages/occt-core/src/occt-core.js
      to: packages/occt-core/src/occt-core.js
      via: reuse of existing pairwise helpers
      pattern: measureExactDistance|suggestExactDistancePlacement|classifyExactRelation
    - from: packages/occt-core/test/core.test.mjs
      to: packages/occt-core/src/occt-core.js
      via: mocked helper parity
      pattern: midpoint|equal-distance|symmetry
    - from: packages/occt-core/test/live-root-integration.test.mjs
      to: packages/occt-core/src/exact-ref-resolver.js
      via: occurrence-scoped exact ref resolution
      pattern: resolveExactElementRef
---

<objective>
Add package-first midpoint, equal-distance, and symmetry-style helper semantics on top of the shipped placement and relation foundation.

Purpose: satisfy `FEAT-05` without widening the root carrier, by proving that `@tx-code/occt-core` can compose the shipped exact measurement, placement, and relation DTOs into reusable higher-level helper semantics.
Output: package-only helper APIs plus mocked and live integration tests that lock occurrence-safe midpoint, equal-distance, and symmetry-style behavior.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-chamfer-constraint-helpers/22-CONTEXT.md
@.planning/phases/22-chamfer-constraint-helpers/22-PATTERNS.md
@.planning/phases/22-chamfer-constraint-helpers/22-01-PLAN.md
@AGENTS.md
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/exact-ref-resolver.js
@packages/occt-core/test/core.test.mjs
@packages/occt-core/test/live-root-integration.test.mjs
@test/exact_placement_contract.test.mjs
@test/exact_relation_contract.test.mjs

<interfaces>
From `packages/occt-core/src/occt-core.js` (pairwise helper building blocks to compose):
```javascript
async measureExactDistance(refA, refB) { ... }
async suggestExactDistancePlacement(refA, refB) { ... }
async classifyExactRelation(refA, refB) { ... }
```

From `test/exact_placement_contract.test.mjs` (midpoint-friendly placement shape):
```javascript
assert.equal(placement?.kind, "distance");
assertPlacementFrame(placement.frame);
assertPlacementAnchors(placement.anchors, 2);
```

From `test/exact_relation_contract.test.mjs` (narrow symmetry gating pattern):
```javascript
assert.equal(relation?.kind, "parallel");
assert.ok(Array.isArray(relation?.anchors) && relation.anchors.length >= 2);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing package tests for midpoint, equal-distance, and symmetry-style helpers</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/22-chamfer-constraint-helpers/22-CONTEXT.md</read_first>
  <files>packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `suggestExactMidpointPlacement derives midpoint geometry from supported distance placement` fails until a package helper can derive midpoint semantics from a supported pairwise distance placement.
    - Test 2: a subtest named `describeExactEqualDistance compares two pairwise distances with explicit tolerance output` fails until a package helper compares two exact distance pairs and returns stable numeric comparison data plus refs.
    - Test 3: a subtest named `suggestExactSymmetryPlacement derives a midplane-style symmetry helper from supported parallel pairs` fails until a package helper reuses distance placement plus relation classification to build a narrow symmetry-style placement result.
    - Test 4: a live subtest named `occt-core package-only helper semantics compose over retained exact placements and relations` fails until the helper family works against the built carrier for at least one supported repeated-geometry or parallel-face scenario.
  </behavior>
  <action>Extend `packages/occt-core/test/core.test.mjs` with mocked tests for the helper family using the exact subtest names from `<behavior>`. Use mocked `measureExactDistance`, `suggestExactDistancePlacement`, and `classifyExactRelation` outputs rather than a live carrier. Extend `packages/occt-core/test/live-root-integration.test.mjs` with one retained-model integration test that proves the package-only helper family composes correctly over the built root placement/relation surface.</action>
  <acceptance_criteria>
    - `rg -n "suggestExactMidpointPlacement derives midpoint geometry from supported distance placement|describeExactEqualDistance compares two pairwise distances with explicit tolerance output|suggestExactSymmetryPlacement derives a midplane-style symmetry helper from supported parallel pairs" packages/occt-core/test/core.test.mjs` returns matches.
    - `rg -n "occt-core package-only helper semantics compose over retained exact placements and relations" packages/occt-core/test/live-root-integration.test.mjs` returns a match.
    - `npm --prefix packages/occt-core test` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix packages/occt-core test</automated>
  </verify>
  <done>The package test surface now fixes the helper-family names, DTO behavior, and live-composition expectations before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement package-only midpoint, equal-distance, and symmetry-style helpers</name>
  <read_first>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs, .planning/phases/22-chamfer-constraint-helpers/22-PATTERNS.md</read_first>
  <files>packages/occt-core/src/occt-core.js, packages/occt-core/test/core.test.mjs, packages/occt-core/test/live-root-integration.test.mjs</files>
  <behavior>
    - Test 1: `OcctCoreClient` exposes package-only helpers for midpoint, equal-distance, and a narrow symmetry-style placement on supported pairwise refs.
    - Test 2: the helper family reuses the existing validation path and keeps explicit typed failures from the underlying measurement, placement, or relation calls.
    - Test 3: midpoint and symmetry helpers return occurrence-space geometry that reuses the shipped frame and anchor vocabulary.
    - Test 4: equal-distance returns stable comparison data including both measured values, their delta, and the effective tolerance instead of viewer-oriented policy.
  </behavior>
  <action>Add the helper family to `packages/occt-core/src/occt-core.js`. Implement midpoint and symmetry-style helpers by composing the existing pairwise placement and relation wrappers; implement equal-distance by composing pairwise distance measurements with an explicit tolerance rule. Keep the APIs additive, package-first, and occurrence-safe. Do not add new root carrier dependencies or viewer/session state. Update the live integration test only as needed to prove the final helper shapes against the built carrier.</action>
  <acceptance_criteria>
    - `rg -n "suggestExactMidpointPlacement|describeExactEqualDistance|suggestExactSymmetryPlacement" packages/occt-core/src/occt-core.js` returns matches.
    - `npm --prefix packages/occt-core test` passes.
    - `npm run test:release:root` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:release:root"</automated>
  </verify>
  <done>`@tx-code/occt-core` now exposes reusable midpoint, equal-distance, and symmetry-style helper semantics over the shipped exact placement and relation foundation without widening the root carrier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| underlying measurement/placement/relation results -> package helper family | Incorrect composition could silently normalize unsupported geometry into misleading helper successes |
| package helper math -> downstream overlay consumers | A midpoint or symmetry helper that does not preserve occurrence transforms would be correct only in local space and fail for repeated geometry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | T/R | package-only helper composition | mitigate | Lock helper-family names, DTOs, and failure propagation with mocked tests before implementation |
| T-22-02-02 | T/I | live occurrence-space helper geometry | mitigate | Add one retained-model integration test proving the helper family composes correctly over the built placement/relation surface |
</threat_model>

<verification>
- Run `npm --prefix packages/occt-core test`.
- Run `npm run test:release:root`.
</verification>

<success_criteria>
Downstream JS can request midpoint, equal-distance, and narrow symmetry-style helper semantics from `@tx-code/occt-core` without reimplementing the geometry math in app code and without adding new root carrier APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/22-chamfer-constraint-helpers/22-02-SUMMARY.md`
</output>
