---
phase: 23-helper-sdk-docs-governance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/occt-core/README.md
  - docs/sdk/measurement.md
  - packages/occt-core/package.json
  - packages/occt-core/src/index.d.ts
  - packages/occt-core/test/package-contract.test.mjs
autonomous: true
requirements:
  - DOCS-03
  - GOV-04
must_haves:
  truths:
    - "`@tx-code/occt-core` is documented as the primary downstream helper SDK entrypoint, while the root README stays the lower-level carrier reference."
    - "The shipped helper family and its narrow support boundaries are documented explicitly instead of being implied only by tests."
    - "`@tx-code/occt-core` publishes an explicit typing/export surface aligned with the package-first helper API."
  artifacts:
    - path: README.md
      provides: lower-level root helper reference
      contains: package-first helper overview, narrowed downstream-boundary wording, and links to the deeper SDK guide
    - path: packages/occt-core/README.md
      provides: primary downstream helper documentation
      contains: helper-family examples, narrow support boundaries, and package-first workflow guidance
    - path: docs/sdk/measurement.md
      provides: deep SDK guide
      contains: the expanded exact measurement-plus-helper walkthrough at the existing stable docs path
    - path: packages/occt-core/package.json
      provides: package metadata
      contains: an explicit `types` entry and export surface aligned with the package typings
    - path: packages/occt-core/src/index.d.ts
      provides: package-local published typings
      contains: `createOcctCore`, `OcctCoreClient`, exact-ref types, and the shipped helper methods/results
    - path: packages/occt-core/test/package-contract.test.mjs
      provides: package-governance checks
      contains: README wording, metadata/export, and helper typing assertions for `@tx-code/occt-core`
  key_links:
    - from: packages/occt-core/src/index.d.ts
      to: packages/occt-core/src/index.js
      via: package public surface parity
      pattern: createOcctCore|OcctCoreClient|describeExactHole|describeExactChamfer
    - from: packages/occt-core/README.md
      to: docs/sdk/measurement.md
      via: package-first helper guide linkage
      pattern: docs/sdk/measurement.md|hole|chamfer|midpoint|equal-distance|symmetry
    - from: packages/occt-core/test/package-contract.test.mjs
      to: packages/occt-core/package.json
      via: typed package export coverage
      pattern: types|exports|src/index.d.ts
---

<objective>
Publish the package-first helper SDK surface clearly and explicitly.

Purpose: satisfy the documentation half of `DOCS-03` and the package-entrypoint half of `GOV-04` by updating the existing SDK docs in place and adding an explicit typing/export contract for `@tx-code/occt-core`.
Output: refreshed root/package/helper docs, a published package-local `.d.ts` surface for `@tx-code/occt-core`, and package tests that lock the helper SDK entrypoint.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-helper-sdk-docs-governance/23-CONTEXT.md
@.planning/phases/23-helper-sdk-docs-governance/23-PATTERNS.md
@AGENTS.md
@README.md
@packages/occt-core/README.md
@docs/sdk/measurement.md
@packages/occt-core/package.json
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@dist/occt-js.d.ts

<interfaces>
From `README.md` / `packages/occt-core/README.md` (existing package-first doc split to preserve):
```markdown
Most downstream JS consumers should start with the package-first adapter surface in `@tx-code/occt-core`.
```

From `packages/occt-core/src/index.js` (public package surface to mirror in typings):
```javascript
export { createOcctCore, OcctCoreClient } from "./occt-core.js";
export { resolveExactElementRef, resolveExactFaceRef, resolveExactEdgeRef, resolveExactVertexRef } from "./exact-ref-resolver.js";
```

From `docs/sdk/measurement.md` (stale boundary wording to replace narrowly):
```markdown
- feature semantics such as hole or chamfer recognition
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing package-governance tests for helper docs and typed entrypoints</name>
  <read_first>packages/occt-core/README.md, packages/occt-core/package.json, packages/occt-core/src/index.js, dist/occt-js.d.ts, .planning/phases/23-helper-sdk-docs-governance/23-CONTEXT.md</read_first>
  <files>packages/occt-core/test/package-contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `occt-core package docs describe the shipped helper family package-first` fails until the package README names `describeExactHole`, `describeExactChamfer`, `suggestExactMidpointPlacement`, `describeExactEqualDistance`, and `suggestExactSymmetryPlacement`.
    - Test 2: a subtest named `occt-core package docs describe narrow helper support boundaries and downstream limits` fails until the README documents the constrained hole/chamfer/symmetry semantics and keeps richer feature discovery plus viewer policy downstream.
    - Test 3: a subtest named `occt-core package metadata exports a published typing surface aligned with the JS barrel` fails until `packages/occt-core/package.json` exposes a `types` entry and export contract for the package-local typings.
    - Test 4: a subtest named `occt-core published typings expose the helper SDK surface` fails until the package-local `.d.ts` surface includes the shipped helper methods and core exact-ref/result types.
  </behavior>
  <action>Create `packages/occt-core/test/package-contract.test.mjs` as a focused package-governance suite. Use Node built-in tests and direct file reads to assert package README wording, package metadata, and package-local typing presence. Do not rely on a live carrier or npm publish flow for these checks.</action>
  <acceptance_criteria>
    - `rg -n "shipped helper family package-first|narrow helper support boundaries|published typing surface aligned with the JS barrel|published typings expose the helper SDK surface" packages/occt-core/test/package-contract.test.mjs` returns matches.
    - `node --test packages/occt-core/test/package-contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/occt-core/test/package-contract.test.mjs</automated>
  </verify>
  <done>The package-governance test surface now fixes the doc and typing expectations before the docs/export changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update helper docs in place and publish `@tx-code/occt-core` typings</name>
  <read_first>README.md, packages/occt-core/README.md, docs/sdk/measurement.md, packages/occt-core/package.json, packages/occt-core/src/index.js, packages/occt-core/test/package-contract.test.mjs, .planning/phases/23-helper-sdk-docs-governance/23-PATTERNS.md</read_first>
  <files>README.md, packages/occt-core/README.md, docs/sdk/measurement.md, packages/occt-core/package.json, packages/occt-core/src/index.d.ts, packages/occt-core/test/package-contract.test.mjs</files>
  <behavior>
    - Test 1: root and package docs name the shipped helper family explicitly and preserve the package-first / lower-level-root split.
    - Test 2: docs replace the stale “hole or chamfer remain downstream” wording with a narrower boundary statement that keeps only richer feature discovery, viewer UX, and app-owned presentation policy downstream.
    - Test 3: `@tx-code/occt-core` publishes an explicit typing surface aligned with its JS barrel and helper methods.
    - Test 4: package-governance tests pass inside the existing `npm --prefix packages/occt-core test` lane.
  </behavior>
  <action>Update `README.md`, `packages/occt-core/README.md`, and `docs/sdk/measurement.md` in place so they document the shipped helper family package-first without creating a second helper-only guide. Add `packages/occt-core/src/index.d.ts` (or equivalent package-local typing file) that mirrors the public package API and helper result surfaces. Extend `packages/occt-core/package.json` with the corresponding `types` and export metadata. Keep the root README lower-level and concise; keep the package README and deep SDK guide as the primary helper docs.</action>
  <acceptance_criteria>
    - `rg -n "describeExactHole|describeExactChamfer|suggestExactMidpointPlacement|describeExactEqualDistance|suggestExactSymmetryPlacement" README.md packages/occt-core/README.md docs/sdk/measurement.md packages/occt-core/src/index.d.ts` returns matches.
    - `rg -n "\"types\"|index.d.ts" packages/occt-core/package.json` returns matches.
    - `npm --prefix packages/occt-core test` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix packages/occt-core test</automated>
  </verify>
  <done>The helper SDK is now documented package-first and `@tx-code/occt-core` publishes an explicit typed entrypoint aligned with its public JS surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped helper implementation -> public package docs | Stale wording could under-document or over-promise helper semantics |
| JS package barrel -> published package typings | Typings that drift from the real JS surface would break the package-first SDK contract for downstream consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | T/R | package docs | mitigate | Lock helper names, support boundaries, and downstream limits in a package-governance test |
| T-23-01-02 | T/I | package typings/export surface | mitigate | Add package metadata and typing parity checks against the JS barrel before publishing |
</threat_model>

<verification>
- Run `node --test packages/occt-core/test/package-contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
</verification>

<success_criteria>
Downstream consumers can discover and type the shipped helper SDK through `@tx-code/occt-core` without inferring broader feature-recognition scope or depending on undocumented package metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/23-helper-sdk-docs-governance/23-01-SUMMARY.md`
</output>
