---
phase: 09-root-import-appearance-mode
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/js-interface.cpp
  - src/importer-xde.cpp
  - test/import_appearance_contract.test.mjs
  - test/exact_model_lifecycle_contract.test.mjs
  - test/test_multi_format_exports.mjs
autonomous: true
requirements:
  - APPR-01
  - APPR-02
  - APPR-05
must_haves:
  truths:
    - "Explicit `colorMode` takes precedence over legacy `readColors`, and omitted `colorMode` preserves legacy behavior."
    - "`OpenExact*` and `OpenExactModel` return the same appearance-mode scene colors/materials as `Read*` while preserving exact bindings."
    - "The precedence policy is stated inline in the public typings and locked by parity/compatibility tests."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/importer.hpp"
    - "src/js-interface.cpp"
    - "src/importer-xde.cpp"
    - "test/import_appearance_contract.test.mjs"
    - "test/exact_model_lifecycle_contract.test.mjs"
    - "test/test_multi_format_exports.mjs"
  key_links:
    - "colorMode precedence -> legacy readColors compatibility"
    - "shared ImportParams -> Read* and OpenExact* appearance parity"
    - "public typings comments -> regression tests"
---

<objective>
Lock legacy `readColors` compatibility and `OpenExact*` appearance-mode parity.

Purpose: satisfy APPR-05 and harden the Phase 9 contract so explicit appearance mode is predictable for both stateless and exact-open import lanes without silently rewriting older `readColors` callers.
Output: root tests for precedence and exact-open parity, plus importer/type updates that make the precedence policy explicit and shared across all root import entry points.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-root-import-appearance-mode/09-RESEARCH.md
@.planning/phases/09-root-import-appearance-mode/09-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/js-interface.cpp
@src/importer-xde.cpp
@test/import_appearance_contract.test.mjs
@test/exact_model_lifecycle_contract.test.mjs
@test/test_multi_format_exports.mjs
@test/ANC101_colored.stp
@test/bearing.igs
@test/ANC101_isolated_components.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for legacy `readColors` compatibility and exact-open appearance parity</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, src/importer-xde.cpp, test/import_appearance_contract.test.mjs, test/exact_model_lifecycle_contract.test.mjs, test/test_multi_format_exports.mjs, .planning/phases/09-root-import-appearance-mode/09-RESEARCH.md</read_first>
  <files>test/import_appearance_contract.test.mjs, test/exact_model_lifecycle_contract.test.mjs, test/test_multi_format_exports.mjs</files>
  <behavior>
    - Test 1: a subtest named `legacy readColors false stays colorless when colorMode is omitted` fails until colored STEP import with `{ readColors: false }` does not force the built-in default color.
    - Test 2: a subtest named `explicit colorMode takes precedence over legacy readColors` fails until `{ readColors: false, colorMode: "source" }` preserves source colors and `{ readColors: true, colorMode: "default" }` still collapses to the built-in default color.
    - Test 3: a subtest named `OpenExact appearance mode keeps exact bindings while matching root colors` fails until `OpenExactStepModel` and `OpenExactModel("brep", ...)` return the same appearance-mode scene colors/materials as their stateless read counterparts while still exposing `exactModelId` and `exactGeometryBindings`.
    - Test 4: a regression assertion in `test/test_multi_format_exports.mjs` fails until generic and format-specific root import entry points keep parity for the new appearance precedence cases.
  </behavior>
  <action>Extend `test/import_appearance_contract.test.mjs` with the exact subtest names from `<behavior>`, reusing `ANC101_colored.stp`, `bearing.igs`, and `ANC101_isolated_components.brep`. Add explicit precedence assertions for `readColors` versus `colorMode`. Extend `test/exact_model_lifecycle_contract.test.mjs` with an exact-open smoke test that imports `simple_part.step` or `ANC101_isolated_components.brep` using `colorMode: "default"` and asserts that `exactModelId`, `exactGeometryBindings.length`, and appearance-mode scene colors/materials all survive together. Extend `test/test_multi_format_exports.mjs` with one or more direct assertions that generic `ReadFile(...)` behavior matches format-specific methods under the new precedence rules.</action>
  <acceptance_criteria>
    - `rg -n "legacy readColors false stays colorless when colorMode is omitted|explicit colorMode takes precedence over legacy readColors|OpenExact appearance mode keeps exact bindings while matching root colors" test/import_appearance_contract.test.mjs test/exact_model_lifecycle_contract.test.mjs` returns matches.
    - `rg -n "readColors: false|colorMode: \"source\"|colorMode: \"default\"|exactGeometryBindings" test/import_appearance_contract.test.mjs test/exact_model_lifecycle_contract.test.mjs test/test_multi_format_exports.mjs` returns matches.
    - `node --test test/import_appearance_contract.test.mjs test/exact_model_lifecycle_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/import_appearance_contract.test.mjs test/exact_model_lifecycle_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_multi_format_exports.mjs"</automated>
  </verify>
  <done>The regression tests prove exact-open parity and the intended precedence policy before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit precedence policy and shared exact-open/read appearance semantics</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, src/importer-xde.cpp, test/import_appearance_contract.test.mjs, test/exact_model_lifecycle_contract.test.mjs, test/test_multi_format_exports.mjs, .planning/phases/09-root-import-appearance-mode/09-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, src/importer-xde.cpp, test/import_appearance_contract.test.mjs, test/exact_model_lifecycle_contract.test.mjs, test/test_multi_format_exports.mjs</files>
  <behavior>
    - Test 1: explicit `colorMode` overrides `readColors` in a deterministic way.
    - Test 2: omitted `colorMode` preserves current `readColors` semantics.
    - Test 3: XDE import readers stop depending on extracted source colors when `default` mode is selected and force source-color extraction when explicit `source` mode is selected.
    - Test 4: `Read*` and `OpenExact*` stay in appearance-mode parity because they share the same importer policy and public type contract.
  </behavior>
  <action>Add helper logic in `src/importer.hpp` / `src/js-interface.cpp` that makes the appearance precedence explicit: `colorMode` present -> explicit mode; `colorMode` omitted -> legacy `readColors`. In `src/importer-xde.cpp`, replace the plain `cafReader.SetColorMode(params.readColors)` calls with a shared policy helper so `default` mode disables source-color extraction, `source` mode forces it on, and legacy mode uses `params.readColors` unchanged. Keep the default-color stamping from `09-01`, but make it depend on the same helper logic used by both `ReadByFormat` and `OpenExactByFormat`. In `dist/occt-js.d.ts`, add concise comments above `colorMode` and `readColors` stating that explicit `colorMode` takes precedence and `readColors` is legacy-only when `colorMode` is omitted. Do not add `defaultColor` yet; that belongs to Phase 10.</action>
  <acceptance_criteria>
    - `rg -n "SetColorMode|colorMode|readColors" src/importer-xde.cpp src/importer.hpp src/js-interface.cpp` returns matches that show the precedence helper is wired into XDE imports.
    - `rg -n "takes precedence|legacy" dist/occt-js.d.ts` returns matches in the public type comments.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/import_appearance_contract.test.mjs test/exact_model_lifecycle_contract.test.mjs` passes.
    - `node test/test_multi_format_exports.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/import_appearance_contract.test.mjs test/exact_model_lifecycle_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_multi_format_exports.mjs"</automated>
  </verify>
  <done>The Phase 9 import appearance contract now has explicit precedence, shared `Read*`/`OpenExact*` semantics, and regression coverage for legacy callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| explicit `colorMode` -> legacy `readColors` fallback | Wrong precedence here would silently break older downstream imports. |
| exact-open scene payload -> retained exact bindings | Appearance-mode changes must not disturb exact model registration or geometry bindings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | T/R | compatibility precedence | mitigate | Lock explicit precedence with regression tests for legacy and explicit modes before finalizing implementation. |
| T-09-02-02 | T/I | exact-open appearance parity | mitigate | Add exact-open parity tests that assert scene colors/materials and exact bindings coexist without regression. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/import_appearance_contract.test.mjs test/exact_model_lifecycle_contract.test.mjs`.
- Run `node test/test_multi_format_exports.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Explicit appearance mode precedence is locked for both stateless and exact-open root imports without breaking legacy `readColors` callers
  </success_criteria>

<output>
After completion, create `.planning/phases/09-root-import-appearance-mode/09-02-SUMMARY.md`
</output>
