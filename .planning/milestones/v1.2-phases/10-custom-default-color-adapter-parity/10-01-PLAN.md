---
phase: 10-custom-default-color-adapter-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/js-interface.cpp
  - test/import_appearance_contract.test.mjs
autonomous: true
requirements:
  - APPR-03
  - APPR-04
must_haves:
  truths:
    - "Root import APIs accept a caller-provided `defaultColor` override on the public contract."
    - "`defaultColor` only affects imports when `colorMode: \"default\"` is selected; it does not implicitly switch legacy modes."
    - "`Read*` and `OpenExact*` honor the same custom-default appearance behavior because they share one root import param path."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/js-interface.cpp"
    - "test/import_appearance_contract.test.mjs"
  key_links:
    - "ParseImportParams -> public defaultColor input"
    - "shared ImportParams.defaultColor -> read/openExact parity"
    - "root appearance contract tests -> custom default and ignored-without-default-mode semantics"
---

<objective>
Expose custom default colors on the root import contract.

Purpose: satisfy APPR-03 and APPR-04 by letting callers provide a custom RGB default color that the root runtime applies consistently across stateless and exact-open imports when default appearance mode is explicitly selected.
Output: root typings, root import param parsing, and contract tests for custom default color parity.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-custom-default-color-adapter-parity/10-RESEARCH.md
@.planning/phases/10-custom-default-color-adapter-parity/10-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/js-interface.cpp
@test/import_appearance_contract.test.mjs
@test/ANC101_colored.stp
@test/bearing.igs
@test/ANC101_isolated_components.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root tests for custom default color overrides</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, test/import_appearance_contract.test.mjs, .planning/phases/10-custom-default-color-adapter-parity/10-RESEARCH.md</read_first>
  <files>test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `defaultColor overrides the built-in CAD color for stateless imports` fails until `ReadFile(..., { colorMode: "default", defaultColor: { r, g, b } })` returns the caller-provided RGB color instead of `[0.9, 0.91, 0.93]`.
    - Test 2: a subtest named `defaultColor is ignored unless colorMode default is selected` fails until `defaultColor` without `colorMode: "default"` leaves legacy/source appearance behavior unchanged.
    - Test 3: a subtest named `OpenExact uses the same custom defaultColor appearance payload as ReadFile` fails until `OpenExact*` and `OpenExactModel` match the stateless read-lane appearance payload while preserving `exactModelId` and `exactGeometryBindings`.
    - Test 4: a subtest named `custom defaultColor parity holds across colored and colorless fixtures` fails until custom default colors work for both `ANC101_colored.stp` and colorless IGES/BREP imports.
  </behavior>
  <action>Extend `test/import_appearance_contract.test.mjs` with the exact subtest names from `<behavior>`. Use object-form `defaultColor` input on `ANC101_colored.stp`, `bearing.igs`, and `ANC101_isolated_components.brep`. Assert that the root `materials` and geometry/face colors use the caller-provided RGB when `colorMode: "default"` is present, and assert that `defaultColor` alone does not silently override explicit `source` or legacy behavior. Add read/openExact parity coverage for the custom default color path.</action>
  <acceptance_criteria>
    - `rg -n "defaultColor overrides the built-in CAD color for stateless imports|defaultColor is ignored unless colorMode default is selected|OpenExact uses the same custom defaultColor appearance payload as ReadFile|custom defaultColor parity holds across colored and colorless fixtures" test/import_appearance_contract.test.mjs` returns matches.
    - `rg -n "defaultColor|colorMode: \"default\"|exactGeometryBindings" test/import_appearance_contract.test.mjs` returns matches.
    - `node --test test/import_appearance_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/import_appearance_contract.test.mjs</automated>
  </verify>
  <done>The root contract tests prove custom default color behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse public defaultColor input on the shared root import path</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, test/import_appearance_contract.test.mjs, .planning/phases/10-custom-default-color-adapter-parity/10-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/js-interface.cpp, test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: the public root typing surface exposes `defaultColor?: OcctJSColor`.
    - Test 2: `ParseImportParams(...)` parses object-form `defaultColor` and normalizes it onto the existing internal `ImportParams.defaultColor`.
    - Test 3: `defaultColor` only changes output when `colorMode: "default"` is selected; otherwise the Phase 9 precedence behavior remains intact.
    - Test 4: `Read*` and `OpenExact*` stay in parity without any separate exact-open color path.
  </behavior>
  <action>Add `defaultColor?: OcctJSColor` to `dist/occt-js.d.ts` with a comment that it only applies when `colorMode: "default"` is selected. In `src/js-interface.cpp`, parse `defaultColor` from object-form `{ r, g, b }`, normalize/clamp it to a valid RGB value, and write it into the existing `ImportParams.defaultColor`. Do not change the importer logic beyond what is necessary to consume the already-existing internal field; the shared root import path should make stateless/exact parity fall out automatically. Keep alpha out of scope for this phase.</action>
  <acceptance_criteria>
    - `rg -n "defaultColor" dist/occt-js.d.ts src/js-interface.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/import_appearance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/import_appearance_contract.test.mjs"</automated>
  </verify>
  <done>The root Wasm carrier now exposes caller-provided custom default colors on the shared import contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS `defaultColor` input -> parsed root RGB | Bad parsing here would corrupt downstream appearance settings. |
| explicit default appearance -> shared read/openExact policy | If the custom color path forks, stateless and exact-open imports will drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | T/R | custom default color contract | mitigate | Lock custom-default behavior and ignored-without-default-mode semantics with failing root tests first. |
| T-10-01-02 | T/I | read/openExact parity | mitigate | Reuse the shared import param path and assert parity in the same root contract suite. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/import_appearance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root imports now support caller-provided custom default colors with explicit default-mode semantics
</success_criteria>

<output>
After completion, create `.planning/phases/10-custom-default-color-adapter-parity/10-01-SUMMARY.md`
</output>
