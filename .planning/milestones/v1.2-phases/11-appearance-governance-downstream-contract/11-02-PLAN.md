---
phase: 11-appearance-governance-downstream-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - test/package_tarball_contract.test.mjs
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - ADAPT-04
must_haves:
  truths:
    - "Packaged appearance typings are pinned for downstream/vendored consumers."
    - "Release-governance checks fail if docs or package surfaces drift from the shipped appearance contract."
    - "The authoritative root release gate remains runtime-first while still covering import appearance contract drift."
  artifacts:
    - "test/package_tarball_contract.test.mjs"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "package tarball contract -> packaged appearance typings"
    - "release governance -> import appearance docs and runtime-first release boundary"
---

<objective>
Lock packaged appearance typings and root release governance.

Purpose: satisfy the packaging-and-governance half of ADAPT-04 by making future releases fail if the packaged type surface or release docs drift from the shipped import appearance contract.
Output: package tarball contract assertions, governance assertions, and a green runtime-first release gate.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-appearance-governance-downstream-contract/11-RESEARCH.md
@.planning/phases/11-appearance-governance-downstream-contract/11-VALIDATION.md
@package.json
@dist/occt-js.d.ts
@README.md
@packages/occt-core/README.md
@test/package_tarball_contract.test.mjs
@test/release_governance_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing package-contract tests for packaged appearance typings</name>
  <read_first>package.json, dist/occt-js.d.ts, README.md, test/package_tarball_contract.test.mjs, .planning/phases/11-appearance-governance-downstream-contract/11-RESEARCH.md</read_first>
  <files>test/package_tarball_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `packed root package ships appearance typings needed by downstream consumers` fails until the packaged `dist/occt-js.d.ts` surface exposes `OcctJSImportColorMode`, `colorMode`, and `defaultColor`.
    - Test 2: a subtest named `package contract keeps import appearance package-first and independent of viewer surfaces` fails until the package-level contract proves appearance guidance is carried by the root package artifacts rather than repo-local demo or viewer layers.
  </behavior>
  <action>Extend `test/package_tarball_contract.test.mjs` with the exact subtest names from `<behavior>`. Use the packed manifest and local `dist/occt-js.d.ts` source to assert that the package ships README plus the appearance typing surface required by downstream consumers, and that nothing about the package contract depends on demo/Babylon/Tauri surfaces for import appearance guidance.</action>
  <acceptance_criteria>
    - `rg -n "packed root package ships appearance typings needed by downstream consumers|package contract keeps import appearance package-first and independent of viewer surfaces" test/package_tarball_contract.test.mjs` returns matches.
    - `node --test test/package_tarball_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/package_tarball_contract.test.mjs</automated>
  </verify>
  <done>The package contract tests now define the packaged appearance surface required by downstream consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden release/package governance around the appearance contract</name>
  <read_first>test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs, package.json, README.md, packages/occt-core/README.md, dist/occt-js.d.ts, .planning/phases/11-appearance-governance-downstream-contract/11-RESEARCH.md</read_first>
  <files>test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: package-contract checks pin the packaged appearance typing surface and shipped README for downstream consumers.
    - Test 2: governance checks pin the runtime-first release boundary while ensuring import appearance docs remain authoritative in root/package docs.
    - Test 3: the final root release gate stays green without adding unconditional demo/Babylon/Tauri verification.
  </behavior>
  <action>Implement the package and governance assertions introduced in Task 1, tightening them only around the already-shipped appearance contract. Keep `npm run test:release:root` runtime-first and rely on its existing structure rather than widening the release gate to secondary surfaces. Re-run the full release gate once the contract tests are green so the milestone closes on the authoritative verification path.</action>
  <acceptance_criteria>
    - `node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs` passes.
    - `npm run test:release:root` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:release:root"</automated>
  </verify>
  <done>The package and governance tests now lock the packaged appearance contract on the authoritative release path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package tarball -> downstream consumers | If packaged typings drift, vendored/package-first consumers lose the appearance contract even if local docs still look correct. |
| release governance tests -> future releases | Weak governance here would allow docs or package surfaces to regress without failing the release gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | T/R | packaged typing surface | mitigate | Add failing tarball tests that pin `colorMode` and `defaultColor` in the packaged contract. |
| T-11-02-02 | R/I | release governance | mitigate | Keep `npm run test:release:root` authoritative and add governance assertions for appearance docs/package surfaces. |
</threat_model>

<verification>
- Run `node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs`.
- Run `npm run test:release:root`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Packaged typings and governance tests now lock the shipped appearance contract on the runtime-first release path
</success_criteria>

<output>
After completion, create `.planning/phases/11-appearance-governance-downstream-contract/11-02-SUMMARY.md`
</output>
