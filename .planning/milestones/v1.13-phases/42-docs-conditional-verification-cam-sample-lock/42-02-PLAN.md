---
phase: 42-docs-conditional-verification-cam-sample-lock
plan: "02"
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - test/secondary_surface_contract.test.mjs
  - test/release_governance_contract.test.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - GOV-06
must_haves:
  truths:
    - "Conditional verification fails on CAM sample workflow drift in docs and maintained browser coverage."
    - "Governance tests keep `test:secondary:contracts` outside `test:release:root`."
    - "Demo browser verification remains conditional secondary-surface evidence, not a root release gate."
  artifacts:
    - path: test/secondary_surface_contract.test.mjs
      provides: conditional docs/browser drift lock
      contains: CAM sample-specific assertions tied to maintained browser and docs seams
    - path: test/release_governance_contract.test.mjs
      provides: release-boundary governance lock
      contains: explicit checks that secondary CAM verification never becomes unconditional root release gating
    - path: demo/tests/demo.spec.mjs
      provides: maintained browser CAM evidence seam
      contains: stable CAM action/result markers consumed by conditional governance tests
  key_links:
    - from: test/secondary_surface_contract.test.mjs
      to: demo/tests/demo.spec.mjs
      via: conditional contract should lock CAM sample workflow drift against maintained browser behavior
      pattern: measurement-action-clearance|measurement-action-step-depth|measurement-action-surface-to-center
    - from: test/release_governance_contract.test.mjs
      to: package.json
      via: governance must keep secondary command and release command separated
      pattern: test:release:root|test:secondary:contracts
    - from: test/secondary_surface_contract.test.mjs
      to: README.md
      via: docs and conditional verification must describe the same CAM sample ownership boundary
      pattern: simplified integration sample|demo-owned|current-result
---

<objective>
Lock CAM sample drift through conditional browser/governance coverage while preserving root release boundaries.

Purpose: satisfy `GOV-06` by tightening secondary and governance contract tests so CAM sample docs/browser drift fails fast, with `npm run test:release:root` unchanged as authoritative root gate.
Output: updated contract tests and maintained browser markers that enforce the conditional verification boundary.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-docs-conditional-verification-cam-sample-lock/42-CONTEXT.md
@.planning/phases/42-docs-conditional-verification-cam-sample-lock/42-RESEARCH.md
@.planning/phases/42-docs-conditional-verification-cam-sample-lock/42-01-PLAN.md
@AGENTS.md
@package.json
@README.md
@test/secondary_surface_contract.test.mjs
@test/release_governance_contract.test.mjs
@demo/package.json
@demo/tests/demo.spec.mjs

<interfaces>
Secondary command contract:
```js
assert.equal(contractCommand, "node --test test/secondary_surface_contract.test.mjs");
assert.equal(releaseCommand.includes("test:secondary:contracts"), false);
```

Maintained browser lane contract:
```js
assert.match(demoPackage.scripts?.["test:e2e"] ?? "", /demo\/tests\/demo\.spec\.mjs/);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing CAM drift assertions to secondary/governance contracts</name>
  <read_first>test/secondary_surface_contract.test.mjs, test/release_governance_contract.test.mjs, demo/tests/demo.spec.mjs, package.json</read_first>
  <files>test/secondary_surface_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: secondary contract fails until maintained CAM sample browser markers are explicitly locked.
    - Test 2: governance contract fails until CAM-focused secondary verification remains explicitly outside root release command routing.
    - Test 3: assertions stay narrow and route-focused to avoid converting conditional checks into release-gate coupling.
  </behavior>
  <action>Write focused red assertions in secondary and governance tests for CAM sample drift and command routing before making any browser-test seam adjustments.</action>
  <acceptance_criteria>
    - `rg -n "clearance|step-depth|surface-to-center|center-to-center|test:secondary:contracts|test:release:root" test/secondary_surface_contract.test.mjs test/release_governance_contract.test.mjs` returns new expectation lines.
    - `node --test test/secondary_surface_contract.test.mjs` or `node --test test/release_governance_contract.test.mjs` fails before Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/secondary_surface_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit 0 }; node --test test/release_governance_contract.test.mjs; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>Red conditional/governance assertions now pin CAM drift and routing boundaries before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align maintained browser seam and green the conditional governance contracts</name>
  <read_first>demo/tests/demo.spec.mjs, test/secondary_surface_contract.test.mjs, test/release_governance_contract.test.mjs</read_first>
  <files>demo/tests/demo.spec.mjs, test/secondary_surface_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: secondary contract now proves docs + maintained browser lane CAM markers together.
    - Test 2: governance contract continues proving root/secondary command separation with no release-script changes.
    - Test 3: maintained browser seams remain representative and do not reintroduce removed productivity workflows.
  </behavior>
  <action>Apply the smallest test and browser-seam adjustments needed to satisfy the new assertions while keeping command routing and sample boundaries unchanged.</action>
  <acceptance_criteria>
    - `node --test test/secondary_surface_contract.test.mjs` passes.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/secondary_surface_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/release_governance_contract.test.mjs"</automated>
  </verify>
  <done>Secondary/governance tests now lock CAM drift and route boundaries in green state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-run maintained conditional browser lane with route guards intact</name>
  <read_first>demo/package.json, demo/tests/demo.spec.mjs, AGENTS.md</read_first>
  <files>demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: maintained browser lane remains green with CAM sample assertions.
    - Test 2: route guards still keep browser lane conditional and outside root release gate.
  </behavior>
  <action>Run the maintained demo browser lane after contract changes and only adjust stable markers when necessary to keep assertions robust.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `rg -n "demo\\/tests\\/demo\\.spec\\.mjs|test:secondary:contracts|test:release:root" demo/package.json package.json` confirms unchanged command routing.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e"</automated>
  </verify>
  <done>Conditional browser/governance coverage is locked for the CAM sample without touching root release routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secondary contract coverage -> CAM sample drift detection | If secondary contract tests do not lock CAM markers, docs/UI drift can pass unnoticed |
| release governance -> root release gate purity | If secondary coverage leaks into root release routing, the runtime-first release boundary regresses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | I | secondary CAM drift coverage | mitigate | Add focused CAM marker assertions tied to maintained browser/doc seams |
| T-42-02-02 | T/R | release command routing | mitigate | Keep explicit governance assertions that `test:secondary:contracts` remains outside `test:release:root` |
</threat_model>

<verification>
- Run `node --test test/secondary_surface_contract.test.mjs`.
- Run `node --test test/release_governance_contract.test.mjs`.
- Run `npm --prefix demo run test:e2e`.
</verification>

<success_criteria>
Conditional verification now fails on CAM sample drift and preserves unchanged authoritative root release routing.
</success_criteria>

<output>
After completion, create `.planning/phases/42-docs-conditional-verification-cam-sample-lock/42-02-SUMMARY.md`
</output>
