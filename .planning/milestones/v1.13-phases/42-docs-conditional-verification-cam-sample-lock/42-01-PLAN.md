---
phase: 42-docs-conditional-verification-cam-sample-lock
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/sdk/measurement.md
  - packages/occt-core/README.md
  - .planning/PROJECT.md
  - test/secondary_surface_contract.test.mjs
autonomous: true
requirements:
  - DOCS-08
must_haves:
  truths:
    - "Canonical docs describe the shipped CAM sample as demo-owned workflow composition over reusable exact primitives."
    - "Root/package/sdk docs keep the same ownership sentence for supported exact action routing, overlay rendering, and current-result behavior."
    - "Active planning truth reflects that Phase 40-41 delivered CAM sample behavior and Phase 42 now locks docs/governance only."
  artifacts:
    - path: README.md
      provides: root-level contract wording
      contains: CAM sample ownership split and unchanged release boundary language
    - path: docs/sdk/measurement.md
      provides: package-first measurement SDK wording
      contains: aligned CAM sample ownership framing without publishing demo workflow policy as package API
    - path: packages/occt-core/README.md
      provides: package-facing wording parity
      contains: consistent wording with root/sdk docs for demo-owned CAM workflow composition
    - path: .planning/PROJECT.md
      provides: active planning truth
      contains: updated current-state text aligned with Phase 40-41 shipped CAM sample scope
    - path: test/secondary_surface_contract.test.mjs
      provides: conditional docs contract lock
      contains: focused assertions that fail on CAM sample wording drift in canonical docs
  key_links:
    - from: README.md
      to: docs/sdk/measurement.md
      via: canonical docs must share the same CAM sample ownership language and downstream boundary
      pattern: simplified integration sample|supported exact action routing|current-result
    - from: packages/occt-core/README.md
      to: docs/plan/cam-measurement-matrix.md
      via: package-facing wording should mirror the documented ownership split without promoting CAM workflow names into package APIs
      pattern: clearance|step depth|center-to-center|surface-to-center
    - from: .planning/PROJECT.md
      to: .planning/phases/41-cam-scenario-ux-sample-first-presentation/41-02-SUMMARY.md
      via: active planning truth should describe the already-shipped CAM sample framing before governance lock work
      pattern: sample-first|optional tool|typed unsupported|overlay cleanup
---

<objective>
Align canonical docs and active planning truth for the `v1.13` CAM measurement sample.

Purpose: satisfy `DOCS-08` by making root/package/sdk docs and active planning truth describe one consistent ownership model: reusable exact primitives ship in runtime/package; CAM workflow naming remains demo-owned composition.
Output: aligned docs plus conditional docs assertions that fail on boundary drift.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-docs-conditional-verification-cam-sample-lock/42-CONTEXT.md
@.planning/phases/42-docs-conditional-verification-cam-sample-lock/42-RESEARCH.md
@.planning/phases/40-demo-cam-workflow-composition/40-02-SUMMARY.md
@.planning/phases/41-cam-scenario-ux-sample-first-presentation/41-02-SUMMARY.md
@docs/plan/cam-measurement-matrix.md
@AGENTS.md
@README.md
@docs/sdk/measurement.md
@packages/occt-core/README.md
@test/secondary_surface_contract.test.mjs

<interfaces>
Canonical docs boundary sentence shape:
```md
The shipped browser demo is a simplified integration sample. It keeps supported exact action routing, overlay rendering, and current-result session behavior in app code.
```

Conditional docs contract seam:
```js
test("secondary-surface docs keep measurement ownership explicit ...")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing docs assertions for CAM sample ownership wording</name>
  <read_first>test/secondary_surface_contract.test.mjs, README.md, docs/sdk/measurement.md, packages/occt-core/README.md</read_first>
  <files>test/secondary_surface_contract.test.mjs</files>
  <behavior>
    - Test 1: conditional docs contract fails until canonical docs explicitly describe the shipped CAM sample ownership split.
    - Test 2: contract fails until root/sdk/package docs expose consistent boundary language.
    - Test 3: assertions remain narrow and avoid turning demo workflow naming into package API commitments.
  </behavior>
  <action>Add the smallest red assertions that encode the required CAM sample ownership wording and doc-surface consistency before editing docs.</action>
  <acceptance_criteria>
    - `rg -n "CAM|clearance|step depth|center-to-center|surface-to-center|simplified integration sample|supported exact action routing|current-result session behavior" test/secondary_surface_contract.test.mjs` returns new expectation lines.
    - `node --test test/secondary_surface_contract.test.mjs` fails before Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/secondary_surface_contract.test.mjs"</automated>
  </verify>
  <done>Red docs-contract expectations now lock the intended CAM sample wording before prose changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite canonical docs and active planning truth to match shipped CAM sample scope</name>
  <read_first>README.md, docs/sdk/measurement.md, packages/occt-core/README.md, .planning/PROJECT.md, docs/plan/cam-measurement-matrix.md</read_first>
  <files>README.md, docs/sdk/measurement.md, packages/occt-core/README.md, .planning/PROJECT.md, test/secondary_surface_contract.test.mjs</files>
  <behavior>
    - Test 1: docs now explicitly place CAM workflow naming in demo-owned composition over shipped exact primitives.
    - Test 2: active planning current-state wording remains consistent with completed Phases 40-41 and Phase 42 scope.
    - Test 3: no new runtime/package workflow promises are introduced.
  </behavior>
  <action>Apply the smallest wording changes needed to align root/sdk/package docs and active planning truth with the shipped CAM sample boundary, then adjust docs assertions to green.</action>
  <acceptance_criteria>
    - `node --test test/secondary_surface_contract.test.mjs` passes.
    - `rg -n "clearance|step depth|center-to-center|surface-to-center|demo-owned|simplified integration sample" README.md docs/sdk/measurement.md packages/occt-core/README.md .planning/PROJECT.md` returns aligned wording evidence.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/secondary_surface_contract.test.mjs"</automated>
  </verify>
  <done>Canonical docs and active planning truth now describe one consistent CAM sample ownership boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconfirm planning integrity after docs-truth updates</name>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <files>.planning/PROJECT.md</files>
  <behavior>
    - Test 1: planning audit stays green after active truth updates.
    - Test 2: docs alignment remains additive and does not disturb release-command governance.
  </behavior>
  <action>Run planning audit after docs/planning edits and only adjust active planning wording if the audit surfaces consistency drift.</action>
  <acceptance_criteria>
    - `npm run test:planning:audit` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run test:planning:audit"</automated>
  </verify>
  <done>Phase 42 docs and active truth updates are planning-consistent and ready for conditional-governance locking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical docs -> downstream contract interpretation | If docs blur CAM naming into package/runtime API claims, downstream users will infer unsupported contract surface |
| active planning truth -> next-phase decisions | If current-state planning text drifts from shipped Phase 40-41 behavior, later planning can regress boundary decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | I | docs ownership wording | mitigate | Add red docs-contract assertions first, then align root/sdk/package wording to demo-owned CAM composition semantics |
| T-42-01-02 | I/T | active planning truth | mitigate | Update `.planning/PROJECT.md` current-state wording to match shipped Phase 40-41 outcomes and Phase 42 scope |
</threat_model>

<verification>
- Run `node --test test/secondary_surface_contract.test.mjs`.
- Run `npm run test:planning:audit`.
</verification>

<success_criteria>
Canonical docs and active planning truth now consistently describe the shipped CAM sample boundary: exact primitives remain reusable runtime/package contract, while CAM workflow naming remains demo-owned composition.
</success_criteria>

<output>
After completion, create `.planning/phases/42-docs-conditional-verification-cam-sample-lock/42-01-SUMMARY.md`
</output>
