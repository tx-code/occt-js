---
phase: 41-cam-scenario-ux-sample-first-presentation
plan: "02"
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - demo/src/components/DropZone.jsx
  - demo/src/components/Toolbar.jsx
  - demo/tests/app-home.spec.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - UX-05
must_haves:
  truths:
    - "Representative workpiece/tool CAM scenarios are presented as explicit sample flows, not as a new workspace or inspection product."
    - "The browser shell reinforces the import-workpiece / optional-tool / run-one-action story without adding history, tolerance, or reporting features."
    - "Representative browser scenarios cover the accepted CAM sample set while preserving current-result and invalidation behavior."
  artifacts:
    - path: demo/src/components/DropZone.jsx
      provides: import-first shell guidance
      contains: explicit sample framing for when and why a second actor is useful
    - path: demo/src/components/Toolbar.jsx
      provides: explicit optional-tool entry point
      contains: wording that keeps tool generation manual and sample-oriented
    - path: demo/tests/app-home.spec.mjs
      provides: home-shell proof for import-first CAM sample framing
      contains: assertions for retained workpiece-first and optional-tool guidance
    - path: demo/tests/demo.spec.mjs
      provides: maintained browser proof for representative CAM sample scenarios
      contains: at least one additional representative workpiece/tool CAM flow plus no-product-drift assertions
  key_links:
    - from: demo/src/components/DropZone.jsx
      to: .planning/ROADMAP.md
      via: shell copy should reinforce explicit workpiece/tool sample flows without inventing new workflow policy
      pattern: workpiece|tool|sample
    - from: demo/tests/demo.spec.mjs
      to: .planning/phases/41-cam-scenario-ux-sample-first-presentation/41-01-PLAN.md
      via: browser verification should extend representative scenarios over the wording/panel baseline already set by 41-01
      pattern: clearance|step depth|center-to-center|surface-to-center|current result
---

<objective>
Cover representative workpiece/tool CAM scenarios and shell framing without adding workflow-product complexity.

Purpose: satisfy the rest of `UX-05` by making the browser demo read like an explicit CAM integration sample from entry shell through current-result behavior, while preserving the reduced one-result boundary from `v1.12`.
Output: clearer workpiece/tool sample framing plus maintained browser coverage for representative CAM scenarios.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-cam-scenario-ux-sample-first-presentation/41-CONTEXT.md
@.planning/phases/41-cam-scenario-ux-sample-first-presentation/41-RESEARCH.md
@.planning/phases/41-cam-scenario-ux-sample-first-presentation/41-01-PLAN.md
@.planning/phases/40-demo-cam-workflow-composition/40-02-SUMMARY.md
@docs/plan/cam-measurement-matrix.md
@AGENTS.md
@demo/src/components/DropZone.jsx
@demo/src/components/Toolbar.jsx
@demo/tests/app-home.spec.mjs
@demo/tests/demo.spec.mjs

<interfaces>
From `demo/src/components/DropZone.jsx`:
```jsx
<div data-testid="drop-zone" />
<Button data-testid="open-generated-tool-panel-empty" />
```

From `demo/src/components/Toolbar.jsx`:
```jsx
<Button data-testid="open-generated-tool-panel-toolbar" />
```

From `demo/tests/demo.spec.mjs`:
```js
getMeasurementSnapshot(page)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing browser tests for representative CAM sample scenarios</name>
  <read_first>demo/tests/app-home.spec.mjs, demo/tests/demo.spec.mjs, demo/src/components/DropZone.jsx, demo/src/components/Toolbar.jsx</read_first>
  <files>demo/tests/app-home.spec.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: browser tests fail until the shell explicitly frames the import-workpiece / optional-tool sample flow.
    - Test 2: browser tests fail until at least one additional representative CAM scenario beyond current Phase 40 coverage is proven.
    - Test 3: browser tests still prove the sample is one-result and not a history/report/tolerance workflow.
  </behavior>
  <action>Write the smallest focused red browser assertions first. Extend the maintained shell and scenario coverage with one representative workpiece/tool CAM flow such as `step depth` or `center-to-center`, and keep the assertions centered on sample framing rather than on a broader product model.</action>
  <acceptance_criteria>
    - `rg -n "Import a workpiece first|Generate Tool|clearance|step depth|center-to-center|current result" demo/tests/app-home.spec.mjs demo/tests/demo.spec.mjs` returns matches.
    - the targeted browser verify for `app-home` plus `demo` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npx playwright test --config playwright.config.mjs demo/tests/app-home.spec.mjs demo/tests/demo.spec.mjs; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>The browser suite has red expectations for the representative CAM sample framing before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement minimal shell and scenario framing for the CAM sample</name>
  <read_first>demo/src/components/DropZone.jsx, demo/src/components/Toolbar.jsx, demo/tests/app-home.spec.mjs, demo/tests/demo.spec.mjs</read_first>
  <files>demo/src/components/DropZone.jsx, demo/src/components/Toolbar.jsx, demo/tests/app-home.spec.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the entry shell and toolbar make it obvious that the tool actor is optional and sample-oriented.
    - Test 2: representative workpiece/tool CAM flows are now explicitly covered in the browser without reopening panel-copy scope from 41-01.
    - Test 3: no history, reporting, tolerance, or session workflow is added.
  </behavior>
  <action>Implement the smallest truthful shell and scenario-copy changes needed to reinforce the CAM sample story from home screen through representative browser flows. Reuse the panel wording established by 41-01; keep this plan focused on shell framing and scenario proof rather than on more inspector copy churn.</action>
  <acceptance_criteria>
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build"</automated>
  </verify>
  <done>The browser shell and representative CAM scenarios now read like a focused integration sample without widening product scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the maintained browser lane for representative CAM sample flows</name>
  <read_first>demo/tests/app-home.spec.mjs, demo/tests/demo.spec.mjs, AGENTS.md</read_first>
  <files>demo/tests/app-home.spec.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the maintained browser lane proves representative shell and scenario wording.
    - Test 2: workpiece/tool CAM flows remain explicit, manual, and one-result.
    - Test 3: mobile and desktop lanes still tolerate the revised sample framing.
  </behavior>
  <action>Run the maintained browser lane and adjust only the assertions needed to lock representative CAM sample flows and shell copy. Keep the suite aligned with the current reduced-surface demo rather than a larger product ambition.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The maintained browser lane now locks the representative CAM sample flow without reviving removed product complexity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell/scenario framing -> product expectation | If the demo shell or scenario copy suggests a larger CAM product, downstream users will infer unsupported behavior |
| representative scenario tests -> reduced sample boundary | If Phase 41 browser tests start expecting reports/history/tolerance semantics, they will reintroduce removed complexity indirectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | I | shell and scenario framing | mitigate | Keep wording explicit, workpiece-first, and optional-tool without adding workflow-state or product claims |
| T-41-02-02 | T/R | browser coverage | mitigate | Prove representative CAM scenarios through one-result flows only and avoid history/report/tolerance assertions |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
</verification>

<success_criteria>
The browser demo reads like an explicit CAM integration sample from entry shell through representative workpiece/tool scenarios, while still exposing only one current result and no inspection-product workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/41-cam-scenario-ux-sample-first-presentation/41-02-SUMMARY.md`
</output>
