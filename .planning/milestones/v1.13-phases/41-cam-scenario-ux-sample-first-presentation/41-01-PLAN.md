---
phase: 41-cam-scenario-ux-sample-first-presentation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/src/lib/measurement-actions.js
  - demo/src/components/SelectionPanel.jsx
  - demo/tests/measurement-actions.test.mjs
  - demo/tests/demo.spec.mjs
autonomous: true
requirements:
  - UX-05
must_haves:
  truths:
    - "Accepted CAM workflows use more task-oriented demo wording where the semantics are already known, while ambiguous selections keep generic exact wording."
    - "The current-result card reads like a sample-first integration surface rather than a report, tolerance view, or persistent inspection workspace."
    - "All wording remains demo-owned and does not imply a wider root/package contract."
  artifacts:
    - path: demo/src/lib/measurement-actions.js
      provides: demo-owned action labels and summary wording
      contains: tighter CAM-facing labels, success/failure summaries, and unsupported guidance
    - path: demo/src/components/SelectionPanel.jsx
      provides: browser-visible sample framing
      contains: updated action-panel subtitle, empty state, section headings, and current-result wording
    - path: demo/tests/measurement-actions.test.mjs
      provides: unit coverage for label and summary behavior
      contains: expectations for accepted CAM wording without broadening the underlying action surface
    - path: demo/tests/demo.spec.mjs
      provides: maintained browser proof for revised sample-facing wording
      contains: focused assertions for panel copy and current-result presentation
  key_links:
    - from: demo/src/lib/measurement-actions.js
      to: docs/plan/cam-measurement-matrix.md
      via: CAM naming remains demo-owned and must not imply a new reusable runtime/package surface
      pattern: Clearance|Step Depth|Center to Center|Surface to Center
    - from: demo/src/components/SelectionPanel.jsx
      to: .planning/phases/40-demo-cam-workflow-composition/40-02-SUMMARY.md
      via: wording changes must preserve the one-current-result sample established in Phase 40
      pattern: current result|sample|supported exact
    - from: demo/tests/demo.spec.mjs
      to: demo/src/components/SelectionPanel.jsx
      via: browser verification should lock the accepted copy seams without reviving removed productivity controls
      pattern: measurement-panel|measurement-current-summary|measurement-current-result
---

<objective>
Tighten the accepted CAM workflow wording so the browser sample reads like a practical integration example instead of a generic exact inspector.

Purpose: satisfy the first half of `UX-05` by making labels, summaries, and current-result framing more CAM-oriented where the semantics are already known, while preserving the reduced `v1.12` sample-first boundary.
Output: narrower demo-owned wording for accepted CAM workflows plus browser-visible sample framing that stays out of inspection-product territory.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-cam-scenario-ux-sample-first-presentation/41-CONTEXT.md
@.planning/phases/41-cam-scenario-ux-sample-first-presentation/41-RESEARCH.md
@.planning/phases/40-demo-cam-workflow-composition/40-02-SUMMARY.md
@docs/plan/cam-measurement-matrix.md
@AGENTS.md
@demo/src/lib/measurement-actions.js
@demo/src/components/SelectionPanel.jsx
@demo/tests/measurement-actions.test.mjs
@demo/tests/demo.spec.mjs

<interfaces>
From `demo/src/lib/measurement-actions.js`:
```js
deriveDemoMeasurementActions({ items })
runDemoMeasurementAction({ core, actionId, items, availability })
```

From `demo/src/components/SelectionPanel.jsx`:
```jsx
<div data-testid="measurement-panel" />
<div data-testid="measurement-current-summary" />
<div data-testid="measurement-current-result" />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for revised CAM wording and sample framing</name>
  <read_first>demo/src/lib/measurement-actions.js, demo/src/components/SelectionPanel.jsx, demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs</read_first>
  <files>demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: unit tests fail until accepted CAM workflows expose the revised demo-owned labels or summaries.
    - Test 2: browser tests fail until the measurement panel and current-result copy reflect the tightened sample-first wording.
    - Test 3: generic exact actions remain present, proving the phase did not relabel the whole inspector as CAM-only.
  </behavior>
  <action>Write the smallest focused red tests around label, summary, and panel-copy seams. Keep expectations specific to the accepted CAM sample and do not introduce assertions that imply a report, tolerance, or history product.</action>
  <acceptance_criteria>
    - `rg -n "Clearance|Step Depth|Center to Center|Surface to Center|current result|sample action|supported exact" demo/tests/measurement-actions.test.mjs demo/tests/demo.spec.mjs` returns matches.
    - unit or targeted browser verification fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit 0 }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>The repo has red expectations locking the revised CAM-facing demo wording before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the CAM wording cleanup in the demo action and panel seams</name>
  <read_first>demo/src/lib/measurement-actions.js, demo/src/components/SelectionPanel.jsx, demo/tests/measurement-actions.test.mjs</read_first>
  <files>demo/src/lib/measurement-actions.js, demo/src/components/SelectionPanel.jsx, demo/tests/measurement-actions.test.mjs, demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: accepted CAM workflows read more like practical tasks where semantics are known.
    - Test 2: current-result and empty-state wording remain clearly sample-first.
    - Test 3: generic exact actions still read honestly and do not get forced into CAM language.
  </behavior>
  <action>Apply the smallest truthful copy changes in the action catalog and selection panel. Tighten labels, summaries, unsupported guidance, and current-result framing for the accepted CAM workflow ids while keeping the rest of the exact action surface generic and honest.</action>
  <acceptance_criteria>
    - `rg -n "Clearance|Step Depth|Center to Center|Surface to Center|current result|sample" demo/src/lib/measurement-actions.js demo/src/components/SelectionPanel.jsx` returns matches.
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build"</automated>
  </verify>
  <done>The browser sample now presents accepted CAM workflows with tighter demo-owned wording and preserved one-result semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock maintained browser copy for the reduced CAM sample</name>
  <read_first>demo/tests/demo.spec.mjs, AGENTS.md</read_first>
  <files>demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the maintained browser suite proves the revised sample wording without reviving removed productivity controls.
    - Test 2: workpiece/tool CAM runs still surface through one current-result card only.
    - Test 3: copy assertions remain narrow enough to survive normal UI churn.
  </behavior>
  <action>Run and adjust the maintained browser lane so it locks the accepted wording seams and sample framing, but keep the assertions centered on boundary-relevant phrases rather than broad snapshots.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The maintained browser lane now locks the revised CAM sample wording without broadening product scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo wording -> reusable contract perception | If the demo copy suggests a bigger kernel/package capability than actually ships, downstream integrators will infer the wrong boundary |
| current-result framing -> inspection-product behavior | If current-result wording starts to look like reporting or tolerance UX, the reduced sample boundary will drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | I | CAM wording | mitigate | Lock accepted CAM wording in unit/browser tests while preserving generic exact wording elsewhere |
| T-41-01-02 | T/R | sample framing | mitigate | Keep one-result panel wording sample-first and avoid adding report/tolerance/history affordances |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
</verification>

<success_criteria>
The browser demo uses tighter, task-oriented wording for the accepted CAM workflows while remaining clearly a reduced one-result integration sample rather than a larger inspection product.
</success_criteria>

<output>
After completion, create `.planning/phases/41-cam-scenario-ux-sample-first-presentation/41-01-SUMMARY.md`
</output>
