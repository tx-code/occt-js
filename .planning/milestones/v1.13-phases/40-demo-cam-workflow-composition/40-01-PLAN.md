---
phase: 40-demo-cam-workflow-composition
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/src/lib/measurement-actions.js
  - demo/src/hooks/useMeasurement.js
  - demo/tests/measurement-actions.test.mjs
  - demo/tests/measurement-overlay.test.mjs
autonomous: true
requirements:
  - DEMO-06
  - BOUND-02
must_haves:
  truths:
    - "CAM-flavored workflows are added as demo-owned compositions over shipped exact primitives rather than as new workflow-shaped root/package APIs."
    - "The demo action runner can execute multi-call compositions such as center-based workflows while preserving typed success, unsupported, and failure results."
    - "Synthetic placement remains honest: reuse existing placement kinds when possible, otherwise stay panel-only."
  artifacts:
    - path: demo/src/lib/measurement-actions.js
      provides: composition-capable demo action catalog and runner
      contains: explicit CAM action ids, composed exact calls, synthetic summaries, and conservative placement reuse
    - path: demo/src/hooks/useMeasurement.js
      provides: state-aligned measurement controller
      contains: hook logic compatible with composed runner outputs and unchanged one-current-result behavior
    - path: demo/tests/measurement-actions.test.mjs
      provides: unit coverage for composed CAM action routing
      contains: clearance/step-depth and center-based action derivation, result normalization, and typed unsupported handling
    - path: demo/tests/measurement-overlay.test.mjs
      provides: overlay contract coverage when composed workflows synthesize placement DTOs
      contains: proof that reused placement kinds still render honestly or stay panel-only
  key_links:
    - from: demo/src/lib/measurement-actions.js
      to: packages/occt-core/src/index.d.ts
      via: new CAM workflows must compose only over shipped exact primitives and helpers
      pattern: measureExactDistance|measureExactThickness|measureExactCenter|measureExactRadius|describeExactHole
    - from: demo/src/lib/measurement-actions.js
      to: demo/src/lib/measurement-overlay.js
      via: composed workflows should reuse existing placement kinds instead of inventing a new overlay grammar
      pattern: kind|anchors|frame|distance|thickness
    - from: demo/src/hooks/useMeasurement.js
      to: demo/src/store/viewerStore.js
      via: runner refactors must preserve the current one-result contract and invalidation boundaries
      pattern: currentMeasurement|setCurrentMeasurement|clearMeasurements
---

<objective>
Refactor the demo action catalog and runner so CAM-flavored workflows can be composed over the shipped exact surface.

Purpose: satisfy the first half of `DEMO-06` and `BOUND-02` by keeping new CAM workflow semantics inside `demo/` while proving they can be built from the existing exact kernel/package surface.
Output: a composition-capable demo action runner with narrow CAM action ids, truthful summaries, and conservative placement reuse.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/40-demo-cam-workflow-composition/40-CONTEXT.md
@.planning/phases/40-demo-cam-workflow-composition/40-RESEARCH.md
@docs/plan/cam-measurement-matrix.md
@AGENTS.md
@demo/src/lib/measurement-actions.js
@demo/src/hooks/useMeasurement.js
@demo/src/lib/measurement-overlay.js
@demo/tests/measurement-actions.test.mjs
@demo/tests/measurement-overlay.test.mjs
@packages/occt-core/src/index.d.ts
@packages/occt-core/src/occt-core.js

<interfaces>
From `demo/src/lib/measurement-actions.js`:
```js
deriveDemoMeasurementActions({ items })
runDemoMeasurementAction({ core, actionId, items, availability })
```

From `packages/occt-core/src/index.d.ts`:
```ts
measureExactDistance(refA, refB)
measureExactThickness(refA, refB)
measureExactCenter(ref)
measureExactRadius(ref)
describeExactHole(ref)
```

From `demo/src/lib/measurement-overlay.js`:
```js
buildMeasurementOverlayState(run)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing unit tests for CAM action derivation and composition</name>
  <read_first>demo/src/lib/measurement-actions.js, demo/tests/measurement-actions.test.mjs, .planning/phases/40-demo-cam-workflow-composition/40-CONTEXT.md</read_first>
  <files>demo/tests/measurement-actions.test.mjs, demo/tests/measurement-overlay.test.mjs</files>
  <behavior>
    - Test 1: unit tests fail until the action matrix exposes the accepted CAM sample set for the supported selections.
    - Test 2: unit tests fail until the runner can execute a center-based composition over shipped exact primitives without pretending it is a root/package API.
    - Test 3: unit tests fail until unsupported selections and unsupported center semantics still produce typed downstream results instead of silent UI disappearance.
  </behavior>
  <action>Write the smallest focused red tests around action derivation, composed center math, and synthetic placement reuse or panel-only fallback. Keep the tests grounded in demo-owned behavior and shipped exact calls only, and make `surface-to-center` explicit as the supported face-pair variant for this phase.</action>
  <acceptance_criteria>
    - `rg -n "clearance|step depth|center-to-center|surface-to-center|measureExactCenter" demo/tests/measurement-actions.test.mjs demo/tests/measurement-overlay.test.mjs` returns matches.
    - `npm --prefix demo test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>The repo has red tests locking the intended CAM composition behavior before runner changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement composition-capable CAM action handlers in the demo runner</name>
  <read_first>demo/src/lib/measurement-actions.js, demo/src/hooks/useMeasurement.js, packages/occt-core/src/index.d.ts, demo/tests/measurement-actions.test.mjs</read_first>
  <files>demo/src/lib/measurement-actions.js, demo/src/hooks/useMeasurement.js, demo/tests/measurement-actions.test.mjs, demo/tests/measurement-overlay.test.mjs</files>
  <behavior>
    - Test 1: the demo action catalog can derive narrow CAM workflows without leaking them into package/root exports.
    - Test 2: composed actions can orchestrate multiple exact calls and normalize them into the same current-result contract used by the existing demo.
    - Test 3: placement reuse remains honest; where no stable placement can be synthesized, the workflow explicitly stays panel-only.
  </behavior>
  <action>Refactor the demo action catalog away from the assumption that every action is one direct core call. Introduce explicit demo-local composition handlers for the accepted Phase 40 workflows, build truthful summaries and values from shipped exact primitives, keep `surface-to-center` scoped to its supported face-pair variant, and keep the hook contract stable for the rest of the demo.</action>
  <acceptance_criteria>
    - `rg -n "measureExactCenter|clearance|step depth|center-to-center|surface-to-center" demo/src/lib/measurement-actions.js demo/src/hooks/useMeasurement.js` returns matches.
    - `npm --prefix demo test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The demo runner now supports narrow CAM compositions over the shipped exact surface without widening the published contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconfirm overlay and one-result behavior for composed workflows</name>
  <read_first>demo/src/lib/measurement-overlay.js, demo/src/hooks/useMeasurement.js, demo/tests/measurement-overlay.test.mjs, demo/tests/viewer-store.test.mjs</read_first>
  <files>demo/tests/measurement-overlay.test.mjs, demo/tests/viewer-store.test.mjs</files>
  <behavior>
    - Test 1: composed workflows that reuse existing placement kinds still produce honest overlay results.
    - Test 2: panel-only behavior remains explicit for composed workflows that cannot produce stable placement.
    - Test 3: the current single-result store model remains unchanged after runner refactoring.
  </behavior>
  <action>Finish node-level stabilization for composed workflows. Add only the overlay/store assertions actually needed to prove that the current-result sample survives the new action-runner shape without history/session drift.</action>
  <acceptance_criteria>
    - `npm --prefix demo test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test"</automated>
  </verify>
  <done>The demo runner can expose Phase 40 CAM compositions while preserving the existing overlay and one-result measurement contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped exact primitives -> demo CAM workflows | If the demo composes over nonexistent or poorly matched exact primitives, it will imply a wider kernel/package surface than actually ships |
| composed action result -> overlay/current-result sample | If synthetic placement or summaries are dishonest, the browser sample will mislead downstream integrators about what exact semantics are available |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | I | demo CAM composition | mitigate | Write red tests that prove the workflows are composed from shipped exact calls before implementation |
| T-40-01-02 | T/R | placement reuse | mitigate | Reuse existing placement kinds where possible and assert panel-only fallback where stable placement cannot be justified |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
</verification>

<success_criteria>
The browser demo can derive and execute a narrow CAM-flavored action set over the existing exact surface, and those actions remain clearly demo-owned compositions instead of new published runtime/package APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/40-demo-cam-workflow-composition/40-01-SUMMARY.md`
</output>
