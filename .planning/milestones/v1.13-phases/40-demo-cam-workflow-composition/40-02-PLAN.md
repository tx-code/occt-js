---
phase: 40-demo-cam-workflow-composition
plan: "02"
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - demo/src/components/SelectionPanel.jsx
  - demo/src/hooks/useMeasurement.js
  - demo/tests/demo.spec.mjs
  - demo/tests/viewer-store.test.mjs
autonomous: true
requirements:
  - DEMO-06
must_haves:
  truths:
    - "The browser demo exposes the new CAM workflows through the same single current-result sample instead of reintroducing history or inspection-product behavior."
    - "Representative workpiece/tool scenarios prove the new workflows in browser-visible flows."
    - "Tool-pose and actor-replacement invalidation still clear the current measurement deterministically after CAM workflow execution."
  artifacts:
    - path: demo/src/components/SelectionPanel.jsx
      provides: browser-visible CAM action and current-result presentation
      contains: sample-first action exposure and truthful current-result wording for the accepted workflows
    - path: demo/src/hooks/useMeasurement.js
      provides: browser-facing execution bridge
      contains: hook behavior aligned with composed CAM results and unchanged current-measurement state
    - path: demo/tests/demo.spec.mjs
      provides: maintained browser regression coverage
      contains: at least one clearance/step-depth path and one center-based path in representative workpiece/tool scenarios
    - path: demo/tests/viewer-store.test.mjs
      provides: invalidation and state-shape coverage
      contains: one-result assertions that stay true after CAM workflow execution
  key_links:
    - from: demo/src/components/SelectionPanel.jsx
      to: demo/src/lib/measurement-actions.js
      via: browser-visible CAM wording must reflect the actual demo-owned action ids and summaries from the composed action catalog
      pattern: measurement-action-|summary|current result|supported exact actions
    - from: demo/tests/demo.spec.mjs
      to: demo/src/components/SelectionPanel.jsx
      via: browser verification should prove the new workflows without reviving measurement history or productivity controls
      pattern: measurement-current-result|measurement-current-summary|measurement-clear-current
    - from: demo/tests/viewer-store.test.mjs
      to: demo/src/store/viewerStore.js
      via: Phase 40 must preserve the one-result store and deterministic invalidation seams
      pattern: currentMeasurement|clearMeasurements|setCurrentMeasurement
---

<objective>
Prove the new CAM workflows in the browser sample through representative workpiece/tool scenarios and preserved current-result behavior.

Purpose: satisfy the rest of `DEMO-06` by showing that the composed workflows read like useful CAM-oriented demo integrations without turning the browser app into a larger inspection workspace.
Output: browser-visible CAM action/result flow, preserved one-result semantics, and maintained invalidation coverage for actor changes and tool pose.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/40-demo-cam-workflow-composition/40-CONTEXT.md
@.planning/phases/40-demo-cam-workflow-composition/40-RESEARCH.md
@.planning/phases/40-demo-cam-workflow-composition/40-01-PLAN.md
@docs/plan/cam-measurement-matrix.md
@AGENTS.md
@demo/src/components/SelectionPanel.jsx
@demo/src/hooks/useMeasurement.js
@demo/src/store/viewerStore.js
@demo/tests/demo.spec.mjs
@demo/tests/viewer-store.test.mjs

<interfaces>
From `demo/src/components/SelectionPanel.jsx`:
```jsx
<div data-testid="measurement-panel">
  <Button data-testid={`measurement-action-${action.id}`} />
  <div data-testid="measurement-current-result" />
</div>
```

From `demo/src/hooks/useMeasurement.js`:
```js
return {
  actions,
  unsupportedReason,
  runningActionId,
  activeMeasurement,
  activeMeasurementOverlay,
  runMeasurement,
  clearMeasurements,
};
```

From `demo/src/store/viewerStore.js`:
```js
currentMeasurement: null
setCurrentMeasurement(measurement)
clearMeasurements()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing browser tests for representative CAM workflows</name>
  <read_first>demo/tests/demo.spec.mjs, demo/src/components/SelectionPanel.jsx, .planning/phases/40-demo-cam-workflow-composition/40-CONTEXT.md</read_first>
  <files>demo/tests/demo.spec.mjs, demo/tests/viewer-store.test.mjs</files>
  <behavior>
    - Test 1: browser tests fail until the sample can run at least one clearance/step-depth workflow in a workpiece/tool scenario.
    - Test 2: browser tests fail until the sample can run at least one center-based workflow without implying a history/reporting product.
    - Test 3: store tests fail until current-measurement invalidation still behaves correctly after the new CAM workflow results are set.
  </behavior>
  <action>Write the smallest focused browser/store tests first. Reuse the current browser sample flow and assert only the one-result, clear, and invalidation behavior that the milestone actually cares about. Keep `surface-to-center` explicit as the supported face-pair variant so the browser expectations stay concrete.</action>
  <acceptance_criteria>
    - `rg -n "clearance|step depth|center-to-center|surface-to-center|currentMeasurement" demo/tests/demo.spec.mjs demo/tests/viewer-store.test.mjs` returns matches.
    - `npm --prefix demo test` or the targeted browser lane fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit 0 }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>The browser and store suites have red expectations locking the new CAM scenarios before UI wiring changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the composed CAM workflows into the browser sample</name>
  <read_first>demo/src/components/SelectionPanel.jsx, demo/src/hooks/useMeasurement.js, demo/tests/demo.spec.mjs</read_first>
  <files>demo/src/components/SelectionPanel.jsx, demo/src/hooks/useMeasurement.js, demo/tests/demo.spec.mjs, demo/tests/viewer-store.test.mjs</files>
  <behavior>
    - Test 1: the selection panel exposes the accepted CAM actions naturally through the current sample-first measurement panel.
    - Test 2: current-result wording is truthful and task-oriented for the new workflows without turning into report/tolerance UX.
    - Test 3: currentMeasurement invalidation remains deterministic after tool-pose and actor changes.
  </behavior>
  <action>Wire the new CAM actions into the existing panel and hook flow with the smallest truthful UI change. Keep the browser sample explicit and task-oriented, keep `surface-to-center` scoped to the supported face-pair variant, and do not add history, compare, or reporting chrome.</action>
  <acceptance_criteria>
    - `npm --prefix demo test` passes.
    - `npm --prefix demo run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix demo run build"</automated>
  </verify>
  <done>The browser sample exposes the new CAM workflows through the existing one-result integration surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the maintained browser lane for CAM sample scenarios</name>
  <read_first>demo/tests/demo.spec.mjs, AGENTS.md</read_first>
  <files>demo/tests/demo.spec.mjs</files>
  <behavior>
    - Test 1: the maintained browser suite proves the new CAM workflows without reviving removed productivity controls.
    - Test 2: mobile/desktop maintained layouts remain compatible with the updated action set.
    - Test 3: browser assertions still fit the single-result sample-first demo established in `v1.12`.
  </behavior>
  <action>Run the maintained browser lane and adjust only the assertions needed to lock the accepted CAM scenarios. Keep the suite centered on sample-first exact integration rather than on a broader inspection product.</action>
  <acceptance_criteria>
    - `npm --prefix demo run test:e2e` passes.
    - `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm --prefix demo run test:e2e; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs"</automated>
  </verify>
  <done>The maintained browser lane now locks the Phase 40 CAM workflows inside the existing one-result sample.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composed CAM runner -> browser sample | If the UI suggests broader inspection-product capability than the composed runner actually supports, the sample will mislead downstream integrators |
| currentMeasurement -> invalidation seams | If the new workflows bypass or weaken invalidation, stale tool/workpiece results could survive pose or actor changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | I | browser CAM sample | mitigate | Add browser tests for representative workflows with explicit current-result assertions instead of product-style history expectations |
| T-40-02-02 | T/R | measurement invalidation | mitigate | Keep store tests and browser invalidation assertions on tool-pose and actor replacement behavior after CAM workflow execution |
</threat_model>

<verification>
- Run `npm --prefix demo test`.
- Run `npm --prefix demo run build`.
- Run `npm --prefix demo run test:e2e`.
- Run `npx playwright test --config playwright.config.mjs demo/tests/demo.spec.mjs`.
</verification>

<success_criteria>
The browser demo proves a narrow CAM-oriented measurement sample for workpiece/tool scenarios while preserving the existing one-result, sample-first, and conditional-verification boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/40-demo-cam-workflow-composition/40-02-SUMMARY.md`
</output>
