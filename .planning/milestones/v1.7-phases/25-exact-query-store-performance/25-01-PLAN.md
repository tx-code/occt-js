---
phase: 25-exact-query-store-performance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/exact-model-store.hpp
  - src/exact-model-store.cpp
  - src/exact-query.cpp
  - test/exact_query_store_performance_contract.test.mjs
  - test/exact_model_lifecycle_contract.test.mjs
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "Retained exact-query hot paths no longer clone full `ExactModelEntry` payloads per operation."
    - "Lifecycle/query failure semantics remain deterministic and typed after store/query internals are optimized."
    - "Phase 25 introduces repeatable store/query regression evidence without widening viewer or helper product scope."
  artifacts:
    - path: src/exact-model-store.hpp
      provides: optimized retained-entry access contract
      contains: non-copy lookup surface replacing per-query copy-out usage
    - path: src/exact-model-store.cpp
      provides: retained-entry storage and lookup behavior
      contains: stable non-copy entry access with lifecycle semantics preserved
    - path: src/exact-query.cpp
      provides: exact-query retained lookup path
      contains: updated geometry lookup path using optimized store access
    - path: test/exact_query_store_performance_contract.test.mjs
      provides: repeatable query-lane regression evidence
      contains: deterministic retained-query loop coverage and typed-failure parity checks
  key_links:
    - from: src/exact-query.cpp
      to: src/exact-model-store.hpp
      via: non-copy retained entry lookup
      pattern: LookupGeometryShape|GetEntry
    - from: test/exact_query_store_performance_contract.test.mjs
      to: src/exact-query.cpp
      via: retained-query behavior and repeatability checks
      pattern: GetExactGeometryType|MeasureExactRadius|MeasureExactFaceArea
---

<objective>
Reduce retained-query/store overhead by removing avoidable per-call entry copying while preserving existing runtime semantics.

Purpose: satisfy the store/query half of `PERF-01` with internal optimization and repeatable regression evidence.
Output: optimized retained-entry lookup path, unchanged lifecycle/query contracts, and focused root test coverage for the hot lane.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-exact-query-store-performance/25-CONTEXT.md
@.planning/phases/25-exact-query-store-performance/25-PATTERNS.md
@AGENTS.md
@src/exact-model-store.hpp
@src/exact-model-store.cpp
@src/exact-query.cpp
@test/exact_model_lifecycle_contract.test.mjs

<interfaces>
From `src/exact-model-store.hpp` (current copy-out hotspot):
```cpp
OcctLifecycleResult GetEntry(int exactModelId, ExactModelEntry& entry);
```

From `src/exact-query.cpp` (current lookup usage):
```cpp
ExactModelEntry entry;
OcctLifecycleResult lifecycle = ExactModelStore::Instance().GetEntry(exactModelId, entry);
```

From `test/exact_model_lifecycle_contract.test.mjs` (lifecycle behavior to preserve):
```javascript
assert.deepEqual(module.RetainExactModel(result.exactModelId), {
  ok: false,
  code: "released-handle",
  message: "Exact model handle has already been released.",
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing retained-query regression tests for optimized store access path</name>
  <read_first>test/exact_model_lifecycle_contract.test.mjs, src/exact-query.cpp, src/exact-model-store.hpp, .planning/phases/25-exact-query-store-performance/25-CONTEXT.md</read_first>
  <files>test/exact_query_store_performance_contract.test.mjs, test/exact_model_lifecycle_contract.test.mjs</files>
  <behavior>
    - Test 1: fixed-iteration retained exact-query loops over one live model keep returning typed success payloads with stable shape semantics.
    - Test 2: releasing the retained model after loop coverage still yields deterministic released-handle failures for representative query calls.
    - Test 3: the new suite is deterministic and does not rely on fragile wall-clock thresholds.
  </behavior>
  <action>Create a focused root suite `test/exact_query_store_performance_contract.test.mjs` that exercises high-frequency retained query operations over real fixtures and verifies deterministic success/failure semantics. Keep this suite contract-oriented (behavioral invariants), not flaky micro-timing assertions.</action>
  <acceptance_criteria>
    - `rg -n "retained query loop|released-handle after loop|deterministic" test/exact_query_store_performance_contract.test.mjs` returns matches.
    - `node --test test/exact_query_store_performance_contract.test.mjs` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_query_store_performance_contract.test.mjs</automated>
  </verify>
  <done>A repeatable retained-query regression suite exists and guards behavioral parity through optimization changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement non-copy store/query lookup path and preserve lifecycle semantics</name>
  <read_first>src/exact-model-store.hpp, src/exact-model-store.cpp, src/exact-query.cpp, test/exact_query_store_performance_contract.test.mjs, test/exact_model_lifecycle_contract.test.mjs</read_first>
  <files>src/exact-model-store.hpp, src/exact-model-store.cpp, src/exact-query.cpp, test/exact_query_store_performance_contract.test.mjs, test/exact_model_lifecycle_contract.test.mjs</files>
  <behavior>
    - Test 1: exact queries no longer require copying full retained entries per call.
    - Test 2: release/retain and query failure codes remain unchanged.
    - Test 3: optimized query lane remains source-compatible for existing JS-facing APIs.
  </behavior>
  <action>Refactor store/query lookup plumbing to provide stable non-copy retained entry access in `ExactModelStore` and consume it in `src/exact-query.cpp`. Preserve existing lifecycle and query contracts while removing avoidable copy overhead from the hot path.</action>
  <acceptance_criteria>
    - `rg -n "GetEntry\\(|LookupGeometryShape" src/exact-model-store.hpp src/exact-model-store.cpp src/exact-query.cpp` reflects the new non-copy path.
    - `node --test test/exact_model_lifecycle_contract.test.mjs test/exact_query_store_performance_contract.test.mjs` passes.
    - `npm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/exact_model_lifecycle_contract.test.mjs test/exact_query_store_performance_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test"</automated>
  </verify>
  <done>Retained exact-query/store path is optimized with behavioral parity locked by root regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained store internals -> exact query surface | Incorrect optimization can break lifecycle failures or query correctness |
| optimization changes -> public runtime contract | Internal refactors must not alter JS-visible DTO semantics or failure codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | T/R | store/query lookup path | mitigate | Add deterministic root regression suite and lifecycle parity assertions |
| T-25-01-02 | D | retained query hot lane | mitigate | Remove per-call entry copies while preserving safe concurrent lifecycle behavior |
</threat_model>

<verification>
- Run `node --test test/exact_query_store_performance_contract.test.mjs`.
- Run `node --test test/exact_model_lifecycle_contract.test.mjs`.
- Run `npm test`.
</verification>

<success_criteria>
Large retained-query workflows avoid full-entry copy overhead while preserving deterministic lifecycle/query behavior and public API compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/25-exact-query-store-performance/25-01-SUMMARY.md`
</output>
