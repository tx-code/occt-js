---
phase: 26-import-staging-and-long-session-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/occt-core/README.md
  - docs/sdk/measurement.md
  - AGENTS.md
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "Root and package docs explicitly describe exact-model lifecycle ownership, disposal expectations, and diagnostics usage without overpromising GC cleanup."
    - "Package-first lifecycle guidance (`@tx-code/occt-core`) is the primary path, while root lifecycle APIs remain documented as lower-level references."
    - "Lifecycle/performance doc contract drift is enforced by governance assertions instead of manual review alone."
  artifacts:
    - path: README.md
      provides: root lifecycle/performance guidance and command routing
      contains: package-first lifecycle expectations, diagnostics guidance, and explicit release-boundary notes
    - path: packages/occt-core/README.md
      provides: adapter-focused lifecycle/performance usage docs
      contains: managed exact-model workflow (`openManagedExact*`, `dispose`, `getExactModelDiagnostics`) and best-effort finalizer warning
    - path: docs/sdk/measurement.md
      provides: focused package-first SDK guide updates
      contains: lifecycle/performance guidance aligned with managed wrappers and explicit disposal semantics
    - path: AGENTS.md
      provides: repository-level instruction contract updates
      contains: lifecycle/performance verification command routing and boundary constraints
    - path: test/release_governance_contract.test.mjs
      provides: docs/governance drift locking
      contains: assertions for lifecycle/performance docs and command routing consistency
  key_links:
    - from: test/release_governance_contract.test.mjs
      to: README.md
      via: root docs lifecycle/perf contract enforcement
      pattern: lifecycle|dispose|diagnostics|test:perf:exact
    - from: test/release_governance_contract.test.mjs
      to: packages/occt-core/README.md
      via: package-first lifecycle/perf guidance contract
      pattern: openManagedExactModel|getExactModelDiagnostics|FinalizationRegistry
---

<objective>
Publish package-first lifecycle/performance guidance and lock it via governance tests.

Purpose: satisfy the documentation half of `DOCS-04` by making lifecycle ownership, disposal expectations, diagnostics usage, and performance-sensitive guidance explicit across root/package docs and repository agent instructions.
Output: synchronized docs + governance assertions that fail on drift.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-import-staging-and-long-session-verification/26-CONTEXT.md
@.planning/phases/26-import-staging-and-long-session-verification/26-PATTERNS.md
@AGENTS.md
@README.md
@packages/occt-core/README.md
@docs/sdk/measurement.md
@test/release_governance_contract.test.mjs
@package.json
@packages/occt-core/src/occt-core.js
@packages/occt-core/src/index.d.ts
@dist/occt-js.d.ts

<interfaces>
From `packages/occt-core/src/index.d.ts` (lifecycle APIs that docs must reflect):
```ts
openManagedExactModel(...): Promise<OcctManagedExactModel>;
getExactModelDiagnostics(): Promise<OcctJSExactModelDiagnostics>;
```

From `packages/occt-core/src/occt-core.js` (cleanup semantics to explain):
```js
// explicit dispose authoritative; finalizer is best-effort fallback
this._managedExactFinalizer = typeof FinalizationRegistry === "function" ? ...
```

From `dist/occt-js.d.ts` (root lower-level reference APIs):
```ts
RetainExactModel(exactModelId: number): OcctJSLifecycleResult;
ReleaseExactModel(exactModelId: number): OcctJSLifecycleResult;
GetExactModelDiagnostics(): OcctJSExactModelDiagnostics;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing governance assertions for lifecycle/performance docs contract</name>
  <read_first>test/release_governance_contract.test.mjs, README.md, packages/occt-core/README.md, docs/sdk/measurement.md, AGENTS.md</read_first>
  <files>test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: governance fails when root/package docs do not mention package-first lifecycle management and diagnostics usage.
    - Test 2: governance fails when docs omit explicit "finalizer is best-effort; explicit dispose/release is authoritative" messaging.
    - Test 3: governance fails when lifecycle/performance verification routing drifts from documented command surface.
  </behavior>
  <action>Extend `test/release_governance_contract.test.mjs` with explicit regex assertions covering lifecycle ownership, managed disposal APIs, diagnostics APIs, and performance command routing references. Keep checks focused on shipped contracts, not speculative future behavior.</action>
  <acceptance_criteria>
    - `rg -n "openManagedExactModel|getExactModelDiagnostics|FinalizationRegistry|test:perf:exact|dispose" test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs` fails before doc updates and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>Governance tests now lock the lifecycle/performance docs contract and fail on drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish synchronized lifecycle/performance guidance across root/package docs and AGENTS</name>
  <read_first>README.md, packages/occt-core/README.md, docs/sdk/measurement.md, AGENTS.md, test/release_governance_contract.test.mjs, package.json</read_first>
  <files>README.md, packages/occt-core/README.md, docs/sdk/measurement.md, AGENTS.md, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: docs show package-first lifecycle workflow using managed wrappers and diagnostics access.
    - Test 2: docs state explicit dispose/release ownership and best-effort finalizer semantics without overpromising GC behavior.
    - Test 3: docs route maintainers to lifecycle/performance verification commands while preserving authoritative root gate boundaries.
  </behavior>
  <action>Update root/package/SDK/agent docs to add explicit lifecycle + performance guidance sections aligned with implemented APIs and test command routing. Keep text additive and consistent with existing release-boundary language. Re-run governance tests until all new assertions pass.</action>
  <acceptance_criteria>
    - `node --test test/release_governance_contract.test.mjs` passes.
    - `rg -n "openManagedExactModel|dispose\\(|getExactModelDiagnostics|best-effort|test:perf:exact" README.md packages/occt-core/README.md docs/sdk/measurement.md AGENTS.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/release_governance_contract.test.mjs"</automated>
  </verify>
  <done>Lifecycle/performance docs are package-first, explicit, and governance-locked against drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implemented lifecycle/perf behavior -> published docs | If docs drift, downstream apps can adopt unsafe disposal or performance assumptions |
| docs command routing -> maintainer release process | Incorrect routing can silently drop required verification for lifecycle/performance regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | T/R | lifecycle docs contract | mitigate | Add governance assertions that lock explicit disposal authority and diagnostics semantics |
| T-26-01-02 | R/I | verification command routing | mitigate | Keep root gate boundaries explicit and enforce doc command consistency via tests |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs`.
</verification>

<success_criteria>
Root/package docs now provide explicit package-first lifecycle/performance guidance, and governance tests fail whenever that contract drifts.
</success_criteria>

<output>
After completion, create `.planning/phases/26-import-staging-and-long-session-verification/26-01-SUMMARY.md`
</output>
