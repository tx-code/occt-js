---
phase: 26-import-staging-and-long-session-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - package.json
  - test/test_exact_lifecycle_soak.mjs
  - test/release_governance_contract.test.mjs
  - README.md
  - packages/occt-core/README.md
  - .planning/phases/26-import-staging-and-long-session-verification/26-SOAK-REPORT.md
autonomous: true
requirements:
  - GOV-05
  - DOCS-04
must_haves:
  truths:
    - "Lifecycle/performance governance enforces explicit command routing for optional soak verification while preserving the authoritative root release gate boundary."
    - "A repeatable long-session exact lifecycle/performance verification lane exists and produces evidence suitable for milestone closeout review."
    - "Secondary surfaces remain conditional; soak/perf lanes do not widen demo/Babylon/Tauri requirements in root release execution."
  artifacts:
    - path: package.json
      provides: command routing contract
      contains: explicit soak command (for example `test:soak:exact`) outside unconditional root gate
    - path: test/test_exact_lifecycle_soak.mjs
      provides: long-session verification lane
      contains: repeated open/query/dispose cycles and deterministic report output
    - path: test/release_governance_contract.test.mjs
      provides: governance drift lock
      contains: assertions for soak command/documentation routing and boundary preservation
    - path: .planning/phases/26-import-staging-and-long-session-verification/26-SOAK-REPORT.md
      provides: milestone-closeout evidence
      contains: command run metadata plus summarized soak results
  key_links:
    - from: package.json
      to: test/test_exact_lifecycle_soak.mjs
      via: explicit optional soak verification command
      pattern: test:soak:exact
    - from: test/release_governance_contract.test.mjs
      to: README.md
      via: governance lock for soak/perf routing and root boundary messaging
      pattern: test:soak:exact|test:release:root
---

<objective>
Extend governance and provide explicit long-session verification evidence for lifecycle/performance closeout.

Purpose: satisfy `GOV-05` and complete Phase 26 by adding optional soak verification routing, locking it through governance checks, and recording repeatable evidence for milestone closure.
Output: soak script + command + governance assertions + committed soak report artifact.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-import-staging-and-long-session-verification/26-CONTEXT.md
@.planning/phases/26-import-staging-and-long-session-verification/26-PATTERNS.md
@.planning/phases/26-import-staging-and-long-session-verification/26-01-PLAN.md
@README.md
@packages/occt-core/README.md
@test/release_governance_contract.test.mjs
@package.json
@test/exact_model_lifecycle_contract.test.mjs
@test/exact_query_store_performance_contract.test.mjs
@test/test_perf_exact_workflows.mjs

<interfaces>
From `package.json` (authoritative root gate boundary to preserve):
```json
"test:release:root": "... && npm --prefix packages/occt-core test && npm test"
```

From existing perf lane:
```json
"test:perf:exact": "node test/test_perf_exact_workflows.mjs"
```

From lifecycle contract tests:
```js
assert.deepEqual(module.ReleaseExactModel(result.exactModelId), { ok: true });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing governance/contract checks for soak command routing and boundary preservation</name>
  <read_first>package.json, test/release_governance_contract.test.mjs, README.md, packages/occt-core/README.md</read_first>
  <files>test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: governance fails if optional soak command is missing or undocumented once Phase 26 is complete.
    - Test 2: governance fails if soak command becomes an unconditional part of `test:release:root`.
    - Test 3: governance continues to enforce that demo/Babylon/Tauri remain conditional secondary surfaces.
  </behavior>
  <action>Extend governance assertions to include soak command routing and root-boundary invariants. Keep tests strict about optional-vs-authoritative command separation.</action>
  <acceptance_criteria>
    - `rg -n "test:soak:exact|test:release:root" test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs` fails before soak routing/docs land and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>Governance now locks soak command routing and root-boundary separation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement long-session soak lane, publish routing docs, and record report evidence</name>
  <read_first>test/exact_model_lifecycle_contract.test.mjs, test/exact_query_store_performance_contract.test.mjs, test/test_perf_exact_workflows.mjs, package.json, README.md, packages/occt-core/README.md</read_first>
  <files>package.json, test/test_exact_lifecycle_soak.mjs, README.md, packages/occt-core/README.md, test/release_governance_contract.test.mjs, .planning/phases/26-import-staging-and-long-session-verification/26-SOAK-REPORT.md</files>
  <behavior>
    - Test 1: soak script runs repeatable multi-iteration lifecycle/query/dispose loops and emits structured report output.
    - Test 2: command routing exposes one explicit optional soak command and keeps it outside `test:release:root`.
    - Test 3: docs explain when to run perf lane vs soak lane and preserve root-vs-secondary verification boundaries.
  </behavior>
  <action>Create `test/test_exact_lifecycle_soak.mjs` as a deterministic long-session lane, add command routing in `package.json`, update root/package docs for the new lane, and run the soak command to generate and commit `26-SOAK-REPORT.md` evidence. Keep the soak lane optional and maintainers-only, not part of unconditional release gating.</action>
  <acceptance_criteria>
    - `npm run test:soak:exact` passes.
    - `node --test test/release_governance_contract.test.mjs` passes.
    - `npm run test:release:root` remains free of direct soak command invocation.
    - `.planning/phases/26-import-staging-and-long-session-verification/26-SOAK-REPORT.md` exists with command metadata and summarized results.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run test:soak:exact; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/release_governance_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test"</automated>
  </verify>
  <done>Phase 26 closes with governance-locked soak routing and committed long-session evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional soak lane -> authoritative release gate | Incorrect routing could unintentionally make heavy soak checks mandatory and destabilize release cadence |
| soak report evidence -> milestone closeout decisions | Missing or inconsistent report output can produce false confidence about lifecycle/performance readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | T/R | command routing boundary | mitigate | Add governance assertions that soak stays optional and release gate remains runtime-first |
| T-26-02-02 | R/I | long-session verification evidence | mitigate | Emit structured soak output and commit a phase-scoped report artifact |
</threat_model>

<verification>
- Run `npm run test:soak:exact`.
- Run `node --test test/release_governance_contract.test.mjs`.
- Run `npm test`.
</verification>

<success_criteria>
Lifecycle/performance closeout includes governance-locked optional soak routing and committed long-session evidence without widening unconditional root release gates.
</success_criteria>

<output>
After completion, create `.planning/phases/26-import-staging-and-long-session-verification/26-02-SUMMARY.md`
</output>
