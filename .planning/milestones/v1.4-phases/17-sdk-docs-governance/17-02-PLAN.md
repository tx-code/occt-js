---
phase: 17-sdk-docs-governance
plan: "02"
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - package.json
  - test/package_tarball_contract.test.mjs
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - GOV-01
must_haves:
  truths:
    - "Packaged README/type assertions include the finalized placement/relation SDK surface."
    - "The authoritative root test and release commands include placement and relation contract coverage."
    - "Root release verification remains runtime-first and independent of secondary surfaces."
  artifacts:
    - "package.json"
    - "test/package_tarball_contract.test.mjs"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "packaged docs/types -> downstream placement/relation contract"
    - "root scripts -> authoritative release gate coverage"
    - "runtime-first release boundary -> no demo/Babylon/Tauri creep"
---

<objective>
Extend packaged-contract and release-gate coverage for the finalized placement/relation SDK surface.

Purpose: satisfy GOV-01 by ensuring tarball checks, public typings, and the authoritative root release gate all lock the same placement/relation contract that shipped in Phases 15 and 16.
Output: updated package scripts plus package/governance assertions and a final green `npm run test:release:root`.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-sdk-docs-governance/17-RESEARCH.md
@.planning/phases/17-sdk-docs-governance/17-VALIDATION.md
@package.json
@README.md
@packages/occt-core/README.md
@docs/sdk/measurement.md
@dist/occt-js.d.ts
@test/package_tarball_contract.test.mjs
@test/release_governance_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing packaged-contract checks for placement and relation SDK guidance</name>
  <read_first>package.json, README.md, packages/occt-core/README.md, docs/sdk/measurement.md, dist/occt-js.d.ts, test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs, .planning/phases/17-sdk-docs-governance/17-RESEARCH.md</read_first>
  <files>test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: tarball/package checks fail until packaged docs mention placement helpers and relation classification.
    - Test 2: package checks fail until packaged typings assertions cover the finalized placement/relation DTO surface.
    - Test 3: the authoritative root script surface fails governance until placement and relation contract tests are part of `npm test` and `npm run test:release:root`.
  </behavior>
  <action>Extend `test/package_tarball_contract.test.mjs` and any remaining governance assertions so packaged docs and typings prove the placement/relation SDK contract. Update the assertions for `package.json` scripts so placement and relation contract tests are required on the authoritative root gate.</action>
  <acceptance_criteria>
    - `rg -n "placement|relation|exact_placement_contract|exact_relation_contract" test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The package-contract tests now prove the packaged placement/relation SDK surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-run the authoritative root release gate against the finalized SDK docs surface</name>
  <read_first>package.json, test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs, .planning/phases/17-sdk-docs-governance/17-VALIDATION.md</read_first>
  <files>package.json, test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: package and governance checks pass with placement/relation docs and typings locked in.
    - Test 2: `npm run test:release:root` stays green after the script and contract updates.
  </behavior>
  <action>Update `package.json` so `npm test` and `npm run test:release:root` include the placement and relation contract files, keep any final package/governance assertions aligned with the authoritative release boundary, and rerun the canonical root release gate. Do not add unconditional demo, Babylon, or Tauri requirements.</action>
  <acceptance_criteria>
    - `node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs` passes.
    - `npm run test:release:root` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:release:root</automated>
  </verify>
  <done>The authoritative root release gate now enforces the final placement/relation SDK package surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged docs/types -> downstream consumption expectations | Drift here can break vendored or package-first consumers without runtime code changes. |
| governance assertions -> release gate | Weak checks would let the finalized SDK contract regress silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | T/R | packaged placement/relation contract | mitigate | Add tarball/governance assertions for packaged docs and typings before final release verification. |
| T-17-02-02 | T/I | release gate drift | mitigate | Re-run `npm run test:release:root` after script and governance updates while keeping the command runtime-first. |
</threat_model>

<verification>
- Run `node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs`.
- Run `npm run test:release:root`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The finalized placement/relation SDK surface is documented, packaged, and enforced by the root release gate
</success_criteria>

<output>
After completion, create `.planning/phases/17-sdk-docs-governance/17-02-SUMMARY.md`
</output>
