---
phase: 17-sdk-docs-governance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/occt-core/README.md
  - docs/sdk/measurement.md
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Package-first docs describe placement and relation workflows through `@tx-code/occt-core`."
    - "Root README stays a lower-level runtime reference and preserves the root release boundary."
    - "Governance tests fail if package-first docs or Phase 17 readiness drift from the active milestone state."
  artifacts:
    - "README.md"
    - "packages/occt-core/README.md"
    - "docs/sdk/measurement.md"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "package-first SDK walkthrough -> `@tx-code/occt-core` exact refs and wrappers"
    - "root README -> lower-level Wasm reference and release boundary"
    - "governance tests -> docs/state drift prevention"
---

<objective>
Finalize package-first SDK docs for exact placement and relation workflows.

Purpose: satisfy DOCS-01 by making `@tx-code/occt-core` the primary documented path for downstream JS consumers while keeping root Wasm reference and release-boundary guidance explicit.
Output: updated root/package docs, a dedicated measurement SDK guide, and governance assertions for Phase 17 execution readiness.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-sdk-docs-governance/17-RESEARCH.md
@.planning/phases/17-sdk-docs-governance/17-VALIDATION.md
@README.md
@packages/occt-core/README.md
@test/release_governance_contract.test.mjs
@dist/occt-js.d.ts
@packages/occt-core/src/occt-core.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing governance tests for placement/relation SDK docs and Phase 17 readiness</name>
  <read_first>README.md, packages/occt-core/README.md, test/release_governance_contract.test.mjs, .planning/STATE.md, .planning/phases/17-sdk-docs-governance/17-RESEARCH.md</read_first>
  <files>test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: governance fails until root/package docs and the dedicated SDK doc mention placement helpers and relation classification.
    - Test 2: governance fails until docs state that overlay rendering, label layout, and measurement UX remain downstream concerns.
    - Test 3: governance fails until the active planning state reflects Phase 17 planned and ready for execution.
  </behavior>
  <action>Extend `test/release_governance_contract.test.mjs` so it asserts package-first placement/relation doc coverage, an explicit downstream-boundary note, and Phase 17 planning readiness. Keep the checks anchored to `README.md`, `packages/occt-core/README.md`, `docs/sdk/measurement.md`, and `.planning/STATE.md`.</action>
  <acceptance_criteria>
    - `rg -n "placement|relation|downstream|Phase 17" test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs` fails before doc updates and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The governance suite proves the final package-first SDK docs before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update root/package docs and add a dedicated measurement SDK guide</name>
  <read_first>README.md, packages/occt-core/README.md, dist/occt-js.d.ts, packages/occt-core/src/occt-core.js, test/release_governance_contract.test.mjs, .planning/phases/17-sdk-docs-governance/17-RESEARCH.md</read_first>
  <files>README.md, packages/occt-core/README.md, docs/sdk/measurement.md, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: package-first docs walk through exact refs, placement helpers, and relation classification via `@tx-code/occt-core`.
    - Test 2: root README documents lower-level Wasm entrypoints without displacing the package-first path.
    - Test 3: docs keep overlay rendering, viewer policy, and app interaction flows explicitly downstream.
  </behavior>
  <action>Update the root README, `packages/occt-core/README.md`, and add `docs/sdk/measurement.md` so they document `suggestExact*Placement(...)`, `classifyExactRelation(refA, refB)`, and the root Wasm equivalents package-first. Keep the guidance additive to existing measurement docs and do not imply viewer-owned behavior is part of the package surface. Update governance assertions alongside the docs.</action>
  <acceptance_criteria>
    - `rg -n "SuggestExact|suggestExact|ClassifyExactRelation|classifyExactRelation|downstream" README.md packages/occt-core/README.md docs/sdk/measurement.md` returns matches.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The shipped docs now describe the placement/relation SDK package-first and keep runtime boundaries explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implemented placement/relation contract -> public docs | Drift here confuses downstream package consumers even when runtime behavior stays correct. |
| milestone state -> governance assertions | If state/tests drift, GSD can route to the wrong next step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | T/R | SDK docs boundary | mitigate | Add failing governance tests for package-first placement/relation docs before editing docs. |
| T-17-01-02 | T/I | planning-state drift | mitigate | Update and test Phase 17 readiness assertions together with docs. |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Package-first placement/relation docs exist and governance catches drift
</success_criteria>

<output>
After completion, create `.planning/phases/17-sdk-docs-governance/17-01-SUMMARY.md`
</output>
