---
phase: 16-exact-relation-classifier-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-query.hpp
  - src/exact-query.cpp
  - src/js-interface.cpp
  - test/exact_relation_contract.test.mjs
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
must_haves:
  truths:
    - "Root Wasm exposes additive exact relation classification beside the shipped measurement and placement APIs."
    - "Valid analytic pairs can return `kind: \"none\"`, while invalid or unsupported inputs still use explicit pairwise failure semantics."
    - "Relation success returns compact supporting geometry suitable for downstream presentation without introducing viewer policy."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/importer.hpp"
    - "src/exact-query.hpp"
    - "src/exact-query.cpp"
    - "src/js-interface.cpp"
    - "test/exact_relation_contract.test.mjs"
  key_links:
    - "existing analytic family detection -> relation classifier semantics"
    - "Phase 15 placement frame/anchor DTOs -> relation supporting geometry"
    - "root relation contract tests -> stable success, none, and failure behavior"
---

<objective>
Add root exact relation classifier semantics and supporting geometry DTOs.

Purpose: satisfy REL-01, REL-02, and REL-03 by exposing an additive root classifier for `parallel`, `perpendicular`, `concentric`, `tangent`, and `none` without changing the shipped exact measurement or placement APIs.
Output: root relation DTO structs and typings, a `ClassifyExactRelation(...)` binding, and root contract tests for supported relations, `none`, and explicit failures.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-exact-relation-classifier-contract/16-CONTEXT.md
@.planning/phases/16-exact-relation-classifier-contract/16-RESEARCH.md
@.planning/phases/16-exact-relation-classifier-contract/16-VALIDATION.md
@AGENTS.md
@src/importer.hpp
@src/exact-query.hpp
@src/exact-query.cpp
@src/js-interface.cpp
@dist/occt-js.d.ts
@test/exact_pairwise_measurement_contract.test.mjs
@test/exact_placement_contract.test.mjs
@test/simple_part.step
@test/bearing.igs
@test/as1_pe_203.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root contract tests for exact relation classification</name>
  <read_first>dist/occt-js.d.ts, src/exact-query.cpp, src/js-interface.cpp, test/exact_pairwise_measurement_contract.test.mjs, test/exact_placement_contract.test.mjs, .planning/phases/16-exact-relation-classifier-contract/16-RESEARCH.md</read_first>
  <files>test/exact_relation_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact relation classifier returns parallel and perpendicular with supporting geometry for analytic pairs` fails until supported line-line or plane-plane pairs return `ok === true`, `kind === "parallel"` or `kind === "perpendicular"`, and stable supporting geometry such as directions, anchors, or frame data.
    - Test 2: a subtest named `exact relation classifier returns concentric and tangent for supported analytic rotational pairs` fails until supported rotational or tangency pairs return `ok === true`, `kind === "concentric"` or `kind === "tangent"`, and relation support geometry such as center, axis, tangent point, anchors, or frame data.
    - Test 3: a subtest named `exact relation classifier returns none for valid analytic pairs with no supported relation` fails until a valid analytic pair returns `ok === true` and `kind === "none"` instead of an error.
    - Test 4: a subtest named `exact relation classifier failures stay explicit for invalid ids and unsupported geometry` fails until invalid ids or unsupported family combinations return explicit `{ ok: false, code: ... }` payloads.
  </behavior>
  <action>Create `test/exact_relation_contract.test.mjs` as a Node built-in test file that loads the built root carrier through `test/load_occt_factory.mjs`, opens existing fixtures such as `test/simple_part.step` and one rotational fixture (`test/bearing.igs` or `test/as1_pe_203.brep`), resolves retained exact refs from `exactGeometryBindings`, and adds the exact subtest names from `<behavior>`. Call the root binding named exactly `ClassifyExactRelation` using the same pairwise exact-ref identity plus two 16-value transforms. Assert supported relation results include `kind` and meaningful support geometry. Assert valid non-related analytic pairs return `kind: \"none\"`. Assert invalid or unsupported cases preserve explicit failure codes.</action>
  <acceptance_criteria>
    - `rg -n "exact relation classifier returns parallel and perpendicular with supporting geometry for analytic pairs|exact relation classifier returns concentric and tangent for supported analytic rotational pairs|exact relation classifier returns none for valid analytic pairs with no supported relation|exact relation classifier failures stay explicit for invalid ids and unsupported geometry" test/exact_relation_contract.test.mjs` returns matches.
    - `rg -n "ClassifyExactRelation|parallel|perpendicular|concentric|tangent|none" test/exact_relation_contract.test.mjs` returns matches.
    - `node --test test/exact_relation_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_relation_contract.test.mjs</automated>
  </verify>
  <done>The root contract test locks relation success, none, and explicit failure semantics before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive exact relation DTOs and root classifier bindings</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, test/exact_relation_contract.test.mjs, .planning/phases/16-exact-relation-classifier-contract/16-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/exact-query.hpp, src/exact-query.cpp, src/js-interface.cpp, test/exact_relation_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports `ClassifyExactRelation` with the same flattened same-model pairwise signature as the current pairwise exact APIs.
    - Test 2: relation success returns additive relation metadata such as `kind` plus stable supporting geometry only where meaningful.
    - Test 3: supported relation classification at minimum covers analytic line-line and plane-plane pairs for `parallel` / `perpendicular`, and analytic rotational or tangency pairs for `concentric` / `tangent` where the retained exact geometry can classify them without mesh heuristics.
    - Test 4: valid analytic non-relations return `kind: "none"`, while invalid or unsupported cases return explicit typed failures instead of ambiguous empty payloads.
  </behavior>
  <action>Add relation DTO structs to `src/importer.hpp` and corresponding TypeScript declarations to `dist/occt-js.d.ts`. In `src/exact-query.hpp` and `src/exact-query.cpp`, add a root helper named exactly `ClassifyExactRelation` that keeps the existing pairwise signature shape `exactModelId, exactShapeHandleA, kindA, elementIdA, exactShapeHandleB, kindB, elementIdB, transformA, transformB`. Reuse the existing retained-exact lookup, analytic family detection, pairwise helpers, and Phase 15 frame/anchor support to classify `parallel`, `perpendicular`, `concentric`, `tangent`, or `none`. In `src/js-interface.cpp`, add DTO serialization and Embind exposure for the new relation result. Preserve the existing `MeasureExact*` and `SuggestExact*Placement` APIs unchanged.</action>
  <acceptance_criteria>
    - `rg -n "ClassifyExactRelation|OcctExactRelation|parallel|perpendicular|concentric|tangent|none" src/importer.hpp src/exact-query.hpp src/exact-query.cpp src/js-interface.cpp dist/occt-js.d.ts` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_relation_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_relation_contract.test.mjs"</automated>
  </verify>
  <done>The root carrier now exposes additive exact relation classification with stable success, none, and failure semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained exact analytic pairs -> relation classification result | Incorrect analytic reasoning would return valid-looking relation kinds to downstream overlays. |
| valid analytic non-relations -> `none` success contract | If `none` is confused with unsupported geometry, downstream code cannot distinguish absent relation from missing kernel support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | T/R | root relation classification semantics | mitigate | Lock supported relation kinds and support-geometry shape in a root contract test before implementation. |
| T-16-01-02 | T/R | `none` vs failure behavior | mitigate | Require explicit `kind: "none"` for valid analytic non-relations and explicit typed failures for invalid or unsupported cases. |
</threat_model>

<verification>
- Run `node --test test/exact_relation_contract.test.mjs`.
- Run `npm run build:wasm:win`.
</verification>

<success_criteria>
Downstream callers can classify supported exact relations through an additive root API that returns `parallel`, `perpendicular`, `concentric`, `tangent`, or `none` with stable supporting geometry and explicit failures.
</success_criteria>

<output>
After completion, create `.planning/phases/16-exact-relation-classifier-contract/16-01-SUMMARY.md`
</output>
