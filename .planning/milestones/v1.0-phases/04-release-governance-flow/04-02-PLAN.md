---
phase: 04-release-governance-flow
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - README.md
  - packages/occt-core/README.md
  - AGENTS.md
  - .codex/skills/releasing-occt-js/SKILL.md
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - CONS-03
  - DIST-02
must_haves:
  truths:
    - "Public docs and the repo release skill point to `npm run test:release:root` as the authoritative root release gate."
    - "Babylon, demo, and Tauri verification stays conditional on touched secondary surfaces rather than becoming a blanket root release prerequisite."
    - "The root Wasm carrier and `occt-core` remain the documented primary product surfaces."
  artifacts:
    - "README.md"
    - "packages/occt-core/README.md"
    - "AGENTS.md"
    - ".codex/skills/releasing-occt-js/SKILL.md"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "README.md -> npm run test:release:root"
    - "AGENTS.md -> root tests first / secondary-surface checks conditional"
    - ".codex/skills/releasing-occt-js/SKILL.md -> release workflow and selective secondary-surface verification"
---

<objective>
Reconcile runtime-first docs and the repository release skill with the authoritative root release command surface.

Purpose: Satisfy CONS-03 and the public-doc portion of DIST-02 by removing unconditional secondary-surface release gates and making the root Wasm carrier the clear authority in docs and release workflow guidance.
Output: Governance test coverage for doc/skill wording plus aligned updates to README, `occt-core` README, AGENTS, and the release skill.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-governance-flow/04-RESEARCH.md
@.planning/phases/03-downstream-consumption-contract/03-03-SUMMARY.md
@README.md
@packages/occt-core/README.md
@AGENTS.md
@.codex/skills/releasing-occt-js/SKILL.md
@package.json
@test/release_governance_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align runtime-first docs and the release skill with the authoritative root release gate</name>
  <read_first>test/release_governance_contract.test.mjs, README.md, packages/occt-core/README.md, AGENTS.md, .codex/skills/releasing-occt-js/SKILL.md, package.json, .planning/phases/04-release-governance-flow/04-RESEARCH.md, .planning/phases/04-release-governance-flow/04-VALIDATION.md</read_first>
  <files>README.md, packages/occt-core/README.md, AGENTS.md, .codex/skills/releasing-occt-js/SKILL.md, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: A subtest named `release docs keep the root Wasm carrier authoritative` requires `README.md`, `packages/occt-core/README.md`, and `AGENTS.md` to mention `npm run test:release:root`, keep the root Wasm carrier authoritative, and describe demo/Babylon/Tauri checks as conditional secondary-surface verification.
    - Test 2: A subtest named `release skill stays a thin AGENTS shim and keeps secondary surfaces conditional` requires `.codex/skills/releasing-occt-js/SKILL.md` to point repo-wide policy back to `AGENTS.md`, use `npm run test:release:root` as the only unconditional root release verification command, and keep demo/browser/Tauri checks in a separate conditional section keyed to touched files.
    - Test 3: The release-skill subtest fails if the skill still requires unconditional `cd demo; npm run build`, `npx playwright test`, mandatory `demo/src/hooks/useOcct.js` scope, or blanket Babylon/Tauri verification for root-only releases.
  </behavior>
  <action>Update `test/release_governance_contract.test.mjs`, `README.md`, `packages/occt-core/README.md`, `AGENTS.md`, and `.codex/skills/releasing-occt-js/SKILL.md` together so the task ends green on its own. Extend the governance test with the exact subtest names from `<behavior>`. In `AGENTS.md`, keep repo policy authoritative: add `npm run test:release:root` as the root release gate, preserve `npm run test:wasm:preflight` and `npm test` as day-to-day entrypoints, and state that demo/viewer/Tauri checks stay conditional on touched secondary surfaces. In the README files, add concise runtime-first release-boundary language without over-expanding them. Rewrite `.codex/skills/releasing-occt-js/SKILL.md` as a thin shim that explicitly points back to `AGENTS.md` for repo-wide policy, then keeps only release-specific mechanics local: version bumps, unconditional `npm run test:release:root`, conditional demo/Babylon/Tauri verification keyed to touched files, ephemeral npm token handling, publish confirmation, and push guidance.</action>
  <acceptance_criteria>
    - `rg -n "npm run test:release:root" README.md packages/occt-core/README.md AGENTS.md .codex/skills/releasing-occt-js/SKILL.md` returns matches in all four files.
    - `rg -n "AGENTS.md|conditional|secondary surface|secondary-surface" .codex/skills/releasing-occt-js/SKILL.md AGENTS.md README.md packages/occt-core/README.md` returns matches showing the skill points to `AGENTS.md` and keeps secondary-surface checks conditional.
    - `node --test test/release_governance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>Runtime-first docs and the release skill converge on the same authoritative root release gate, and the skill stays a thin `AGENTS.md` pointer plus release-specific mechanics only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> release docs and skill | Humans rely on docs and skill wording to decide what is required before publishing. |
| release skill -> release execution | Incorrect mandatory steps here can block or misdirect root-only releases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | D | `.codex/skills/releasing-occt-js/SKILL.md` | mitigate | Require `npm run test:release:root` as the only unconditional root release gate and forbid unconditional demo/Babylon/Tauri checks in the governance test. |
| T-04-02-02 | S/R | `README.md`, `AGENTS.md`, `packages/occt-core/README.md` | mitigate | Lock the root-carrier-first wording and conditional secondary-surface guidance in `test/release_governance_contract.test.mjs`. |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs`.
- Run `rg -n "npm run test:release:root" README.md packages/occt-core/README.md AGENTS.md .codex/skills/releasing-occt-js/SKILL.md`.
</verification>

<success_criteria>
The repo’s public docs and release skill all point at the same canonical root release command and no longer make demo, Babylon, or Tauri checks unconditional for root-only releases.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-governance-flow/04-02-SUMMARY.md`
</output>
