---
phase: 02-root-runtime-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_multi_format_exports.mjs
  - src/js-interface.cpp
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "Downstream JS can import STEP, IGES, and BREP through both direct entrypoints and `ReadFile(...)` and receive the same successful top-level payload contract."
    - "Successful root import results always expose `success`, `sourceFormat`, `rootNodes`, `geometries`, `materials`, `warnings`, and `stats`, plus `sourceUnit` and `unitScaleToMeters` whenever the importer provides them."
    - "Direct and generic entrypoints preserve the same structural counts and fixture-specific signals such as names, colors, and topology for the same CAD bytes."
  artifacts:
    - "test/test_multi_format_exports.mjs"
    - "src/js-interface.cpp"
  key_links:
    - "src/js-interface.cpp:ReadStepFile/ReadIgesFile/ReadBrepFile/ReadFile -> ReadByFormat -> BuildResult"
    - "test/test_multi_format_exports.mjs -> test/load_occt_factory.mjs -> dist/occt-js.js"
---

<objective>
Tighten the canonical root-import payload contract across direct and generic Wasm entrypoints.

Purpose: Make CORE-02 explicit by proving that STEP, IGES, and BREP all return the same successful payload shape through both the format-specific APIs and the generic dispatcher.
Output: Stronger multi-format contract coverage plus the minimum `src/js-interface.cpp` changes needed to keep the canonical payload stable.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-root-runtime-contract/02-RESEARCH.md
@AGENTS.md
@package.json
@src/js-interface.cpp
@src/importer.hpp
@test/load_occt_factory.mjs
@test/test_multi_format_exports.mjs

<interfaces>
From `src/js-interface.cpp`:
```cpp
val ReadStepFile(const val& content, const val& jsParams);
val ReadIgesFile(const val& content, const val& jsParams);
val ReadBrepFile(const val& content, const val& jsParams);
val ReadFile(const std::string& format, const val& content, const val& jsParams);
val BuildResult(const OcctSceneData& scene, const std::string& sourceFormat);
```

From `src/importer.hpp`:
```cpp
struct OcctSceneData {
    bool success = false;
    std::string error;
    std::vector<OcctNodeData> nodes;
    std::vector<OcctMeshData> meshes;
    std::vector<int> rootNodeIndices;
    std::string sourceUnit;
    double unitScaleToMeters = 0.0;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock direct-vs-generic canonical payload parity for STEP, IGES, and BREP</name>
  <files>test/test_multi_format_exports.mjs</files>
  <read_first>
    .planning/phases/02-root-runtime-contract/02-RESEARCH.md
    src/js-interface.cpp
    src/importer.hpp
    test/load_occt_factory.mjs
    test/test_multi_format_exports.mjs
  </read_first>
  <behavior>
    - Test 1: `ReadStepFile(simple_part.step, {})` and `ReadFile("step", simple_part.step, {})` both succeed and expose the same canonical top-level fields.
    - Test 2: `ReadIgesFile(cube_10x10.igs, {})` and `ReadFile("iges", cube_10x10.igs, {})` match on `sourceFormat`, root/material/geometry counts, `warnings`, `stats`, and unit-metadata presence.
    - Test 3: `ReadBrepFile(as1_pe_203.brep, {})` and `ReadFile("brep", as1_pe_203.brep, {})` match on the same canonical contract.
    - Test 4: Existing realistic name/color/topology assertions still run after the new contract helpers are introduced.
  </behavior>
  <action>Refactor `test/test_multi_format_exports.mjs` into a contract test centered on reusable helpers such as `assertCanonicalResultShape(result, label)` and `assertEntryPointParity(...)`. Use `simple_part.step`, `cube_10x10.igs`, and `as1_pe_203.brep` as the baseline parity fixtures. For every successful result, assert `success === true`, `sourceFormat` equals the normalized lower-case format string, `rootNodes`, `geometries`, `materials`, and `warnings` are arrays, and `stats` exposes `rootCount`, `nodeCount`, `partCount`, `geometryCount`, `materialCount`, `triangleCount`, and `reusedInstanceCount`. When `sourceUnit` and `unitScaleToMeters` are present on one path, assert the other path matches exactly. Keep the existing invalid-data coverage and the current realistic color/name/topology checks so this task tightens CORE-02 without dropping earlier regression coverage.</action>
  <acceptance_criteria>
    - `node test/test_multi_format_exports.mjs` passes.
    - `rg -n "assertCanonicalResultShape|assertEntryPointParity|sourceFormat|rootNodes|geometries|materials|warnings|stats|sourceUnit|unitScaleToMeters" test/test_multi_format_exports.mjs` shows the new field-level contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>node test/test_multi_format_exports.mjs</automated>
  </verify>
  <done>The multi-format contract test fails whenever a supported direct or generic import path drops or reshapes the canonical successful payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep the Wasm bindings on one canonical success-payload builder</name>
  <files>src/js-interface.cpp</files>
  <read_first>
    src/js-interface.cpp
    src/importer.hpp
    src/importer-xde.cpp
    src/importer-brep.cpp
    test/test_multi_format_exports.mjs
    AGENTS.md
  </read_first>
  <behavior>
    - Test 1: `BuildResult(...)` is still the single serialization path for successful root imports.
    - Test 2: Direct wrappers remain aliases over the same normalized `ReadByFormat(...)` dispatcher.
    - Test 3: Successful results emit the canonical top-level fields required by Task 1 without adding new public APIs.
  </behavior>
  <action>Modify `src/js-interface.cpp` only as needed to satisfy the new parity assertions. Keep the public API surface exactly `ReadFile`, `ReadStepFile`, `ReadIgesFile`, and `ReadBrepFile`. Preserve lower-case `sourceFormat`, make `BuildResult(...)` the authoritative serializer for successful import results, and ensure it always emits `rootNodes`, `geometries`, `materials`, `warnings`, and `stats`, with `sourceUnit` and `unitScaleToMeters` only when `OcctSceneData` provides them. If parity failures come from dispatch differences, fix them inside `ReadByFormat(...)` or `BuildResult(...)`; do not add package-layer behavior, Babylon/demo coupling, or any new root Wasm API.</action>
  <acceptance_criteria>
    - `powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_multi_format_exports.mjs; exit $LASTEXITCODE"` passes.
    - `rg -n "BuildResult|result\\.set\\(\"sourceFormat\"|result\\.set\\(\"rootNodes\"|result\\.set\\(\"geometries\"|result\\.set\\(\"materials\"|result\\.set\\(\"warnings\"|result\\.set\\(\"stats\"|normalizedFormat" src/js-interface.cpp` shows the canonical serialization path.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_multi_format_exports.mjs; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The root Wasm bindings regenerate `dist/` and pass a parity-focused import contract test proving direct and generic entrypoints share one canonical success-payload builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS caller -> Wasm import bindings | Untrusted `format`, bytes, and params enter the root dispatcher and must not silently reshape the public payload contract |
| `ReadByFormat(...)` -> format-specific importers | Dispatch drift can make direct and generic entrypoints return different payloads for the same input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `src/js-interface.cpp` | mitigate | Keep `BuildResult(...)` as the single success serializer and lock its exact top-level fields with direct-vs-generic parity tests |
| T-02-02 | D | `test/test_multi_format_exports.mjs` | mitigate | Add explicit contract helpers for STEP, IGES, and BREP so payload-shape regressions fail before downstream code sees them |
</threat_model>

<verification>
Run `node test/test_multi_format_exports.mjs` while tightening the test. Before closing the plan, rebuild the Wasm artifacts with `npm run build:wasm:win` and rerun `node test/test_multi_format_exports.mjs` against the regenerated `dist/` output because this plan touches `src/js-interface.cpp`.
</verification>

<success_criteria>
Phase 02 has an automated import contract test that proves STEP, IGES, and BREP all return the same successful root payload shape through both direct and generic entrypoints, and the root Wasm bindings pass that test after a fresh `dist/` rebuild.
</success_criteria>

<output>
After completion, create `.planning/phases/02-root-runtime-contract/02-01-SUMMARY.md`
</output>
