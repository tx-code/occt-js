---
phase: 02-root-runtime-contract
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_step_iges_root_mode.mjs
  - test/test_brep_root_mode.mjs
  - src/importer-xde.cpp
  - src/importer-brep.cpp
  - src/importer.hpp
autonomous: true
requirements:
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - "STEP and BREP root-mode semantics are explicit for default, `one-shape`, and `multiple-shapes`, including realistic multi-root fixtures."
    - "IGES root-mode behavior is documented as an explicit supported contract instead of being inferred from current smoke coverage."
    - "Source-unit metadata is verified on direct and generic XDE imports when available, while BREP results explicitly remain unitless."
  artifacts:
    - "test/test_step_iges_root_mode.mjs"
    - "test/test_brep_root_mode.mjs"
    - "src/importer-xde.cpp"
    - "src/importer-brep.cpp"
    - "src/importer.hpp"
  key_links:
    - "src/js-interface.cpp:ParseImportParams -> ImportParams::RootMode"
    - "src/importer-xde.cpp:AppendRootNodes -> scene.rootNodeIndices + scene.sourceUnit"
    - "src/importer-brep.cpp:CollectRootShapes -> scene.rootNodeIndices"
---

<objective>
Make root-mode and unit-metadata semantics explicit for the supported root import formats.

Purpose: Close CORE-03 and the unit-metadata portion of CORE-02 by turning scattered count checks into a stable contract for STEP, IGES, and BREP root selection behavior.
Output: Root-mode contract tests for XDE/BREP imports plus the minimum importer changes needed to keep those semantics stable.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-root-runtime-contract/02-RESEARCH.md
@AGENTS.md
@src/js-interface.cpp
@src/importer.hpp
@src/importer-xde.cpp
@src/importer-brep.cpp
@test/test_step_iges_root_mode.mjs
@test/test_brep_root_mode.mjs

<interfaces>
From `src/importer.hpp`:
```cpp
struct ImportParams {
    enum class RootMode {
        OneShape,
        MultipleShapes
    };
    RootMode rootMode = RootMode::OneShape;
};
```

From `src/importer-xde.cpp`:
```cpp
void AppendRootNodes(const TDF_LabelSequence& freeShapes,
                     const Handle(XCAFDoc_ShapeTool)& shapeTool,
                     const Handle(XCAFDoc_ColorTool)& colorTool,
                     const ImportParams& params,
                     OcctSceneData& scene);

OcctSceneData ImportXdeFromMemory(..., const ImportParams& params, const std::string& format);
```

From `src/importer-brep.cpp`:
```cpp
std::vector<TopoDS_Shape> CollectRootShapes(const TopoDS_Shape& shape, const ImportParams& params);
OcctSceneData ImportBrepFromMemory(..., const ImportParams& params);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Turn root-mode and unit-metadata expectations into explicit contract tests</name>
  <files>test/test_step_iges_root_mode.mjs, test/test_brep_root_mode.mjs</files>
  <read_first>
    .planning/phases/02-root-runtime-contract/02-RESEARCH.md
    src/js-interface.cpp
    src/importer.hpp
    src/importer-xde.cpp
    src/importer-brep.cpp
    test/test_step_iges_root_mode.mjs
    test/test_brep_root_mode.mjs
  </read_first>
  <behavior>
    - Test 1: STEP default and explicit `one-shape` return one logical root, while `multiple-shapes` exposes each free shape on both synthetic and realistic multi-root STEP fixtures.
    - Test 2: IGES default, explicit `one-shape`, and explicit `multiple-shapes` all remain explicit contract cases on `cube_10x10.igs`, and generic `ReadFile("iges", ...)` matches each result.
    - Test 3: BREP default and explicit `one-shape` keep one root, while `multiple-shapes` splits supported compound/compsolid fixtures and generic dispatch matches those counts.
    - Test 4: STEP and IGES unit metadata are checked on both direct and generic paths when available, while BREP explicitly omits `sourceUnit` and `unitScaleToMeters`.
  </behavior>
  <action>Expand `test/test_step_iges_root_mode.mjs` so STEP semantics are expressed with helper-based assertions instead of one-off root counts. Keep the current `two_free_shapes.step` and `chassis-2roots.stp` coverage, then add explicit IGES contract cases for `cube_10x10.igs`: default import returns one root, `rootMode: "one-shape"` returns one root, `rootMode: "multiple-shapes"` also returns one root on this fixture, and `ReadFile("iges", ...)` matches each direct result. In the same file, tighten source-unit checks by asserting `ReadStepFile(as1-oc-214_inches.stp, ...)` and `ReadFile("step", ...)` both report the same `sourceUnit` and `unitScaleToMeters`; for IGES, assert direct and generic paths either both expose the same unit metadata or both omit it. Expand `test/test_brep_root_mode.mjs` to document default/one-shape parity, multi-root stability for `nonmanifold_cells.brep`, and the absence of `sourceUnit` and `unitScaleToMeters` on both direct and generic BREP results. Keep the scope on the root Wasm contract only. This task implements the tests-first portion of CORE-02 and CORE-03.</action>
  <acceptance_criteria>
    - `node test/test_step_iges_root_mode.mjs && node test/test_brep_root_mode.mjs` passes.
    - `rg -n "one-shape|multiple-shapes|sourceUnit|unitScaleToMeters|cube_10x10\\.igs|as1-oc-214_inches\\.stp|nonmanifold_cells\\.brep" test/test_step_iges_root_mode.mjs test/test_brep_root_mode.mjs` shows the explicit contract coverage.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node test/test_step_iges_root_mode.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_brep_root_mode.mjs; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The repository has focused contract tests that spell out exactly what each supported format does for root selection and unit metadata instead of leaving those semantics implied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep importer root selection and unit metadata aligned with the explicit contract</name>
  <files>src/importer-xde.cpp, src/importer-brep.cpp, src/importer.hpp</files>
  <read_first>
    src/importer.hpp
    src/importer-xde.cpp
    src/importer-brep.cpp
    src/js-interface.cpp
    test/test_step_iges_root_mode.mjs
    test/test_brep_root_mode.mjs
    AGENTS.md
  </read_first>
  <behavior>
    - Test 1: XDE imports use the synthetic `__root__` assembly wrapper for default and explicit `one-shape` when multiple free shapes exist.
    - Test 2: XDE `multiple-shapes` exposes each free shape directly and preserves `sourceUnit` / `unitScaleToMeters` mapping for STEP and IGES.
    - Test 3: BREP default and `one-shape` keep the unsplit root, while `multiple-shapes` only splits compounds/compsolids with more than one direct child.
    - Test 4: No new root modes or fabricated BREP unit metadata are introduced.
  </behavior>
  <action>Update `src/importer-xde.cpp`, `src/importer-brep.cpp`, and `src/importer.hpp` only as required to satisfy Task 1. Keep `ImportParams::RootMode` limited to `OneShape` and `MultipleShapes`. In `src/importer-xde.cpp`, ensure the default path and explicit `one-shape` both use the synthetic `__root__` assembly wrapper whenever `freeShapes.Length() > 1`, while explicit `multiple-shapes` keeps each free shape as its own root. Preserve the source-unit mapping in `ImportXdeFromMemory(...)` as `MM`, `CM`, `M`, `INCH`, `FOOT`, or `CUSTOM`, and keep that metadata available to both STEP and IGES results. In `src/importer-brep.cpp`, keep default and `one-shape` returning the unsplit shape and only let `multiple-shapes` split direct children when the loaded root is a multi-child `COMPOUND` or `COMPSOLID`. Do not widen scope into `occt-core`, packaging, or demo behavior, and do not invent BREP unit metadata.</action>
  <acceptance_criteria>
    - `powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_step_iges_root_mode.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_brep_root_mode.mjs; exit $LASTEXITCODE"` passes.
    - `rg -n "RootMode::OneShape|RootMode::MultipleShapes|scene\\.sourceUnit|scene\\.unitScaleToMeters|__root__|CollectRootShapes" src/importer.hpp src/importer-xde.cpp src/importer-brep.cpp` shows the explicit contract hooks.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_step_iges_root_mode.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node test/test_brep_root_mode.mjs; exit $LASTEXITCODE"</automated>
  </verify>
  <done>STEP, IGES, and BREP root-mode behavior is explicit in both tests and importer code, and a fresh `dist/` rebuild passes the focused root-mode/unit-metadata suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS params -> importer root selection | `rootMode` comes from caller-controlled input and must not silently change top-level shape semantics |
| importer metadata -> exported scene payload | Incorrect unit mapping or root splitting would mislead downstream consumers about scale and structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T | `src/importer-xde.cpp`, `src/importer-brep.cpp` | mitigate | Lock default/`one-shape`/`multiple-shapes` semantics in dedicated STEP/IGES/BREP contract tests before adjusting importer code |
| T-02-04 | I | `scene.sourceUnit`, `scene.unitScaleToMeters` | mitigate | Keep the XDE unit mapping explicit and verify direct-vs-generic parity for STEP/IGES while asserting BREP stays unitless |
</threat_model>

<verification>
Run `node test/test_step_iges_root_mode.mjs` and `node test/test_brep_root_mode.mjs` while shaping the tests. Before closing the plan, rebuild with `npm run build:wasm:win` and rerun both focused tests because this plan changes importer C++ code.
</verification>

<success_criteria>
Phase 02 explicitly documents what `one-shape` and `multiple-shapes` mean for the supported formats, preserves XDE unit metadata where available, keeps BREP unitless, and proves those semantics after a fresh Wasm rebuild.
</success_criteria>

<output>
After completion, create `.planning/phases/02-root-runtime-contract/02-02-SUMMARY.md`
</output>
