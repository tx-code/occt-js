---
phase: 08-pairwise-measurement-contract-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - package.json
  - README.md
  - packages/occt-core/README.md
  - test/release_governance_contract.test.mjs
  - test/dist_contract_consumers.test.mjs
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
  - MEAS-05
must_haves:
  truths:
    - "Package docs and typings describe exact pairwise measurement entrypoints from the root wasm carrier and `occt-core`, not from viewer layers."
    - "Root release verification and npm test surfaces keep exact pairwise measurement coverage mandatory."
    - "Planning traceability reflects the completed Phase 7 single-entity contract and the active Phase 8 pairwise contract."
  artifacts:
    - "package.json"
    - "README.md"
    - "packages/occt-core/README.md"
    - "test/release_governance_contract.test.mjs"
    - ".planning/REQUIREMENTS.md"
  key_links:
    - "README / occt-core README -> exact pairwise measurement entrypoints"
    - "package.json release gate -> exact pairwise measurement contract tests"
    - "planning traceability -> Phase 7 completed requirements and Phase 8 active requirements"
---

<objective>
Finalize package adapters, typings, docs, and release verification.

Purpose: satisfy ADAPT-01 and ADAPT-02 by making the exact pairwise measurement foundation discoverable, package-first, and permanently covered by the root release/test surface.
Output: updated package docs and scripts, release-governance contract tests for exact pairwise measurement, and planning traceability synchronized to the post-Phase-7/pre-Phase-8 state.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-pairwise-measurement-contract-hardening/08-RESEARCH.md
@.planning/phases/08-pairwise-measurement-contract-hardening/08-VALIDATION.md
@AGENTS.md
@package.json
@README.md
@packages/occt-core/README.md
@dist/occt-js.d.ts
@packages/occt-core/src/index.js
@packages/occt-core/src/occt-core.js
@test/dist_contract_consumers.test.mjs
@test/release_governance_contract.test.mjs
@test/exact_pairwise_measurement_contract.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing docs and release-governance tests for exact pairwise measurement</name>
  <read_first>package.json, README.md, packages/occt-core/README.md, test/release_governance_contract.test.mjs, test/dist_contract_consumers.test.mjs, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/08-pairwise-measurement-contract-hardening/08-RESEARCH.md</read_first>
  <files>test/release_governance_contract.test.mjs, test/dist_contract_consumers.test.mjs</files>
  <behavior>
    - Test 1: a test named `authoritative root release command surface includes exact pairwise measurement coverage` fails until the root release gate or npm test surface includes `test/exact_pairwise_measurement_contract.test.mjs`.
    - Test 2: a test named `package docs describe exact pairwise measurement entrypoints without viewer prerequisites` fails until both `README.md` and `packages/occt-core/README.md` mention the root/adapter exact pairwise measurement entrypoints.
    - Test 3: a test named `planning traceability marks Phase 7 requirements complete before Phase 8 execution` fails until `.planning/REQUIREMENTS.md` marks `REF-03` and `MEAS-03` as `Completed`.
  </behavior>
  <action>Extend `test/release_governance_contract.test.mjs` and `test/dist_contract_consumers.test.mjs` with the exact test names from `<behavior>`. Assert that the root release or npm test surface contains `test/exact_pairwise_measurement_contract.test.mjs`, that `README.md` and `packages/occt-core/README.md` mention exact pairwise measurement entrypoints through the root wasm carrier and `createOcctCore(...)`, and that `.planning/REQUIREMENTS.md` marks `REF-03` and `MEAS-03` as `Completed` before Phase 8 execution begins.</action>
  <acceptance_criteria>
    - `rg -n "authoritative root release command surface includes exact pairwise measurement coverage|package docs describe exact pairwise measurement entrypoints without viewer prerequisites|planning traceability marks Phase 7 requirements complete before Phase 8 execution" test/release_governance_contract.test.mjs test/dist_contract_consumers.test.mjs` returns matches.
    - `node --test test/release_governance_contract.test.mjs test/dist_contract_consumers.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/release_governance_contract.test.mjs test/dist_contract_consumers.test.mjs</automated>
  </verify>
  <done>The release/docs contract tests prove that exact pairwise measurement is now a required package-first surface before documentation and script updates land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update package docs, release gates, and planning traceability for the exact pairwise contract</name>
  <read_first>package.json, README.md, packages/occt-core/README.md, test/release_governance_contract.test.mjs, test/dist_contract_consumers.test.mjs, .planning/REQUIREMENTS.md, .planning/STATE.md, test/exact_pairwise_measurement_contract.test.mjs</read_first>
  <files>package.json, README.md, packages/occt-core/README.md, test/release_governance_contract.test.mjs, test/dist_contract_consumers.test.mjs, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <behavior>
    - Test 1: `npm test` and `npm run test:release:root` both keep exact pairwise measurement coverage in the authoritative root verification surface.
    - Test 2: root docs and `occt-core` docs describe the exact pairwise measurement entrypoints with package-first examples and explicitly keep viewer UX/semantic feature recognition out of scope.
    - Test 3: planning traceability marks Phase 7 single-entity requirements complete and keeps Phase 8 requirements/plans current for the pairwise milestone closeout.
  </behavior>
  <action>Update `package.json` so the authoritative root test surface includes `test/exact_pairwise_measurement_contract.test.mjs` in `npm test` and in `npm run test:release:root` if it is not already covered transitively. Update `README.md` and `packages/occt-core/README.md` with package-first exact pairwise measurement examples using the concrete entrypoints `MeasureExactDistance`, `MeasureExactAngle`, `MeasureExactThickness`, and `createOcctCore(...).measureExactDistance/Angle/Thickness`, while stating that overlay rendering, selection UX, and semantic feature recognition remain downstream concerns. Update `.planning/REQUIREMENTS.md` so `REF-03` and `MEAS-03` are marked `Completed`, and update `.planning/STATE.md` so the current focus stays on Phase 08 execution readiness after planning/implementation are complete. Keep all release-governance assertions aligned to these exact strings.</action>
  <acceptance_criteria>
    - `rg -n "exact_pairwise_measurement_contract|MeasureExactDistance|MeasureExactAngle|MeasureExactThickness|createOcctCore" package.json README.md packages/occt-core/README.md` returns matches.
    - `rg -n "\\| REF-03 \\| Phase 7 \\| Completed \\||\\| MEAS-03 \\| Phase 7 \\| Completed \\|" .planning/REQUIREMENTS.md` returns matches.
    - `node --test test/release_governance_contract.test.mjs test/dist_contract_consumers.test.mjs` passes.
    - `npm run test:release:root` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --test test/release_governance_contract.test.mjs test/dist_contract_consumers.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:release:root"</automated>
  </verify>
  <done>The package surface, release gate, and planning traceability all treat exact pairwise measurement as a first-class root/runtime capability rather than a viewer-only add-on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release/docs contract -> downstream package consumers | Missing pairwise docs or release coverage would let root contract drift without a visible failure. |
| planning traceability -> milestone governance | Stale requirement statuses would misstate what the milestone has already completed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | T/R | release-governance test surface | mitigate | Add explicit assertions that the root release path includes exact pairwise measurement coverage. |
| T-08-03-02 | I | package docs | mitigate | Document the concrete root and `occt-core` pairwise entrypoints and keep viewer UX explicitly out of scope. |
| T-08-03-03 | R | planning traceability | mitigate | Sync `.planning/REQUIREMENTS.md` and `.planning/STATE.md` to the post-Phase-7 pairwise-contract state. |
</threat_model>

<verification>
- Run `node --test test/release_governance_contract.test.mjs test/dist_contract_consumers.test.mjs`.
- Run `npm run test:release:root`.
</verification>

<success_criteria>
The exact pairwise measurement foundation is discoverable through package docs, guarded by the root release gate, and accurately reflected in project traceability artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pairwise-measurement-contract-hardening/08-03-SUMMARY.md`
</output>
