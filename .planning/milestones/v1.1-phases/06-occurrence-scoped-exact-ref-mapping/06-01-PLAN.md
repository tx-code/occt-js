---
phase: 06-occurrence-scoped-exact-ref-mapping
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/exact-model-store.hpp
  - src/exact-model-store.cpp
  - src/importer-step.cpp
  - src/importer-iges.cpp
  - src/importer-brep.cpp
  - src/importer-xde.cpp
  - src/js-interface.cpp
  - test/exact_ref_mapping_contract.test.mjs
autonomous: true
requirements:
  - REF-01
must_haves:
  truths:
    - "Exact open returns one exact-definition binding per exported geometry without changing the current stateless import lane."
    - "The root exact lane keeps imported topology ids definition-local and ready for later exact lookup."
    - "Repeated geometry definitions reuse one exported binding per geometry definition rather than one binding per occurrence."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/exact-model-store.hpp"
    - "src/exact-model-store.cpp"
    - "src/js-interface.cpp"
    - "test/exact_ref_mapping_contract.test.mjs"
  key_links:
    - "OpenExact* -> exactGeometryBindings"
    - "exact-model-store -> geometry definition bindings"
    - "test/exact_ref_mapping_contract.test.mjs -> OpenExact* / exactGeometryBindings"
---

<objective>
Add internal imported-topology-id mapping and exact-definition binding DTOs.

Purpose: satisfy the definition-identity half of Phase 6 by making the retained exact-model lane expose one exact binding per exported geometry definition, ready for occurrence-scoped ref resolution in `occt-core`.
Output: exact-open results with `exactGeometryBindings`, typed root bindings, and root tests that prove binding alignment under reused geometry definitions.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-occurrence-scoped-exact-ref-mapping/06-RESEARCH.md
@.planning/phases/06-occurrence-scoped-exact-ref-mapping/06-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/exact-model-store.hpp
@src/exact-model-store.cpp
@src/importer-utils.cpp
@src/importer-step.cpp
@src/importer-iges.cpp
@src/importer-brep.cpp
@src/importer-xde.cpp
@src/js-interface.cpp
@test/exact_model_lifecycle_contract.test.mjs
@test/assembly.step
@test/simple_part.step
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a failing root contract test for exact-definition binding alignment</name>
  <read_first>test/exact_model_lifecycle_contract.test.mjs, src/js-interface.cpp, dist/occt-js.d.ts, .planning/phases/06-occurrence-scoped-exact-ref-mapping/06-RESEARCH.md, .planning/phases/06-occurrence-scoped-exact-ref-mapping/06-VALIDATION.md</read_first>
  <files>test/exact_ref_mapping_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `exact open exposes one exactGeometryBinding per exported geometry` fails until exact STEP open returns `exactGeometryBindings.length === geometries.length` and every binding has a numeric `exactShapeHandle`.
    - Test 2: a subtest named `reused geometry definitions do not inflate exactGeometryBindings beyond geometries.length` fails until an assembly fixture with `reusedInstanceCount > 0` still returns exactly one binding per exported geometry definition.
    - Test 3: a subtest named `stateless ReadStepFile remains exactGeometryBindings-free` fails unless the current stateless lane still returns no `exactGeometryBindings` property.
  </behavior>
  <action>Create `test/exact_ref_mapping_contract.test.mjs` as a Node built-in test file that loads the built root carrier via `test/load_occt_factory.mjs`, opens `test/simple_part.step` and `test/assembly.step` through `OpenExactStepModel`, and adds the exact subtest names from `<behavior>`. Assert that `exactGeometryBindings` is an array, that its length equals `geometries.length`, that each binding includes `typeof exactShapeHandle === "number"`, that the `assembly.step` result reports `stats.reusedInstanceCount > 0`, and that `ReadStepFile(...)` does not expose `exactGeometryBindings`.</action>
  <acceptance_criteria>
    - `rg -n "exact open exposes one exactGeometryBinding per exported geometry|reused geometry definitions do not inflate exactGeometryBindings beyond geometries.length|stateless ReadStepFile remains exactGeometryBindings-free" test/exact_ref_mapping_contract.test.mjs` returns matches.
    - `rg -n "exactGeometryBindings|exactShapeHandle|OpenExactStepModel|ReadStepFile" test/exact_ref_mapping_contract.test.mjs` returns matches.
    - `node --test test/exact_ref_mapping_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/exact_ref_mapping_contract.test.mjs</automated>
  </verify>
  <done>The root ref-mapping contract test proves that exact-open results expose definition bindings aligned to exported geometries and that stateless imports remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact-definition bindings on the root exact-open lane</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/exact-model-store.hpp, src/exact-model-store.cpp, src/importer-utils.cpp, src/importer-xde.cpp, src/importer-brep.cpp, src/js-interface.cpp, test/exact_ref_mapping_contract.test.mjs, .planning/phases/06-occurrence-scoped-exact-ref-mapping/06-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/exact-model-store.hpp, src/exact-model-store.cpp, src/importer-step.cpp, src/importer-iges.cpp, src/importer-brep.cpp, src/importer-xde.cpp, src/js-interface.cpp, test/exact_ref_mapping_contract.test.mjs</files>
  <behavior>
    - Test 1: exact-open results expose `exactGeometryBindings` typed on `OcctJSExactOpenResult`.
    - Test 2: each exported geometry definition receives one stable numeric `exactShapeHandle` inside the retained model entry.
    - Test 3: the current face/edge/vertex ids remain definition-local and aligned with the exact geometry binding they belong to.
    - Test 4: stateless `Read*` entrypoints remain signature-compatible and do not expose exact-definition binding metadata.
  </behavior>
  <action>Extend the exact-model store so each retained exact-model entry can register exact geometry-definition bindings keyed to the exported geometry-definition order used by the exact-open result. Update the exact import paths in `src/importer-step.cpp`, `src/importer-iges.cpp`, `src/importer-brep.cpp`, and `src/importer-xde.cpp` so exact opens capture one exact subshape per exported geometry definition while preserving the current `OcctSceneData` payload. Update `src/importer.hpp` with DTO structs for exact geometry bindings and any internal imported-topology mapping metadata needed later by measurement calls. In `src/js-interface.cpp`, serialize `exactGeometryBindings` only for exact-open results, keeping it index-aligned with `geometries`, and update `dist/occt-js.d.ts` with concrete types such as `OcctJSExactGeometryBinding`. Do not add occurrence resolution APIs in this plan.</action>
  <acceptance_criteria>
    - `rg -n "exactGeometryBindings|exactShapeHandle" dist/occt-js.d.ts src/importer.hpp src/js-interface.cpp` returns matches.
    - `rg -n "Register.*exactShapeHandle|geometry binding|exactGeometryBindings" src/exact-model-store.hpp src/exact-model-store.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/exact_ref_mapping_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/exact_ref_mapping_contract.test.mjs"</automated>
  </verify>
  <done>The root exact-open contract now exposes definition-level exact geometry bindings that stay aligned with exported geometry definitions and reused-definition behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exact-open result -> downstream JS callers | Definition-level exact binding metadata leaves wasm here and becomes part of the public contract. |
| importer topology ids -> retained exact-definition binding store | The root runtime must preserve correct mapping between exported topology ids and retained exact geometry definitions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T/R | exact-definition binding metadata | mitigate | Keep bindings index-aligned with `geometries` and lock it with root tests. |
| T-06-01-02 | S/T | reused geometry definitions | mitigate | Bind definitions, not occurrences, and prove reused assemblies do not inflate binding counts beyond `geometries.length`. |
| T-06-01-03 | D | stateless import lane | mitigate | Keep `Read*` exact-binding-free and assert it explicitly in root tests. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/exact_ref_mapping_contract.test.mjs`.
</verification>

<success_criteria>
Exact-open results expose one exact-definition binding per exported geometry, those bindings remain definition-scoped under reused geometry, and the stateless import lane remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-occurrence-scoped-exact-ref-mapping/06-01-SUMMARY.md`
</output>
