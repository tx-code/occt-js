---
phase: 13-appearance-preset-adapter-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/js-interface.cpp
  - test/import_appearance_contract.test.mjs
autonomous: true
requirements:
  - APPR-07
  - APPR-08
must_haves:
  truths:
    - "Root import APIs accept a small named `appearancePreset` contract without removing the shipped primitive appearance fields."
    - "Preset resolution stays on the shared root import path, so `Read*` and `OpenExact*` remain in parity."
    - "Explicit `colorMode`, `defaultColor`, and `defaultOpacity` precedence over preset-derived defaults is regression-tested."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/importer.hpp"
    - "src/js-interface.cpp"
    - "test/import_appearance_contract.test.mjs"
  key_links:
    - "appearancePreset -> shared ImportParams resolution"
    - "preset bundle defaults -> explicit override precedence"
    - "root contract tests -> read/openExact preset parity"
---

<objective>
Add named appearance presets to the root import contract.

Purpose: satisfy APPR-07 and APPR-08 by letting callers choose a small set of runtime-owned default-appearance bundles while keeping the shipped primitive contract and read/exact parity intact.
Output: root typings, preset parsing/resolution, and root contract tests for named preset behavior.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-appearance-preset-adapter-parity/13-RESEARCH.md
@.planning/phases/13-appearance-preset-adapter-parity/13-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/js-interface.cpp
@test/import_appearance_contract.test.mjs
@test/ANC101_colored.stp
@test/bearing.igs
@test/ANC101_isolated_components.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root tests for named preset mapping and parity</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, test/import_appearance_contract.test.mjs, .planning/phases/13-appearance-preset-adapter-parity/13-RESEARCH.md</read_first>
  <files>test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `appearancePreset cad-solid matches explicit default appearance` fails until the root carrier maps the solid preset onto the shipped default appearance behavior.
    - Test 2: a subtest named `appearancePreset cad-ghosted applies the built-in ghost opacity across read and exact lanes` fails until preset-driven ghost opacity survives read/openExact parity.
    - Test 3: a subtest named `explicit appearance params override preset-derived defaults` fails until explicit `colorMode`, `defaultColor`, and `defaultOpacity` remain authoritative.
  </behavior>
  <action>Extend `test/import_appearance_contract.test.mjs` with the exact subtest names from `<behavior>`. Reuse the shipped Phase 12 helpers. Assert that root presets map onto the same raw appearance payloads as explicit params, and that the same preset result appears through stateless and exact-open entrypoints across STEP, IGES, and BREP where appropriate.</action>
  <acceptance_criteria>
    - `rg -n "appearancePreset cad-solid matches explicit default appearance|appearancePreset cad-ghosted applies the built-in ghost opacity across read and exact lanes|explicit appearance params override preset-derived defaults" test/import_appearance_contract.test.mjs` returns matches.
    - `node --test test/import_appearance_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/import_appearance_contract.test.mjs</automated>
  </verify>
  <done>The root contract tests prove named preset semantics before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse root presets and resolve them through the shared appearance contract</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, test/import_appearance_contract.test.mjs, .planning/phases/13-appearance-preset-adapter-parity/13-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: the public root typing surface exposes a canonical `appearancePreset` enum.
    - Test 2: preset resolution happens before the shipped primitive appearance fields, but explicit primitive fields can override the preset-derived defaults.
    - Test 3: `Read*` and `OpenExact*` remain in parity because preset resolution happens on the shared root import-param path.
  </behavior>
  <action>Add `OcctJSImportAppearancePreset` and `appearancePreset?: ...` to `dist/occt-js.d.ts` with precedence comments. Extend `ImportParams` / `ParseImportParams(...)` so the root carrier can resolve presets such as `cad-solid` and `cad-ghosted` into the shipped default appearance primitives without adding importer-specific preset logic. Keep the runtime contract root-first and avoid introducing viewer-facing preset descriptors.</action>
  <acceptance_criteria>
    - `rg -n "appearancePreset|OcctJSImportAppearancePreset" dist/occt-js.d.ts src/importer.hpp src/js-interface.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/import_appearance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/import_appearance_contract.test.mjs"</automated>
  </verify>
  <done>The root carrier now supports named presets on top of the shipped appearance primitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preset string input -> shared root appearance resolution | Bad mapping here would make presets ambiguous or format-specific. |
| preset-derived defaults -> explicit appearance params | If precedence is unclear, callers cannot predict the final appearance policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | T/R | root preset contract | mitigate | Lock solid/ghosted mapping and parity with failing root tests first. |
| T-13-01-02 | T/I | preset precedence | mitigate | Type and test explicit override precedence before adding adapter convenience. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/import_appearance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root presets now resolve into the shipped appearance contract without breaking read/exact parity
</success_criteria>

<output>
After completion, create `.planning/phases/13-appearance-preset-adapter-parity/13-01-SUMMARY.md`
</output>
