---
phase: 14-appearance-expansion-governance
plan: "02"
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - test/package_tarball_contract.test.mjs
  - test/release_governance_contract.test.mjs
autonomous: true
requirements:
  - ADAPT-06
must_haves:
  truths:
    - "Packaged README/types assertions include the finalized preset/defaultOpacity contract."
    - "The authoritative root release gate enforces the final appearance-expansion package surface."
    - "Package verification remains runtime-first and independent of secondary surfaces."
  artifacts:
    - "test/package_tarball_contract.test.mjs"
    - "test/release_governance_contract.test.mjs"
  key_links:
    - "packaged README/types -> downstream preset/defaultOpacity guidance"
    - "release governance -> finalized appearance-expansion surface"
    - "runtime-first release gate -> no secondary-surface creep"
---

<objective>
Extend packaging and release governance for the final appearance-expansion surface.

Purpose: finish ADAPT-06 by locking packaged docs/types and the authoritative root release gate to the finalized preset/defaultOpacity contract.
Output: tarball/governance coverage plus a final green `npm run test:release:root`.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-appearance-expansion-governance/14-RESEARCH.md
@.planning/phases/14-appearance-expansion-governance/14-VALIDATION.md
@README.md
@packages/occt-core/README.md
@dist/occt-js.d.ts
@test/package_tarball_contract.test.mjs
@test/release_governance_contract.test.mjs
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing packaged-contract checks for presets and opacity</name>
  <read_first>README.md, dist/occt-js.d.ts, test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs, .planning/phases/14-appearance-expansion-governance/14-RESEARCH.md</read_first>
  <files>test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: tarball/package checks fail until packaged README and types mention `appearancePreset` and `defaultOpacity`.
    - Test 2: package checks fail until the packaged root surface stays package-first and independent of viewer/demo surfaces while describing the expanded appearance options.
  </behavior>
  <action>Extend `test/package_tarball_contract.test.mjs` and any remaining governance assertions so packaged README/type coverage explicitly mentions presets, ghost opacity/default opacity, and downstream setting responsibility. Keep the checks anchored to the packaged root carrier, not repo-local secondary surfaces.</action>
  <acceptance_criteria>
    - `rg -n "appearancePreset|defaultOpacity|cad-ghosted" test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs` returns matches.
    - `node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs</automated>
  </verify>
  <done>The package-contract tests now prove the final packaged appearance-expansion surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-run the authoritative root release gate against the finalized governance surface</name>
  <read_first>test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs, .planning/phases/14-appearance-expansion-governance/14-RESEARCH.md, .planning/phases/14-appearance-expansion-governance/14-VALIDATION.md</read_first>
  <files>test/package_tarball_contract.test.mjs, test/release_governance_contract.test.mjs</files>
  <behavior>
    - Test 1: package/governance checks pass with the final preset/defaultOpacity wording locked in.
    - Test 2: `npm run test:release:root` stays green after the governance updates.
  </behavior>
  <action>After the docs and package-contract assertions are updated, re-run the canonical root release gate and keep any final governance assertions aligned with the resulting milestone state. Do not add secondary-surface requirements to the authoritative release command.</action>
  <acceptance_criteria>
    - `node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs` passes.
    - `npm run test:release:root` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:release:root</automated>
  </verify>
  <done>The authoritative root release gate now enforces the final appearance-expansion package surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged README/types -> downstream consumption expectations | Drift here can break vendored/package-first consumers without code changes. |
| governance assertions -> release gate | If these checks are weak, the final milestone surface can regress silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | T/R | packaged appearance contract | mitigate | Add tarball/governance assertions for preset/defaultOpacity docs and types before final release verification. |
| T-14-02-02 | T/I | release gate drift | mitigate | Re-run `npm run test:release:root` after governance updates and keep the command runtime-first. |
</threat_model>

<verification>
- Run `node --test test/package_tarball_contract.test.mjs test/release_governance_contract.test.mjs`.
- Run `npm run test:release:root`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The final preset/defaultOpacity package surface is documented, packaged, and enforced by the root release gate
</success_criteria>

<output>
After completion, create `.planning/phases/14-appearance-expansion-governance/14-02-SUMMARY.md`
</output>
