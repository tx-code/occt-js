---
phase: 12-root-alpha-opacity-fallback
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/js-interface.cpp
  - test/import_appearance_contract.test.mjs
autonomous: true
requirements:
  - APPR-06
must_haves:
  truths:
    - "Root import APIs accept an explicit `defaultOpacity?: number` without overloading `defaultColor`."
    - "`defaultOpacity` only affects imports when `colorMode: \"default\"` is selected; it does not implicitly switch legacy/source modes."
    - "Raw Wasm colors and materials can expose additive `opacity` values so default appearance no longer requires downstream repaint hacks."
  artifacts:
    - "dist/occt-js.d.ts"
    - "src/importer.hpp"
    - "src/js-interface.cpp"
    - "test/import_appearance_contract.test.mjs"
  key_links:
    - "public defaultOpacity input -> shared ImportParams"
    - "opacity-aware OcctColor -> raw Mesh/material serialization"
    - "root contract tests -> default-mode-only opacity semantics"
---

<objective>
Expose explicit opacity fallback on the root import contract.

Purpose: satisfy APPR-06 by letting callers request default-appearance opacity from the root Wasm carrier itself instead of patching viewer materials after import.
Output: root typings, root import-param parsing, additive raw DTO opacity support, and stateless contract tests for `defaultOpacity`.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-root-alpha-opacity-fallback/12-RESEARCH.md
@.planning/phases/12-root-alpha-opacity-fallback/12-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/js-interface.cpp
@test/import_appearance_contract.test.mjs
@test/ANC101_colored.stp
@test/bearing.igs
@test/ANC101_isolated_components.brep
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing root tests for explicit defaultOpacity semantics</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, test/import_appearance_contract.test.mjs, .planning/phases/12-root-alpha-opacity-fallback/12-RESEARCH.md</read_first>
  <files>test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `defaultOpacity sets the default-appearance material opacity for stateless imports` fails until `ReadFile(..., { colorMode: "default", defaultOpacity })` returns raw materials/colors with the requested opacity.
    - Test 2: a subtest named `defaultOpacity composes with built-in and custom default colors` fails until explicit opacity works with both built-in default CAD color and caller-provided `defaultColor`.
    - Test 3: a subtest named `defaultOpacity is ignored unless colorMode default is selected` fails until `source` mode and legacy no-`colorMode` calls keep the shipped v1.2 behavior.
  </behavior>
  <action>Extend `test/import_appearance_contract.test.mjs` with the exact subtest names from `<behavior>`. Use `ANC101_colored.stp`, `bearing.igs`, and `ANC101_isolated_components.brep` where useful. Assert that default appearance surfaces the requested opacity on raw `materials`, mesh color, and face color payloads when `colorMode: "default"` is present, and assert that `defaultOpacity` alone or together with `colorMode: "source"` does not silently widen legacy/source behavior.</action>
  <acceptance_criteria>
    - `rg -n "defaultOpacity sets the default-appearance material opacity for stateless imports|defaultOpacity composes with built-in and custom default colors|defaultOpacity is ignored unless colorMode default is selected" test/import_appearance_contract.test.mjs` returns matches.
    - `rg -n "defaultOpacity|opacity|colorMode: \"default\"" test/import_appearance_contract.test.mjs` returns matches.
    - `node --test test/import_appearance_contract.test.mjs` fails before implementation and passes after the remaining tasks in this plan land.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/import_appearance_contract.test.mjs</automated>
  </verify>
  <done>The root contract tests prove explicit stateless default-opacity behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse defaultOpacity and expose additive raw opacity on the root carrier</name>
  <read_first>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, test/import_appearance_contract.test.mjs, .planning/phases/12-root-alpha-opacity-fallback/12-RESEARCH.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/js-interface.cpp, test/import_appearance_contract.test.mjs</files>
  <behavior>
    - Test 1: the public root typing surface exposes `defaultOpacity?: number` and additive `opacity?: number` on raw colors/materials.
    - Test 2: `ParseImportParams(...)` parses and clamps `defaultOpacity` onto the shared root appearance state.
    - Test 3: default appearance results expose raw opacity without requiring viewer-side post-processing.
    - Test 4: legacy/source imports continue to behave like the shipped v1.2 contract when `defaultOpacity` is absent or ignored.
  </behavior>
  <action>Add `defaultOpacity?: number` to `OcctJSReadParams` in `dist/occt-js.d.ts` with a comment that it only applies when `colorMode: "default"` is selected. Widen `OcctJSColor` and `OcctJSMaterial` with optional `opacity?: number`. Extend `OcctColor` / `ImportParams` in `src/importer.hpp` so default appearance can carry clamped opacity, then parse that option in `src/js-interface.cpp` and serialize raw color/material opacity additively. Keep `defaultColor` RGB-only and do not let `defaultOpacity` implicitly switch appearance modes.</action>
  <acceptance_criteria>
    - `rg -n "defaultOpacity|opacity" dist/occt-js.d.ts src/importer.hpp src/js-interface.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/import_appearance_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/import_appearance_contract.test.mjs"</automated>
  </verify>
  <done>The root Wasm carrier now exposes explicit default-opacity fallback for stateless imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS `defaultOpacity` input -> parsed root appearance state | Bad parsing or precedence here would corrupt downstream import settings. |
| shared root appearance state -> raw color/material DTOs | If opacity is not serialized additively and deterministically, downstream apps still need viewer patching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | T/R | root opacity contract | mitigate | Lock explicit default-opacity semantics and ignored-without-default-mode behavior with failing root tests first. |
| T-12-01-02 | T/I | raw DTO widening | mitigate | Add additive raw `opacity` typing/serialization and rebuild the root carrier before considering the task complete. |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/import_appearance_contract.test.mjs`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root imports now support explicit default-opacity fallback on the public carrier contract
</success_criteria>

<output>
After completion, create `.planning/phases/12-root-alpha-opacity-fallback/12-01-SUMMARY.md`
</output>
