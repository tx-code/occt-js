---
phase: 27-revolved-tool-spec-wasm-builder
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/revolved-tool.hpp
  - src/revolved-tool.cpp
  - src/js-interface.cpp
  - test/revolved_tool_spec_contract.test.mjs
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
must_haves:
  truths:
    - "The root Wasm carrier exposes one additive `ValidateRevolvedToolSpec(spec)` entrypoint for the normalized revolved-tool contract without importing any app-specific schema logic."
    - "The published root type surface defines the strict revolved-tool spec, typed diagnostics, and validation result vocabulary before build-path work lands."
    - "Unsupported units, non-finite coordinates, negative radii, degenerate segments, invalid arcs, invalid partial-revolve ranges, and non-closed explicit profiles fail with explicit typed diagnostics before OCCT geometry construction."
  artifacts:
    - path: dist/occt-js.d.ts
      provides: public TypeScript contract for strict validation
      contains: revolved-tool spec types, diagnostics types, and the `ValidateRevolvedToolSpec(...)` module signature
    - path: src/importer.hpp
      provides: shared C++ DTO seam
      contains: revolved-tool validation structs that match the published type surface
    - path: src/revolved-tool.hpp
      provides: generated-tool seam declarations
      contains: normalized spec DTOs and validation entrypoints
    - path: src/revolved-tool.cpp
      provides: strict validation implementation
      contains: spec normalization, diagnostics creation, and no-OCCT early-rejection logic for malformed specs
    - path: src/js-interface.cpp
      provides: Embind surface
      contains: JS object parsing, validation DTO serialization, and additive export registration
    - path: test/revolved_tool_spec_contract.test.mjs
      provides: built-runtime validation contract coverage
      contains: strict validation success and failure cases over the built dist runtime
  key_links:
    - from: src/js-interface.cpp
      to: src/revolved-tool.cpp
      via: thin Embind glue over the dedicated generated-tool validation seam
      pattern: ValidateRevolvedToolSpec
    - from: dist/occt-js.d.ts
      to: src/importer.hpp
      via: published validation types and result DTO parity
      pattern: diagnostics|OcctJSRevolvedTool
    - from: test/revolved_tool_spec_contract.test.mjs
      to: dist/occt-js.d.ts
      via: root validation contract and explicit diagnostics vocabulary
      pattern: ValidateRevolvedToolSpec|unsupported-unit|invalid-arc
---

<objective>
Define the strict revolved-tool spec and land the root validation surface before geometry generation work begins.

Purpose: satisfy `TOOL-01`, `TOOL-02`, and the validation half of `TOOL-03` by introducing a normalized spec contract, explicit diagnostics vocabulary, and one additive root validation API without yet building the OCCT revolve output path.
Output: strict root spec typings + validation seam + built-runtime contract tests.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-CONTEXT.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-RESEARCH.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-PATTERNS.md
@.planning/phases/27-revolved-tool-spec-wasm-builder/27-VALIDATION.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/js-interface.cpp
@test/load_occt_factory.mjs
@test/exact_hole_contract.test.mjs
@test/exact_placement_contract.test.mjs

<interfaces>
From `src/js-interface.cpp` (existing additive export pattern to mirror):
```cpp
function("ReadFile", &ReadFile);
function("OpenExactModel", &OpenExactModel);
function("AnalyzeOptimalOrientation", &AnalyzeOptimalOrientation);
```

From `dist/occt-js.d.ts` (existing utility-result vocabulary to stay explicit about):
```ts
export interface OcctJSExactQueryFailure {
  ok: false;
  code: string;
  message: string;
}
```

From `test/load_occt_factory.mjs` (root contract test harness to reuse):
```js
const factory = loadOcctFactory();
const module = await factory();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing built-runtime validation contract tests for the strict revolved-tool spec</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, test/load_occt_factory.mjs, test/exact_hole_contract.test.mjs, .planning/phases/27-revolved-tool-spec-wasm-builder/27-RESEARCH.md</read_first>
  <files>test/revolved_tool_spec_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `ValidateRevolvedToolSpec accepts a minimal strict line-and-arc revolved tool spec` fails until the built root runtime exposes `ValidateRevolvedToolSpec(spec)` and returns `{ ok: true, diagnostics: [] }` for a valid normalized spec.
    - Test 2: a subtest named `ValidateRevolvedToolSpec rejects unsupported units and negative radii with typed diagnostics` fails until unsupported unit values and negative radius coordinates produce explicit diagnostics instead of silent coercion.
    - Test 3: a subtest named `ValidateRevolvedToolSpec rejects invalid arc definitions and explicit non-closed profiles` fails until inconsistent `arc_center`, degenerate `arc_3pt`, and non-closed `explicit` profiles return explicit diagnostics.
    - Test 4: a subtest named `ValidateRevolvedToolSpec rejects invalid partial revolve ranges without touching scene-build APIs` fails until zero-or-negative or over-360 partial-revolve requests are diagnosed explicitly.
  </behavior>
  <action>Create `test/revolved_tool_spec_contract.test.mjs` as a built-runtime contract file using `test/load_occt_factory.mjs`. Use inline JS spec objects, not external fixtures. Lock the test names from `<behavior>`. Assert that diagnostics are typed objects with stable `code`, `message`, and optional path/segment hints; do not accept string-only error arrays.</action>
  <acceptance_criteria>
    - `rg -n "ValidateRevolvedToolSpec accepts a minimal strict line-and-arc revolved tool spec|ValidateRevolvedToolSpec rejects unsupported units and negative radii with typed diagnostics|ValidateRevolvedToolSpec rejects invalid arc definitions and explicit non-closed profiles|ValidateRevolvedToolSpec rejects invalid partial revolve ranges without touching scene-build APIs" test/revolved_tool_spec_contract.test.mjs` returns matches.
    - `rg -n "ValidateRevolvedToolSpec|diagnostics|unsupported-unit|invalid-arc|negative-radius" test/revolved_tool_spec_contract.test.mjs` returns matches.
    - `npm run build:wasm:win && node --test test/revolved_tool_spec_contract.test.mjs` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/revolved_tool_spec_contract.test.mjs"</automated>
  </verify>
  <done>The built root runtime now has a locked strict-validation contract for the normalized revolved-tool spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict revolved-tool spec DTOs diagnostics and the additive root validation binding</name>
  <read_first>src/importer.hpp, src/js-interface.cpp, dist/occt-js.d.ts, test/revolved_tool_spec_contract.test.mjs, .planning/phases/27-revolved-tool-spec-wasm-builder/27-PATTERNS.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/revolved-tool.hpp, src/revolved-tool.cpp, src/js-interface.cpp, test/revolved_tool_spec_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports `ValidateRevolvedToolSpec(spec)` as an additive utility API.
    - Test 2: the published root type surface defines the strict revolved-tool spec, diagnostics DTO, and validation result types without mentioning app-specific schema adapters.
    - Test 3: malformed specs fail before OCCT construction with explicit typed diagnostics instead of string-only `error` payloads.
    - Test 4: valid strict specs return deterministic normalized-success behavior with no diagnostics.
  </behavior>
  <action>Add shared validation DTOs to `src/importer.hpp`, create `src/revolved-tool.hpp/.cpp` for normalized spec validation, and wire one additive `ValidateRevolvedToolSpec(spec)` binding through `src/js-interface.cpp`. Update `dist/occt-js.d.ts` with the strict spec and diagnostics types. Keep validation root-first and explicit: no package wrappers, no app-schema adapters, and no OCCT revolve construction in this plan.</action>
  <acceptance_criteria>
    - `rg -n "ValidateRevolvedToolSpec|OcctJSRevolvedToolSpec|OcctJSRevolvedToolValidationResult|OcctJSRevolvedToolDiagnostic" dist/occt-js.d.ts src/importer.hpp src/revolved-tool.hpp src/revolved-tool.cpp src/js-interface.cpp` returns matches.
    - `npm run build:wasm:win && node --test test/revolved_tool_spec_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/revolved_tool_spec_contract.test.mjs"</automated>
  </verify>
  <done>The repository now has a strict root revolved-tool validation surface and published spec typings ready for geometry-build work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied JS spec -> root validation seam | Malformed specs can become OCCT crashes, null shapes, or silent geometry corruption if they bypass strict validation |
| published spec typings -> runtime diagnostics behavior | If types drift from runtime behavior, downstream apps will normalize the wrong contract before build-path work lands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | T/R | strict validation seam | mitigate | Add built-runtime validation contract tests before implementation and reject malformed specs before any OCCT build path |
| T-27-01-02 | T/I | public root type surface | mitigate | Update `dist/occt-js.d.ts` and `src/importer.hpp` together, then verify against the built-runtime validation contract |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/revolved_tool_spec_contract.test.mjs`.
</verification>

<success_criteria>
Downstream JS can validate one strict normalized revolved-tool spec through the built root runtime and receive explicit typed diagnostics without any app-specific schema logic entering the root carrier.
</success_criteria>

<output>
After completion, create `.planning/phases/27-revolved-tool-spec-wasm-builder/27-01-SUMMARY.md`
</output>
