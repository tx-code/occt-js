---
phase: 29-occt-core-sdk-and-governance-for-revolved-shapes
plan: "01"
type: execute
wave: 1
depends_on:
  - 28-02
files_modified:
  - packages/occt-core/src/occt-core.js
  - packages/occt-core/src/index.d.ts
  - packages/occt-core/src/model-normalizer.js
  - packages/occt-core/test/core.test.mjs
  - packages/occt-core/README.md
autonomous: true
requirements:
  - SDK-01
must_haves:
  truths:
    - "`@tx-code/occt-core` exposes package-first wrappers for revolved-shape validation, build, and exact-open flows."
    - "Published package typings mirror the root runtime DTOs instead of inventing a second revolved-shape schema."
    - "Package tests fail if the wrapper surface drifts from the loaded Wasm module."
---

# Phase 29 Plan 01

<objective>
Ship the revolved-shape contract package-first through `@tx-code/occt-core` so downstream JS does not need to call raw Wasm bindings directly.
</objective>

<implementation_notes>

- Add `validateRevolvedShapeSpec`, `buildRevolvedShape`, and `openExactRevolvedShape` wrappers to `OcctCoreClient`.
- Publish the wrapper surface in `packages/occt-core/src/index.d.ts`.
- Normalize generated revolved-shape payloads without introducing a second contract layer.
- Cover success and missing-method failure paths in package tests.

</implementation_notes>

<verification>
- `npm --prefix packages/occt-core test`
- `npm test`
</verification>

<output>
After completion, create `.planning/phases/29-occt-core-sdk-and-governance-for-revolved-shapes/29-01-SUMMARY.md`
</output>
