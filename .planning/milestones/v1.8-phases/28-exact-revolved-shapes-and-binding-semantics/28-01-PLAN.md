---
phase: 28-exact-revolved-shapes-and-binding-semantics
plan: "01"
type: execute
wave: 1
depends_on:
  - 27-02
files_modified:
  - src/importer.hpp
  - src/revolved-tool.hpp
  - src/revolved-tool.cpp
  - src/js-interface.cpp
  - dist/occt-js.d.ts
  - test/generated_revolved_tool_contract.test.mjs
  - test/exact_generated_revolved_tool_contract.test.mjs
autonomous: true
requirements:
  - GEOM-03
  - GEOM-02
must_haves:
  truths:
    - "The root Wasm carrier exposes `OpenExactRevolvedShape(spec, options?)` and reuses the retained exact-model lifecycle instead of inventing a parallel ownership scheme."
    - "Generated revolved shapes return `exactModelId`, `exactGeometryBindings`, and exact/mesh validation metadata alongside the canonical scene payload."
    - "Invalid revolved-shape specs fail explicitly without leaking retained exact handles."
---

# Phase 28 Plan 01

<objective>
Add the exact-open lane for generated revolved shapes so downstream callers can build one normalized revolved profile and immediately reuse the existing retained exact-model query surface against it.
</objective>

<implementation_notes>

- Extend the generated-shape runtime surface with `OpenExactRevolvedShape(...)`.
- Keep the exact-open result additive: canonical scene payload plus `exactModelId`, `exactGeometryBindings`, and `revolvedShape.shapeValidation`.
- Cover success, exact-store registration, representative exact-family queries, and invalid-spec failure behavior in root contract tests.

</implementation_notes>

<verification>
- `npm run build:wasm:win`
- `node --test test/generated_revolved_tool_contract.test.mjs test/exact_generated_revolved_tool_contract.test.mjs`
</verification>

<output>
After completion, create `.planning/phases/28-exact-revolved-shapes-and-binding-semantics/28-01-SUMMARY.md`
</output>
