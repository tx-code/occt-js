---
phase: 28-exact-revolved-shapes-and-binding-semantics
plan: "02"
type: execute
wave: 1
depends_on:
  - 28-01
files_modified:
  - src/importer.hpp
  - src/revolved-tool.hpp
  - src/revolved-tool.cpp
  - src/js-interface.cpp
  - dist/occt-js.d.ts
  - test/generated_revolved_tool_contract.test.mjs
  - test/exact_generated_revolved_tool_contract.test.mjs
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - GEOM-02
must_haves:
  truths:
    - "Generated revolved shapes expose stable `revolvedShape.faceBindings` with segment index plus optional segment id/tag instead of depending on raw face order."
    - "Closure, axis, cap, profile, and degenerated surfaces are reported through explicit runtime-owned `systemRole` values."
    - "Default face colors and material grouping are derived from runtime semantics, not caller-supplied colors."
---

# Phase 28 Plan 02

<objective>
Capture stable revolve-history semantics so downstream apps can reason about generated faces deterministically and render them with runtime-owned semantic grouping.
</objective>

<implementation_notes>

- Promote OCCT revolve history into `revolvedShape.faceBindings`.
- Preserve caller segment provenance for axis-touching and coplanar cases.
- Emit deterministic default appearance groups tied to `systemRole` and segment tags.
- Lock the behavior with root contract tests for full revolve, partial revolve, and semantic edge cases.

</implementation_notes>

<verification>
- `npm run build:wasm:win`
- `node --test test/generated_revolved_tool_contract.test.mjs test/exact_generated_revolved_tool_contract.test.mjs`
- `npm test`
</verification>

<output>
After completion, create `.planning/phases/28-exact-revolved-shapes-and-binding-semantics/28-02-SUMMARY.md`
</output>
