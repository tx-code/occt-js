---
phase: 30-shared-profile-kernel
plan: "02"
type: execute
wave: 1
depends_on:
  - 30-01
files_modified:
  - dist/occt-js.d.ts
  - package.json
  - src/importer.hpp
  - src/profile-2d.hpp
  - src/profile-2d.cpp
  - src/revolved-tool.hpp
  - src/revolved-tool.cpp
  - src/js-interface.cpp
  - test/revolved_tool_spec_contract.test.mjs
  - test/generated_revolved_tool_contract.test.mjs
  - test/exact_generated_revolved_tool_contract.test.mjs
  - packages/occt-core/test/core.test.mjs
autonomous: true
requirements:
  - PROF-03
must_haves:
  truths:
    - "`BuildRevolvedShape(...)` and `OpenExactRevolvedShape(...)` consume the shared profile kernel rather than duplicating family-local profile parsing and continuity logic."
    - "Revolved-family synthetic closure behavior such as `auto_axis` remains family-owned and preserves runtime-owned provenance for existing face-binding and appearance semantics."
    - "Generated-scene, exact-open, and package-wrapper behavior for the shipped revolved-shape contract remains stable after the shared-kernel extraction."
  artifacts:
    - path: src/revolved-tool.cpp
      provides: revolved family adapter over the shared profile kernel
      contains: explicit-to-shared adaptation, family-owned synthetic closure generation, build/openExact reuse, and preserved metadata semantics
    - path: src/profile-2d.cpp
      provides: shared normalized segment/provenance seam
      contains: normalized segment output reused by revolved validation and build paths
    - path: test/generated_revolved_tool_contract.test.mjs
      provides: generated-scene regression coverage
      contains: build, face-binding, shape-validation, and appearance-group invariants after the shared-kernel refactor
    - path: test/exact_generated_revolved_tool_contract.test.mjs
      provides: exact-open regression coverage
      contains: retained exact lifecycle and exact-family alignment invariants after the shared-kernel refactor
    - path: packages/occt-core/test/core.test.mjs
      provides: downstream wrapper drift guard
      contains: package-facing revolved-shape wrapper tests remain green against the refactored root runtime
  key_links:
    - from: src/revolved-tool.cpp
      to: src/profile-2d.cpp
      via: revolved builder/openExact flows consume normalized shared profile segments and diagnostics
      pattern: NormalizeProfile2D|BuildProfile2DWire
    - from: test/generated_revolved_tool_contract.test.mjs
      to: src/revolved-tool.cpp
      via: generated-scene contract proves the family adapter preserved face-binding and appearance semantics
      pattern: BuildRevolvedShape|faceBindings|shapeValidation
    - from: packages/occt-core/test/core.test.mjs
      to: dist/occt-js.d.ts
      via: package wrapper surface remains compatible with the refactored root contract
      pattern: buildRevolvedShape|openExactRevolvedShape
---

<objective>
Refactor the revolved-shape runtime to consume the shared profile kernel without public API regressions.

Purpose: satisfy `PROF-03` by moving build/openExact flows onto the new shared profile seam while preserving the shipped generated-scene, face-binding, exact-open, and package-wrapper behavior.
Output: revolved family adapter over the shared kernel + regression coverage proving no public drift.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-shared-profile-kernel/30-CONTEXT.md
@.planning/phases/30-shared-profile-kernel/30-RESEARCH.md
@.planning/phases/30-shared-profile-kernel/30-PATTERNS.md
@.planning/phases/30-shared-profile-kernel/30-VALIDATION.md
@.planning/phases/30-shared-profile-kernel/30-01-PLAN.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/profile-2d.hpp
@src/profile-2d.cpp
@src/revolved-tool.hpp
@src/revolved-tool.cpp
@src/js-interface.cpp
@test/revolved_tool_spec_contract.test.mjs
@test/generated_revolved_tool_contract.test.mjs
@test/exact_generated_revolved_tool_contract.test.mjs
@packages/occt-core/test/core.test.mjs

<interfaces>
From `src/revolved-tool.cpp` (current family-owned synthetic closure behavior to preserve):
```cpp
if (spec.profile.closure == "auto_axis") {
    const std::array<double, 2> axisCurrent = { 0.0, currentPoint[1] };
    const std::array<double, 2> axisStart = { 0.0, spec.profile.start[1] };
    ...
}
```

From `test/generated_revolved_tool_contract.test.mjs` (public build behavior already locked):
```js
assert.equal(result.revolvedShape.hasStableFaceBindings, true);
assert.ok(result.revolvedShape.faceBindings.some((binding) => binding.systemRole === "closure"));
```

From `test/exact_generated_revolved_tool_contract.test.mjs` (public exact-open behavior already locked):
```js
assert.equal(result?.sourceFormat, "generated-revolved-shape");
assert.equal(typeof result?.exactModelId, "number");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression tests that lock revolved build and exact-open behavior through the shared-kernel refactor</name>
  <read_first>test/generated_revolved_tool_contract.test.mjs, test/exact_generated_revolved_tool_contract.test.mjs, test/revolved_tool_spec_contract.test.mjs, packages/occt-core/test/core.test.mjs, .planning/phases/30-shared-profile-kernel/30-RESEARCH.md</read_first>
  <files>test/revolved_tool_spec_contract.test.mjs, test/generated_revolved_tool_contract.test.mjs, test/exact_generated_revolved_tool_contract.test.mjs, packages/occt-core/test/core.test.mjs</files>
  <behavior>
    - Test 1: generated-scene tests fail until explicit and `auto_axis` revolved builds both preserve current face-binding and semantic appearance behavior while using the shared profile kernel underneath.
    - Test 2: exact-open tests fail until retained exact generated shapes still expose the current lifecycle, exact-geometry bindings, and representative exact-family mappings after the refactor.
    - Test 3: package wrapper tests fail until the `occt-core` package still calls the same revolved runtime entrypoints successfully against the refactored root module.
  </behavior>
  <action>Extend the existing built-runtime and package tests only where needed to make the shared-kernel extraction observable. Prefer regression guards over new public behavior in this plan. Keep test names and assertions focused on no-drift guarantees, not on internal implementation details.</action>
  <acceptance_criteria>
    - `rg -n "shared profile kernel|shared-kernel|auto_axis" test/generated_revolved_tool_contract.test.mjs test/exact_generated_revolved_tool_contract.test.mjs packages/occt-core/test/core.test.mjs` returns matches after adding drift guards.
    - `npm run build:wasm:win && node --test test/generated_revolved_tool_contract.test.mjs test/exact_generated_revolved_tool_contract.test.mjs && npm --prefix packages/occt-core test` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/generated_revolved_tool_contract.test.mjs test/exact_generated_revolved_tool_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test"</automated>
  </verify>
  <done>The repository now has explicit no-drift regression guards for the revolved build/openExact/package surfaces during the shared-kernel refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor revolved build and exact-open flows onto the shared profile kernel while preserving public behavior</name>
  <read_first>src/profile-2d.hpp, src/profile-2d.cpp, src/revolved-tool.hpp, src/revolved-tool.cpp, src/importer.hpp, src/js-interface.cpp, dist/occt-js.d.ts, test/generated_revolved_tool_contract.test.mjs, test/exact_generated_revolved_tool_contract.test.mjs, packages/occt-core/test/core.test.mjs</read_first>
  <files>dist/occt-js.d.ts, package.json, src/importer.hpp, src/profile-2d.hpp, src/profile-2d.cpp, src/revolved-tool.hpp, src/revolved-tool.cpp, src/js-interface.cpp, test/revolved_tool_spec_contract.test.mjs, test/generated_revolved_tool_contract.test.mjs, test/exact_generated_revolved_tool_contract.test.mjs, packages/occt-core/test/core.test.mjs</files>
  <behavior>
    - Test 1: `BuildRevolvedShape(...)` and `OpenExactRevolvedShape(...)` adapt current caller specs into the shared profile kernel instead of keeping duplicated family-local profile parsing.
    - Test 2: family-owned `auto_axis` synthesis remains outside the shared kernel and keeps runtime-owned provenance distinct from caller profile segments.
    - Test 3: current generated-scene metadata, exact-open payloads, and package wrapper behavior remain stable.
    - Test 4: root npm test routing includes the new shared-profile contract file so the internal refactor cannot drift silently.
  </behavior>
  <action>Refactor `src/revolved-tool.cpp` so the revolved runtime builds on the shared normalized profile seam for validation and wire construction, while keeping family-only concerns in the revolved layer. Preserve existing public result shapes and diagnostics vocabulary. Update root test routing if the new shared-profile contract file is not yet included.</action>
  <acceptance_criteria>
    - `rg -n "profile-2d|ValidateProfile2DSpec|BuildRevolvedShape|OpenExactRevolvedShape" src/revolved-tool.cpp src/profile-2d.cpp src/js-interface.cpp dist/occt-js.d.ts` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs test/generated_revolved_tool_contract.test.mjs test/exact_generated_revolved_tool_contract.test.mjs` passes.
    - `npm --prefix packages/occt-core test` passes.
    - `npm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs test/generated_revolved_tool_contract.test.mjs test/exact_generated_revolved_tool_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm --prefix packages/occt-core test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test"</automated>
  </verify>
  <done>The shipped revolved-shape runtime now consumes the shared profile kernel internally while preserving its public validation, build, exact-open, and package-wrapper behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared profile kernel -> revolved build/openExact family adapter | If family-owned closure/provenance is flattened during refactor, existing face-binding and appearance semantics will silently change |
| refactored root runtime -> package wrapper surface | Internal runtime extraction can still break package callers if tests do not lock the public wrapper behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | T/R | revolved family adapter | mitigate | Add generated-scene and exact-open regression guards before refactoring the runtime onto the shared kernel |
| T-30-02-02 | T/I | root/package contract routing | mitigate | Keep root test commands and package-wrapper tests green so internal extraction cannot drift the shipped public surface unnoticed |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs test/generated_revolved_tool_contract.test.mjs test/exact_generated_revolved_tool_contract.test.mjs`.
- Run `npm --prefix packages/occt-core test`.
- Run `npm test`.
</verification>

<success_criteria>
The revolved-shape runtime reuses the shared profile kernel internally, yet downstream callers and package wrappers still observe the same public revolved validation/build/exact-open behavior they already rely on.
</success_criteria>

<output>
After completion, create `.planning/phases/30-shared-profile-kernel/30-02-SUMMARY.md`
</output>
