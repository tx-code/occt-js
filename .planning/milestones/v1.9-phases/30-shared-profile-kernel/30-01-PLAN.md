---
phase: 30-shared-profile-kernel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/occt-js.d.ts
  - package.json
  - src/importer.hpp
  - src/profile-2d.hpp
  - src/profile-2d.cpp
  - src/revolved-tool.hpp
  - src/revolved-tool.cpp
  - src/js-interface.cpp
  - test/profile_2d_spec_contract.test.mjs
  - test/revolved_tool_spec_contract.test.mjs
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
must_haves:
  truths:
    - "The root Wasm carrier exposes one additive generic `ValidateProfile2DSpec(spec)` entrypoint and published `Profile2D` DTOs without reintroducing tool-coupled naming."
    - "Shared profile diagnostics cover continuity, explicit closure, unsupported segment kinds, and degenerate arc/segment input using family-neutral vocabulary."
    - "`ValidateRevolvedShapeSpec(spec)` reuses the shared profile kernel while preserving its current caller-visible validation behavior for units, plane, closure mode, radius, and revolve angle."
  artifacts:
    - path: dist/occt-js.d.ts
      provides: published shared profile contract
      contains: `OcctJSProfile2DSpec`, shared segment types, shared diagnostics, and `ValidateProfile2DSpec(...)`
    - path: src/importer.hpp
      provides: shared native DTO seam
      contains: family-neutral profile structs and diagnostics plus adapted revolved-shape DTOs
    - path: src/profile-2d.hpp
      provides: generic profile-kernel declarations
      contains: parse/normalize/validate entrypoints and provenance-carrying shared segment types
    - path: src/profile-2d.cpp
      provides: generic profile-kernel implementation
      contains: local-2D profile parsing, continuity/closure validation, and typed diagnostics
    - path: src/js-interface.cpp
      provides: additive Embind validation surface
      contains: `ValidateProfile2DSpec(...)` binding and shared diagnostic serialization
    - path: test/profile_2d_spec_contract.test.mjs
      provides: built-runtime contract coverage for the generic shared profile validator
      contains: valid closed profile, discontinuity/open-loop failures, and non-revolve-local-coordinate coverage
    - path: test/revolved_tool_spec_contract.test.mjs
      provides: public drift guard for revolved validation
      contains: assertions proving current revolved validation behavior still holds after routing through the shared kernel
  key_links:
    - from: src/js-interface.cpp
      to: src/profile-2d.cpp
      via: thin Embind glue over the generic shared profile validator
      pattern: ValidateProfile2DSpec
    - from: src/revolved-tool.cpp
      to: src/profile-2d.cpp
      via: revolved validation adapter consumes the shared profile normalization/diagnostic seam
      pattern: ParseProfile|ValidateProfile2D
    - from: test/profile_2d_spec_contract.test.mjs
      to: dist/occt-js.d.ts
      via: new root validator contract and published shared DTO vocabulary
      pattern: ValidateProfile2DSpec|OcctJSProfile2D
---

<objective>
Define the shared 2D profile DTOs, diagnostics, and root validation seam before any family build-path refactor lands.

Purpose: satisfy `PROF-01` and `PROF-02`, and lay the `PROF-03` migration boundary, by introducing a generic local-2D profile contract and one additive root validator while preserving the shipped revolved caller contract.
Output: shared `Profile2D` typings + native DTO seam + additive root validator + revolved validation drift guards.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-shared-profile-kernel/30-CONTEXT.md
@.planning/phases/30-shared-profile-kernel/30-RESEARCH.md
@.planning/phases/30-shared-profile-kernel/30-PATTERNS.md
@.planning/phases/30-shared-profile-kernel/30-VALIDATION.md
@AGENTS.md
@docs/specs/2026-04-21-revolved-shape-runtime-boundary-design.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/revolved-tool.hpp
@src/revolved-tool.cpp
@src/js-interface.cpp
@test/load_occt_factory.mjs
@test/revolved_tool_spec_contract.test.mjs

<interfaces>
From `dist/occt-js.d.ts` (current published revolved caller contract to preserve):
```ts
export interface OcctJSRevolvedShapeSpec {
  units: OcctJSRevolvedShapeUnits;
  profile: {
    plane?: OcctJSRevolvedShapePlane;
    start: [number, number];
    closure: OcctJSRevolvedShapeClosure;
    segments: OcctJSRevolvedShapeSegment[];
  };
}
```

From `src/js-interface.cpp` (additive utility binding rhythm to follow):
```cpp
function("ValidateRevolvedShapeSpec", &ValidateRevolvedShapeSpecBinding);
function("BuildRevolvedShape", &BuildRevolvedShapeBinding);
```

From `src/revolved-tool.cpp` (generic logic currently trapped in the family file):
```cpp
bool TryParsePoint2(...);
bool ValidateArcCenterSegment(...);
bool ValidateArc3PointSegment(...);
void ParseSegment(...);
void ParseProfile(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing built-runtime contract tests for the generic shared profile validator and revolved validation drift guards</name>
  <read_first>dist/occt-js.d.ts, src/js-interface.cpp, src/revolved-tool.cpp, test/load_occt_factory.mjs, test/revolved_tool_spec_contract.test.mjs, .planning/phases/30-shared-profile-kernel/30-RESEARCH.md</read_first>
  <files>test/profile_2d_spec_contract.test.mjs, test/revolved_tool_spec_contract.test.mjs</files>
  <behavior>
    - Test 1: a subtest named `ValidateProfile2DSpec accepts a closed line-and-arc profile in generic local 2D coordinates` fails until the built root runtime exposes `ValidateProfile2DSpec(spec)` and accepts a closed profile without treating local coordinates as radius-only.
    - Test 2: a subtest named `ValidateProfile2DSpec rejects discontinuity open loops and malformed arcs with typed diagnostics` fails until discontinuous segments, explicit open loops, and malformed arcs return typed shared diagnostics.
    - Test 3: a subtest named `ValidateRevolvedShapeSpec still rejects negative radius and invalid revolve settings after shared-profile extraction` fails until the current revolved validator keeps its family-owned diagnostics even after routing through the shared kernel.
  </behavior>
  <action>Create `test/profile_2d_spec_contract.test.mjs` using the built runtime via `test/load_occt_factory.mjs`. Use inline JS specs only. Keep the validator generic-first by including at least one valid shared profile that would be invalid under revolved radius semantics. Extend `test/revolved_tool_spec_contract.test.mjs` only to add explicit drift guards for the shared-kernel extraction.</action>
  <acceptance_criteria>
    - `rg -n "ValidateProfile2DSpec accepts a closed line-and-arc profile in generic local 2D coordinates|ValidateProfile2DSpec rejects discontinuity open loops and malformed arcs with typed diagnostics|ValidateRevolvedShapeSpec still rejects negative radius and invalid revolve settings after shared-profile extraction" test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs` returns matches.
    - `rg -n "ValidateProfile2DSpec|profile-not-closed|profile-discontinuous|invalid-arc" test/profile_2d_spec_contract.test.mjs` returns matches.
    - `npm run build:wasm:win && node --test test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs"</automated>
  </verify>
  <done>The built runtime now has a locked generic shared-profile validation contract and a public drift guard around revolved validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared profile DTOs diagnostics and additive root validation while preserving revolved validation behavior</name>
  <read_first>src/importer.hpp, src/revolved-tool.hpp, src/revolved-tool.cpp, src/js-interface.cpp, dist/occt-js.d.ts, test/profile_2d_spec_contract.test.mjs, test/revolved_tool_spec_contract.test.mjs, .planning/phases/30-shared-profile-kernel/30-PATTERNS.md</read_first>
  <files>dist/occt-js.d.ts, package.json, src/importer.hpp, src/profile-2d.hpp, src/profile-2d.cpp, src/revolved-tool.hpp, src/revolved-tool.cpp, src/js-interface.cpp, test/profile_2d_spec_contract.test.mjs, test/revolved_tool_spec_contract.test.mjs</files>
  <behavior>
    - Test 1: the root module exports additive `ValidateProfile2DSpec(spec)` and published shared `Profile2D` DTOs.
    - Test 2: the shared validator uses local-2D family-neutral diagnostics rather than reusing revolved-only rules such as negative radius or revolve angle.
    - Test 3: `ValidateRevolvedShapeSpec(spec)` adapts current caller input into the shared kernel while preserving shipped family-owned diagnostics and result shape.
    - Test 4: root npm test routing includes the new shared-profile contract file once it exists.
  </behavior>
  <action>Add shared profile DTOs to `src/importer.hpp`, implement the generic kernel in `src/profile-2d.hpp/.cpp`, and expose `ValidateProfile2DSpec(...)` through `src/js-interface.cpp` and `dist/occt-js.d.ts`. Update the revolved validation path to call the shared kernel after applying family-owned parsing/validation rules. Route the new root contract file through `package.json` if `npm test` needs explicit enumeration.</action>
  <acceptance_criteria>
    - `rg -n "ValidateProfile2DSpec|OcctJSProfile2DSpec|OcctJSProfile2DValidationResult|OcctProfile2D" dist/occt-js.d.ts src/importer.hpp src/profile-2d.hpp src/profile-2d.cpp src/js-interface.cpp` returns matches.
    - `rg -n "profile_2d_spec_contract" package.json` returns matches if the root test scripts enumerate contract files explicitly.
    - `npm run build:wasm:win && node --test test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs"</automated>
  </verify>
  <done>The repository now owns a generic shared-profile validation seam and published DTOs while the shipped revolved validator remains stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied shared profile -> root validator | If the shared validator remains revolve-first, future solid families will inherit the wrong geometry rules and callers will validate against the wrong contract |
| shared profile kernel -> revolved family validator | If the family adapter loses revolved-only checks during extraction, the shipped revolved caller contract will silently drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | T/R | shared profile validator | mitigate | Add built-runtime contract tests proving the shared kernel accepts generic local-2D input and rejects discontinuity/open-loop/malformed-arc cases with typed diagnostics |
| T-30-01-02 | T/I | revolved validation adapter | mitigate | Extend the existing revolved validation contract tests to lock negative-radius and revolve-specific diagnostics after the extraction |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/profile_2d_spec_contract.test.mjs test/revolved_tool_spec_contract.test.mjs`.
</verification>

<success_criteria>
Downstream JS can validate one generic local-2D profile directly through the root Wasm carrier, and existing revolved-shape callers continue to receive their shipped validation behavior while reusing that shared kernel internally.
</success_criteria>

<output>
After completion, create `.planning/phases/30-shared-profile-kernel/30-01-SUMMARY.md`
</output>
