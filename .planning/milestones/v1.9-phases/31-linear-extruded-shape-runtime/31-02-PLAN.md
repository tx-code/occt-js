---
phase: 31-linear-extruded-shape-runtime
plan: "02"
type: execute
wave: 1
depends_on:
  - 31-01
files_modified:
  - dist/occt-js.d.ts
  - src/importer.hpp
  - src/extruded-shape.hpp
  - src/extruded-shape.cpp
  - src/js-interface.cpp
  - test/generated_extruded_shape_contract.test.mjs
  - test/exact_generated_extruded_shape_contract.test.mjs
autonomous: true
requirements:
  - MAP-03
  - MAP-04
must_haves:
  truths:
    - "Extruded wall faces preserve caller segment provenance, while `start_cap` and `end_cap` remain runtime-owned bindings that never claim caller segment ids or tags."
    - "Generated extruded shapes expose deterministic runtime-owned wall/cap appearance grouping without caller-supplied face colors."
    - "Representative exact face families stay aligned with the semantic bindings: line walls remain planar, circular-arc walls canonize to cylinders where supported, and caps remain planes."
  artifacts:
    - path: dist/occt-js.d.ts
      provides: published extruded semantic metadata contract
      contains: `extrudedShape` face-binding, role, and appearance-related typings
    - path: src/importer.hpp
      provides: native extruded semantic metadata DTO seam
      contains: extruded face-binding, shape-validation, and segment descriptor structs needed by the new family
    - path: src/extruded-shape.cpp
      provides: semantic metadata implementation
      contains: stable wall/cap face binding resolution, runtime role assignment, deterministic appearance grouping, and representative exact-family alignment
    - path: src/js-interface.cpp
      provides: additive metadata serialization
      contains: `extrudedShape` metadata emission for generated scene and exact-open results
    - path: test/generated_extruded_shape_contract.test.mjs
      provides: generated-scene semantic regression coverage
      contains: stable face bindings, runtime roles, semantic appearance grouping, and topology/validation invariants
    - path: test/exact_generated_extruded_shape_contract.test.mjs
      provides: exact-open semantic regression coverage
      contains: retained exact lifecycle plus representative exact-family mapping for wall/cap bindings
  key_links:
    - from: src/extruded-shape.cpp
      to: occt/src/BRepPrimAPI/BRepPrimAPI_MakePrism.hxx
      via: prism history drives wall and cap binding resolution
      pattern: Generated|FirstShape|LastShape
    - from: test/generated_extruded_shape_contract.test.mjs
      to: src/extruded-shape.cpp
      via: generated-scene contract proves wall/cap semantics and runtime-owned appearance grouping
      pattern: BuildExtrudedShape|faceBindings|shapeValidation
    - from: test/exact_generated_extruded_shape_contract.test.mjs
      to: src/extruded-shape.cpp
      via: exact-open contract proves semantic bindings remain aligned with representative exact families
      pattern: OpenExactExtrudedShape|GetExactGeometryType
---

<objective>
Capture stable semantic bindings and deterministic runtime-owned appearance for generated extruded shapes.

Purpose: satisfy `MAP-03` and `MAP-04` by attaching stable `wall` / `start_cap` / `end_cap` metadata, deterministic face-group colors, and representative exact-family regressions to the additive generated-extruded-shape family built in Plan 31-01.
Output: `extrudedShape` semantic metadata + stable face binding regression coverage + exact-family alignment guards.
</objective>

<execution_context>
@C:/Users/tangx/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tangx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-linear-extruded-shape-runtime/31-CONTEXT.md
@.planning/phases/31-linear-extruded-shape-runtime/31-RESEARCH.md
@.planning/phases/31-linear-extruded-shape-runtime/31-PATTERNS.md
@.planning/phases/31-linear-extruded-shape-runtime/31-VALIDATION.md
@.planning/phases/31-linear-extruded-shape-runtime/31-01-PLAN.md
@AGENTS.md
@dist/occt-js.d.ts
@src/importer.hpp
@src/extruded-shape.hpp
@src/extruded-shape.cpp
@src/js-interface.cpp
@test/generated_revolved_tool_contract.test.mjs
@test/exact_generated_revolved_tool_contract.test.mjs
@test/generated_extruded_shape_contract.test.mjs
@test/exact_generated_extruded_shape_contract.test.mjs

<interfaces>
From `occt/src/BRepPrimAPI/BRepPrimAPI_MakePrism.hxx` (history hooks to resolve semantic faces):
```cpp
Standard_EXPORT virtual const TopTools_ListOfShape& Generated(const TopoDS_Shape& S) Standard_OVERRIDE;
Standard_EXPORT TopoDS_Shape FirstShape(const TopoDS_Shape& theShape);
Standard_EXPORT TopoDS_Shape LastShape(const TopoDS_Shape& theShape);
```

From `test/generated_revolved_tool_contract.test.mjs` (generated-family semantic precedent to mirror):
```js
assert.equal(result.revolvedShape.hasStableFaceBindings, true);
assert.ok(result.revolvedShape.faceBindings.some((binding) => binding.systemRole === "closure"));
```

From `test/exact_generated_revolved_tool_contract.test.mjs` (exact-open semantic precedent to mirror):
```js
assert.equal(result?.sourceFormat, "generated-revolved-shape");
assert.equal(typeof result?.exactModelId, "number");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression tests for extruded semantic face bindings and runtime-owned appearance groups</name>
  <read_first>test/generated_revolved_tool_contract.test.mjs, test/exact_generated_revolved_tool_contract.test.mjs, test/generated_extruded_shape_contract.test.mjs, test/exact_generated_extruded_shape_contract.test.mjs, .planning/phases/31-linear-extruded-shape-runtime/31-RESEARCH.md</read_first>
  <files>test/generated_extruded_shape_contract.test.mjs, test/exact_generated_extruded_shape_contract.test.mjs</files>
  <behavior>
    - Test 1: generated-scene tests fail until wall faces keep caller segment ids/tags while `start_cap` and `end_cap` stay runtime-owned.
    - Test 2: generated-scene tests fail until deterministic default colors group matching wall semantics and keep cap appearance distinct.
    - Test 3: exact-open tests fail until representative exact families stay aligned with wall/cap semantic bindings.
  </behavior>
  <action>Extend the new extruded-family regression suites only where needed. Keep test names focused on stable `wall` / `start_cap` / `end_cap` behavior, runtime-owned appearance groups, and representative exact-family alignment rather than internal implementation details.</action>
  <acceptance_criteria>
    - `rg -n "wall|start_cap|end_cap|semantic appearance|stable face bindings" test/generated_extruded_shape_contract.test.mjs test/exact_generated_extruded_shape_contract.test.mjs` returns matches.
    - `npm run build:wasm:win && node --test test/generated_extruded_shape_contract.test.mjs test/exact_generated_extruded_shape_contract.test.mjs` fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/generated_extruded_shape_contract.test.mjs test/exact_generated_extruded_shape_contract.test.mjs"</automated>
  </verify>
  <done>The repository now has explicit extruded-family regression guards for stable wall/cap semantics, appearance grouping, and exact-family alignment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stable wall/cap bindings and deterministic semantic appearance for the generated extruded family</name>
  <read_first>src/importer.hpp, src/extruded-shape.hpp, src/extruded-shape.cpp, src/js-interface.cpp, dist/occt-js.d.ts, test/generated_extruded_shape_contract.test.mjs, test/exact_generated_extruded_shape_contract.test.mjs, .planning/phases/31-linear-extruded-shape-runtime/31-PATTERNS.md</read_first>
  <files>dist/occt-js.d.ts, src/importer.hpp, src/extruded-shape.hpp, src/extruded-shape.cpp, src/js-interface.cpp, test/generated_extruded_shape_contract.test.mjs, test/exact_generated_extruded_shape_contract.test.mjs</files>
  <behavior>
    - Test 1: wall faces derive stable bindings from profile-segment provenance using prism history rather than face order.
    - Test 2: caps remain runtime-owned `start_cap` / `end_cap` bindings and never claim caller segment ids or tags.
    - Test 3: deterministic face colors/material groups derive from wall segment tags and cap roles without caller-supplied colors.
    - Test 4: representative exact-family checks stay aligned with semantic bindings for at least one mixed line-and-arc extruded profile.
  </behavior>
  <action>Implement semantic face-binding and appearance metadata in the extruded family, reusing the generated-family pattern already proven in the revolved runtime where possible. Publish the additive `extrudedShape` metadata fields through the root surface and keep canonical geometry DTOs unchanged.</action>
  <acceptance_criteria>
    - `rg -n "wall|start_cap|end_cap|faceBindings|hasStableFaceBindings|shapeValidation" dist/occt-js.d.ts src/importer.hpp src/extruded-shape.cpp src/js-interface.cpp` returns matches.
    - `npm run build:wasm:win` succeeds.
    - `node --test test/generated_extruded_shape_contract.test.mjs test/exact_generated_extruded_shape_contract.test.mjs` passes.
    - `npm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "npm run build:wasm:win; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/generated_extruded_shape_contract.test.mjs test/exact_generated_extruded_shape_contract.test.mjs; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test"</automated>
  </verify>
  <done>The shipped generated-extruded-shape family now exposes stable wall/cap bindings, deterministic semantic appearance, and representative exact-family alignment without expanding the caller-owned spec boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCCT prism history -> extruded semantic metadata | If wall/cap provenance is reconstructed from face order instead of build history, bindings will drift silently on topology changes |
| runtime-owned semantic coloring -> downstream scene consumers | If wall/cap appearance grouping is unstable, downstream apps cannot rely on deterministic generated-family visuals or material counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | T/R | extruded face-binding adapter | mitigate | Add generated-scene and exact-open regression guards proving wall provenance and runtime-owned caps survive representative mixed line/arc profiles |
| T-31-02-02 | T/I | semantic appearance grouping | mitigate | Lock deterministic role/tag grouping through contract tests and run the full root test lane after metadata changes |
</threat_model>

<verification>
- Run `npm run build:wasm:win`.
- Run `node --test test/generated_extruded_shape_contract.test.mjs test/exact_generated_extruded_shape_contract.test.mjs`.
- Run `npm test`.
</verification>

<success_criteria>
Generated extruded shapes expose stable semantic wall/cap metadata and deterministic runtime-owned appearance groups, and representative exact-family checks prove the exported semantic bindings still align with the retained exact geometry.
</success_criteria>

<output>
After completion, create `.planning/phases/31-linear-extruded-shape-runtime/31-02-SUMMARY.md`
</output>
