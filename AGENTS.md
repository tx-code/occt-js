# AGENTS.md

This file is the primary repository-level guidance for coding agents working in this repository.

## Project Overview

`occt-js` is a WebAssembly build of OpenCASCADE Technology (OCCT) v7.9.3 for CAD import and triangulation.

The repository has three active surfaces:

- the root npm package that produces `dist/occt-js.js` and `dist/occt-js.wasm`
- the browser demo under `demo/`
- the Windows desktop MVP under `demo/src-tauri/`

The desktop app is additive. It must not replace or break the web app or the root package publishing flow.

## Repository Structure

- `src/`
  - C++ bridge, importers, and Embind-facing API
- `occt/`
  - OCCT source as a git submodule
- `tools/`
  - Wasm build and setup scripts
- `test/`
  - root Wasm/runtime verification
- `dist/`
  - distributable Wasm loader artifacts consumed by tests, web dev, and desktop packaging
- `demo/`
  - React + Vite viewer app
- `demo/src-tauri/`
  - Tauri desktop shell
- `packages/`
  - package-layer modules such as `occt-core`, `occt-babylon-loader`, `occt-babylon-viewer`, and `occt-babylon-widgets`

### Babylon Package Surfaces

- `packages/occt-babylon-loader/`
  - Babylon node/material construction from OCCT model data
- `packages/occt-babylon-viewer/`
  - Babylon viewer runtime for caller-supplied scenes
- `packages/occt-babylon-widgets/`
  - Optional Babylon viewer widgets with no React dependency

## Canonical Build Contracts

### Root Wasm Artifacts

The root package's canonical runtime artifacts are:

- `dist/occt-js.js`
- `dist/occt-js.mjs`
- `dist/occt-js.wasm`
- `dist/occt-js.d.ts`

Those files are consumed by:

- root runtime tests
- local web development paths
- the Tauri desktop app bundle

If C++ code, bindings, or `CMakeLists.txt` change, regenerate `dist/` before claiming the repository is healthy.
`dist/occt-js.d.ts` is a tracked package file and is not regenerated by the Wasm build scripts. If it is missing, restore it from git instead of expecting the Wasm build to recreate it.

### Module Export Contract (Browser/Vite)

- `import("@tx-code/occt-js")` must resolve to a pure ESM entry and return a callable factory at `default`.
- Consumer code must not require compatibility branches for `module.exports`, named `OcctJS`, or nested `default` shapes.
- Keep export routing explicit in `package.json`:
  - `exports["."].import` and `exports["."].default` -> `./dist/occt-js.mjs`
  - `exports["."].require` and `exports["."].node` -> `./dist/occt-js.js`
  - `exports["."].types` -> `./dist/occt-js.d.ts`
- Browser bundlers (Vite) must support this chain without fallbacks:
  - `import("@tx-code/occt-js")`
  - `import("@tx-code/occt-js/dist/occt-js.wasm?url")`
  - `OcctJS({ locateFile })` where `locateFile` returns the wasm URL.
- ESM runtime bootstrap must not depend only on `document.currentScript`; preserve `import.meta.url`-based fallback behavior.

### Windows Wasm Toolchain

On Windows, the Emscripten SDK is expected to live under:

- `build/wasm/emsdk`

Do not assume a repository-root `emsdk/` directory exists.

## Build And Test Entry Points

### Wasm Build

If the clone or worktree does not already have OCCT sources:

```bash
git submodule update --init --recursive occt
```

Windows:

```bash
tools/setup_emscripten_win.bat
npm run build:wasm:win
```

If a Windows parallel build fails and the retained log points to a transient compile issue, retry with:

```bash
set BUILD_JOBS=1 && tools\build_wasm_win.bat Release
```

Linux/macOS:

```bash
bash tools/build_wasm.sh
```

### Root Tests

Maintainers have two root verification entrypoints:

- `npm run test:wasm:preflight`
  - fast prerequisite and `dist/` consumer checks
- `npm test`
  - full root runtime verification after `npm run build:wasm:win`

Maintainers have one root release gate and three separate optional audits:

- `npm run test:release:root`
  - canonical runtime-first release verification for the root Wasm carrier
  - covers the Windows Wasm build, governance contract tests, `packages/occt-core`, and the full root runtime suite
  - excludes unconditional demo, Babylon, and Tauri checks
- `npm run test:planning:audit`
  - optional planning/archive audit for `.planning/` milestone and archive consistency
  - separate from the authoritative root release gate
- `npm run test:secondary:contracts`
  - optional manifest/docs audit for conditional demo, Babylon, and Tauri verification routing
  - separate from the authoritative root release gate
- `npm run test:perf:exact` and `npm run test:soak:exact`
  - optional lifecycle/performance evidence lanes for maintainers
  - separate from the authoritative root release gate

Root tests require the `dist/` Wasm artifacts to exist first:

```bash
npm test
```

### Demo App

From `demo/`:

```bash
npm run test
npm run test:e2e
npm run dev
npm run build
npm run tauri:dev
npm run tauri:build
```

Use `tauri:*` only for desktop work. Root npm publishing must remain independent of Tauri.

## Architecture Notes

### Core Flow

```text
CAD bytes (Uint8Array)
    -> js-interface.cpp
    -> importer-step.cpp / importer-iges.cpp / importer-brep.cpp
    -> importer-utils.cpp
    -> structured JS result via Embind
```

### Key C++ Files

- `src/importer.hpp`
  - shared data structures
- `src/importer-step.cpp`
  - STEP import and XDE traversal
- `src/importer-iges.cpp`
  - IGES import
- `src/importer-brep.cpp`
  - BREP import
- `src/importer-utils.cpp`
  - triangulation and geometry extraction
- `src/js-interface.cpp`
  - Embind bindings and JS-facing API
- `CMakeLists.txt`
  - OCCT module selection and Wasm build setup

### Demo / Desktop Runtime Split

- `demo/` is the single UI project for both browser and desktop
- desktop-specific behavior belongs in `demo/src-tauri/` and desktop-focused helpers inside `demo/src/`
- do not make the web app depend on Tauri-only runtime assumptions

## Output Format

The root API returns structured scene data aligned with the Babylon-facing consumer contract:

```text
{ sourceFormat, rootNodes, geometries, materials, warnings, stats }
```

Important details:

- geometry arrays are typed arrays or typed-array-compatible data
- `rootNodes` form a tree, not a flat list
- transform matrices are column-major for Babylon compatibility

## Testing Expectations

- root Wasm/runtime tests belong under `test/`
- browser and viewer interaction tests belong under `demo/tests/`
- if the root Wasm contract changes, verify root tests first
- if viewer behavior changes, verify `demo` build and relevant viewer tests
- if package export or loader bootstrap logic changes, run:
  - `node --test test/esm_entry_contract.test.mjs`
  - `node --test test/package_tarball_contract.test.mjs test/dist_contract_consumers.test.mjs`
  - `npm --prefix demo run test:e2e` (includes browser dynamic-import + wasm-url contract lane)
- use `npm run test:release:root` for root release verification
- use `npm run test:planning:audit` only when intentionally auditing `.planning` milestone/archive consistency
- use `npm run test:secondary:contracts` only when intentionally auditing the conditional secondary-surface command/docs contract
- use `npm run test:perf:exact` and `npm run test:soak:exact` only when intentionally collecting lifecycle/performance stress evidence
- keep conditional secondary-surface verification limited to changes under `demo/`, `demo/src-tauri/`, or Babylon package surfaces
- `npm --prefix demo run test:e2e` is the explicit demo browser lane for app-home smoke plus measurement interaction regression; it remains conditional secondary-surface verification, not a root release gate
- changes under `demo/`, `demo/src/`, or `demo/tests/`: run `npm --prefix demo test`, `npm --prefix demo run test:e2e`, and `npm --prefix demo run build`
- changes under `demo/src-tauri/`: run `npm --prefix demo run tauri:build`
- changes under `packages/occt-babylon-loader/`: run `npm --prefix packages/occt-babylon-loader test`
- changes under `packages/occt-babylon-viewer/`: run `npm --prefix packages/occt-babylon-viewer test`
- changes under `packages/occt-babylon-widgets/`: run `npm --prefix packages/occt-babylon-widgets test`

## Release Boundaries

- the root package publishes the Wasm runtime
- import appearance options such as `appearancePreset`, `colorMode`, `defaultColor`, and `defaultOpacity` are part of that root runtime contract
- `demo/` is not the published npm package
- desktop work must not become a prerequisite for npm release
- `.planning` audits remain separate process checks outside the authoritative root release gate
- `npm run test:secondary:contracts` remains a separate process/doc-routing audit outside the authoritative root release gate
- `npm run test:perf:exact` and `npm run test:soak:exact` remain explicit optional lifecycle/performance verification lanes outside the authoritative root release gate
- demo, Babylon, and Tauri checks are conditional secondary-surface verification, not unconditional root release gates
- documentation for agent behavior should live here, not be duplicated in tool-specific files
- settings persistence and viewer overrides remain downstream concerns outside the root runtime scope

## Versioning Semantics

- GSD milestone versions such as `v1.4` are planning and delivery markers. They describe a shipped repository milestone, not the npm package semver by themselves.
- npm package versions are controlled separately through `package.json` and any repo-local package manifests that are intentionally version-aligned.
- Archiving a milestone or creating a git tag such as `v1.4` does not, by itself, mean `@tx-code/occt-js` has been version-bumped, published to npm, or vendored into downstream apps.
- Downstream vendored snapshots such as `imos-app/third_party/npm/@tx-code/occt-js` can legitimately lag behind the current repository HEAD until an explicit vendor refresh is performed.
- When preparing a real package release, make the semver bump explicit, run `npm run test:release:root`, and update downstream vendor snapshots only as a separate deliberate step.

## Workflow Norms

- GSD is the default project workflow in this repository for milestone creation, requirements, roadmap updates, phase planning, phase execution, pause/resume, and milestone closeout.
- When the user has already chosen GSD for the repository, continue in GSD by default instead of switching to superpowers planning or spec-writing flows.
- Use superpowers only for narrow supporting tasks that GSD does not cover well enough on its own, such as structured debugging, strict TDD, explicit code review, worktree isolation, or final verification discipline.
- Do not route normal milestone-definition work through superpowers brainstorming when the milestone direction and repository workflow have already been decided; capture those decisions in `.planning/` via GSD instead.

## Instruction Precedence

- Repo-local `AGENTS.md` rules and this repository's GSD workflow requirements take priority over generic superpowers habits or helper skills.
- Choose the correct GSD entrypoint first, then use superpowers only as helpers inside that path.
- If a superpowers skill suggests a conflicting planning flow, direct-edit shortcut, or different isolation model, follow this file and the active GSD workflow unless the user explicitly asks to bypass them.

## GSD + Superpowers Coordination

- Treat `GSD` as the outer workflow owner for milestone/phase tracking, `.planning/` artifacts, plan state, execution state, and closeout.
- Treat `Superpowers` as an inner execution-method helper set, not a second planning system.
- Keep one source of truth:
  - GSD artifacts own plan, status, progress, and milestone truth.
  - Code, tests, debugging method, verification evidence, and review discipline may use superpowers workflows inside that boundary.
- Start with the correct GSD entrypoint, then layer superpowers skills inside that run when they improve execution quality:
  - `gsd-discuss-phase`, `gsd-plan-phase`, and `gsd-execute-phase` remain the canonical workflow for normal milestone delivery.
  - Superpowers is appropriate inside execution for strict TDD, systematic debugging, explicit verification-before-completion, code review discipline, or worktree isolation.
- Do not let GSD and superpowers create competing plans for the same slice of work. If both are relevant, GSD owns the canonical plan and superpowers only refines execution method.

### Mandatory Execution Gates

- In this repository, superpowers only counts as useful when it changes execution behavior. Do not merely mention a skill name; apply its discipline to the current GSD step.
- During `gsd-execute-phase`, any non-trivial code-bearing plan should start with `superpowers:test-driven-development` before implementation. Write or tighten the smallest failing test first, then implement to green.
- If a bug, regression, flaky test, semantic mismatch, or unexpected runtime behavior appears during execution, switch to `superpowers:systematic-debugging` before attempting speculative fixes.
- Before claiming a task, plan, or phase is complete, apply `superpowers:verification-before-completion`. Run the relevant commands from this file, the active PLAN.md, and any touched surface rules before closeout.
- If a phase materially changes behavior, public contract, lifecycle rules, selection semantics, measurement semantics, or downstream-visible UX, run `superpowers:requesting-code-review` before final closeout.
- If review feedback is received, handle it through `superpowers:receiving-code-review` before applying changes. Verify the feedback technically; do not accept or reject it mechanically.
- Use `superpowers:using-git-worktrees` only when isolation is actually needed. Do not force worktrees for every small repository change.
- Do not route routine milestone definition, requirements capture, roadmap edits, or normal phase planning through superpowers planning skills when GSD already owns that work.
- If a mandatory superpowers gate is intentionally skipped because the change is trivial or the repository state makes the gate inapplicable, say so explicitly in commentary and record the reason in the relevant `*-SUMMARY.md` or closeout notes. Do not imply the gate ran when it did not.

### Preferred Mapping

- `gsd-discuss-phase` / `gsd-plan-phase`: stay in GSD unless there is a narrow supporting need. Do not replace them with superpowers brainstorming or parallel planning flows.
- `gsd-execute-phase` start: prefer `superpowers:test-driven-development`.
- execution blockers or failed verification: prefer `superpowers:systematic-debugging`.
- pre-closeout verification: prefer `superpowers:verification-before-completion`.
- post-implementation quality gate for meaningful changes: prefer `superpowers:requesting-code-review`.
- applying review feedback: prefer `superpowers:receiving-code-review`.

## Working Norms

- Prefer isolated git worktrees over modifying `master` directly when feature work is in flight.
- Treat `AGENTS.md` as the source of truth for repository-level agent instructions.
- Keep tool-specific shim files minimal and pointed here.
